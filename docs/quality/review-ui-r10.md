# DERIVA – Review Runde 10: UI/UX & Hotkeys (Dim. 3) und User Flows (Dim. 4)

**Reviewer-Rolle:** Senior UX-Designer · Accessibility-Auditor · Trading-Desk-Power-User
**Datum:** 2026-09-04 · **Modus:** Re-Review only, keine Quellcode-Änderungen · **Baseline:** `review-ui-r9.md` (Runde 9: UI 98,8, Flows 98,1)

## 0. Prüfstand

| | |
|---|---|
| Repo-Stand | Branch `claude/derivatives-trading-platform-1arsyu`, HEAD `8fbf579` (v0.3.0); Web-Fix-Commit `4c83c3b` („feat(web,product): Restore-Undo, Register-Token, Typerkennung, quotes“). Web-Diff seit R9 (`d470e6a`): 35 Dateien, +2.092/−337 – `lib/focus.ts` (+56: `focusFirstEnabled`, `focusNeighbourAfterRemoval`, `escapeCloses`), `App.tsx` (Restore-Toast ohne Aktion), `components/CommandPalette.tsx` (`resetPortfolioWithConfirm`), `views/Blotter.tsx` (+131: `CsvTypeDialog`, `handleCsvResult` mit Typ-Herkunft), `lib/portfolio-io.ts` (+121: `csvTypeFromFileName`/`csvTypeFromColumns`/`detectCsvType`, `CsvTypeMissingError`), `lib/quick-parser.ts` (+108: `isIndexToken` aus dem Register, `basisLegIndex`, `unknownTokenError` mit `curveCurrencies`), `lib/register-envelope.ts` (+72: `CurveQuotesEntry`, `validateQuotesBlock`, `quotesProblems`), `state/store.ts` (+285: `UndoEntry kind:"portfolio"`, `resetPortfolioWithConfirm`, `snapshotQuoteSpecs`, `parRiskSpecs`), `views/CurvesView.tsx` (+80: `initialCurrency`/`lastRegistered`, Fokusübergabe, `curves-leave-import`), `views/AddCurrencyForm.tsx` (+49: `registrationToast`, `group`-Wrap, `<code>` statt Backticks), `views/MarketView.tsx` (+111: ✕ mit `focusNeighbourAfterRemoval`, `quotes` im Export, `marketLabelDe`), `views/PricingWorkspace.tsx` (`parRiskSpecs`), `lib/i18n.ts` (`marketLabelDe`), `lib/snapshot-import.ts` (`validateQuotesBlock`) |
| Bundle | `vite build` (vite 8.2.2/Rolldown, `vite-build-r10.log`): `index-Bmng274t.js` 121,2 KB (39,8 KB gz), `store-DqbrRHN8.js` 98,8 KB (28,9 gz), `core-DSTEES4h.js` 269,5 KB (87,2 gz), `react-DLh8G0Wt.js` 192,9 KB (61,0 gz), `echarts-BDKLMkZV.js` 560,6 KB (185,4 gz, lazy), `MarketView` 48,5 KB (13,6 gz), `PricingWorkspace` 66,1 KB (17,7 gz), `CurvesView` 38,5 KB (11,6 gz), `Popover` 49,4 KB, `register` 2,3 KB, `sw.js` 4,9 KB; `scripts/size-limit.mjs`: **Initial-Load 233,5 kB gz (8 Dateien)** (R9: 229,6), „Service worker precache: 19 assets“, „Size budget OK“. Preview auf :5011 (serviert exakt diesen `dist`-Stand, gleicher Chunk-Hash) |
| Unit-/E2E-Tests | `npx vitest run` in `apps/web`: **44 Dateien, 391 Tests** – 389 grün, **2 Timeouts (10 s)** in `App.r6.test.tsx` (R6-04/R6-F1/R6-F2) und `App.r7.test.tsx` (R7-2/R7-F1) **unter Parallel-Last** (E2E + Sonden auf 4 Kernen); **isolierter Wiederholungslauf beider Dateien: 8/8 grün in 23 s** → Last-Artefakt, kein Produktbefund. `E2E_PORT=5012 PLAYWRIGHT_CHROMIUM_PATH=/opt/pw-browsers/chromium node e2e/smoke.mjs`: **E2E OK (529 checks)** |
| Browser-Audit | Playwright 1.62.1 + Chromium (`/opt/pw-browsers/chromium`), Locale `de-DE`, Clipboard-Rechte, Viewports **1440×900** und **1024×768** (Formular zusätzlich 1280), Dark + Light, Print-Emulation + `page.pdf()`, Offline (`context.setOffline`) in **frischem Browserkontext**, CDP-Drosselung (1,5 Mbit/s, 250 ms), `page.route`-Abbruch des ECharts-Chunks **ohne Service Worker** (mit SW liefert der Precache den Chunk – korrekt), **zwei frische Browserkontexte** für Snapshot-Import (DKK mit `quotes`, CZK-Envelope). Skripte in `…/scratchpad/r10-ui/`: die R9-Skripte `verify/verify2/verify3/misc.mjs` (39+12+6+1 Checks) und `flows/flows2/flows3.mjs` (37+16+4) unverändert gegen den R10-Build (rot gelaufene Checks einzeln triagiert: Regex-Drift durch neue Toast-Teile „Bootstrap-Quotes für …“, ein-`Tab`-Variante des „+ Paar“-Checks, Restore-Toast vor dem Undo-Toast, State-Kontamination), `sweep.mjs` (**128 Zustände** × a11y + Kontrast + Überlauf + Textscan inkl. Backticks + Screenshot), `lazy.mjs` (6), neu **`r10a.mjs`** (19 Checks: R9-01…05, Toast-Fokus, Restore-Toast 1440/1024, Reset→Undo, leerer Blotter, CSV-Typ-Dialog a11y/Kontrast dark+light), **`r10b.mjs`** (22: R9-F1…F3, Markt R9-1 `quotes`/Par-Risiko, Markt R9-3 Basis-Legs, Markt R9-4 Typerkennung 11 Vorlagen + Dateinamen + API-Vokabular, gerolltes Label, Esc in Zahlenfeldern, 3-Zeilen-Entfernen, „+ Paar“, Reset mit Quote-Änderung und Hedge-Doku), **`r10c.mjs`** (4: API-Spaltensignaturen mit neutralen Dateinamen, ungültige `quotes`-Blöcke, Toast-Reihenfolge nach Reload), **`r10d.mjs`/`r10e.mjs`** (Chunk-Ausfall ohne SW, IRS-API-Zeile mit `tenor`), `api-snap.mts` (API-Snapshot mit Envelope **und `quotes`** über `buildApp`/`inject`). Messwerte `results-{v,v2,v3,f,f2,f3,s,l,misc,r10a,r10b,r10c,r10d}.json`, 173 Screenshots, 10 PDFs |
| Konsole | **Keine** JS-Fehler, keine Warnungen, keine fehlgeschlagenen Requests in allen Läufen (Ausnahme: absichtlich abgebrochene ECharts-Chunks) |
| Tastatur | Chords `o t/g/k/c/p/r`, `x c/b`, `y i`, `y y`, `g …`, `n s/c/w/f/o/b/a/i/x/z/r`; Tab-Sequenzen ab Skip-Link (Blotter-Zeile nach 34 Tabs, Markt-Fixings-Zeile nach 31, Kurven 49, Pricing 70); Fixings-/FX-Fixings-/CDS-Zeilen `↵/Tab…/↵/Esc` und **✕ per Tastatur**; Vol-Gitter ein Tabstopp je Gitter/Paar; „+ Kurve“/„+ Währung“ per `↵` **mit Fokusübergabe** und `Esc`; „+ Paar“ `↵ Tab Tab 11,25 ↵`; `Shift+P`, `Shift+K`, `Shift+T`, `t`, `d`, `Shift+D`, `Space`, `]`/`0`, `Alt+2`, `?`; `Ctrl+Z` auf Quotes, „+ Währung“, Kurve, Cube, Hedge-Doku, Import/Verlassen, **„Beispielportfolio laden“** |

Screenshot-Verzeichnis: `/tmp/claude-0/-home-user-general/ba34afa7-bb32-5710-8abf-0fcec9f55ee0/scratchpad/r10-ui/` (Übersicht Abschnitt 8).

---

## 1. Scores

### 1.1 UI/UX & Hotkeys: **99,0 / 100** (R1 62 → R2 88 → R3 91 → R4 95 → R5 96 → R6 98 → R7 98 → R8 98 → R9 98,8)

**Verifiziert behoben (Belege Abschnitt 2):** R9-01, R9-03, R9-05 vollständig; R9-02 und R9-04 im Kern. **„+ Währung“ + „+ Kalender“ bleibt bei 1024/1280/1440 px in der Karte** (Kartenbreite 636/892/1012 px mit offenem Inspector, `main`-Scroll 0, kein Control rechts der Karte; Sweep über 128 Zustände: **0 Überläufe**). **`↵` auf „+ Kurve“ → Fokus „Währung“-Select, `Tab` → Index → Spot; `↵` auf „+ Währung“ → Fokus „Währung (ISO-Code)“; `Esc` in Select/Textfeld/Textarea schließt das Formular und kehrt auf den Button zurück**, Mausklick übergibt ebenso. **Unter importiertem Snapshot** sagt der „+ Währung“-Toast „im Import-Modus ist „+ Kurve“ gesperrt: nach „Zum Sample-Markt“ mit „+ Kurve“ eine RON-Kurve anlegen oder einen Snapshot mit RON-Kurve importieren“, der **Fokus landet auf dem aktiven „Zum Sample-Markt“** (nie auf dem gesperrten Button), der Button-Tooltip sagt dasselbe, und nach „Zum Sample-Markt“ belegt „+ Kurve“ **RON** vor. **✕ per Tastatur in der Fixings-Tabelle → Fokus auf der Zeile an derselben Position** (Text der ehemals zweiten Zeile), ein Zeilen-Tabstopp, Serie „↓ ↵ Tab… ↵“ bleibt in der Tabelle. **0 literale Backticks** in Text und `title`-Attributen aller Views, Formulare, Hilfe und Palette (Sweep-Backtick-Scan über 128 Zustände: 0 Treffer; E2E-Scan). Neu und sauber: **CSV-Typ-Dialog** („CSV-Import: Produkttyp wählen“, benanntes Select „Produkttyp der CSV-Zeilen“, 11 Optionen „IRS – Zinsswap“ …, Button „Als Zinsswap importieren“ folgt der Auswahl, Fokusfalle Select → Abbrechen → Importieren → ✕, `Esc` → Fokus „⤓ Export ▾“, ohne Import; a11y-Audit sauber, Kontrast ≥ 4,5 dark + light), **Restore-Toast als reine Information** (480 × 46 px, passt bei 1024 px; erster Tabstopp ist jetzt sein ✕, `↵` dort lässt den Bestand unangetastet – Festsatz 2,95 bleibt), **„Beispielportfolio laden“ mit Rückfrage** („Bestand (13 Trades, Marktänderungen, Hedge-Dokumentation) durch das Beispielportfolio ersetzen? (rückgängig mit Ctrl+Z)“ – die Teile erscheinen nur, wenn sie zutreffen), **gerolltes Snapshot-Label** „Sample EoD (gerollt auf 01.10.2026)“ in Toast, Chip, Markt-Panel, Kurven-Hinweis und Pricing-Fußzeile (kein „rolled to“, kein ISO). **Sweep: 0 unbenannte Steuerelemente/Composites/Dialoge, 0 doppelte IDs, 0 positive Tabindizes, 0 Überschriftensprünge, 0 Tabellen ohne Namen, keine Seite scrollt horizontal; 7.415 gemessene Textpaare, alle Textfarben ≥ 4,5:1** (einzige Werte < 4,5 wie seit R7: Rail-Icon-Glyphen 18 px im aktiven Light-Tab 4,13:1 – Nicht-Text ≥ 3:1 –, Logo-Glyphe auf Gradient = Mess-Artefakt). Druck: Report 1 Seite, KID/Geeignetheit/Confirmation 3, Termsheet 1, Hedge 1 (Einheit 3 px), Kurven 2, Markt 4, Blotter 1, Pricing 1, Rail/Status/Topbar ausgeblendet. Chunk-Ausfall (ohne SW): Karte „Diagramm nicht verfügbar … Neu laden · Erneut versuchen · Der Rest der Anwendung läuft weiter“ als `role=alert`, „Erneut versuchen“ → Chart da, ebenso Wiederbetreten, 0 Page-Errors.

**Abzüge (kumuliert −1,0):**
- Kosmetisch −0,2 (Rest von R9-02): **`Esc` in einem Inline-Zahlenfeld der Formulare („Spot EUR/DKK“ in „+ Kurve“, „Spot-Lag“ in „+ Währung“) wirft den Fokus auf `body`**, das Formular bleibt offen, ein zweites `Esc` tut nichts (R10-01).
- Kosmetisch −0,2 (Rest von R9-04): **✕ per Tastatur in FX-Fixings und CDS-Quotes trifft die Nachbarzeile nicht** – bei zwei Zeilen landet der Fokus auf „+ Zeile“/„+ CDS-Quote“, obwohl eine Zeile bleibt; bei drei CDS-Zeilen auf der ehemals dritten statt der zweiten (R10-02).
- Kosmetisch −0,2: **Toast-Aktion per Tastatur („Rückgängig (Ctrl+Z)“, „Meldung schließen ✕“) → Fokus `body`** (R10-03).
- Kosmetisch −0,2: **leerer Blotter: „Beispielportfolio laden“ fragt „Bestand (0 Trades) durch das Beispielportfolio ersetzen?“** und lässt den Fokus danach auf `body` (R10-04).
- Kosmetisch −0,2: **„+ Währung“ unter einem Snapshot, der die Kurve der Währung bereits enthält** (HUF-HUFONIA ohne Envelope): der Toast schickt zum Kurvenanlegen/Snapshot-Import, obwohl `irs huf …` sofort bewertet (PV −42.376) (R10-05).

### 1.2 User Flows: **99,3 / 100** (R1 57 → R2 82 → R3 94 → R4 98 → R5 97 → R6 98 → R7 97,5 → R8 98,3 → R9 98,1)

- (a) **Indikation im Kundengespräch: sehr gut, vollständig per Tastatur** – `Ctrl+K` → `collar 7y 3,5/1,5 6m @Kunde GmbH` → `↵` in 799 ms (unter Parallel-Last) bis PV 48.973, Fokus „Bezeichnung“; `Shift+P` → „Zero-Cost-Collar“ PV 2; `y i` Indikationstext mit Prämie/DV01/Kontrahent/Stichtag; `g s` 627 ms, Heatmap `→ ↵` → What-if −200 bp/+5 %, `o t` mit Stress-Banner, Fokusfalle (Drucken/Markdown erreichbar), `Esc` → `main#main`; `o k`, `o g` (Geeignetheit), `o c`, `o r` Hashes + MaRisk; `Shift+K` ohne CVA/DVA. Kein Abzug.
- (b) **Treasurer: CSV-Import mit allen 11 Vorlagen in beiden Vokabularen: sehr gut, mit zwei Randlücken** – 11 Vorlagen importieren ihre Beispielzeile ohne Dialog, 0 Fehler-Badges; **alle 11 Vorlagen ohne Spalte `type` (neutraler Dateiname) werden aus dem Spaltensatz erkannt** („1 Trades aus CSV (Typ IRS aus dem Spaltensatz) importiert“, je 1 Trade, 0 Dialoge, 0 Badges); **Dateiname** („deriva-import-vorlage-ccs.csv“, „FxForward-2026.csv“, „portfolio-basis.csv“) → „(Typ … aus dem Dateinamen)“; **API-Vokabular ohne `type`** (CCS `pair/domesticNotional/tenor/fxSpot`, FXF `pair/baseAmount/rate/deliveryDate`, FXO `expiryDate`, SWPT `payerReceiver/expiry/tenor/settlement`, FRA `start=3x9`, BASIS `receiveIndex/payIndex`) → je 1 Trade aus dem Spaltensatz; ohne erkennbaren Typ → **Typ-Dialog**, „Als Zinsswap importieren“ → 14 Trades, 0 Badges; defekte Datei → „4 Zeilen übersprungen“ mit deutschen Ursachen (FXF nennt beide Vokabulare); Kollision → Ersetzen; `o p`; **„Portfolio als JSON“ 27 → 54 Trades, 0 Fehler**. **Reibung −0,2 (R10-F2):** die IRS-Zeile im API-Vokabular mit `tenor` statt `maturity` (die API dokumentiert `tenor` als Alias) wird als „Laufzeit/Enddatum fehlt“ abgewiesen – mit und ohne `type`; **Reibung −0,2 (R10-F3):** ein Typ-Token im Dateinamen schlägt den Spaltensatz: IRS-Zeilen in „kredit-cap-2026.csv“ → Typ CAP → jede Zeile „Strike fehlt“, ohne Weg außer Umbenennen.
- (c) **Prüfer: Marktdaten → What-if → Vergleich → Snapshot → Overrides → Undo: sehr gut, eine Lücke** – Quote `↑↑↵` → „modifiziert“; `]` in 70 ms; Vergleich unter What-if; `Ctrl+Z` „Rückgängig: Quote OIS 1W 2,0150 → 2,0200 %“. Snapshot Export → Quote → Import → **Snapshot-ID `bf5e2753659bb206` und Report-Hash `c257b7c896685bba` identisch**, Toast „verworfen: Quote-Änderungen“; Spot-Override 1,25 → „importiert · modifiziert“, andere ID, überlebt den Reload, `Ctrl+Z` → Hash zurück; Undo des Imports; Snapshot 30.10.2026 setzt den Stichtag; ungültige Envelopes deutsch. **Neu:** Export einer „+ Kurve“-Kurve trägt `quotes` (`DKK-DESTR:DESTR:6 Quotes`), Toast „Bootstrap-Quotes für DKK-DESTR“, Import „Bootstrap-Quotes für 1 Kurve (Par-Risiko)“, **ID mit `quotes`-Block identisch (`6a82236b78b92cce`)**, **Par-Risiko des DKK-Swaps unter dem Import 616 EUR wie im Sample-Modus**, auch im frischen Browser; **API-Snapshot** (`GET /api/market/snapshot` mit `quotes`) → `irs czk` PV −59.175, Par-DV01 1.866 EUR; ungültige `quotes`-Blöcke deutsch („Währung SEK passt nicht zur Kurve (DKK)“, „Kurve nicht im Snapshot“, „Feld „quotes“ muss eine Liste sein“, „Index „FOOBAR“ ist nicht registriert – …“), Markt bleibt unangetastet. **Reibung −0,3 (R10-F1):** der Workstation-Export trägt **nur** die Specs der „+ Kurve“-Kurven – nach Export → Re-Import zeigt IRS-0001 „Par-Risiko nur für Kurven mit Quotes (0 von 4). Die Kurven des importierten Snapshots tragen keine Bootstrap-Quotes …“ (Sample-Modus: 6.815 EUR), obwohl der Export die Sample-Bootstrap-Quotes kennt.
- (d) **Hedge Accounting: sehr gut** – `↵` auf „Effektivität testen“ → „✓ effektiv“; Ratio `↓` → Veraltet-Badge, „Erneut testen“, Markdown „veraltet“ ohne ISO/camelCase; Reload behält Ergebnis + Ratio 99; Reset → Rückfrage → **Fokus „Effektivität testen“**; `Ctrl+Z` → Ratio 99 und „Zurücksetzen“ zurück; CAP Innerer Wert + Vol einfrieren → CoH-Karte. Kein Abzug.
- (e) **„+ Währung“ CZK mit „+ Kalender“ → „+ Kurve“ → „+ Paar“ → „+ Fläche“ → Swap/Swaption/FX-Option/Cap/Forward → Export mit Envelope + `quotes` → frischer Browser → API-Snapshot: sehr gut** – Vorschau „2 Indizes CZEONIA, PRIBOR-6M · Konventionen CZK · Kalender CZ (neu, 6 Feiertage, ab 05.07.2027)“, Toast, **Fokus „+ Kurve“**, `↵` → **CZK/CZEONIA vorbelegt**, Kurve aus 6 Quotes, Fokus auf dem Tab; `irs czk …` → „FX-Spot fehlt … „+ Paar““ → **„+ Paar“ `↵ Tab Tab 24,6 ↵`** → PV −150.306, Editor CZEONIA/„PRIBOR-6M (ohne Kurve)“/CZK-CSA; `Shift+P` Par 3,2000 %, `y i` (DV01 1.910), 16 Szenarien, Report nennt CZK-CZEONIA 5× und Kalender CZ; „+ Fläche“ Cube CZK, Caplet CZK-CZEONIA, FX EUR/CZK; Swaption „Underlying CZEONIA“, Option, Cap, Forward ohne Level 3; frischer Browser: Envelope registriert, ID identisch, 18 Trades 0 Fehler; API-Snapshot mit Envelope + `quotes` → `irs czk` bewertet mit Par-Risiko, Reload ok, Export re-emittiert den Envelope. **R9-F1 ✓:** `irs huf … bubor6m|bubor-6m|BUBOR-6M` → „⚠ Kurve HUF-BUBOR-6M fehlt – … „+ Kurve““, `… hufonia` → HUFONIA, `fra huf 3x9 … bubor6m` → BUBOR-6M, `… bubor3m` → „Unbekannter Index „bubor3m“ – für HUF registriert: BUBOR-6M, HUFONIA“; **Markt R9-3 ✓:** `basis huf 5y 1d/6m` → HUFONIA vs BUBOR-6M, `basis 5y 6m/1d` → EURIBOR-6M vs ESTR (PV 81.926), `basis 5y euribor3m/estr`, `basis huf 5y 3m/6m` → „Für HUF ist kein 3M-Index registriert – registriert: BUBOR-6M, HUFONIA“, `3m/3m` → „Beide Legs zeigen auf EURIBOR-3M …“; **R9-F2 ✓:** „+ Kurve“ nach HUF → **HUF**, nach RON → **RON**, zweite HUF-Kurve (BUBOR-6M) → `irs huf` nimmt BUBOR-6M ohne Hinweis, PV −32.771; **R9-F3 ✓:** Snapshot mit HUF-Kurve ohne Envelope → `irs|swpt|cap huf` → „⚠ Währung „HUF“ hat eine Kurve im Markt, ist aber nicht registriert – Für HUF sind keine Swap-Konventionen registriert – in der Kurvenansicht mit „+ Währung“ registrieren (oder Snapshot mit „conventions“/„indices“ importieren) …“, Editor „HUF (nicht registriert)“. Kein Abzug (R10-05 ist ein Textbefund der Dim. 3, kein Doppelabzug).
- (f) **Onboarding / Persistenz / Offline / Lazy / Reset: sehr gut** – Theme, View, Stichtag 30.10.2026, Feldänderung überleben den Reload („Bestand aus lokalem Speicher geladen (13 Trades) – Beispielportfolio über die Palette (Ctrl+K „Beispielportfolio“)“); **R9-F4 ✓:** kein „Zurücksetzen“ im Toast; Palette „Beispielportfolio laden (Bestand zurücksetzen) – ersetzt alle Trades, Marktänderungen und Hedge-Dokumentationen – fragt nach, rückgängig mit Ctrl+Z“ → Abbrechen behält 14 Trades; Bestätigen → 13 Trades, Toast „Beispielportfolio geladen · Rückgängig“, Statusleiste „Ctrl+Z ↶ Beispielportfolio geladen (14 Trades ersetzt)“, HUF-Tab weg, `irs huf` wieder „Unbekannte Währung“; **`Ctrl+Z` → 14 Trades, Quote-Änderung (Chip „modifiziert“, OIS 1W 2,015), HUF-Registrierung + Kurve HUF-HUFONIA (PV −32.763), HUF-Cube, Hedge-Doku (Ratio 99, „Zurücksetzen“) und Testergebnis zurück, 0 Fehler-Badges, überlebt den Reload; unter importiertem Snapshot stellt `Ctrl+Z` den Import wieder her** (Chip „importiert“). Offline nach dem ersten Besuch: 21 Cache-Einträge, alle Views mit Diagrammen, alle fünf „+“-Formulare, Editor, Termsheet, KID, „+ Währung“-Registrierung offline (F3); Kaltstart Shell 742 ms / `g s` 480 ms (unter Last), gedrosselt Skeleton (`aria-busy`, `aria-live=polite`, „Ansicht wird geladen …“) 1.032 ms ohne Doppel-Platzhalter, warm 36–326 ms, `Alt+2` 100 ms, Reduced Motion respektiert; Chunk-Ausfall mit Recovery. Kein Abzug.
- Performance (213 Trades, gemessen bei parallel laufenden Sonden): Import 485 ms, `j`×10 750 ms, Palette 301 ms, `]` 584 ms, Szenarien 1.704 ms, Markt 521 ms, Portfolio-Report 970 ms, Reload 733 ms, localStorage 118 KB.

---

## 2. Status der Runde-9-Befunde

Legende: ✅ behoben · 🔶 teilweise · ❌ offen. Belege = Feld in `results-r10a/r10b/r10c/r10d.json` (`r10a.mjs` …), `results-s.json` (`sweep.mjs`), `results-f2/f3.json`, `results-v2/v3.json` oder Screenshot.

| # | Titel (R9) | Status | Beleg / Rest |
|---|---|---|---|
| R9-01 | „+ Währung“-Formular läuft bei 1024 px über die Karte | ✅ | `AddCurrencyForm.tsx` `group = { flexWrap: "wrap", minWidth: 0 }` auf allen `label.row`, `NumInput width="100%"`, Hinweis/Vorschau `flex: 1 1 220px`, Karte `maxWidth: 100%`; `r10a.r901 {1024: cardW 636, mainScroll 0, cardScroll 0, over []; 1280: 892/0/0/[]; 1440: 1012/0/0/[]}` jeweils mit „+ Kalender“ und offenem Inspector; Sweep `summary.overflow []` über 128 Zustände (R9: 45 px `main`-Scroll); E2E „1024px + Währung form stays inside its card“ (`r10-add-currency-1024.png`) |
| R9-02 | „+ Kurve“/„+ Währung“ per `↵` ohne Fokusübergabe ans Formular | 🔶 | `CurvesView.tsx:653-656/:671-673` `focusWhenPresent('[data-testid="add-curve-ccy"|"add-currency-code"]')` vor `setAdding`, `focus.ts` `escapeCloses` als `onKeyDown` beider Karten; `r10a.r902.curve {afterEnter add-curve-ccy, tab1 add-curve-index, tab2 add-curve-spot, clickFocus add-curve-ccy}`, `r10a.r902.ccy {afterEnter add-currency-code}`, `f2.ff {curve.afterEnter add-curve-ccy, ccy.afterEnter add-currency-code}`; `Esc` aus Select/Textfeld/Textarea: `r10b.esc.curveTextarea {formOpen 0, focus add-curve}`, `r10b.esc.ccyText {formOpen 0, focus add-currency}`, E2E „Esc … returns the focus to + Kurve“. **Rest → R10-01:** `Esc` im Inline-Zahlenfeld → `body` |
| R9-03 | „+ Währung“ unter Import nennt gesperrtes „+ Kurve“, Fokus `body` | ✅ | `AddCurrencyForm.tsx` `registrationToast(summary, code, marketSource)`, `focus.ts` `focusWhenPresent` überspringt `disabled`, `focusFirstEnabled(['add-curve','curves-leave-import','add-currency'])`, `CurvesView.tsx` Tooltip im Import-Modus; `r10a.r903 {addCurveDisabled true, toast „… im Import-Modus ist „+ Kurve“ gesperrt: nach „Zum Sample-Markt“ mit „+ Kurve“ eine RON-Kurve anlegen oder einen Snapshot mit RON-Kurve importieren“, focus curves-leave-import, focusDisabled false, ccyTitle „… erst nach „Zum Sample-Markt“ …“, leave.preselect RON}` (`r10-add-currency-import.png`); E2E R9-03-Checks. **Neuer Randfall → R10-05:** Snapshot enthält die Kurve bereits |
| R9-04 | Zeile per Tastatur entfernen → Fokus `body` | 🔶 | `focus.ts` `focusNeighbourAfterRemoval(tr, fallback)` vor `remove(i)` in Fixings (`MarketView.tsx:1414-1418`), FX-Fixings (`:1019-1023`), CDS (`:1194-1197`), neue Testids `fixings-add-row`; Fixings: `r10a.r904.fixings.after {focus fixings-row-first, inTable true, focusedRowText = expectedRowText (ehemals 2. Zeile), stops 1, modified 1}`, `second.inTable true`, E2E „removing a fixing by keyboard focuses the neighbour row“ (`r10-fixing-removed.png`). **Rest → R10-02:** FX-Fixings/CDS mit `key={i}` |
| R9-05 | Literale Backticks in Kartentitel/Tooltip | ✅ | `AddCurrencyForm.tsx:233` `<code className="mono">POST /api/market/indices|conventions|calendars</code>`, `:296` „(Kern-Typ CustomCalendar)“; `r10a.r901[*].bt {text [], titles []}`, `r10a.bt` (Blotter, Kurven, Markt inkl. „+ Fläche“/„+ Paar“, Szenarien, Report, Vergleich, Hedge, Pricing, Hilfe: alle `[]`), Sweep-Backtick-Scan 0 Treffer in 128 Zuständen; E2E-Scan Text + `title` |
| R9-F1 | Index-Token registrierter Indizes in der Schnelleingabe unbekannt | ✅ | `quick-parser.ts:121-129` `isIndexToken` (Register + Familienpräfix ≥ 3 Zeichen, bei jedem Aufruf gelesen) statt `INDEX_RE`; `r10b.tok.q {bubor6m/buborDash/buborUpper „… ⚠ Kurve HUF-BUBOR-6M fehlt – … „+ Kurve““, hufonia „… · HUFONIA“, fra „FRA HUF 3x9 … BUBOR-6M …“, bubor3m „Unbekannter Index „bubor3m“ – für HUF registriert: BUBOR-6M, HUFONIA“}`; E2E „quick entry knows the registered index token bubor6m“ |
| R9-F2 | „+ Kurve“ nach „+ Währung HUF“ belegt DKK vor | ✅ | `CurvesView.tsx:229-237` `initialCurrency` (aus `onDone(ccy)` → `lastRegistered`) → letzte Registrierung ohne Kurve → erste Währung ohne Kurve; `r10b.tok.preselect1 HUF, preselect2 RON`, `r10a.r903.leave.preselect RON`, `f2.czk.curve.ccy CZK`; E2E „+ Kurve preselects the currency just registered“. Randfall: nach zwei HUF-Kurven belegt „+ Kurve“ RON vor (letzte Registrierung ohne Kurve) – nachvollziehbar |
| R9-F3 | Snapshot-Kurve nicht registrierter Währung: nur „Unbekannte Währung“ | ✅ | `quick-parser.ts:155-163` `unknownTokenError(tok, grammar, { curveCurrencies })` → `unregisteredCurrencyHint`; `r10b.f3 {qe/qeSwpt/qeCap „⚠ Währung „HUF“ hat eine Kurve im Markt, ist aber nicht registriert – Für HUF sind keine Swap-Konventionen registriert – in der Kurvenansicht mit „+ Währung“ registrieren (oder Snapshot mit „conventions“/„indices“ importieren) …“, editorCcyOpts [„HUF (nicht registriert)“]}` (`r10-huf-noenv-import.png`) |
| R9-F4 | Restore-Toast „Zurücksetzen“ ohne Rückfrage/Undo | ✅ | `App.tsx:150-160` Info-Toast ohne Aktion, `store.ts` `UndoEntry kind:"portfolio"` + `resetPortfolioWithConfirm()` (Palette, leerer Blotter), Undo registriert zuerst den Envelope, dann Extras, Marktquelle, Bestand; `r10a.rt1440/rt1024 {buttons [„✕“], tab1 „Meldung schließen“, afterEnter {n 13, rate 2,95, dialog null}}`, `r10a.reset {dismiss.n 14, accept {n 13, toast „Beispielportfolio geladen · Rückgängig“, status „Ctrl+Z ↶ Beispielportfolio geladen (14 Trades ersetzt)“, hufTab 0}, undo {n 14, hufTab 1, swptBtns … HUF, verdict 1, badges 0, huf.pv −32.763}, afterReload.n 14, imp.afterUndo.chip „importiert“}`, `r10b.rq {dialog „Bestand (13 Trades, Marktänderungen, Hedge-Dokumentation) …“, chipUndo modifiziert, qvUndo 2,015, ratioUndo 99, hedgeResetUndo 1}`; Doku `03-ui-konzept-und-hotkeys.md` §11/§17 aktualisiert; E2E R9-F4-Checks. **Randfall → R10-04:** leerer Bestand |

Zusätzlich verifiziert (kein R9-Befund): R8-01/R8-03 Zeilen- und Gittermodell (Fixings `↵`→Index, `Tab`-Zyklus, `↵` zurück zur Zeile; FX-Gitter ein Tabstopp je Paar), **R8-04 „+ Paar“ per `↵ Tab Tab 11,25 ↵`** (`r10b.pp {suggestion EURSEK, tab1 add-spot-pair, tab2 add-spot-rate, after {row 1, value 11,25, formOpen 0, focus add-spot}}`; der rote „R8-04“-Check in `verify.mjs`/`flows.mjs` tippt mit nur **einem** `Tab` ins Paar-Feld – Skriptartefakt), R8-05 `d`/Hedge-Reset-Fokus, R8-06 Reparaturhinweis unter Import, R8-F1 DKK-Swaption auf DESTR mit `Shift+P`, R8-F2 Extras über Import → Reload → Verlassen → Reload (`f2`), Markt R8-5 Snapshot-Kurven-Tabs (API-Kurve mit derselben ID wie eine „+ Kurve“-Kurve erscheint unter dem aktiven Tab – wie R9 notiert, korrekt), Hilfe-Overlay (Zeilen-/Gittermodell, „+ Währung / + Kalender“, „+ Paar“, „+ Fläche“).

---

## 3. Neue Befunde (Runde 10)

Schweregrade wie in R1–R9. Reproduktion gegen das Preview-Bundle; Belege in `results-r10*.json` (Feldname) bzw. Screenshot.

| # | Schwere | Wo | Was ist falsch (Beleg) | Konkreter Fix |
|---|---|---|---|---|
| R10-01 | Kosmetisch (Tastatur; Rest R9-02) | `components/NumInput.tsx:160-172` (Inline-Variante: `Escape` → `cancel()` mit `blur()`), `lib/focus.ts` `escapeCloses` (sieht das Event nie, weil `stopPropagation` und der Fokus schon weg ist) | „+ Kurve“ → Fokus im Feld „Spot EUR/DKK“ → `Esc` → **Fokus `body`, Formular offen**; zweites `Esc` ohne Wirkung; ebenso „+ Währung“ → „Spot-Lag (Geschäftstage)“ (`r10b.esc.curveSpot {afterEsc1 {focus BODY, formOpen 1}, afterEsc2 {BODY, 1}}`, `ccySpotLag1/2 {BODY, 1}`); aus Select, Textfeld und Textarea schließt `Esc` korrekt und kehrt auf den Button zurück (`curveTextarea {formOpen 0, focus add-curve}`, `ccyText {0, add-currency}`). Der Code-Kommentar „Number and date fields consume their own Esc first, so this fires on the second press“ trifft für die Inline-Variante nicht zu. | In der Inline-`NumInput` bei `Escape` den Wert zurücksetzen **ohne** `blur()` (oder `blur()` nur, wenn ein Zeilen-/Gitter-Container den Fokus zurücknimmt – `closest('[role=row],[role=grid]')`); alternativ `escapeCloses` im Capture (`onKeyDownCapture`) und dort `e.target.value === initial` prüfen → schließen, sonst Feld zurücksetzen lassen. Test: `Esc` ×2 im Spot-Feld → `add-curve` fokussiert. |
| R10-02 | Kosmetisch (Fokus; Rest R9-04) | `views/MarketView.tsx:979` (`<tr key={i}>` FX-Fixings), `:1154` (`<tr key={i}>` CDS), `lib/focus.ts` `focusNeighbourAfterRemoval` (`rows.filter(r => r !== tr)`) | Positionsschlüssel: nach dem Entfernen der ersten Zeile **verwendet React den Knoten der entfernten `tr` für die nächste Zeile weiter**; der Filter wirft genau diesen Knoten heraus. FX-Fixings mit 2 Zeilen: ✕ auf Zeile 1 → **Fokus „+ Zeile“, obwohl eine Zeile bleibt** (`r10a.r904.fxf.after1 {focus fx-fixing-add, inTable false, rows 1}`); CDS mit 2 Quotes ebenso (`r10a.r904.cds.after1 {focus cds-add}`); CDS mit 3 Quotes: Fokus auf der **ehemals dritten** Zeile statt der zweiten (`r10b.rm3.cds {focusedText „… 168 bp 96,69 %“, expected „… 169 bp 98,33 %“}`). Fixings-Tabelle (Originalindex als Key) korrekt. | Nachbarn per **DOM-Position** wählen statt per Identität: nach dem Re-Render `rows = tbody.querySelectorAll(':scope > tr')` und `rows[min(index, rows.length-1)]` ohne Filter (die Zeile an der alten Position ist per Definition der Nachbar); oder stabile Keys (`pair|date`, `tenor`) in FX-Fixings/CDS. E2E-Check „FX-Fixing 1 von 2 entfernen → Fokus `tr`“. |
| R10-03 | Kosmetisch (Fokus) | `App.tsx:436-450` Toast-Stack (Aktions-Button und ✕ verschwinden mit dem Toast, keine Fokusrückgabe) | Toast-Aktion per Tastatur: `Shift+D` → `Tab` ab Skip-Link → „Rückgängig (Ctrl+Z)“ → `↵` → Trade zurück, **Fokus `body`**, `Tab` beginnt am Seitenanfang (`r10a.toastFocus.afterUndo {focus BODY, n 13}`); Restore-Toast „Meldung schließen ✕“ → `↵` → `body` (`r10a.rt1440.afterEnter.focus BODY`). Maus-Nutzer merken nichts; der Tastaturnutzer verliert nach jeder Toast-Aktion die Position. | Vor dem Fokussieren des Toasts den Ursprung merken (`document.activeElement` beim `focusin` auf den Stack) und nach `run()`/`dismiss` dorthin zurück (`restoreFocus`, `Modal.tsx:31`), Fallback `main#main`; Undo-Aktionen, die eine Zeile wiederherstellen, dürfen wie `d` die Zeile fokussieren (`focusWhenPresent('tr[data-id=…]')`). |
| R10-04 | Kosmetisch (Dialog/Fokus) | `state/store.ts:2652-2660` `resetPortfolioWithConfirm` (fragt immer), `views/Blotter.tsx:168` `blotter-load-sample` | Leerer Blotter (alle Trades gelöscht, Markt unverändert): „Beispielportfolio laden“ → **„Bestand (0 Trades) durch das Beispielportfolio ersetzen? (rückgängig mit Ctrl+Z)“** – eine Rückfrage ohne Verlustrisiko; nach dem Laden Fokus `body` (der Button ist weg) (`r10a.empty {n 0, emptyState 1, dialog „Bestand (0 Trades) …“, after {n 13, focus BODY}}`). Onboarding-Pfad „leer → Beispiel“ bekommt so eine unnötige Hürde. | Rückfrage nur, wenn `parts.length > 1 || trades.length > 0` (bei 0 Trades, unmodifiziertem Sample-Markt, leerem Register und ohne Hedge-Doku direkt laden, Undo-Eintrag trotzdem); danach `focusWhenPresent('table.blotter tbody tr[tabindex="0"]')`. |
| R10-05 | Kosmetisch (Hinweistext; Randfall R9-03) | `views/AddCurrencyForm.tsx:107-115` `registrationToast` (prüft nur `marketSource`, nicht `baseMarket.discountCurveId[code]`) | Snapshot **mit** HUF-Kurve, aber ohne Envelope importiert (R9-F3-Fall) → „+ Währung“ HUF → Toast „Registriert: … – im Import-Modus ist „+ Kurve“ gesperrt: nach „Zum Sample-Markt“ mit „+ Kurve“ eine HUF-Kurve anlegen oder einen Snapshot mit HUF-Kurve importieren“, Fokus „Zum Sample-Markt“ – **die Kurve ist längst da**, `irs huf 5y pay 6% 100m` bewertet sofort (PV −42.376, Vorschau „HUFONIA (Kurve vorhanden; BUBOR-6M ohne Kurve)“) (`r10b.f3.reg.toast`, `r10b.f3.afterReg`). Der Toast schickt in die falsche Richtung; „Zum Sample-Markt“ würde die Kurve sogar verwerfen. | `registrationToast` um `hasCurve = !!baseMarket.discountCurveId[code]` erweitern: „Registriert: … – die HUF-Kurve des Snapshots ist jetzt bewertbar (Schnelleingabe `irs huf …`)“, Fokus dann auf „+ Währung“ oder den Kurven-Tab „HUFONIA (aus Snapshot)“ statt auf „Zum Sample-Markt“. |

### Flow-Befunde

| # | Schwere | Wo | Was ist falsch (Beleg) | Konkreter Fix |
|---|---|---|---|---|
| R10-F1 | Niedrig (Prüferpfad) | `state/store.ts:769-779` `snapshotQuoteSpecs` (nur `extraCurves` + re-emittierte Import-Specs), `:786-799` `parRiskSpecs` (Import-Modus: nur `quotes`-Block) | Export → Re-Import desselben Snapshots (der Kern-Pfad des Prüferflows, ID identisch): **IRS-0001 hat kein Par-Risiko mehr** – Karte „Par-Risiko nur für Kurven mit Quotes (0 von 4). Die Kurven des importierten Snapshots tragen keine Bootstrap-Quotes – „Zum Sample-Markt“ wechseln oder die Kurve mit „+ Kurve“ aus Quotes anlegen.“, Par-DV01 0 EUR / Zero-DV01 6.971 EUR (Sample-Modus: 6.815 EUR, 4 Kurven); nur die „+ Kurve“-Kurve DKK-DESTR bringt ihre Quotes mit (616 EUR unter Import) (`r10b.q.eurSample {total 6.815 EUR}`, `r10b.q.eurImp {coverage „… (0 von 4) …“, total 0 EUR}`, `r10b.q.dkkImp {total 616 EUR}`, `r10-par-risk-import.png`). Der Export kennt die Sample-Specs (`sampleBootstrapSpecs(valuationDate, quotes)`), schreibt sie aber nicht. Die Snapshot-ID hängt nicht vom `quotes`-Block ab (`idBefore = idAfter = 6a82236b78b92cce`). | In `snapshotQuoteSpecs` im Sample-Modus **alle** Kurven mit Quotes aufnehmen: `Object.values(sampleBootstrapSpecs(valuationDate, quotes))` + `extraCurveSpec` (Filter `m.curves[id]`); Import-Modus re-emittiert wie bisher. Toast „Bootstrap-Quotes für 9 Kurven“, Par-Risiko-Abdeckung nach Re-Import „4 von 4“. Test in `store.r9.test.ts`: Export → Import → `parRiskSpecs` enthält EUR-ESTR. |
| R10-F2 | Kosmetisch (Vokabular-Parität) | `lib/portfolio-io.ts:947` (IRS), `:972` (AMORT), `:1032` (BASIS), `:1111` (CAP): `if (!rec.maturity) throw new Error("Laufzeit/Enddatum fehlt")` – CCS/SWPT/IMM lesen `rec.tenor ?? rec.maturity` (`:1006/:1135/:1175`); API `csv-import.ts:150` dokumentiert für IRS „`maturity` tenor or date (alias `tenor`)“ | IRS-Zeile im API-Vokabular mit `tenor` (`id;currency;notional;payReceive;fixedRate;effectiveDate;tenor;index`) → **„Laufzeit/Enddatum fehlt“**, mit und ohne Spalte `type`; mit `maturity` importiert sie (`r10d.irs {upload-6 errDlg 1 „Laufzeit/Enddatum fehlt“, upload-6b (mit type) errDlg 1, upload-6c (maturity) errDlg 0}`). Die API-Vorlagendatei selbst (Header `maturity`) importiert unverändert – nur der von der API akzeptierte Alias fehlt im Web. | In den vier Zweigen `const maturity = rec.maturity ?? rec.tenor` (wie CCS); Fehlertext „Laufzeit/Enddatum fehlt (Spalte „maturity“ oder „tenor“)“; Testzeile in `portfolio-io.test.ts`. |
| R10-F3 | Kosmetisch (Fehlerpfad) | `lib/portfolio-io.ts:300-305` `detectCsvType` (Dateiname **vor** Spaltensatz), `:239-250` `csvTypeFromFileName` (nackter Token `irs|fxf|cap|…` zwischen Nicht-Buchstaben) | IRS-Zeilen (Workstation-Vokabular ohne `type`) in „kredit-cap-2026.csv“ → Typ **CAP** aus dem Dateinamen → jede Zeile „Strike fehlt“, 0 importiert; der Toast nennt zwar die Quelle („(Typ CAP aus dem Dateinamen)“), der Fehlerdialog bietet aber **keinen Weg zum Typ-Dialog** – nur Umbenennen hilft; der Spaltensatz hätte eindeutig IRS ergeben (`r10b.csv.mis {errDlg 1, rows [„2 Strike fehlt“], toast „… (Typ CAP aus dem Dateinamen)“}`, `r10-csv-misdetect.png`). Mischdatei ohne `type` (FXF+FXO im API-Vokabular) → FXO aus dem Spaltensatz, FXF-Zeile „Nominal fehlt“ – vertretbar (Mischdateien brauchen `type`), aber gleicher fehlender Ausweg. | Bei einem **nackten** Dateinamen-Token den Spaltensatz gegenprüfen und bei Widerspruch den Spaltensatz nehmen (Vorlagen-/API-Dateinamen bleiben Vorrang); im Fehlerdialog bei abgeleitetem Typ einen Button „Anderen Produkttyp wählen …“ (öffnet `CsvTypeDialog` mit dem gepufferten Text). |

Ohne Abzug notiert: nach einem Reload steht das ✕ des Restore-Toasts vor dem „Rückgängig“ eines späteren Toasts in der Tab-Reihenfolge (2. statt 1. Stopp, 8 s lang; `r10c.order.tabs [„Meldung schließen“, „Rückgängig (Ctrl+Z)“, …]`) – Design des Toast-Stacks. `hedge-reset` erscheint erst nach gespeicherter Beziehung (Ratio-Änderung), nicht nach dem Test allein – wie seit R3; deshalb nennt die Reset-Rückfrage „Hedge-Dokumentation“ nur bei gespeicherter Doku, das Testergebnis wird per Undo trotzdem wiederhergestellt. Der Undo-Stack überlebt den Reload nicht (Design). Die zwei Vitest-Timeouts sind Last-Artefakte (isoliert grün). Lazy-/Perf-Zeiten wurden bei parallel laufenden Sonden gemessen und liegen deshalb über R9.

---

## 4. User Journeys (Schritt für Schritt, tastaturgeführt)

### (a) Berater bewertet Collar → Termsheet/KID/Erklärung → Report
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `Ctrl+K` · `collar 7y 3,5/1,5 6m @Kunde GmbH` · `↵` | 799 ms (unter Last) bis PV 48.973; Kontrahent; Fokus „Bezeichnung“ | – |
| `Esc` · `Shift+P` · `y i` | „Zero-Cost-Collar: Floor-Strike übernommen (Prämie 0)“, PV 2; Indikation mit Prämie/DV01/Kontrahent/Stichtag | – |
| `g s` · Heatmap `→ ↵` · `o t` · `Esc` | 627 ms; What-if −200 bp/+5 %; Termsheet mit Stress-Banner; `Esc` → `main#main` | – |
| `0` · `o t` · `Tab`×6 | Fokusfalle, Drucken/Markdown erreichbar, Rail nie | – |
| `o k` · `o g` · `o c` · `o r` · `Shift+K` | KID, Geeignetheitserklärung, Confirmation, Report mit Hashes/MaRisk, Kundenmodus ohne CVA/DVA | – |

### (b) Treasurer: 11 Vorlagen · ohne `type` · Dateiname · API-Vokabular · Typ-Dialog · defekt · Kollision · JSON
| Schritt | Beobachtung | Reibung |
|---|---|---|
| Export ▾ → 11× Vorlage → 11× Import | je „1 Trades aus CSV importiert“, 0 Dialoge, 0 Badges | – |
| 11× Vorlage **ohne Spalte `type`** (neutraler Name) | je „(Typ … aus dem Spaltensatz)“, 1 Trade, 0 Badges | – |
| „deriva-import-vorlage-ccs.csv“ / „FxForward-2026.csv“ / „portfolio-basis.csv“ | „(Typ CCS/FXF/BASIS aus dem Dateinamen)“ | – |
| API-Vokabular ohne `type` (CCS, FXF, FXO, SWPT, FRA, BASIS) | je 1 Trade aus dem Spaltensatz | – |
| API-IRS mit `tenor` | „Laufzeit/Enddatum fehlt“ (auch mit `type`) | **R10-F2** |
| IRS-Zeilen in „kredit-cap-2026.csv“ | CAP aus dem Dateinamen → „Strike fehlt“, kein Ausweg außer Umbenennen | **R10-F3** |
| Datei ohne erkennbaren Typ | Dialog „CSV-Import: Produkttyp wählen“ → „Als Zinsswap importieren“ → 14 Trades | – |
| defekte Datei · Kollision · `o p` · JSON-Roundtrip | 4 Zeilen deutsch begründet; Ersetzen; Portfolio-Report; 27 → 54 Trades, 0 Fehler | – |

### (c) Prüfer: Marktdaten → What-if → Vergleich → Snapshot (`quotes`) → Overrides → Undo
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `g c` · Quote `↑↑↵` · `]` · `g v` · `0` · `Ctrl+Z` | „modifiziert“; `]` 70 ms; Vergleich unter What-if; „Rückgängig: Quote OIS 1W 2,0150 → 2,0200 %“ | – |
| Export · Quote ändern · Import · `o r` | Snapshot-ID und Report-Hash identisch; Toast „verworfen: Quote-Änderungen“ | – |
| Spot 1,25 · Reload · `Ctrl+Z` · `Ctrl+Z` | „importiert · modifiziert“, andere ID, überlebt Reload; Hash zurück; Import rückgängig | – |
| „+ Kurve“ DKK · Export · Import | `quotes` `DKK-DESTR` (6 Quotes), Toasts „Bootstrap-Quotes …“, ID identisch, **DKK-Par-Risiko 616 EUR unter Import** | – |
| IRS-0001 unter demselben Re-Import | „Par-Risiko nur für Kurven mit Quotes (0 von 4) …“, Par-DV01 0 EUR (Sample 6.815) | **R10-F1** |
| API-Snapshot (`quotes`) · `irs czk` | PV −59.175, Par-DV01 1.866 EUR | – |
| ungültige `quotes`/Envelopes | deutsch: Währung passt nicht, Kurve nicht im Snapshot, muss Liste sein, Index nicht registriert, Kalender „HU“ → „+ Kalender“ | – |
| Snapshot „Sample EoD (rolled to 2026-10-01)“ | überall „(gerollt auf 01.10.2026)“, Stichtag gesetzt | – |

### (d) Hedge Accounting
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `g h` · „Effektivität testen“ `↵` · Ratio `↓` `Tab` · Markdown | „✓ effektiv“; Veraltet-Badge, „Erneut testen“, Markdown „veraltet“ deutsch | – |
| Reload · „Zurücksetzen“ `↵` · `Ctrl+Z` | Ergebnis + Ratio 99 da; Rückfrage; Fokus „Effektivität testen“; Undo → Ratio 99, „Zurücksetzen“ zurück | – |
| CAP · Innerer Wert · Vol einfrieren | CoH-Karte, „✓ effektiv“ | – |

### (e) „+ Währung“ CZK/HUF → „+ Kalender“ → „+ Kurve“ → „+ Paar“ → „+ Fläche“ → Trades → Export → frischer Browser → API
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `g c` · „+ Währung“ `↵` | Fokus „Währung (ISO-Code)“ (R9-02 ✓), CZK vorbelegt; `Esc` schließt (aus Zahlenfeld nicht) | **R10-01** |
| OIS/IBOR · „+ Kalender“ CZ 6 Feiertage · „Währung registrieren“ | Vorschau, Toast, **Fokus „+ Kurve“**; 1024 px ohne Überlauf (R9-01 ✓), keine Backticks (R9-05 ✓) | – |
| `↵` auf „+ Kurve“ | **CZK/CZEONIA vorbelegt** (HUF → HUF, RON → RON, R9-F2 ✓), Kurve aus 6 Quotes, Fokus Tab | – |
| `irs czk …` · `g m` · „+ Paar“ `↵ Tab Tab 24,6 ↵` | „FX-Spot fehlt … „+ Paar““ → Spot angelegt, PV −150.306 | – |
| `Shift+P` · `y i` · `g s` · `o r` · „+ Fläche“ ×3 | Par 3,2000 %, DV01 1.910, 16 Szenarien, Report mit CZK-CZEONIA und Kalender CZ; Cube/Caplet/FX-Fläche | – |
| `swpt/fxo/cap/fxf …` | bewertet ohne Level 3, Swaption „Underlying CZEONIA“ | – |
| `irs huf … bubor6m|hufonia`, `fra huf …`, `basis huf 5y 1d/6m` | erkannt (R9-F1 ✓, Markt R9-3 ✓); `bubor3m`/`3m/6m` deutsch mit Registerliste | – |
| Export · frischer Browser · Import · Portfolio-JSON | Envelope registriert, ID identisch, alle Trades bewertet, Reload ok | – |
| API-Snapshot (Envelope + `quotes`) | Toast mit Registrierung und Bootstrap-Quotes, `irs czk` mit Par-Risiko, Export re-emittiert | – |
| Snapshot ohne CZK · „+ Währung“ RON | Toast nennt „Zum Sample-Markt“/Snapshot, Fokus „Zum Sample-Markt“ (R9-03 ✓), danach „+ Kurve“ mit RON | – |
| Snapshot mit HUF-Kurve ohne Envelope · `irs huf` · „+ Währung“ HUF | Palette „… hat eine Kurve im Markt, ist aber nicht registriert – … „+ Währung““ (R9-F3 ✓); nach Registrierung bewertet `irs huf` sofort, **Toast schickt trotzdem zum Kurvenanlegen** | **R10-05** |

### (f) Persistenz / Reset / Offline / Lazy / Chunk-Ausfall
| Schritt | Beobachtung | Reibung |
|---|---|---|
| Reload · `Tab` · `↵` | Info-Toast ohne Aktion; ✕ als erster Stopp, `↵` lässt den Bestand (R9-F4 ✓) – Fokus danach `body` | **R10-03** |
| Palette „Beispielportfolio“ · Abbrechen / Bestätigen · `Ctrl+Z` · Reload | Rückfrage mit Bestandteilen; 13 Trades + Toast „Rückgängig“; Undo stellt Trades, Quotes, Register, Kurve, Cube, Hedge-Doku und Import-Modus wieder her; überlebt Reload | – |
| leerer Blotter · „Beispielportfolio laden“ | Rückfrage „Bestand (0 Trades) …“, Fokus danach `body` | **R10-04** |
| Toast „Rückgängig“ per `Tab` `↵` | Aktion wirkt, Fokus `body` | **R10-03** |
| frischer Browser · offline · Reload | Chip „⚠ offline – lokaler Bestand“, alle Views/Charts/Formulare/Editor/Termsheet/KID, Registrierung offline; 21 Cache-Einträge | – |
| Kaltstart `g s` · gedrosselt · warm · `Alt+2` | 480 ms; Skeleton 1.032 ms (`aria-busy`); 36–326 ms; 100 ms | – |
| ECharts-Chunk abgebrochen (ohne SW) · „Erneut versuchen“ / Wiederbetreten | Karte „Diagramm nicht verfügbar“ (`role=alert`, deutsch, „Der Rest der Anwendung läuft weiter“) → Chart da, 0 Page-Errors | – |

---

## 5. Hotkey-Matrix (verifiziert)

| Aktion | Tasten | Ergebnis |
|---|---|---|
| Termsheet / Erklärung / KID / Confirmation / Portfolio-Report / Report | `o t` / `o g` / `o k` / `o c` / `o p` / `o r` | ✅ Fokusfalle, `Esc` → `main#main` |
| Indikation / Zeile kopieren | `y i` / `y y` | ✅ auch CZK/HUF |
| Neue Trades | `n s/c/w/f/o/b/a/i/x/z/r` | ✅ 11 Editoren, Fokus „Bezeichnung“; `d` → „Bezeichnung“ / neue Zeile |
| What-if / Reset | `]` `[` `\` · `+` `-` `0` | ✅ 70 ms |
| Par-Satz / fairer Preis | `Shift+P` | ✅ Collar (PV 2), CZK-Swap 3,2000 %, DKK-Swaption |
| Kundenmodus / Stichtag / Theme / Hilfe | `Shift+K` / `Shift+T` / `t` / `?` | ✅ |
| Duplizieren / Löschen / Vergleich / Neu bewerten | `d` / `Shift+D` / `Space` / `r` | ✅ Toasts mit Rückgängig; ⚠ Toast-Aktion per Tastatur → Fokus `body` (R10-03) |
| Rückgängig | `Ctrl+Z` | ✅ Trades, Quotes, Vols, Fixings, Spot-Overrides, „+ Paar“/„+ Fläche“/„+ Währung“, Import/Verwerfen/Verlassen, Kurven, Hedge-Doku inkl. Ergebnis, **„Beispielportfolio laden“ (ganzer Bestand inkl. Import-Modus)**; nach Reload leer (Design) |
| Tabellen (Zeilen) | `↑/↓` `Home/End` `PgUp/PgDn` `↵`/`F2` · `Tab`/`⇧Tab` `↵` `Esc` · ✕ | ✅ ein Zeilen-Tabstopp; ✕ per Tastatur → Nachbarzeile in Fixings (R9-04 ✓); ⚠ FX-Fixings/CDS mit 2–3 Zeilen (R10-02) |
| Vol-Gitter | `←/→/↑/↓` `Home/End` `Ctrl+Home/End` `↵`/`F2` `↵` `Esc` `Tab` | ✅ ein Tabstopp je Gitter/Paar, `↵` kehrt zur Zelle zurück |
| „+ Kurve“ / „+ Währung“ | `↵` · Felder · `Esc` / „… anlegen/registrieren“ | ✅ Fokus im ersten Feld (R9-02 ✓), `Esc` schließt aus Select/Text/Textarea; ⚠ aus Inline-Zahlenfeld → `body` (R10-01) |
| „+ Kalender“ / „+ Paar“ / „+ Fläche“ | `↵` · `Tab` … `↵` | ✅ „+ Paar“ `↵ Tab Tab 11,25 ↵` legt an, Fokus zurück auf den Button |
| CSV-Typ-Dialog | `Tab` Select → Abbrechen → Importieren → ✕ · `Esc` | ✅ Fokusfalle, `Esc` → „⤓ Export ▾“ |
| Toast-Aktion | `Tab` ab Skip-Link · `↵` | ✅ wirkt; nach Reload ist der 1. Stopp das ✕ des Info-Toasts, „Rückgängig“ der 2.; ⚠ Fokus danach `body` (R10-03) |
| Palette | `Ctrl+K` · `↑/↓` · `↵` · `Esc` | ✅ Token registrierter Indizes, Basis-Legs über das Register, „+ Währung“-Hinweis bei Kurve ohne Register, „Beispielportfolio laden“ mit Rückfrage |
| Ansichten | `g …` · `Alt+1…8` · Rail | ✅ 36–326 ms warm |

---

## 6. Barrierefreiheits-Sweep (128 Zustände: alle Views inkl. „+ Währung“/„+ Kalender“, Snapshot-Kurven-Tab, Markt mit Extras, DKK-Swaption-Editor, Par-Risiko unter Import; 11 Editoren, 4 Dokumente + Kundenmodus, Dialoge/Menüs/Popover, 1440 + 1024, Dark + Light; zusätzlich CSV-Typ-Dialog dark + light, Restore-Toast 1440/1024)

| Prüfung | Ergebnis |
|---|---|
| Unbenannte Inputs/Checkboxen/Buttons/Links/Bilder/Dialoge/Composite-Rollen, doppelte IDs, positive Tabindizes, leere `kbd` | **0** in allen 128 Zuständen (`summary.a11yBad []`) und im CSV-Typ-Dialog („Produkttyp der CSV-Zeilen“, Dialogtitel „CSV-Import: Produkttyp wählen“) |
| Tabellen ohne Namen | **0** |
| Überschriften | `h1` „DERIVA“ einmal, `h2` = View-Titel, `h3` Karten; **0 Sprünge** |
| Landmarks / Live-Region | `nav[aria-label=Hauptnavigation]`, `main#main` (`tabIndex=-1`), Skip-Link erster Tabstopp, Toast-Stack `role=status` direkt dahinter (jetzt ohne destruktive Aktion); Skeleton `aria-busy` + `aria-live=polite`; Chunk-Fehlerkarte `role=alert` |
| Rollen / Roving | Blotter `grid`; Amortisationsplan, Fixings, FX-Fixings, CDS `row`-Zeilen mit einem Tabstopp und Tab-Zyklus im Feld; Vol-Gitter `grid`/`row`/`gridcell`; Heatmaps `grid`; Kurven-Tabs `group` mit `aria-pressed` |
| Kontrast | **7.415 Textpaare gemessen, alle Textfarben ≥ 4,5:1** (Dark + Light, 1440 + 1024); < 4,5 nur Rail-Icon-Glyphen 18 px im aktiven Light-Tab (4,13:1, Nicht-Text ≥ 3:1) und die Logo-Glyphe auf Gradient (Mess-Artefakt) – unverändert seit R7 |
| Texte | keine Engine-Texte, keine ISO-Daten außer Format-Beispielen (Hilfe, Feiertags-Fehler „… oder 2027-07-06“, Palette „stichtag 2026-12-31“), Domänenbegriffe „Live“, „Cash“, „Float“, „Reset“ (MtM-Reset), „Modified Following“, Grammatik-Beispiele „3.1%“/„3.5“; **0 literale Backticks** in Text und Tooltips (R9-05 ✓); gerolltes Label deutsch |
| Layout | **keine horizontale Scrollbarkeit in keinem Zustand** (R9-01 ✓, „+ Währung“ + „+ Kalender“ bei 1024 px: Kartenbreite 636 px, 0 px Überlauf); Restore-Toast 480 × 46 px innerhalb des Viewports |
| Tabstopps (Seite gesamt, 1440 dark) | Blotter-Zeile nach **34** Tabs ab Skip-Link (R9: 34), Markt-Fixings-Zeile nach 31 (R9: 30), Kurven 49, Pricing 70 |
| Druck | Report 1, Termsheet 1, KID 3, Geeignetheit 3, Confirmation 3, Hedge 1 (Einheit 3 px), Kurven 2, Markt 4, Blotter 1, Pricing 1 Seiten; Rail/Status/Topbar `display: none` |
| Bewegung | Skeleton-Puls unter `prefers-reduced-motion: reduce` aus |

---

## 7. Was für 100 noch fehlt

1. **R10-F1** Workstation-Export mit den Sample-Bootstrap-Specs im `quotes`-Block (Par-Risiko nach Re-Import für den Standardbestand) – ~20 min.
2. **R10-01 / R10-02** Inline-`NumInput` ohne `blur()` bei `Esc` (oder `escapeCloses` im Capture); Nachbarzeile nach ✕ per DOM-Position statt Identität (oder stabile Keys in FX-Fixings/CDS) – ~30 min.
3. **R10-03 / R10-04** Fokusrückgabe nach Toast-Aktion/✕; Rückfrage „Beispielportfolio laden“ nur bei Verlustrisiko, danach Fokus auf die erste Blotter-Zeile – ~20 min.
4. **R10-05** „+ Währung“-Toast unter Import, wenn der Snapshot die Kurve schon enthält – ~10 min.
5. **R10-F2 / R10-F3** `tenor`-Alias in IRS/AMORT/BASIS/CAP; Dateinamen-Token gegen den Spaltensatz prüfen bzw. „Anderen Produkttyp wählen …“ im Fehlerdialog – ~30 min.

Erwartete Wirkung bei Umsetzung 1–5: UI/UX & Hotkeys ≈ 100, User Flows ≈ 100.

---

## 8. Artefakte

Basis: `/tmp/claude-0/-home-user-general/ba34afa7-bb32-5710-8abf-0fcec9f55ee0/scratchpad/r10-ui/`

- Skripte/Messwerte: `lib.mjs` (Helfer, Kontrast-Sweep, a11y-Audit; `OUT` auf `r10-ui`), R9-Skripte `verify.mjs` (39), `verify2.mjs` (12), `verify3.mjs` (6), `misc.mjs` (1), `flows.mjs` (37), `flows2.mjs` (16), `flows3.mjs` (4), `sweep.mjs` (7 Sammel-Checks über 128 Zustände), `lazy.mjs` (6); neu `r10a.mjs` (19), `r10b.mjs` (22), `r10c.mjs` (4), `r10d.mjs` (2), `r10e.mjs` (Chunk-Retry), `api-snap.mts` (API-Snapshot mit Envelope + `quotes`); `results-{v,v2,v3,misc,f,f2,f3,s,l,r10a,r10b,r10c,r10d}.json`, `*.log`, `vitest-web.log` (44 Dateien, 391 Tests, 2 Last-Timeouts), `vitest-r6r7.log` (isoliert 8/8 grün), `e2e.log` (E2E OK, 529), `preview.log`; `../vite-build-r10.log`
- Downloads: `r10-snap-pre.json`, `r10b-snap-pre.json`, `r10-snap-dkk.json` (mit `quotes`), `r10-snap-huf-noenv.json`, `r10-snap-rolled.json`, `r10-snap-badq{1,2,3,4}.json`, `api-snap-czk.json` (API mit Envelope + `quotes`), `f-snap-czk.json`, `f-snap-api-out.json`, `tmpl-0…10.csv`, `r10-notype-0…10.csv`, `deriva-import-vorlage-ccs.csv`, `FxForward-2026.csv`, `portfolio-basis.csv`, `api-*.csv`, `upload-1…7.csv`, `upload-6{b,c}.csv`, `kredit-cap-2026.csv`, `api-mixed.csv`, `r10-noType.csv`, `f-import*.csv`, `f-portfolio*.json`, `f-hedge-doc.md`
- PDFs (10): `report-print.pdf`, `doc-{termsheet,kid,suitability,confirmation}-print.pdf`, `hedge-print.pdf`, `curves-print.pdf`, `market-print.pdf`, `blotter-print.pdf`, `pricing-print.pdf`
- Sweep-Screenshots (128): `{1440,1024}-{dark,light}-{blotter,pricing,pricing-fxo,pricing-swpt,curves,curves-add,curves-add-currency,market,market-add-spot,market-add-vol,market-dkk,pricing-dkk,pricing-swpt-dkk,scenarios,report,compare,hedge,palette,help}.png`, `{1440,1024}-dark-editor-{irs,cap,swpt,fxf,fxo,basis,amort,imm,fxs,ccs,fra}.png`, `{1440,1024}-dark-doc-{termsheet,kid,suitability,confirmation,termsheet-customer}.png`, `{1440,1024}-dark-{export-menu,csv-errors,market-import-override,curves-import,curves-snapshot-tab,pricing-par-coverage,toast,valdate-popover,context-menu,onboarding}.png`
- Befunde/Belege: `r10-add-currency-{1024,1280,1440}.png` (R9-01 ✓), `r10-add-currency-import.png` (R9-03 ✓), `r10-fixing-removed.png` (R9-04 ✓ Fixings), `r10-restore-toast-{1440,1024}.png` (R9-F4 ✓), `r10-csv-type-dialog-{dark,light}.png`, `r10-csv-misdetect.png` (R10-F3), `r10-huf-noenv-import.png` (R9-F3 ✓ / R10-05), `r10-par-risk-import.png` (R10-F1), `r10-chunk-error.png`, `r10-chunk-recovered.png`, `f-add-currency-czk.png`, `f-curves-czk.png`, `f-pricing-czk.png`, `f-market-czk.png`, `f-pricing-swpt-czk.png`, `f-blotter-czk.png`, `f-report-czk.png`, `f-fresh-czk-import.png`, `f-api-snapshot-czk.png`, `f-blotter-templates.png`, `v2-after-reset.png`, `l-skeleton-scenarios.png`, `l-offline-reload.png`, `l-offline-first.png`
