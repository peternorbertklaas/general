# DERIVA – Review Runde 8: UI/UX & Hotkeys (Dim. 3) und User Flows (Dim. 4)

**Reviewer-Rolle:** Senior UX-Designer · Accessibility-Auditor · Trading-Desk-Power-User
**Datum:** 2026-09-04 · **Modus:** Re-Review only, keine Quellcode-Änderungen · **Baseline:** `review-ui-r7.md` (Runde 7: UI 98, Flows 97,5)

## 0. Prüfstand

| | |
|---|---|
| Repo-Stand | Branch `claude/derivatives-trading-platform-1arsyu`, HEAD `1cd266e`; Web-Fix-Commit `55aea60` („fix(web): UI- und Flow-Befunde Runde 7 behoben“). Web-Diff seit R7 (`f93e099`): 29 Dateien, +2.409/−176 (Quelle ohne Tests/E2E: 27 Dateien, +2.164/−167) – neu `lib/register.ts` (Währungs-/Index-/Paar-Listen aus Register + Markt), `lib/focus.ts` (`focusWhenPresent`), `hooks/useGridNav.ts` (Roving-Gitter), `components/echarts-lib.ts` (dynamischer Bibliotheks-Import); geändert `views/MarketView.tsx` (+704: `useRowNav`, `AddFxSpotForm` „+ Paar“, `AddVolSurfaceForm` „+ Fläche“, `VolCardReset`, `GridKeysHint`), `state/store.ts` (`ExtraCurve.fxSpot`, `extraCurveSpots`, `leaveImport` über `rebuildMarket`, Hedge-Undo mit `result`), `lib/quick-parser.ts` (`chooseIndex`, `missingCurveNote`, `unknownIndexError`, `normaliseIndexToken`), `components/TradeEditor.tsx` (`useRegisterOptions`), `components/EChart.tsx` (`loadChart`/`retryChart`), `lib/portfolio-io.ts` (CCS-Header `headers`), `lib/i18n.ts` (Reparaturhinweise), `styles/app.css` (`[role=gridcell]:focus-visible`), `PricingWorkspace.tsx` (`.seg.wrap`) |
| Bundle | `vite build` (vite 8.2.2/Rolldown) frisch: `index-BrADGROO.js` 115 KB (37,8 KB gz), `store-BzZVG8ko.js` 78,5 KB (23,7 KB gz), `core-Cr-FKUJ2.js` 255 KB (82,3 KB gz), `react-DLh8G0Wt.js` 193 KB (61 KB gz), `echarts-BDKLMkZV.js` 561 KB (185,4 KB gz, lazy; enthält jetzt `echarts-lib`), `MarketView` 45 KB (12,6 KB gz), `PricingWorkspace` 62,7 KB, `register` 1,9 KB, `EChartImpl` 0,75 KB, `sw.js` 4,9 KB; `scripts/size-limit.mjs`: **Initial-Load 220,6 kB gz (8 Dateien)**, „Service worker precache: 19 assets“, „Size budget OK“; ECharts wird nach dem Start per Idle-Prefetch geladen (`App.tsx:116`), nicht im Start-Pfad. Preview `vite preview --port 5011 --strictPort` |
| Unit-/E2E-Tests | `npx vitest run` in `apps/web`: **38 Dateien, 335 Tests** – ein Lauf mit 38 parallelen Workern hatte 2 Timeouts (10 s) in `App.r7.test.tsx` „R7-2 / R7-F1: + Paar / + Fläche“; die Datei isoliert: **5/5 grün in 11 s** – reine Lastfrage wie in R7, kein Abzug. `E2E_PORT=5012 PLAYWRIGHT_CHROMIUM_PATH=/opt/pw-browsers/chromium node e2e/smoke.mjs`: **E2E OK (437 checks)** |
| Browser-Audit | Playwright + Chromium (`/opt/pw-browsers/chromium`), Locale `de-DE`, Clipboard-Rechte, Viewports **1440×900** und **1024×768**, Dark + Light, Print-Emulation + `page.pdf()`, Offline (`context.setOffline`) in **frischem Browserkontext** (ein Online-Aufruf), CDP-Drosselung (1,5 Mbit/s, 250 ms), `page.route`-Abbruch von View- und ECharts-Chunk. Skripte in `…/scratchpad/r8-ui/`: `verify.mjs` (33 Checks: alle R7-Befunde + Randfälle), `probe2.mjs` (11: Zeilentabellen, Gitter-Commit, Hedge-Undo, CCS, sauberer Import-Pfad, Flächen über Import/Verlassen), `probe3.mjs` (10: Tabfolgen ab Skip-Link, „+ Paar“-Persistenz, ↵-Submit, JSON-Roundtrip, 11 Vorlagen, 213 Trades, Offline), `probe4.mjs` (4: Fokus nach Reset/`d`, Fixing-Select, „Portfolio als JSON“, Swaption-Editor DKK), `flows.mjs` (46: 6 Journeys), `sweep.mjs` (**112 Zustände**: 17 Views/Zustände × 2 Viewports × 2 Themes inkl. neu „+ Paar“-Formular, „+ Fläche“-Formular, Markt mit DKK-Flächen, DKK-Pricing; 11 Editoren, 4 Dokumente + Kundenmodus, Export-Menü, CSV-Dialog, Import-Modus, Toast, Stichtag-Popover, Kontextmenü, Onboarding; a11y + Kontrast + Überlauf + Textscan + Screenshot je Zustand; 10 Druck-PDFs), `lazy.mjs` (6). Messwerte `results-{v,p2,p3,p4,f,s,l}.json`, 145 Screenshots, 10 PDFs. Rot gelaufene Checks wurden einzeln nachgeprüft; Sonden-Artefakte (`active()`-Text an Buttons, OIS-1W-Quote bewegt IRS-0001 < 1 EUR wie in R7 dokumentiert, `data-testid="export-portfolio-json"` ist der Portfolio-*Report*, Toast-Tab ab `body` statt ab Skip-Link, `curves` als Array im Snapshot) sind aus den Befunden entfernt |
| Konsole | **Keine** JS-Fehler, keine Warnungen, keine fehlgeschlagenen Requests in allen Läufen (Ausnahme: die absichtlich abgebrochenen Chunks) |
| Tastatur | Chords `o t/g/k/c/p/r`, `x c/b`, `y i`, `y y`, `g …`, `n s/c/w/f/o/b/a/i/x/z/r`; Tab-Sequenzen ab Skip-Link (Blotter, Pricing, Kurven, Markt); Fixings-Tabelle `↓/PgDn/End/Home/↵/F2/Esc/Tab`; Vol-Gitter `←/→/↑/↓/End/Ctrl+End/↵/F2/Esc/Tab`; „+ Kurve“/„+ Paar“/„+ Fläche“ per `↵`; `Shift+P`, `Shift+K`, `Shift+T`, `t`, `d`, `Shift+D`, `Space`, `r`, `]`/`0`, `Alt+2`, `?`; `Ctrl+Z` auf Spot-Override, „+ Paar“, „+ Fläche“, Import, Verwerfen, Verlassen, Kurve anlegen/entfernen, Hedge-Reset |

Screenshot-Verzeichnis: `/tmp/claude-0/-home-user-general/ba34afa7-bb32-5710-8abf-0fcec9f55ee0/scratchpad/r8-ui/` (Übersicht Abschnitt 8).

---

## 1. Scores

### 1.1 UI/UX & Hotkeys: **98 / 100** (97,9; R1 62 → R2 88 → R3 91 → R4 95 → R5 96 → R6 98 → R7 98)

**Verifiziert behoben (Belege Abschnitt 2):** alle sechs R7-Befunde. Markt-View **44 Tabstopps im `main`** (R7: 489–510), Fixings-/FX-Fixings-/CDS-Tabellen und alle drei Vol-Gitter **je ein Tabstopp**, Fixings-Zeile nach **30 Tabs ab Skip-Link**, ein `Tab` verlässt die Tabelle (R7: 241); Gitter `→↓` → Zelle 1,1, `End` → 1,8, `Ctrl+End` → 10,8, `↵` → Input, `Esc` → Zelle, Maus-Klick in eine Zelle + `Esc` → Zelle (R7-01). Trade-Editor listet **DKK/DESTR** für den per Schnelleingabe angelegten Swap, Optionen „EUR, CHF, DKK, GBP, JPY, USD, NOK (ohne Kurve), PLN (ohne Kurve), SEK (ohne Kurve)“, Index „DESTR, CIBOR-3M (ohne Kurve), CIBOR-6M (ohne Kurve)“, Kollateral „DKK-CSA“; Währungswechsel EUR→DKK zieht den Float-Index auf DESTR und bewertet (PV 20.904), →NOK zeigt Feldhinweis + Fehler mit „+ Kurve“; FX-Editoren (Kauf-/Verkaufswährung DKK, Paar EURDKK), Swaption „DKK (ohne Vol-Cube – Fallback-Vol, „+ Fläche“ …)“, FRA-Index DESTR (R7-02). Fokus nach `n s`, `n c` (aus der Marktansicht) und Palette-`↵` auf **„Bezeichnung“**, `Esc` verlässt das Feld; „Kurve anlegen“ → **Tab der neuen Kurve**, „Abbrechen“ → „+ Kurve“ (R7-03). Key-Rate-Selektor bei FXO-0001/1440 px **umbrechend (2 Zeilen), rechter Rand 1.331 < Karte 1.424 px**, CCS/FXF/1024 innerhalb (R7-04). ECharts-Chunk-Ausfall: Karte „DIAGRAMM NICHT VERFÜGBAR“, „Erneut versuchen“ lädt `echarts-BDKLMkZV.js?retry=…` und rendert **alle** Diagramme der View (Kurven 1, Szenarien mit mehreren Karten → 0 Karten, Canvas da), Retry bei anhaltendem Ausfall stabil, Wiederbetreten per `g c` nach Netzrückkehr repariert ohne Klick, 0 Page-Errors (R7-05). Hedge-Reset → Toast-Undo (erster Tabstopp ab Skip-Link) und `Ctrl+Z` stellen **Ratio 99 + Verdict „✓ effektiv“ + Veraltet-Badge** wieder her, auch nach Reload (R7-06). **Sweep über 112 Zustände: 0 unbenannte Steuerelemente/Composites/Dialoge, 0 doppelte IDs, 0 positive Tabindizes, 0 Überschriftensprünge, 0 Tabellen ohne Namen, keine Seite scrollt horizontal, keine beschnittene Karte; 6.525 gemessene Textpaare, alle Textfarben ≥ 4,5:1** (einzige Werte < 4,5 wie in R7: Rail-Icon-Glyphen 18 px im aktiven Light-Tab 4,13:1 – Nicht-Text ≥ 3:1, Label ≥ 4,5:1; Logo-Glyphe auf Gradient = Mess-Artefakt). Druck: Report 1 Seite, KID/Geeignetheit/Confirmation 3, Termsheet 1, Hedge 1 (Einheit 3 px), Kurven 2, **Markt 3**, Blotter 1, Pricing 1, Rail/Status/Topbar ausgeblendet. Lazy: Kaltstart Shell 323 ms, `g s` 406 ms, gedrosselt Skeleton (`aria-busy`, `aria-live=polite`) 936 ms ohne Doppel-Platzhalter, warme Chords 16–131 ms, `Alt+2` 49 ms, Reduced Motion respektiert. Offline nach dem **ersten** Besuch: 21 Cache-Einträge (inkl. `echarts`, `register`), alle 8 Views mit Diagrammen, Amortisations-Editor, „+ Kurve“-, „+ Fläche“- und „+ Paar“-Formulare, Termsheet.

**Abzüge (kumuliert ≈ −2,1):**
- Niedrig −0,8: **Zeilentabellen des Marktes (Fixings, FX-Fixings, CDS): nur das erste Steuerelement je Zeile ist per Tastatur erreichbar.** `↵`/`F2` fokussieren das Index-Select; `Tab` aus dem Select verlässt die Tabelle (→ „weitere 60 anzeigen“), `←/→` im Select **ändern den Index** (Markt „modifiziert“, Zeile remountet, Fokus auf `body`) – Datum, Wert und „Entfernen“ sind ohne Maus nicht bedienbar; vor R7 waren es 240 Stopps, jetzt 1 unerreichbar (R8-01).
- Niedrig −0,4: **FX-Vol-Gitter verliert seinen Tabstopp nach Wechsel auf eine kleinere Fläche** (EUR/USD 8×5, Zelle 7,4 → EUR/GBP 6×3: 0 Zellen mit `tabIndex=0`, Tab-Folge überspringt das Gitter) (R8-02).
- Kosmetisch −0,3: **`↵`-Commit in Gitterzelle, Fixing-Wert und Amortisationsfeld setzt den Fokus auf `body`** (`NumInput` blurt bei Enter); `Shift+Tab` führt zwar zur aktiven Zelle zurück, der Hinweistext verspricht aber „Esc zurück zur Zelle“ (R8-03).
- Kosmetisch −0,2: **„+ Paar“: `↵` im Kurs-Feld legt den Spot nicht an** (Fokus `body`, Formular offen), `↵` im Paar-Feld schon (R8-04).
- Kosmetisch −0,2: **Fokus auf `body` nach `d` (Duplikat wird im Pricing geöffnet) und nach bestätigtem Hedge-Reset** – das R7-03-Muster ist dort nicht angewandt (R8-05).
- Kosmetisch −0,2: **Reparaturhinweis passt nicht zum Zustand:** unter importiertem Snapshot heißt es „… in der Kurvenansicht mit „+ Kurve“ eine DKK-Kurve anlegen“, „+ Kurve“ ist dort aber gesperrt (`IMPORT_LOCK`); der eigentliche Weg „Zum Sample-Markt“ steht nur im Tooltip des gesperrten Buttons (R8-06).

### 1.2 User Flows: **98,3 / 100** (R1 57 → R2 82 → R3 94 → R4 98 → R5 97 → R6 98 → R7 97,5)

- (a) **Indikation im Kundengespräch: sehr gut, vollständig per Tastatur** – `Ctrl+K` → `collar 7y 3,5/1,5 6m @Kunde GmbH` → `↵` in **311 ms** bis PV 48.973; `Shift+P` → „Zero-Cost-Collar“, PV 2 EUR; `y i` Indikationstext mit Kontrahent/Stichtag; `g s` 450 ms, Heatmap `→ ↵` → What-if, `o t` mit Stress-Banner, **392 ms**, Fokusfalle, `Esc` → `main#main`; `o k`, `o g` (§ 64 Abs. 4 WpHG), `o c`, `o r` Hashes + MaRisk; `Shift+K` ohne CVA/DVA/Marge. Kein Abzug.
- (b) **Treasurer: CSV-Import mit allen 11 Vorlagen → Fehler → Kollision → Portfolio: sehr gut** – 11 Vorlagen, jede importiert ihre Beispielzeile **ohne Dialog, 0 Fehler-Badges**; **CCS-Vorlage mit API-Spalten** `type;id;name;counterparty;book;pair;domesticNotional;spread;fixedRate;fxSpot;domesticPayReceive;effectiveDate;maturity;collateralCurrency;status`, importiert und bewertet (Leg 1 EUR/ESTR, Leg 2 USD/SOFR 11.700.000, CSA USD), die alten Spaltennamen importieren weiterhin (Markt R7-5 ✓); defekte Datei → „4 Zeilen übersprungen“, „1 gültige Zeile importieren“; Kollision → Überspringen/Ersetzen/Umbenennen; `o p`; **„Portfolio als JSON“ (Array, 13 Trades) → Import → Kollisionsdialog → Umbenennen → 26 Trades, 0 Fehler**; ein versehentlich importierter Portfolio-*Report* wird deutsch abgewiesen („Datei enthält keine Trade-Liste – erwartet wird ein Array von Trades oder ein Objekt mit „trades“ (Export „Portfolio als JSON“)“). Kein Abzug.
- (c) **Prüfer: Marktdaten → What-if → Vergleich → Snapshot → Overrides → Undo: sehr gut** – Quote `↑↑↵` → „modifiziert“ (IRS-0001 reagiert auf die OIS-1W-Quote wie in R7 < 1 EUR – kein Fehler); `]` in **44 ms**; Vergleich unter What-if; `Ctrl+Z`. Snapshot Export → Quote → Import → **ID `3803d0191a972626` und Report-Hash identisch**; Spot-Override 1,25 → „modifiziert“, Report-Label, Export, Reload, Undo → Hash zurück; Undo des Imports, Stichtags-Verwerfen (Dialog, rückgängig), „Zum Sample-Markt“ rückgängig inkl. Vol-Override 77; Snapshot 30.10.2026 setzt den Stichtag, überlebt den Reload; ungültige Snapshots deutsch, „Vol-Typ fehlt“. Kein Abzug.
- (d) **Hedge Accounting: sehr gut** – `↵` auf „Effektivität testen“ → „✓ effektiv“; Ratio `↓` → Veraltet-Badge, „Erneut testen“, Markdown „veraltet“ ohne ISO/camelCase; nach Reload Ergebnis + Ratio 99 + Badge; **Reset → Toast-Undo/`Ctrl+Z` → Ratio 99, Verdict, Badge zurück, auch nach Reload** (R7-06 ✓); CAP Innerer Wert + Vol einfrieren → CoH-Karte. Kein Abzug.
- (e) **„+ Kurve“ DKK/NOK → „+ Paar“ → „+ Fläche“ → Swap/Swaption/FX-Option in der neuen Währung → Import → Verlassen → Reload: gut mit zwei Lücken** – „+ Kurve“ per `↵` (1,98 s Bedienzeit inkl. Formular), DESTR-Default, Parser-Fehler bei „abc“, Toast mit Undo, **Fokus auf dem neuen Tab**; **`irs dkk 5y pay 3% 10m` → Vorschau „· DESTR (Kurve vorhanden; CIBOR-6M ohne Kurve)“ → PV 12.391, Par, `y i`, Szenarien, Report mit DKK-DESTR** (R7-F2 ✓ für Swap/Cap/FRA; `cap dkk`, `fra dkk` folgen, `… cibor6m` → „⚠ Kurve DKK-CIBOR-6M fehlt“, `nibor9m` → „Unbekannter Index …“, `euribor3m` → EURIBOR-3M); NOK-NOWA **ohne Spot** → NOK-Swap „FX-Spot fehlt … mit „+ Paar“ ergänzen“ → **„+ Paar“ schlägt EURNOK vor**, 11,7 → PV −31.209; NIBOR-6M-Kurve („Dual-Curve“, Spot nicht erneut gefragt) → `irs nok` NIBOR-6M ohne Hinweis; **„+ Fläche“** schlägt DKK vor (Swaption-Cube 11×9 aus EUR-Achsen, flach 60 bp), Caplet DKK-DESTR, FX EUR/DKK (Werte aus EURUSD), Badges „angelegt“, „Entfernen“ + `Ctrl+Z`; `fxo eurdkk call 7.5 3m 5m` → **PV 59.254 ohne Level-3-Fallback**; Reload behält Kurven, Spots, Flächen; Snapshot enthält DKK/NOK-Kurven, EURDKK/EURNOK, Swaption DKK, FX EURDKK, Re-Import → identische ID; **Prüfer-Snapshot ohne DKK → Fehler nennen den Weg → „Zum Sample-Markt“ → Reload: DKK-Tab aktiv, EUR/DKK-Spot da, DKK-/NOK-Swaps bewertet** (R7-F1 ✓ für Kurven-Spots); Stichtags-Verwerfen ebenso. **Reibung −1,0 (R8-F1):** die **Swaption** in der neuen Währung bleibt unbewertbar – `swpt dkk 1y5y payer 3% 10m` baut den Underlying-Swap mit CIBOR-6M (Konvention), Vorschau **ohne Warnung**, Pricing „Kurve DKK-CIBOR-6M nicht im Markt-Snapshot“, `Shift+P` „Kein Par-Wert“, der Swaption-Editor bietet keinen Index (nur „Underlying-Swap folgt den Marktkonventionen“); erst „+ Kurve“ DKK-CIBOR-6M hilft (PV 27.486). **Reibung −0,7 (R8-F2):** **„+ Paar“-Spots und „+ Fläche“-Flächen überleben Import → Verlassen nicht** (Kurven und ihre Spots tun es): SEK-Kurve ohne Spot + „+ Paar“ EUR/SEK → Import des Prüfer-Snapshots (Toast „vorherige Marktänderungen verworfen“) → „Zum Sample-Markt“ → SEK-Trade „FX-Spot fehlt“, nach Reload dauerhaft; DKK-Cube und EUR/DKK-Fläche ebenso weg, die EUR/DKK-Option fällt auf „Fallback-Vol 8 % (IFRS-13 Level 3)“ zurück – 2× `Ctrl+Z` direkt danach stellt alles her, nach Reload nicht mehr.
- (f) **Offline / Lazy / Persistenz: sehr gut** – frischer Browser, ein Aufruf: Cache `deriva-shell-265b82fcb5a3` mit 21 Einträgen, Offline-Reload mit Chip „⚠ offline – lokaler Bestand“, alle Views + Charts + Editor + alle drei „+“-Formulare + Termsheet, 0 Page-Errors; Theme, View, Stichtag 30.10.2026, Feldänderung überleben den Reload („Bestand aus lokalem Speicher geladen (13 Trades) · Zurücksetzen“); Onboarding deutsch. Chunk-Ausfall View + Bibliothek mit Recovery (R7-05 ✓).
- Performance (213 Trades): Import 257 ms, `j`×10 329 ms, Palette 154 ms, `]` 177 ms, Szenarien 928 ms, Markt 451 ms, Portfolio-Report 566 ms, Reload 856 ms, localStorage 118 KB; Einzeltrade: DKK-Swap per Schnelleingabe 682 ms, Termsheet 392 ms.

---

## 2. Status der Runde-7-Befunde

Legende: ✅ behoben · 🔶 teilweise · ❌ offen. Belege = Feld in `results-v.json` (`verify.mjs`), `results-p2/p3/p4.json`, `results-f.json` (`flows.mjs`), `results-s.json` (`sweep.mjs`) oder Screenshot.

| # | Titel (R7) | Status | Beleg / Rest |
|---|---|---|---|
| R7-01 | Markt-View ≈ 500 Tabstopps, Fixings-Editor 241 Tabs zum Verlassen | ✅ | `MarketView.tsx:58` `useRowNav` (`useTableNav` + `F2`/Esc-Capture), 14× `tabIndex={-1}` an Zeilen-Controls, `useGridNav` an Swaption-/Caplet-/FX-Gitter (`:120/:202/:298`), `GridKeysHint`; `v.m {total 44, fixings 1, swaption 1, caplet 1, fxVol 1, total1024 44}`, `p2.fxf.stops 1`, `p2.cds.stops 1`, `p3.tabsMarket` **30 Stopps bis zur Fixings-Zeile**, `afterFixingsTab fixings-more`; Fixings `f1 ≠ f0, PgDn → 11, End → 59, ↵ → „Index Fixing 1323“, Esc → TR, F2 → Select`; Gitter `g11 „1,1“, End „1,8“, Ctrl+End „10,8“, ↵ → „Swaption-Vol 20Y × 30Y“, Esc → gridcell 10,8`, `mouse {Input → Esc → gridcell}`; nach „weitere anzeigen“ (120 Zeilen) und Filter weiterhin 1 Stopp; Sweep `1440-dark-market.tabStops 65` (Seite gesamt, R7: 510) (`v-market-roving.png`). **Rest → R8-01/R8-02/R8-03:** nur erstes Control der Zeile erreichbar; FX-Gitter ohne Stopp nach Flächenwechsel; ↵-Commit → `body` |
| R7-02 | Editor-Listen fest auf G5 (DKK-Swap zeigt „EUR / EURIBOR-3M“) | ✅ | `lib/register.ts` (`currencyOptions`, `indexOptions`, `defaultIndexFor`, `fxCurrencies`, `knownPairs`), `TradeEditor.tsx:305` `useRegisterOptions`, `setSwapCurrency`; `v.j.editor {ccy DKK, idx DESTR, ccyOpts [EUR, CHF, DKK, GBP, JPY, USD, NOK (ohne Kurve), PLN (ohne Kurve), SEK (ohne Kurve)], idxOpts [DESTR, CIBOR-3M (ohne Kurve), CIBOR-6M (ohne Kurve)], coll […, DKK-CSA]}`, `v.j.switch {EUR→DKK: idx DESTR, PV 20.904, err 0; →NOK: hint „Keine Diskontkurve für NOK – „+ Kurve“ …“, err „… eine NOK-Kurve anlegen“}`, `v.j.fxf.buy [EUR, CHF, DKK, GBP, JPY, USD]`, `v.j.fxo.pairs [… EURDKK …]`, `v.j.swpt.ccys [… „DKK (ohne Vol-Cube – Fallback-Vol, „+ Fläche“ in der Marktansicht)“]`, `v.j.fra.idx DESTR` (`v-editor-dkk.png`, `v-editor-nok.png`) |
| R7-03 | Fokus nach `n s`/Palette auf `body`, „Kurve anlegen“ → `body` | ✅ | `lib/focus.ts` `focusWhenPresent`/`focusEditorField`, `App.tsx:277`, `CommandPalette.tsx:201`, `CurvesView.tsx:548-549`; `v.nf {afterChord Bezeichnung, afterChordFromMarket Bezeichnung, afterPalette Bezeichnung, afterEsc BODY}`, `v.af {focusAfterSubmit curve-tab-SEK-SWESTR, focusAfterCancel add-curve}`, `f.ac.after.focus curve-tab-DKK-DESTR`, `f.ac.dkkTrade.focus Bezeichnung`. **Rest → R8-05:** `d` und Hedge-Reset weiterhin `body` |
| R7-04 | Key-Rate-Selektor läuft 12 px über die Karte | ✅ | `PricingWorkspace.tsx:403` `.seg.wrap`; `v.kr {wrap „wrap“, n 5, rows 2, cardRight 1424, maxRight 1331; CCS −104 px, FXF −93 px, 1024 −91 px}` (`v-keyrate-fxo.png`) |
| R7-05 | „Erneut versuchen“ bei ECharts-Chunk wirkungslos | ✅ | `components/echarts-lib.ts` + `EChart.tsx` `loadChart`/`retryChart` (`chunkUrlOf` + `?retry=`), `lazy.ts` `mounted`-Set (ein Retry rendert alle Instanzen), `vite.config.ts` `manualChunks`; `v.ec {attempts 5, cards 1, btns [Neu laden, Erneut versuchen], retryBlocked {cards 1, canvas 0}, afterRetry {urls […?retry=…], cards 0, canvas 1}, multi (Szenarien) {cards 1 → afterRetry cards 0, canvas 1}, nav {cards 1 → back cards 0, canvas 1}, errs []}` (`v-echarts-failure.png`) |
| R7-06 | Hedge-Reset-Undo ohne Testergebnis | ✅ | `store.ts:142` `UndoEntry kind:"hedge"` mit `result`, `:1427` Undo schreibt `hedgeResults`, `removeHedgeRelationship` merkt `prevResult`; `p2.hr {tab1 „Rückgängig (Ctrl+Z)“, afterUndo {ratio 99, verdict 1 „✓ effektiv“, stale 1, btn „Erneut testen“}, afterReload {verdict 1, ratio 99, stale 1}, ctrlZ {verdict 1, ratio 99}}`; E2E-Checks R7-06 |
| R7-F1 | „+ Kurve“-Spot geht bei Import/Verlassen/Reload verloren, kein „+ Paar“ | 🔶 | `store.ts:185` `ExtraCurve.fxSpot`, `extraCurveSpots` in `buildMarket`, `leaveImport` über `rebuildMarket` (`:1858`), Persistenz auch im Import-Modus (`:2009`), `MarketView.tsx:705` `AddFxSpotForm` „+ Paar“ mit Vorschlag; `v.j.leave {spotRow 1, tab enabled, badges 0}`, `leaveReload {badges 0, spotRow 1}`, `dateDiscard.badges 0`, `snap {spot 7.46, reimport id identisch}`, `p2.f1 {imp.rowBadge 1 „… „+ Kurve“ eine DKK-Kurve anlegen“, leave.n 0}`, `f.ac.pre.leave.spots [… EURDKK, EURNOK]`, `f.ac.nok.pair {suggestion EURNOK → PV −31.209}`; „+ Paar“ unter Import = Override mit Undo (`v.j.pairImp`), Validierungstexte deutsch (`v.j.pairVal`). **Rest → R8-F2:** „+ Paar“-Spots (Quote-Set) und „+ Fläche“-Flächen (`volSurfaces`) werden von `importSnapshot` (`store.ts:1819/:1822` `quotes: cloneQuotes(SAMPLE_QUOTES)`, `volSurfaces: {}`) verworfen und kommen nach „Zum Sample-Markt“ nicht zurück (`p3.pp.leave.badges „FX-Spot fehlt … SEKEUR“`, `p3.pp.afterReload.n 1`; `p2.vs.leave.swptBtns` ohne DKK, `fxPairs` ohne EUR/DKK, `fxo.hint „… Fallback-Vol 8 % (IFRS-13 Level 3) …“`, `permanent.afterUndo` ohne DKK) |
| R7-F2 | Schnelleingabe `irs dkk …` wählt CIBOR-6M ohne Kurve | 🔶 | `quick-parser.ts:698` `chooseIndex` (Konvention → OIS → beliebiger Index mit Kurve), `missingCurveNote`, `unknownIndexError`, `register.ts` `normaliseIndexToken`; `v.j.qe {irsDefault „· DESTR (Kurve vorhanden; CIBOR-6M ohne Kurve)“ ohne ⚠, cap DESTR, fra DESTR, cibor „⚠ Kurve DKK-CIBOR-6M fehlt – … „+ Kurve“ anlegen“, badIdx „Unbekannter Index „nibor9m“ – für DKK registriert: CIBOR-3M, CIBOR-6M, DESTR“, eurNorm EURIBOR-3M}`, `v.j.trade {pv 12.391, err 0}`, `p2.nok {q1 „NOWA (Kurve vorhanden; NIBOR-6M ohne Kurve)“, q2 ohne Hinweis}`; `i18n.ts:284-291` Reparaturhinweise. **Rest → R8-F1:** Swaption-Zweig (`quick-parser.ts:781` `makeSwaption` ohne Index) und Swaption-Editor (`TradeEditor.tsx:1161/:1187`) nutzen die Konvention – `p2.vs.swpt {pv „–“, err „Kurve DKK-CIBOR-6M nicht im Markt-Snapshot“, underlying [CIBOR-6M]}`, `p4.sw.indexSelects 0`, Workaround `p4.sw.cibor.trade {pv 27.486}` |
| Markt R7-2 | Vol-Flächen für neue Währungen/Paare | ✅ | `MarketView.tsx:470` `AddVolSurfaceForm` „+ Fläche“ (`buildVolSurface`, Validierung + Plausibilität), `VolCardReset` „Entfernen“, Badge „angelegt“; `v.j.vol {kind swaption, ccy DKK, ccyOpts [DKK], template EUR, preview „DKK: 11 × 9 (Expiry × Tenor)“, swpt.toast „Swaption-Cube DKK angelegt (flach 60 bp)“, badge „angelegt“, focus add-vol; capForm {ccy DKK, idx DESTR} → „Caplet-Fläche DKK-DESTR angelegt“; fxForm {pair EURDKK} → „FX-Vol-Fläche EUR/DKK angelegt (Werte aus EURUSD)“}`, `v.j.qe2 {swpt/fxo ohne ⚠, fxoTrade {pv 55.609, kein Level 3}, capTrade 21.467}`, `v.j.rm {btn „Entfernen“ → ohne DKK → Ctrl+Z mit DKK}`, `p2.volForm.fxProblem „Alle Spot-Paare haben eine FX-Vol-Fläche – … „+ Paar“ anlegen“`; Reload behält die Flächen (`v.j.persist`) (`v-add-vol-form.png`, `v-market-dkk-surfaces.png`) |
| Markt R7-5 | CCS-CSV-Spalten wie die API | ✅ | `portfolio-io.ts:187` `headers`, `csvHeaderName`, Aliasse `domesticnotional`/`fixedrate`/`domesticpayreceive`/`effectivedate`/`collateralcurrency`; `p2.ccs.lines[0] „type;id;name;counterparty;book;pair;domesticNotional;spread;fixedRate;fxSpot;domesticPayReceive;effectiveDate;maturity;collateralCurrency;status“`, `badges.n 0`, Editor Leg 1 EUR/ESTR, Leg 2 USD/SOFR 11.700.000, CSA USD; `v.ccs.legacy.added 2` (alte Namen); `p3.templates.imports` 11× `added 1, dlg 0, err 0` (`p2-ccs-template.png`, `p3-blotter-templates.png`) |

Zusätzlich verifiziert (kein R7-Befund): Import-Modus-Sperren (R6-04), „Vol-Typ fehlt“ (R6-05), CSV-Dialog (R6-06), Fokus nach `Esc` aus Chord-Dokument (R6-03), Amortisationsplan-Roving (R6-02), Undo-Semantik Import/Verwerfen/Verlassen (R6-F2), Spot-Override (R6-F1), Hedge-Ergebnis nach Reload (R5-F3).

---

## 3. Neue Befunde (Runde 8)

Schweregrade wie in R1–R7. Reproduktion gegen das Preview-Bundle; Belege in `results-*.json` (Feldname) bzw. Screenshot.

| # | Schwere | Wo | Was ist falsch (Beleg) | Konkreter Fix |
|---|---|---|---|---|
| R8-01 | Niedrig (Tastatur/Barrierefreiheit) | `views/MarketView.tsx:58-77` `useRowNav` (`onEnter` fokussiert nur das erste Control), Zeilen-Controls `tabIndex={-1}` (Fixings `:1290-1332`, FX-Fixings `:903-936`, CDS `:1074-1113`), Zeilen-`key` `${f.index}-${f.date}-${i}` (`:1290`); Doku `03-ui-konzept-und-hotkeys.md:116` verspricht „Index-Select, Datum, Wert“ | Die Roving-Umstellung macht **Datum, Wert und „Entfernen“ einer Fixing-Zeile ohne Maus unerreichbar**: `↵`/`F2` → „Index Fixing 1323“; `Tab` aus dem Select → **`fixings-more` (Tabelle verlassen)**; `→` im Select **ändert den Index** (Chip „modifiziert“, kein Toast), die Zeile remountet (neuer `key`) und der Fokus fällt auf **`body`** (`p2.fx {tabFromSelect {focus fixings-more, inTable false}, arrowInSelect {focus BODY, chip modifiziert}, rowControls 5× tabIndex −1}`, `p4.fixSel {focus BODY, sameNode false}`); identisch bei FX-Fixings (`p2.fxf.tabFromFirst add-vol`) und CDS (`p2.cds.tabFromFirst „+ EURIBOR-6M heute“`). Mit der Maus geht alles, in R7 waren alle Controls Tabstopps – die Bedienbarkeit per Tastatur ist für 3 von 4 Controls verloren gegangen. | Zeile als Zelle-Roving wie `useGridNav` (jede `td` mit Control ist `gridcell`, `←/→` wechseln die Zelle, `↵`/`F2` öffnen das Control) **oder** im Zeilenmodus `Tab`/`Shift+Tab` innerhalb der Zeile zyklisch über die Controls laufen lassen (`onKeyDown` in `useRowNav`: `Tab` im Control → nächstes Control derselben `tr`, `Esc` → Zeile); Zeilen-`key` auf einen stabilen Schlüssel (Index im Array oder eigene ID) statt `index-date`, damit ein Select-Wechsel nicht remountet; E2E: „Wert Fixing“ per Tastatur ändern. |
| R8-02 | Niedrig (Tastatur) | `hooks/useGridNav.ts:39/:133` (`active` nicht auf die aktuelle Zeilen-/Spaltenzahl geklemmt), `MarketView.tsx:1704` `<FxVolCard id={fxId} …>` ohne `key` (Zustand überlebt den Paarwechsel) | EUR/USD-Smile (8 Verfälle × 5 Spalten) → Zelle 7,4 (`Ctrl+End`) → Paar EUR/GBP (6 × 3): **0 Zellen mit `tabIndex=0`, 0 Stopps im Gitter**, die Tab-Folge ab dem Paar-Selektor springt direkt zu den nächsten Paar-Buttons (`v.m.fxSwitch {rows 6, cols 3, stops 0, activeCells 0, tabSeq [EUR/CHF, GBP/USD, …]}`, `v-market-grid-switch.png`); zurück zu EUR/USD → wieder 1 Stopp. Swaption-/Caplet-Flächen sind gleich groß (11×9 / 9×8) und daher nicht betroffen; „+ Fläche“ kopiert Achsen. | In `cellProps` bzw. beim Rendern `active` auf `min(r, rows−1)`/`min(c, cols−1)` klemmen (wie `useTableNav.rowProps` mit `fallback`), oder `key={id}` an `FxVolCard`/`SwaptionVolCard`/`CapletVolCard`, damit der Gitterzustand je Fläche neu startet; Test in `review-r7.test.tsx`. |
| R8-03 | Kosmetisch (Fokus) | `components/NumInput.tsx:167` (`↵` → `commit` + `blur()`), `useGridNav.onKeyDownCapture` behandelt nur `Escape`; Hinweistexte `GridKeysHint`/Fixings „Esc zurück zur Zelle/Zeile“ | Wert per Tastatur ändern und mit `↵` bestätigen setzt den Fokus in allen Roving-Tabellen auf **`body`**: Swaption-Gitter (`p2.grid {enter „Swaption-Vol 4W × 2Y“, afterCommit BODY, edited 1}`), Fixing-Wert (`p2.fx.wertCommit {focus BODY, modified 1}`), Amortisationsplan (`p2.amort.afterCommit BODY`); `Shift+Tab` führt zur (noch aktiven) Zelle zurück (`p2.grid.shiftTab {role gridcell, rc 0,1}`), aber der Hinweis nennt nur `Esc`. Bei einer Serie von Vol-Edits muss der Power-User nach jedem `↵` zurücknavigieren. | In `useGridNav.onKeyDownCapture` und `useRowNav.onKeyDownCapture` auch `Enter` behandeln: nach dem Commit (`setTimeout 0`) die Zelle/Zeile fokussieren; alternativ `NumInput` um `onCommit`-Callback erweitern und dort `cell.focus()`; Hinweistext „↵ übernimmt und kehrt zur Zelle zurück“. |
| R8-04 | Kosmetisch (Tastatur) | `MarketView.tsx:746` (`onKeyDown Enter → submit` nur am Paar-Feld), Kurs-`NumInput` (`:763`) ohne Submit | „+ Paar“ per Tastatur: Paar-Vorschlag EURSEK steht schon, `Tab` → Kurs `11,2` → **`↵` legt nichts an** (Formular bleibt offen, Fokus `body`, `p3.pp.rateEnter {row 0, formOpen 1, focus BODY}`); `↵` im Paar-Feld legt an (`pairEnter.row 1`). Analog legt `↵` im Spot-Feld von „+ Kurve“ nicht an (`p3.pp.curveEnter.tab 0` – dort wegen des Quotes-Textareas vertretbar). | `onCommit`/`onKeyDown Enter` am Kurs-Feld → `submit()`; Fokus danach auf „+ Paar“ (bereits vorhanden). |
| R8-05 | Kosmetisch (Fokus) | `App.tsx` Aktion `trade.duplicate` (`d`, öffnet die Kopie im Pricing) ohne `focusEditorField`; `HedgeView.tsx:454` „Zurücksetzen“ verschwindet nach der Bestätigung | `d` auf IRS-0001 → Toast „Dupliziert: IRS-0003“, Pricing zeigt die Kopie, **Fokus `body`** (`p4.dup {crumb „/ Pricing“, focus BODY, id IRS-0003}`); Hedge-Reset per `↵` + Bestätigung → Button weg, **Fokus `body`**, nächster `Tab` → „⤓ Dokumentation“ (`p4.hedgeResetFocus.after {focus BODY, resetBtnStill 0}`). | `void focusEditorField()` nach `duplicate`; nach dem Reset `focusWhenPresent('[data-testid="hedge-test"]')` (oder den Toast-Undo-Button). |
| R8-06 | Kosmetisch (Fehlertext) | `lib/i18n.ts:286-291` (Reparaturhinweis unabhängig vom `marketSource`), `CurvesView.tsx:594` „+ Kurve“ `disabled` unter Import (`IMPORT_LOCK`) | Unter importiertem Snapshot ohne DKK-Kurve sagt Blotter-Badge und Pricing-Fehler „… in der Kurvenansicht mit „+ Kurve“ eine DKK-Kurve anlegen“ – dort ist „+ Kurve“ **gesperrt**; den passenden Weg („Zum Sample-Markt“ bzw. Snapshot mit Kurve importieren) nennt nur der Tooltip des gesperrten Buttons (`p2.f1.imp {badges „… „+ Kurve“ …“, addCurve.disabled true, title „Kurven stammen aus dem importierten Snapshot … („Zum Sample-Markt“ …)“, hintMatchesState false}`, `p2-import-dkk-error.png`). | `translateCoreMessage` mit Kontext (`marketSource === "import"` → „… der importierte Snapshot enthält keine DKK-Kurve – Snapshot mit Kurve importieren oder „Zum Sample-Markt“ wechseln“); im Import-Modus die Zusatzkurven-Tabs mit Tooltip „nach „Zum Sample-Markt“ wieder aktiv“. |

### Flow-Befunde

| # | Schwere | Wo | Was ist falsch (Beleg) | Konkreter Fix |
|---|---|---|---|---|
| R8-F1 | Niedrig (Erlernbarkeit/Fehlerpfad, Rest von R7-F2) | `lib/quick-parser.ts:742-792` Swaption-Zweig (`makeSwaption({ currency … })` ohne Index, kein `chooseIndex`/`missingCurveNote`), `components/TradeEditor.tsx:1161` `setCurrency` (Underlying aus Konventionen), Hint `:1187` „Underlying-Swap folgt den Marktkonventionen“ | Nach „+ Kurve“ DKK-DESTR **und** „+ Fläche“ DKK-Cube: `swpt dkk 1y5y payer 3% 10m` → Vorschau „Payer-Swaption DKK 1Yx5Y @ 3,000 % · Nominal 10.000.000“ **ohne Warnung** → Trade „**nicht bewertet** – Kurve DKK-CIBOR-6M nicht im Markt-Snapshot – … „+ Kurve“ anlegen“, `Shift+P` „Kein Par-Wert … (Bewertung fehlgeschlagen)“ (`p2.vs.swpt {pv „–“, underlying [CIBOR-6M]}`, `f.ac.swptTrade`, `p2-swpt-dkk.png`); Editor `n w` → Währung „DKK (Vol-Cube)“ → derselbe Fehler, **kein Index-Feld** (`p4.sw {indexSelects 0, hint „Underlying-Swap folgt den Marktkonventionen der Währung“}`, `p4-swpt-dkk-editor.png`). Erst „+ Kurve“ DKK-CIBOR-6M macht die Swaption bewertbar (`p4.sw.cibor.trade.pv 27.486`) – der Nutzer erfährt das nur über den Umweg. Swap/Cap/FRA sind seit R7 kuratiert (DESTR, Hinweis in der Vorschau). | Im Swaption-Zweig `chooseIndex(ccy, opts)` verwenden und den Index an `makeSwaption` (bzw. den Underlying-Builder) durchreichen, Vorschau „· Underlying DESTR (Kurve vorhanden; CIBOR-6M ohne Kurve)“; im Editor `setCurrency` mit `reg.defaultIndex(ccy)` und ein Feld „Underlying-Index“ (Optionen `reg.index(ccy)`); Warnung `CURVE_NOT_FOUND` im Swaption-Kontext um „oder Underlying-Index im Editor wechseln“ ergänzen; E2E `swpt dkk …` nach „+ Kurve“. |
| R8-F2 | Niedrig (Reproduzierbarkeit, Rest von R7-F1) | `state/store.ts:1803-1822` `importSnapshot` (`quotes: cloneQuotes(SAMPLE_QUOTES)`, `volSurfaces: {}`), `leaveImport` `:1858` baut aus `get().quotes` (jetzt Sample) + `extraCurves`; „+ Paar“ im Sample-Modus schreibt in `quotes.fxSpots`, „+ Fläche“ in `volSurfaces` | Die neuen Reparaturwege werden vom Import wieder verworfen, während die Kurve (und ihr Spot) bleibt: **„+ Paar“** EUR/SEK für eine SEK-Kurve ohne Spot → SEK-Swap PV 8.253 → Prüfer-Snapshot importieren (Toast „… vorherige Marktänderungen verworfen (Rückgängig stellt sie wieder her)“) → „Zum Sample-Markt“ → **„FX-Spot fehlt: Kein FX-Spot für SEKEUR verfügbar“**, nach Reload dauerhaft (`p3.pp {leave.spots ohne EURSEK, leave.badges 1, afterReload.n 1}`, `p3-after-leave-sek.png`); **„+ Fläche“** DKK-Cube + EUR/DKK-Smile → Import → Verlassen → **beide weg**, die EUR/DKK-Option fällt still auf „Fallback-Vol 8 % (IFRS-13 Level 3)“ zurück (PV 59.254 zufällig gleich, Hinweis im Editor) (`p2.vs.leave {swptBtns ohne DKK, fxPairs ohne EUR/DKK, fxo.hint „Keine FX-Vol-Fläche für EUR/DKK …“}`, `p2-fxo-after-leave.png`); `Ctrl+Z` ×2 direkt danach stellt alles her (`p2.vs.undo2`), nach einem Reload zwischen Import und Verlassen nicht mehr (`p2.vs.permanent.afterUndo` ohne DKK). Der Treasurer, der einen Prüfer-Snapshot lädt und wieder verlässt, verliert die für DKK/SEK angelegten Spots und Flächen. | Analog zu R7-F1: „+ Paar“-Spots und „+ Fläche“-Flächen als **strukturelle Zusätze** persistieren (z. B. `extraSpots`/`extraVolSurfaces` neben `extraCurves`, in `rebuildMarket` gemischt, von `importSnapshot` unberührt) statt als Quote-/Vol-Edit; oder `importSnapshot` lässt `quotes`/`volSurfaces` unverändert und wechselt nur `marketSource` (die Sample-Änderungen bleiben latent und kommen mit „Zum Sample-Markt“ zurück); Import-Toast konkret („… verworfen: Spot EUR/SEK, Swaption-Cube DKK, FX-Fläche EUR/DKK“); E2E: „+ Paar“/„+ Fläche“ → Import → Verlassen → Reload → Trade bewertet ohne Level 3. |

Ohne Abzug notiert: R8-06 (Fehlertext unter Import) wirkt auch als Flow-Reibung, ist aber dieselbe Ursache wie in Dim. 3 (kein Doppelabzug). `data-testid="export-portfolio-json"` bezeichnet den Portfolio-Report, „⤓ Portfolio als JSON“ (der re-importierbare Trade-Export) hat keine `data-testid` – nur für Tests relevant, nicht nutzerwirksam.

---

## 4. User Journeys (Schritt für Schritt, tastaturgeführt)

### (a) Berater bewertet Collar → Termsheet/KID/Erklärung → Report
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `Ctrl+K` · `collar 7y 3,5/1,5 6m @Kunde GmbH` · `↵` | **311 ms** bis PV 48.973; Kontrahent „Kunde GmbH“; Fokus auf „Bezeichnung“ | – (R7-03 ✓) |
| `Esc` · `Shift+P` | „Zero-Cost-Collar: Floor-Strike übernommen (Prämie 0)“, PV 2 EUR, Rückgängig im Toast | – |
| `y i` | Indikationstext mit Prämie, PV, DV01, Kontrahent, Stichtag | – |
| `g s` · Heatmap `→ ↵` · `o t` | Szenarien 450 ms; What-if; Termsheet mit Stress-Banner; `Esc` → `main#main` | – |
| `0` · `o t` · `Tab`×3 · `Esc` | Termsheet **392 ms**, Markdown → Drucken → Schließen | – |
| `o k` · `o g` · Kunde · Erzeugen · `o c` · `o r` | KID, Geeignetheitserklärung § 64 Abs. 4 WpHG, Confirmation, Report mit Hashes/MaRisk | – |
| `Shift+K` | Kundenansicht ohne CVA/DVA/Margenformel | – |

### (b) Treasurer: 11 CSV-Vorlagen → defekte Datei → Kollision → Portfolio → JSON-Roundtrip
| Schritt | Beobachtung | Reibung |
|---|---|---|
| Export ▾ → 11× „⤓ Vorlage …“ | `deriva-import-vorlage-{irs,fxf,cap,swpt,fxo,ccs,fra,fxs,basis,amort,imm}.csv`; **CCS-Header mit API-Namen** | – (R7-5 ✓) |
| 11× „⤒ CSV importieren“ | je „1 Trades aus CSV importiert“, kein Dialog, 0 Fehler-Badges (CCS: Leg EUR/ESTR −20 bp vs USD/SOFR, CSA USD) | – |
| CCS mit alten Spaltennamen | importiert ebenfalls (Alias) | – |
| defekte Datei (5 Zeilen, 4 defekt) | Dialog „4 Zeilen übersprungen“, deutsche Ursachen, „1 gültige Zeile importieren“, Fehler-CSV | – |
| Kollision | Überspringen/Ersetzen/Umbenennen → „2 Trades aus CSV importiert“ | – |
| `o p` · „⤓ Portfolio als JSON“ · „⤒ JSON importieren“ | Portfolio-Report; Array mit 13 Trades → Kollisionsdialog → Umbenennen → 26 Trades, 0 Fehler; Report-JSON wird deutsch abgewiesen | – |

### (c) Prüfer: Marktdaten → What-if → Vergleich → Snapshot → Overrides → Undo
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `g c` · Quote `↑↑↵` · `]` | „modifiziert“; `]` 44 ms | – |
| `g b` · `Space` · `j` · `Space` · `g v` · `0` · `Ctrl+Z` | Vergleich unter What-if; „Rückgängig: Quote …“ | – |
| `g m` · Export · Quote ändern · Import · `o r` | ID `3803d0191a972626` und Report-Hash identisch; Toast nennt verworfene Änderungen | – |
| Spot EUR/USD 1,25 · `o r` · Reload · `Ctrl+Z` | „importiert · modifiziert“, Marker „Snapshot 1,1625“, Export 1,25, Reload behält, Undo stellt Hash wieder her | – |
| `Ctrl+Z` auf Import · Stichtag 04.09. · `Ctrl+Z` · „Zum Sample-Markt“ · `Ctrl+Z` | jede Marktquellen-Aktion rückgängig inkl. Vol-Override 77 | – |
| Quote/Interpolation/ToY/„+ Kurve“ im Import-Modus | `disabled` mit Hinweis, 0 Toasts | – |
| „+ Paar“ EUR/SEK unter Import | Override-Zeile, Chip „modifiziert“, Fokus zurück auf „+ Paar“, `Ctrl+Z` entfernt | – |
| ungültige Snapshots | deutsch, „Vol-Typ fehlt“ | – |

### (d) Hedge Accounting
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `g h` · „Effektivität testen“ `↵` | „✓ effektiv“, Herleitung | – |
| Ratio `↓` `Tab` · Markdown | Veraltet-Badge, „Erneut testen“, Markdown „veraltet“, deutsch | – |
| Reload | Ergebnis, Ratio 99, Badge vorhanden | – |
| „Zurücksetzen“ `↵` · Skip-Link `Tab` `↵` | Rückfrage; Undo → **Ratio 99, „✓ effektiv“, Badge zurück**, auch nach Reload; `Ctrl+Z` ebenso | – (R7-06 ✓); Fokus nach Bestätigung `body` (R8-05) |
| CAP · Innerer Wert · Vol einfrieren | CoH-Karte, „✓ effektiv“ | – |

### (e) „+ Kurve“ DKK/NOK → „+ Paar“ → „+ Fläche“ → Swap/Swaption/FX-Option → Import → Verlassen → Reload
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `irs dkk …` ohne Kurve · `irs sek …` · `… foo` · `… 10m 20m` · `… nibor9m` | „⚠ Keine Kurve für DKK im Markt – … „+ Kurve“ …“, „Unbekannte Währung“, „Betrag doppelt“, **„Unbekannter Index „nibor9m“ – für EUR registriert: …“** | – |
| `g c` · `+ Kurve` `↵` · DKK · Spot 7,46 · `↵` auf „Kurve anlegen“ | DESTR-Default, Parser-Fehler bei „abc“, Toast mit Undo, Tab „DESTR“, Chip „modifiziert“, **Fokus auf dem neuen Tab** | – (R7-03 ✓) |
| `irs dkk 5y pay 3% 10m @Nordea` · `↵` | Vorschau „· DESTR (Kurve vorhanden; CIBOR-6M ohne Kurve)“ → **PV 12.391**, Fokus „Bezeichnung“, Editor DKK/DESTR, `Shift+P`, `y i`, Szenarien, Report mit DKK-DESTR | – (R7-F2/-02 ✓) |
| `cap dkk 5y 3% 8m` · `fra dkk 3x6 pay 3% 10m` · `irs dkk … cibor6m` | DESTR mit Hinweis; „⚠ Kurve DKK-CIBOR-6M fehlt – … „+ Kurve“ anlegen“ | – |
| NOK: „+ Kurve“ NOWA **ohne Spot** · `irs nok 5y pay 4% 10m` | Vorschau „· NOWA (Kurve vorhanden; NIBOR-6M ohne Kurve)“; Trade „FX-Spot fehlt: Kein FX-Spot für NOKEUR verfügbar – … mit „+ Paar“ ergänzen“ | – (Fehlerpfad nennt den Weg) |
| `g m` · „+ Paar“ `↵` · Kurs 11,7 · `↵` | Vorschlag **EURNOK** vorbelegt; `↵` im Kurs-Feld legt nicht an → „Spot anlegen“ klicken → „Spot EUR/NOK 11,7000 angelegt“, NOK-Swap PV −31.209, Fokus zurück auf „+ Paar“ | R8-04 |
| „+ Kurve“ NOK NIBOR-6M · `irs nok …` | „Dual-Curve gegen NOK-NOWA“, Spot nicht erneut gefragt, Swap NIBOR-6M PV −31.199 ohne Hinweis | – |
| „+ Fläche“ `↵` · „Fläche anlegen“ `↵` · „+ Fläche“ → FX → EUR/DKK | Swaption-Cube DKK (Vorschlag, 11×9, flach 60 bp) und FX-Fläche EUR/DKK, Badges „angelegt“, Fokus zurück auf „+ Fläche“; „Entfernen“ + `Ctrl+Z` | – |
| `fxo eurdkk call 7.5 3m 5m` · `↵` | Vorschau ohne ⚠, **PV 59.254, kein Level-3-Hinweis**, Paar-Liste enthält EURDKK/EURNOK | – |
| `swpt dkk 1y5y payer 3% 10m` · `↵` · `Shift+P` | Vorschau ohne Warnung → „nicht bewertet – Kurve DKK-CIBOR-6M nicht im Markt-Snapshot“, „Kein Par-Wert“; Editor ohne Index-Feld | **R8-F1** |
| Reload · Snapshot exportieren · importieren | Kurven, Spots, Flächen bleiben; Datei mit DKK/NOK-Kurven, EURDKK/EURNOK, Swaption DKK, FX EURDKK; identische ID | – |
| Prüfer-Snapshot (ohne DKK) importieren | Badges „Keine Diskontkurve für DKK … „+ Kurve“ …“ / „FX-Spot fehlt …“; „+ Kurve“ dort gesperrt | R8-06 |
| „Zum Sample-Markt“ · Reload | DKK-Tab aktiv, EUR/DKK + EUR/NOK-Spot da, DKK-/NOK-Swaps bewertet (R7-F1 ✓); **DKK-Cube und EUR/DKK-Fläche weg, EUR/DKK-Option auf Level-3-Fallback; „+ Paar“-Spot EUR/SEK weg → „FX-Spot fehlt“** | **R8-F2** |
| „✕ Kurve entfernen“ · `Ctrl+Z` | Rückfrage nennt betroffene Trades; Spot geht mit der Kurve; Undo stellt Bewertung wieder her | – |

### (f) Offline / Lazy Views / Chunk-Ausfall
| Schritt | Beobachtung | Reibung |
|---|---|---|
| frischer Browser, **ein** Aufruf, offline, Reload | Chip „⚠ offline – lokaler Bestand“, alle 8 Views mit Charts, `n a`-Editor, „+ Kurve“/„+ Fläche“/„+ Paar“-Formulare, Termsheet; 21 Cache-Einträge | – |
| Kaltstart · `g s` sofort | 406 ms, Skeleton kurz | – |
| gedrosselt · `g s` | 936 ms mit Skeleton (`aria-busy`, `aria-live`), danach Kurven 84 / Markt 111 / Hedge 48 / Report 17 ms | – |
| warm · alle Chords · `Alt+2` | 16–131 ms, 0 Skeletons, 49 ms | – |
| View-Chunk 404 · Retry / `g s` nach Netzrückkehr | deutsche Karte, Recovery | – |
| ECharts-Chunk 404 · Retry (Kurven, Szenarien mit mehreren Karten) · Wiederbetreten | Karte „Diagramm nicht verfügbar“, **Retry lädt `?retry=` und rendert alle Charts**, Wiederbetreten repariert ohne Klick | – (R7-05 ✓) |

---

## 5. Hotkey-Matrix (verifiziert)

| Aktion | Tasten | Ergebnis |
|---|---|---|
| Termsheet / Erklärung / KID / Confirmation / Portfolio-Report / Report | `o t` / `o g` / `o k` / `o c` / `o p` / `o r` | ✅ Termsheet 392 ms; Fokus nach `Esc` auf `main#main` |
| Indikation / Zeile kopieren | `y i` / `y y` | ✅ auch für DKK-Trades |
| Neue Trades | `n s/c/w/f/o/b/a/i/x/z/r` | ✅ 11 Editoren; **Fokus auf „Bezeichnung“** (R7-03 ✓); `d` → `body` (R8-05) |
| What-if ±10 bp / Reset | `]` `[` `\` · `+` `-` `0` | ✅ 44–177 ms, auch im Import-Modus |
| Par-Satz / fairer Preis | `Shift+P` | ✅ Collar, DKK-OIS-Swap; unbewertbare DKK-Swaption → „Kein Par-Wert“ (R8-F1) |
| Kundenmodus / Stichtag / Theme / Hilfe | `Shift+K` / `Shift+T` / `t` / `?` | ✅ |
| Duplizieren / Löschen / Vergleich / Neu bewerten | `d` / `Shift+D` / `Space` / `r` | ✅ Toasts mit Rückgängig |
| Rückgängig | `Ctrl+Z` | ✅ Trades, Quotes, Vols, Fixings, Spot-Overrides, **„+ Paar“, „+ Fläche“**, Import/Verwerfen/Verlassen, Kurve anlegen/entfernen, **Hedge-Doku inkl. Ergebnis** (R7-06 ✓) |
| Tabellen (Zeilen) | `↑/↓` `Home/End` `PgUp/PgDn` `↵`/`F2` `Esc` `Tab` | ✅ ein Zeilen-Tabstopp: Blotter, Cashflows, Amortisationsplan, **Fixings/FX-Fixings/CDS** (R7-01 ✓); **Rest:** in Markt-Zeilen nur das erste Control erreichbar (R8-01), `↵`-Commit → `body` (R8-03) |
| Vol-Gitter | `←/→/↑/↓` `Home/End` `Ctrl+Home/End` `PgUp/PgDn` `↵`/`F2` `Esc` `Tab` | ✅ ein Tabstopp je Gitter, `role=grid`, benannte Zellen; **Rest:** FX-Gitter ohne Stopp nach Wechsel auf kleinere Fläche (R8-02) |
| „+ Kurve“ / „+ Paar“ / „+ Fläche“ | `↵` auf Button · Felder · `↵` auf „… anlegen“ | ✅ Fokus danach auf Tab/Button; `↵` im Kurs-Feld von „+ Paar“ legt nicht an (R8-04) |
| Toast-Aktion | `Tab` ab Skip-Link · `↵` | ✅ erster Stopp „Rückgängig (Ctrl+Z)“ |
| Palette | `Ctrl+K` · `↑/↓` · `↵` · `Esc` | ✅ Token-Fehler, Index-Hinweise „(Kurve vorhanden; … ohne Kurve)“, „Unbekannter Index“, `stichtag …` |
| Ansichten | `g …` · `Alt+1…8` · Rail | ✅ Prefetch, 16–131 ms warm |

---

## 6. Barrierefreiheits-Sweep (112 Zustände: alle Views inkl. „+ Paar“/„+ Fläche“/DKK-Markt/DKK-Pricing, 11 Editoren, 4 Dokumente + Kundenmodus, Dialoge/Menüs/Popover, 1440 + 1024, Dark + Light)

| Prüfung | Ergebnis |
|---|---|
| Unbenannte Inputs/Checkboxen/Buttons/Links/Bilder/Dialoge/Composite-Rollen, doppelte IDs, positive Tabindizes, leere `kbd` | **0** in allen 112 Zuständen (`summary.a11yBad []`), inkl. „+ Paar“ („Neues Währungspaar“, „Spot EUR/SEK“, Fehler `role=alert`), „+ Fläche“ („Art/Währung/Index/Währungspaar der neuen Vol-Fläche“, „Vorlagenfläche“, „Flache Vol“), Vol-Gitter (`role=grid` mit `aria-label`, Zellen „4W × 2Y“, „1M Strike 1,00 %“, „1M ATM“) |
| Tabellen ohne Namen | **0** |
| Überschriften | `h1` „DERIVA“ einmal, `h2` = View-Titel, `h3` Karten (auch „+ Vol-Fläche anlegen“); **0 Sprünge** |
| Landmarks / Live-Region | `nav[aria-label=Hauptnavigation]`, `main#main` (`tabIndex=-1`), Skip-Link erster Tabstopp, Toast-Stack `role=status` direkt dahinter; Skeleton `aria-busy` + `aria-live=polite` |
| Rollen / Roving | Blotter `grid`; Amortisationsplan, Fixings, FX-Fixings, CDS `row`-Zeilen mit einem Tabstopp; Vol-Gitter `grid`/`row`/`gridcell` mit `:focus-visible`-Outline; Heatmaps `grid`; Kurven-Tabs `group` mit `aria-pressed` |
| Kontrast | **6.525 Textpaare gemessen, alle Textfarben ≥ 4,5:1** (Dark + Light, 1440 + 1024); < 4,5 nur Rail-Icon-Glyphen 18 px im aktiven Light-Tab (4,13:1, Nicht-Text ≥ 3:1, Label ≥ 4,5:1) und die Logo-Glyphe auf Gradient (Mess-Artefakt) |
| Texte | keine Engine-Texte, keine ISO-Daten außer Format-Beispielen (Hilfe, Stichtag-Popover, CSV-Echo), Domänenbegriffe „Live“, „Cash“, „Reset“ (MtM-Reset), „Modified Following“, Grammatik-Beispiele „3.1%“/„3.5“ |
| Layout | keine horizontale Scrollbarkeit, keine beschnittene Karte; Key-Rate-Selektor umbrechend (R7-04 ✓); Vol-Gitter bei 1024 px in der Karte; „+ Fläche“-Formular bei 1024 px umbrechend |
| Tabstopps (Seite gesamt, 1440 dark) | Blotter 40 (Zeile nach **34** Tabs ab Skip-Link), Pricing 66–87 je Editor, Kurven 56, **Markt 65** (R7: 489–510; Fixings-Zeile nach 30 Tabs), Szenarien 42, Report 46, Vergleich 23, Hedge 40 |
| Bewegung | Skeleton-Puls unter `prefers-reduced-motion: reduce` aus |

---

## 7. Was für 100 noch fehlt

1. **R8-F1** Swaption-Zweig der Schnelleingabe und Swaption-Editor auf den kuratierten Index (`chooseIndex`/`reg.defaultIndex`) umstellen, Index-Feld im Editor, Vorschau-Hinweis – ~45 min.
2. **R8-F2** „+ Paar“-Spots und „+ Fläche“-Flächen als strukturelle Zusätze persistieren (oder `importSnapshot` ohne Quote-/Vol-Reset), Import-Toast konkret, E2E Import → Verlassen → Reload – ~1 h.
3. **R8-01** Zeilen-Roving der Markt-Tabellen um Zellwechsel (`←/→` bzw. `Tab` innerhalb der Zeile) ergänzen, stabile Zeilen-`key`s – ~45 min.
4. **R8-02 / R8-03** `useGridNav` klemmt `active` (oder `key={id}` an den Karten); `↵`-Commit kehrt zur Zelle/Zeile zurück – ~30 min.
5. **R8-04 / R8-05 / R8-06** `↵` im Kurs-Feld von „+ Paar“, Fokus nach `d`/Hedge-Reset, zustandsabhängiger Reparaturhinweis unter Import – ~30 min.

Erwartete Wirkung bei Umsetzung 1–5: UI/UX & Hotkeys ≈ 100, User Flows ≈ 100.

---

## 8. Artefakte

Basis: `/tmp/claude-0/-home-user-general/ba34afa7-bb32-5710-8abf-0fcec9f55ee0/scratchpad/r8-ui/`

- Skripte/Messwerte: `lib.mjs` (Helfer, generischer Kontrast-Sweep, a11y-Audit), `verify.mjs` (33 Checks, R7-Befunde + Randfälle), `probe2.mjs` (11), `probe3.mjs` (10), `probe4.mjs` (4), `flows.mjs` (46: 6 Journeys, 11 Vorlagen, Persistenz), `sweep.mjs` (8 Sammel-Checks über 112 Zustände), `lazy.mjs` (6), `show.mjs`; `results-{v,p2,p3,p4,f,s,l}.json`, `verify.log`, `probe2.log`, `probe3.log`, `probe4.log`, `flows.log`, `sweep.log`, `lazy.log`; `../r8-e2e.log` (E2E OK, 437), `../r8-vitest-web.log`, `../r8-vitest-r7file.log`, `../r8-preview.log`
- Downloads: `v-snap-pre.json`, `v-snap-dkk.json`, `p2-snap-pre.json`, `p3-snap-pre.json`, `f-snap-pre.json`, `f-snap-extra.json` (DKK/NOK-Kurven, Spots, Flächen), `f-snap-date.json`, `v-tmpl-ccs.csv`, `v-tmpl-ccs-legacy.csv`, `p2-tmpl-ccs.csv`, `tmpl-0…10.csv`, `p3-tmpl-0…10.csv`, `f-import*.csv`, `f-import-fehler.csv`, `p3-portfolio.json` (Report), `p4-portfolio.json` (Trades), `p3-portfolio-200.json`, `f-hedge-doc.md`
- PDFs (10): `report-print.pdf`, `doc-{termsheet,kid,suitability,confirmation}-print.pdf`, `hedge-print.pdf`, `curves-print.pdf`, `market-print.pdf`, `blotter-print.pdf`, `pricing-print.pdf`
- Sweep-Screenshots (112): `{1440,1024}-{dark,light}-{blotter,pricing,pricing-fxo,curves,curves-add,market,market-add-spot,market-add-vol,market-dkk,pricing-dkk,scenarios,report,compare,hedge,palette,help}.png`, `{1440,1024}-dark-editor-{irs,cap,swpt,fxf,fxo,basis,amort,imm,fxs,ccs,fra}.png`, `{1440,1024}-dark-doc-{termsheet,kid,suitability,confirmation,termsheet-customer}.png`, `{1440,1024}-dark-{export-menu,csv-errors,market-import-override,curves-import,toast,valdate-popover,context-menu,onboarding}.png`, `print-{report,doc-*,hedge}.png`
- Befunde: `v-market-roving.png`, `v-market-grid-switch.png` (R8-02), `v-editor-dkk.png`, `v-editor-nok.png`, `v-keyrate-fxo.png`, `v-echarts-failure.png`, `v-import-dkk-error.png`, `v-add-vol-form.png`, `v-market-dkk-surfaces.png`, `v-pricing-fxo-dkk.png`, `p2-ccs-template.png`, `p2-import-dkk-error.png` (R8-06), `p2-swpt-dkk.png` (R8-F1), `p2-fxo-after-leave.png` (R8-F2), `p2-blotter-after-remove.png`, `p3-after-leave-sek.png` (R8-F2), `p3-blotter-templates.png`, `p4-swpt-dkk-editor.png` (R8-F1), `f-curves-dkk.png`, `f-pricing-dkk.png`, `f-market-dkk.png`, `f-pricing-swpt-dkk.png`, `f-blotter-after-leave-reload.png`, `f-blotter-templates.png`, `l-skeleton-scenarios.png`, `l-offline-reload.png`, `l-offline-first.png`
