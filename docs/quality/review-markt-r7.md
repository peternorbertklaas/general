# Re-Review Runde 7: Marktabdeckung Features & Module (Dimension 1, Gewicht 20 %) – DERIVA 0.2.0

**Reviewer-Rolle:** Senior Product Analyst, Treasury- und Derivateberatungssoftware (LPA Capmatix/Captano, Finbridge, d-fine, Bloomberg SWPM/OVML/VCUB/DLIB/MARS, LSEG Swap Pricer, Murex, FIS Front Arena, Numerix, Quantifi, FINCAD, ORE/QuantLib) · **Stand:** 04.09.2026, Branch `claude/derivatives-trading-platform-1arsyu`, Commit `f93e099` (Core-`dist` frisch gebaut 06:10 UTC, keine Quelldatei neuer als `dist`) · **Modus:** Review only, keine Quelldatei geändert
**Baseline:** `docs/quality/review-markt-r6.md` (Runde 6, Score 98 / 100) mit den Befunden R6-1…R6-6
**Hinweis zum Arbeitsbaum:** Während dieser Review (ab 06:47 UTC) legte eine parallele Session **nicht committete** Änderungen an `calendar.ts`, `fra-pricer.ts`, `fx-pricer.ts`, `price.ts`, `upfront.ts`, `sensitivities.ts`, `vitest.config.ts`, `ci.yml` und Package-Dateien ab (Quant-/Architektur-Maßnahmen Runde 7, z. B. `tradeCurrencies(…, { upfront })`). Sie sind **nicht** Gegenstand dieser Bewertung; alle Proben liefen gegen den zu `f93e099` gebauten `dist` (06:10 UTC) und die committeten API-/Web-Quellen.
**Toolchain:** vitest 5.0 / vite 8.2 (Rolldown) / jsdom 30 – erste Bewertung nach dem Toolchain-Sprung
**Geprüft (Laufzeit):** `npx vitest run` je Paket → **Core 377/378** im parallelen Vollauf (`par-risk.test.ts › off-market swap carries €STR risk` lief in den 5-s-Timeout, isoliert **7/7 grün** in 3,9 s – Last-Flake wie in Runde 6, kein Defekt), **API 114/114**, **Web 307/307**; `vite build` erfolgreich; Web-App unter `vite preview --port 5001 --strictPort` mit Playwright/Chromium 1194 durchgeklickt (`probe-web-r7.mjs`, `probe-web2-r7.mjs`, `probe-web3-r7.mjs`, `probe-web4-r7.mjs`, Anhang C, keine `pageerror`); offizielle E2E-Suite `apps/web/e2e/smoke.mjs` auf Port 5001 → **E2E OK (395 checks)**; API per `buildApp` + `app.inject` (`probe-api-r7.mts`, `probe-api2-r7.mts`, `probe-api3-r7.mts`, `probe-api4-r7.mts`, Anhang B); Core-Proben gegen `packages/pricing-core/dist` (`probe-core-r7.mjs`, Anhang A). Alle Zahlen unten stammen aus diesen Läufen.
**Roadmap-Regel:** In `02-epics-und-user-stories.md` / README als ⏳ dokumentierte Posten (Bermudan/CMS/Range Accrual, TARF/Participating Forward, Marktdaten-Adapter & Kurven-Governance, OIDC/Rollen/DB, Excel-Add-in, Monte-Carlo-Netting-XVA/FVA, PDF-Template-Service, Inflations-ZC-Swap, PRIIPs Annex-II-MC, SIMM/CRIF, VaR/Attribution, Batch-EoD/Webhooks, Designationsmemo, EMIR Tabelle 3/UPI/XML) werden **nicht** als Lücke gewertet.

---

## 1. Score

### **Marktabdeckung Features & Module: 98 / 100** (rechnerisch 98,4; Runde 1: 60, Runde 2: 86, Runde 3: 97, Runde 4: 98, Runde 5: 99, Runde 6: 98)

**In einem Satz:** Alle sechs Runde-6-Befunde sind in Core, API **und** UI nachweisbar geschlossen – die Schnelleingabe lehnt jedes unbekannte Token ab und kennt `imm`, `cash|physical`, `ndf` und `barrier uo|ui|do|di <level>`, alle elf Produkt-Vorlagen existieren in Web und API (Round-Trip geprüft), `none` und Zeilen-Schemaprüfung funktionieren im API-CSV, unplausible Vol-Flächen erzeugen `VOL_IMPLAUSIBLE:` in Core/API/UI, NOK/SEK/DKK/PLN sind mit Konventionen und Kalendern registriert und per „+ Kurve“ bzw. `POST /api/market/bootstrap` bootstrapbar, und der Beispielmarkt trägt 1 323 historische Fixings, sodass das Beispielbuch ohne ⚠ startet. **Die Bewertung bleibt trotzdem bei 98, weil die Prüfung des neuen Fremdwährungs-Flusses fünf Restposten zeigt, die ein Berater sofort bemerkt:** nach „+ Kurve NOK-NOWA“ legt `irs nok 5y pay 4% 10m` einen Trade mit Status **„Fehler“** an (Default-Index NIBOR-6M ohne Kurve, keine Vorwarnung in der Palette), dessen Editor **„EUR“ / „EURIBOR-3M“** anzeigt, weil Währungs- und Index-Selects auf G5 festverdrahtet sind; Vol-Flächen für neue Währungen/Paare sind nur per API, nicht in der Marktansicht anlegbar (NOK-Cap Fallback 60 bp, EUR/NOK-Option 8 % → Level 3); die API setzt für eine neu gebootstrappte Währung keine Diskontkurven-Zuordnung (`NO_DISCOUNT_CURVE`, Umweg über den Snapshot-Roundtrip) und behält beim Stichtagswechsel die alten Beispiel-Fixings (1 323 statt 1 452 in der UI → `MISSING_FIXING` nur in der API); die CCS-CSV-Spalten von Web und API sind nicht kompatibel; und weitere Währungen (CZK/HUF – für DACH-Exporteure Alltag) bleiben ohne Codeänderung unerreichbar, weil `registerRateIndex` nur im Core zur Laufzeit existiert (kein API-/UI-/Snapshot-Pfad). Die Summe der Abzüge sinkt von −1,9 auf −1,6; gerundet bleibt es bei 98.

**Abzugsherleitung** (Rubrik: kritisch −10…−25, fehlendes Kernfeature −3…−8, UX-Reibung −1…−3, kosmetisch −0,2…−1; Roadmap-dokumentiert = 0; keine Doppelzählung mit Quant/UI/Flows/API; Teilbeträge unterhalb der Bänder wie in den Vorrunden für Randfälle):

| Bereich | R1 | R2 | R3 | R4 | R5 | R6 | R7 | Restlücken (Nummern → Abschnitt 3) |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| A Instrumente & Strukturen | −10 | −3,7 | −0,8 | −0,5 | 0 | 0 | 0 | alle Trade-Typen inkl. Barrier (`hit`), Digital, NDF, MtM-CCS, Fest-Fest-CCS, ZC, Lookback, Prämien-Leg bewertet (Anhang A) |
| B Kurven & Marktdaten | −5 | −0,7 | −0,2 | 0 | −0,3 | −0,5 | −0,9 | Registrierung weiterer Währungen nur Core-Laufzeit, kein API/UI/Snapshot-Pfad (R6-5 Rest −0,3); Vol-Flächen für neue Währung/Paar nicht in der UI anlegbar (R7-2 −0,2); API: neue Kurve ohne Diskontzuordnung, `discountCurveId` nicht per `PUT` (R7-3 −0,2); API behält alte Beispiel-Fixings beim Stichtagswechsel (R7-4 −0,2) |
| C Modelle | −1 | 0 | 0 | 0 | 0 | 0 | 0 | – |
| D Sensitivitäten | −2 | −0,8 | 0 | 0 | 0 | 0 | 0 | DV01/Buckets/Vega/Par-Risiko auch für NOK-NOWA (Anhang B) |
| E Szenarien / VaR | −1 | −0,5 | 0 | 0 | 0 | 0 | 0 | 16 + 6; VaR = Roadmap |
| F XVA | −2,5 | −0,5 | 0 | 0 | 0 | 0 | 0 | NOK-Swap CVA Sorensen-Bollier; Netting = Roadmap |
| G Hedge Accounting | −4 | −3,0 | 0 | 0 | 0 | 0 | 0 | Grundgeschäfts-Währung kennt NOK nach „+ Kurve“ |
| H Regulatorik | −4 | −1,5 | 0 | 0 | 0 | 0 | 0 | EMIR-Datensatz NOK (SRCCSP, Notional-Ccy NOK) |
| I Dokumente | −2,5 | −1,0 | 0 | 0 | 0 | 0 | 0 | Termsheet/KID/Confirmation „Payer-Zinsswap (NOK)“ |
| J Workflow / Beratung | −2,5 | −0,3 | 0 | 0 | 0 | 0 | 0 | Freigabe = Roadmap |
| K UI | −0,5 | −1,3 | −0,8 | −1,5 | 0 | −0,5 | −0,5 | Neue-Währung-Fluss: Default-Index ohne Kurve → „Fehler“-Trade ohne Vorwarnung, Editor-Selects Währung/Index/Collateral fest auf G5 und zeigen falsche Werte (R7-1 −0,5) |
| L Integration | −2,5 | −0,5 | −0,3 | −0,2 | −0,2 | −0,7 | −0,2 | CCS-CSV-Spalten Web ≠ API, Dateien nicht wechselseitig importierbar (R7-5 −0,2) |
| M Admin | −2,5 | 0 | 0 | 0 | 0 | 0 | 0 | Rollen/DB = Roadmap |
| Sonstiges (Beispielmarkt) | – | −0,5 | −0,5 | −0,2 | −0,2 | −0,2 | 0 | Beispielbuch ohne Warnung, 1 323 Fixings (R6-6 behoben) |
| **Summe** | **−40** | **−14,3** | **−2,6** | **−2,4** | **−0,7** | **−1,9** | **−1,6** | → **98,4 ≈ 98 / 100** |

Einordnung (gleiche Skala, v1-Workstation-Scope, Einschätzung): Bloomberg SWPM+OVML+VCUB+MARS ≈ 85 (kein Hedge Accounting, keine deutschen Beratungsdokumente), LPA Capmatix OTC ≈ 75, ORE ≈ 60, Kyriba/Coupa ≈ 55. Die funktionale Abdeckung für Vanilla-Zins/FX in G5 ist vollständig und jetzt auf G5 + NOK/SEK/DKK/PLN erweitert; die Restdifferenz zu 100 liegt fast ausschließlich in der **Rundung des neuen Fremdwährungs-Flusses** (Palette → Editor → Marktansicht → API) und in der Erweiterbarkeit jenseits der neun registrierten Währungen.

---

## 2. Status der Runde-6-Befunde R6-1…R6-6

Legende: **behoben** = im Code vorhanden **und** per Probe/UI reproduziert · **teilweise** = Kern vorhanden, benannter Rest · **offen** = unverändert.

| # | Befund R6 | Status | Nachweis (Datei / Probe / UI) | Rest |
|---|---|---|---|---|
| **R6-1** | Schnelleingabe verschluckt unbekannte Tokens, zweiter Betrag überschreibt Nominal, kein `imm`, kein `cash` | **behoben** | `apps/web/src/lib/quick-parser.ts`: `knownCurrencies()` aus `SWAP_CONVENTIONS` (`:146`), `MODIFIERS`/`BARRIER_WORDS`/`COMMANDS` mit `imm` (`:160–207`), `unknownTokenError` (`:294`), `duplicate`/`AmountSlot` (`:308–336`), `noCurveError` (`:302`), `GRAMMAR` je Zweig (`:338–348`); jeder Zweig endet in `notional.take(t)` → Fehler statt `else`-Verwerfen (`:386, 469, 531, 593, 659`); `swpt … cash|physical` (`:687–698`), `fxf … ndf` (`:736–760`), `fxo … barrier <typ> <level>` (`:775–831`); Doku `03-ui-konzept-und-hotkeys.md:123–137`, Hilfe (`HotkeyOverlay.tsx:47, 109`). UI (Palette, Anhang C): `irs sek 5y pay 3% 10m` → „⚠ Keine Kurve für SEK im Markt – in der Kurvenansicht mit „+ Kurve“ aus Quotes anlegen (Währungen mit Kurve: EUR, USD, GBP, CHF, JPY)“; `irs xyz …` → „Unbekannte Währung „XYZ“ – Währungen: EUR, USD, GBP, CHF, JPY, NOK, SEK, DKK, PLN; erwartet: irs\|swap\|ois\|imm\|amort [ccy] <tenor> …“; `irs 10y pay 3.1% 10m mtm` → „Unbekanntes Token „mtm““; `ccs … foo`, `fxs … xyz`, `basis … foo` → Fehler; `irs 10y pay 3.1% 10m 6m` → „Betrag doppelt angegeben („10m“ und „6m“)“; `… 3.1% 3.2% …` → „Satz doppelt angegeben“; `fxo … barrier 1.05` / `barrier di` → „Barriere unvollständig – Format: barrier uo\|ui\|do\|di <level>“; **neu:** `swpt usd 1y5y payer 3.5% 10m cash` → „Payer-Swaption USD 1Yx5Y @ 3,500 % · Nominal 10.000.000 · Barausgleich“, `fxo eurusd put 1.15 3m 9m barrier do 1.05` → „… · Barriere Down-and-Out 1,0500“, `fxf eurusd 2m 1.1725 6m ndf` → „… · NDF (Ausgleich in USD)“, `imm 2y pay 3% 10m` → „IMM-Payer-Swap EUR 2Y @ 3,000 % · … · IMM ab 16.09.2026“, `imm usd …` → SOFR-IMM-Swap; `@Sparkasse Musterstadt`, `step 2.5/3.0/3.5`, `31.02.2027` → „Ungültiges Datum“ weiterhin korrekt. Web-Tests `lib/review-r6.test.ts` (5 Blöcke) | Index-Token neuer Währungen werden nicht normalisiert: `irs nok … nibor6m` → **englische** Core-Meldung „Unknown rate index: NIBOR6M“, nur `nibor-6m`/`nowa` funktionieren (`quick-parser.ts:591` ersetzt nur `EURIBOR`) – ohne eigenen Abzug, Teil des Flusses R7-1 |
| **R6-2** | Keine CSV-Vorlage für FX-Swap, Basis, Amort, IMM (UI + API), kein `stepUp` | **behoben** | Web `portfolio-io.ts:176–177` `CSV_TRADE_TYPES` 11 Typen, Vorlagen `FXS`/`BASIS`/`AMORT`/`IMM` (`:361–448`), `stepUp` (`:192–207, 755–767`), Barriere-Spalten (`:279–291, 1075–1087`), Builder-Zweige (`:818–902`); API `csv-import.ts:43–56` `CSV_TRADE_TYPES` 11, `CSV_TEMPLATES` FxSwap/BasisSwap/AmortisingSwap/ImmSwap (`:156–213`, `tradeType` → `InterestRateSwap`), `STEP_UP_NOTE`. **UI:** Export-Menü **11 Vorlagen** („⤓ Vorlage FX-Swap / Tenor-Basis-Swap / Amortisierender Swap / IMM-Swap“); CSV-Import einer Datei mit FXS/BASIS/AMORT/IMM/IRS-Staffel/CCS-none/FXS-defekt → Dialog „7 Nominal fehlt oder ≤ 0 · 8 Far-Valuta muss nach der Near-Valuta liegen“, „5 gültige Zeilen importieren“ → „5 Trades aus CSV importiert“, Blotter enthält `FXS-R7`, `BASIS-R7`, `AMORT-R7`, `IMM-R7`, `IRS-R7-STEP` (Zeile 7 scheiterte an **meiner** API-Spaltenbezeichnung `domesticNotional`, siehe R7-5). **API:** `?type=` Enum 11 Werte; OpenAPI-Beschreibung nennt `none`, `FxSwap`, `AmortisingSwap`, `ImmSwap`, `stepUp`, `barrierType`; Round-Trip je Vorlage → `imported 1` mit PV (FxSwap −6.970, BasisSwap 94.403, AmortisingSwap −565.381 mit `stepUp`, ImmSwap −119.312); deutsche Aliase `Paar;Betrag;Nahkurs;Fernkurs;Valuta nah;Valuta fern;Referenz` → 200; `barrierType/-Level/-Rebate/-Hit` → 2 importiert, `Sideways` abgelehnt; JSON-Import FxSwap weiter 200 | Spaltennamen der **älteren** CCS-Vorlage unterscheiden sich zwischen Web und API → **R7-5** |
| **R6-3** | API-CSV ohne `none` für CSA; Schema-Fehler einer Zeile → 400 für den ganzen Upload | **behoben** | `csv-import.ts:428` `UNCOLLATERALISED` (`none`, `unbesichert`, `ohne`, `-`, `null`, `keine`, …), `:484–492` (leer → Default, `none` → `null`, sonst ISO oder Zeilenfehler); `routes/trades.ts` `preValidation` prüft jeden gebauten Trade gegen das `Trade`-Schema → `rejected` mit `CSV_ROW_INVALID` (OpenAPI-Text `:306`). Probe: CCS `none`/`USD`/leer/`unbesichert`/`XX` → **200, `imported 4`, `rejected 1`**; IRS gültig + `2026-13-08` + `id "IRS CSV SPACE"` + ohne ID → **200, `imported 2`, `rejected 2`** (vorher 400 für alles); Vertragstest `review-r6.test.ts:138–212` | – |
| **R6-4** | Vol-Flächen strukturell, nicht plausibel geprüft (Lognormal mit Normal-Zahlen → PV ≈ 0 ohne Signal) | **behoben** | `vol-validation.ts:221–330` `VOL_IMPLAUSIBLE_PREFIX`, `VOL_PLAUSIBILITY`, `volSurfaceWarnings`, Pricer-Wiederholung je Bewertung; API `routes/market.ts:359–366` (`volSurfacePlausibilityWarnings` → `warnings[]`), Snapshot-Import ebenso; UI Toast + Übersetzung. Core: Lognormal-USD-Cube → `["VOL_IMPLAUSIBLE: swaptionVols.USD: median lognormal vol 0.86 % is below 1 % – the numbers look like normal (bp) vols; check the volType of the import"]`, Null-Cube → „is degenerate – every vol is 0“, FX-ATM ×100 → „8 of 8 lognormal vols above 300 %“, Caplet-Lognormal → Warnung, Beispielmarkt → `[]`; Swaption auf dem Cube: PV 2 **mit** `VOL_IMPLAUSIBLE:` (vorher `warnings []`), Null-Cube PV 0 mit Warnung. API: `PUT /api/market` → **200 mit `warnings[]`**, `/api/price` → PV 2 + Warnung, `/api/report` und `GET /api/trades?price=1` wiederholen sie, `PUT /api/market/snapshot` → 200 mit Warnung. UI: Snapshot-Import → zwei Toasts „Snapshot „Sample EoD“ importiert · ID …“ und „Snapshot importiert – Hinweis: Vol-Fläche unplausibel: Swaption-Cube USD – Median der Lognormal-Vols 0,86 % liegt unter 1 %; die Zahlen sehen wie Normal-Vols (bp) aus – volType des Imports prüfen“, Marktansicht nennt den Hinweis, `Ctrl+Z` stellt den Sample-Markt wieder her (R6-F2) | Hinweis ohne Abzug (Quant-/Compliance-Dimension): die IFRS-13-Heuristik bleibt auf einer als unplausibel markierten Fläche bei **Level 2** (`/api/report` `ifrs13Level 2`) – ein IPV-Prozess würde hier eher Level 3 oder „nicht freigabefähig“ erwarten |
| **R6-5** | Geschlossenes Index-/Währungsregister (NOK/SEK/PLN/CZK/HUF weder bootstrapbar noch als Swap), kein „+ Kurve“ | **teilweise** | **Core:** `index-definitions.ts:83–94` NIBOR-3M/-6M, NOWA, STIBOR-3M/-6M, SWESTR, CIBOR-3M/-6M, DESTR, WIBOR-3M/-6M, POLONIA; `SWAP_CONVENTIONS` NOK/SEK/DKK/PLN (`:209–260`, Interdealer-Konventionen, Kalender NO/SE/DK/PL – Stichproben korrekt: 17.05.2027 NO, Midsommarafton 25.06.2027 SE, 2. Pinsedag 17.05.2027 DK, Store Bededag 22.04.2027 **Arbeitstag** (abgeschafft 2024), Boże Ciało 27.05.2027 PL, 3. Mai PL, 31.12.2027 DK/SE frei); `registerRateIndex`/`registerSwapConventions` (`:309–368`, validiert → `INVALID_CURVE_SPEC`), `knownCurrencies`/`knownIndices`. Probe: NOK-NOWA-Bootstrap 8 Knoten, NOK-OIS-Swap Par 4,000 %, NIBOR-6M dual-curve gegen NOWA → Swap PV 1.333.812 Par 4,300 %, FRA NOK 3x9 wählt NOWA (Index mit Kurve), EURNOK-Forward PV 46.403, Snapshot-Roundtrip mit NOK-Kurve ID-gleich; CZK per `registerRateIndex(CZEONIA/PRIBOR-6M)` + `registerSwapConventions` → `knownCurrencies` enthält CZK, Bootstrap + Swap funktionieren. **API:** `GET /api/market` liefert `currencies` (9) und `indices` (21); `POST /api/market/bootstrap` NOK/NOWA, SEK/SWESTR, DKK/DESTR, PLN/POLONIA, PLN/WIBOR-6M → **200**; `index` ohne Enum (Pattern); NOK-Swap nach Snapshot-Roundtrip: Report Level 2, DV01 46.447 auf `NOK-NOWA`, Par-Risiko, Vega, 16 Szenarien, Grid, CVA 2.563 (Sorensen-Bollier), Termsheet/KID/Confirmation „Payer-Zinsswap (NOK)“, EMIR `SRCCSP` Notional-Ccy NOK – alle 200. **UI:** Kurvenansicht „+ Kurve“ (`CurvesView.tsx:214–330`): Währungen „CHF, DKK (ohne Kurve), EUR, GBP, JPY, NOK (ohne Kurve), PLN (ohne Kurve), SEK (ohne Kurve), USD“, NOK-Indizes „NIBOR-3M (IBOR 3M, ACT/360, NO) \| NIBOR-6M \| NOWA (OIS, ACT/365F, NO)“, Spot EUR/NOK, Quotes `Tenor;Satz` → Toast „Kurve NOK-NOWA aus 7 Quotes angelegt · Spot EUR/NOK 11,6200“ (Rückgängig), neuer Tab „NOWA“, Chip „modifiziert“, zweite Kurve NIBOR-6M „Depots bis 6M, darüber Par-Swaps · Dual-Curve gegen NOK-NOWA“; `ois nok 5y pay 4.1% 10m` → Report „FAIR VALUE (BILATERAL, IFRS 13 LEVEL 2)“, nennt `NOK-NOWA`; Reporting-Währung NOK nach 5× `c`; Hedge-Grundgeschäft kennt NOK; persistiert (`extraCurves`); FX-Fixings-Paar EUR/NOK erscheint | **(a)** Registrierung **weiterer** Währungen/Indizes (CZK, HUF, CNH, TRY …) gibt es nur als Core-Funktion zur Laufzeit: kein API-Endpunkt (`POST /api/market/bootstrap` CZK/CZEONIA und HUF/BUBOR-6M → **422 `UNKNOWN_INDEX`**), kein Snapshot-Block (`serializeMarket` ohne `indices`/`conventions` – ein per Code registriertes CZK überlebt keinen Export), kein UI-Weg (Select zeigt nur die neun registrierten) → **−0,3**; **(b)** der UI-Fluss nach „+ Kurve“ hat Kanten → **R7-1**, Vol-Flächen → **R7-2**, API-Diskontzuordnung → **R7-3** |
| **R6-6** | Beispielmarkt ohne historische Fixings, `IRS-0001` mit ⚠ | **behoben** | `sample-market.ts:663–698` `sampleFixings`/`SAMPLE_FIXINGS` (EURIBOR-6M/-3M ab 03.06.2024, €STR ab 02.01.2026, je TARGET-Geschäftstag bis exklusive Bewertungstag), `:714` im `buildSampleMarket`; US-2.9 aktualisiert. Core: **1 323 Fixings** (576/576/171, 03.06.2024–02.09.2026), `IRS-0001` PV −171.352 **`warnings []`**, laufende EURIBOR-3M-/€STR-Swaps ohne Warnung, Bewertungstag 05.10.2026 → 1 389 Fixings, weiter warnungsfrei. API: `GET /api/trades?price=1` – alle 10 Trades `warnings []`, `GET /api/market` `fixings 1323`, Snapshot-Reimport ID-gleich. UI: Blotter **ohne ⚠** (`IRS-0001 … -279.451 6.971 Live OK`), Fixings-Karte „1323 Fixings · neueste zuerst“ mit Filter | Nur EUR-Indizes: importierte **laufende** USD/GBP/CHF/JPY-Swaps tragen weiter `MISSING_FIXING` (SOFR/SONIA/SARON/TONA) – fachlich korrekt (Beispielbuch startet dort am Spot), ohne Abzug; API behält die 1 323 Fixings beim Stichtagswechsel → **R7-4** |

**Weitere Runde-6-Maßnahmen (geprüft, ohne eigene Befundnummer):** Prämien-Leg `Upfront premium` mit Cashflow `Premium` (Theta-Einfachzählung), `barrier.hit`/`analytics.barrierState`/`BARRIER_STATE_UNKNOWN:` im Editor („Barriere bereits berührt“) und in der CSV-Vorlage, `hazardFromSpread` ACT/360, `@deriva/pricing-core/internal` (246 öffentliche Namen im `dist/index.js`), Warnungs-Katalog mit `BARRIER_STATE_UNKNOWN:`/`VOL_IMPLAUSIBLE:` (10 Präfixe), Hedge-Ergebnis mit „veraltet“-Badge, Fixings-Editor mit Filter/Seiten. Web-CSV-Fehlerdialog zählt korrekt („5 gültige Zeilen importieren“ = „5 Trades aus CSV importiert“, R6-06).

**Runde-1…5-Posten:** unverändert geschlossen; G14/G24/G26 bleiben dokumentierte Roadmap (kein Abzug).

---

## 3. Neue Befunde (Runde 7)

Severity: **Niedrig** = Lücke zweiter Ordnung für eine v1-Persona · **Kosmetisch** = Voreinstellung/Konsistenz/Beispieldaten.

| # | Sev. (Abzug) | Ort | Befund (reproduziert) | Fix |
|---|---|---|---|---|
| **R7-1** | Niedrig (−0,5) | `apps/web/src/lib/quick-parser.ts:559–626` (`irs`-Zweig: Default-Index über `makeVanillaSwap` = `SWAP_CONVENTIONS[ccy].floatIndex`, `noCurveError` `:302` prüft nur `discountCurveId`), `apps/web/src/components/TradeEditor.tsx:146–147` (`INDICES = ["EURIBOR-3M","EURIBOR-6M","ESTR","SOFR","SONIA","SARON","TONA"]`, `CCYS = ["EUR","USD","GBP","CHF","JPY"]`, verwendet `:825, 925, 1005, 1515`), Collateral-Select `:1119` | **Der neue Fremdwährungs-Fluss endet im Standardfall in einem „Fehler“-Trade, den der Editor nicht reparieren kann.** UI (Anhang C): „+ Kurve“ NOK-NOWA → `irs nok 5y pay 4% 10m` zeigt die Vorschau „Payer-Swap NOK 5Y @ 4,000 % · Nominal 10.000.000“ **ohne Hinweis**, Enter → Blotter `IRS-0003 … – – Indikation Fehler`, Pricing „nicht bewertet – Eingaben prüfen · Kurve nicht im Markt-Snapshot: Kurve NOK-NIBOR-6M nicht im Markt-Snapshot“ (Screenshot `06-nok-irs-editor.png`); der Editor zeigt **Währung „EUR“** und **Index „EURIBOR-3M“** für den NOK-Trade (Select-Wert nicht in den Optionen → erste Option), Collateral-Select ohne NOK-CSA; Index/Währung sind nicht auf NOWA/NOK umstellbar. `cap nok 5y 4.5% 10m` ebenso „Fehler“. Erst eine zweite Kurve NIBOR-6M heilt beide Trades automatisch (gut), doch das weiß der Berater nicht – der FRA-Zweig macht es vor (`fra nok 3x9` wählt NOWA, weil `fraIndexForPeriod` nur Indizes mit Kurve nimmt). API-Import verhält sich konsistent: NOK-Swap ohne Index → `rejected CURVE_NOT_FOUND`. | Palette: Index-Default aus den Indizes **mit Kurve im Markt** wählen (OIS-Index, wenn nur die OIS-Kurve existiert; analog `fraIndexForPeriod`), sonst Fehler „Keine Kurve für NIBOR-6M – `ois nok …` verwenden oder NIBOR-6M-Kurve anlegen“; `INDEX_RE`-Token für neue Indizes normalisieren (`nibor6m` → `NIBOR-6M`, deutsche Fehlermeldung); Editor: `CCYS` aus `reportingCurrencies(baseMarket)`/`knownCurrencies()`, `INDICES` aus `knownIndices()` gefiltert auf Kurven im Markt (mit „(ohne Kurve)“-Hinweis), Collateral-Optionen aus `discountCurveId`; Unit-Test „NOK-Trade im Editor zeigt NOK/NOWA“. |
| **R7-2** | Kosmetisch (−0,2) | `apps/web/src/views/MarketView.tsx:921–931` (`swptKeys`/`fxKeys`/`capKeys` = vorhandene Schlüssel des Markts, keine Neuanlage), Palette-Warnung `quick-parser.ts:286–289` | **Für eine neue Währung/ein neues Paar lässt sich in der Workstation keine Vol-Fläche anlegen – Optionen bleiben auf Level 3.** UI: nach NOK-Kurven zeigt die Marktansicht weiter nur die fünf Cubes/Caplet-Flächen und zehn FX-Paare (kein NOK, kein EUR/NOK); `cap nok 5y 4.5% 10m` → „⚠ 1 · Keine Caplet-Vol-Fläche – 60 bp Normal-Vol verwendet“, Report „Rückfall-Volatilität 60 bp Normal-Vol (keine Fläche)“; `swpt nok …` → „⚠ keine Swaption-Vol-Fläche“ (Fallback 70 bp); `fxo eurnok call 11.7 1m 6m` → „⚠ keine FX-Vol-Fläche für EUR/NOK (Fallback 8 %, Level 3)“, PV 281.688 NOK. Die API kann es: `PUT /api/market { fxVols: { EURNOK: … } }` → 200, Option danach `warnings []`, PV 267.428. Einzig der Snapshot-Import trägt Flächen in die UI. | Marktansicht „+ Fläche“ (Swaption-Cube je Währung mit Kurve, Caplet-Fläche je Index, FX-Fläche je Paar mit Spot) mit Vorlage aus einer vorhandenen Fläche (Achsen kopieren, Werte eingeben/pasten), undo-fähig und persistiert wie `extraCurves`; Palette-Warnung nennt den Weg („in der Marktansicht mit „+ Fläche“ anlegen“). |
| **R7-3** | Kosmetisch (−0,2) | `apps/api/src/routes/market.ts:271–299` (`replaceCurve` fügt die Kurve ein, setzt aber kein `discountCurveId`), `apps/api/src/schemas.ts:849–871` (`marketPutSchema` ohne `discountCurveId`, `additionalProperties: false`) | **Die API kennt die UI-Regel „erste Kurve einer Währung = Diskontkurve“ nicht.** Probe: `POST /api/market/curves` NOK-NOWA → 200, `GET /api/market/snapshot` → `discountCurveId` weiter nur G5; NOK-Swap-Import → `rejected NO_DISCOUNT_CURVE "No discount curve configured for NOK"`; `PUT /api/market { discountCurveId: { NOK: "NOK-NOWA" } }` → **400 `VALIDATION_ERROR` „must NOT have additional properties“**. Einziger Weg: `GET`/`PUT /api/market/snapshot` mit ergänztem `discountCurveId` (funktioniert, R6-Test macht genau das). Für einen TMS-/EoD-Klienten ist der Snapshot-Roundtrip (≈ 100 kB) für eine Zuordnung unverhältnismäßig. | `replaceCurve`: wenn die Währung keine Diskontkurve hat und die Kurve OIS-Index-Kurve ist (oder als einzige existiert), `discountCurveId[ccy] = id` setzen (Antwortfeld `discountCurveSet: true`, Audit); `marketPutSchema` um `discountCurveId` (Map Währung → vorhandene Kurven-ID, Existenzprüfung → 422 `CURVE_NOT_FOUND`) und `collateralDiscountCurveId` ergänzen; Vertragstest. |
| **R7-4** | Kosmetisch (−0,2) | `apps/api/src/lib/store.ts:89–97` (`rebuild`: `fixings: prev.fixings ?? fresh.fixings` – Beispiel-Fixings des alten Stichtags werden behalten) | **API und UI liefern nach einem Stichtagswechsel unterschiedliche Beispiel-Fixings.** UI: Palette `stichtag 03.11.2026` → Fixings-Karte **1 452** Fixings (Core `buildSampleMarket(03.11.2026)` erzeugt sie bis zum 02.11.2026). API: `PUT /api/market { valuationDate: "2026-11-03" }` → `GET /api/market` **1 323**, letztes Fixing-Datum 02.09.2026; ein EURIBOR-3M-Swap ab 15.09.2026 wird danach mit `MISSING_FIXING: Missing fixing for EURIBOR-3M on 2026-09-11; used 3M forward from 2026-11-03` bewertet – in der UI mit demselben Stichtag nicht. Der Rebuild-Kommentar begründet das für **FX-Fixings** („history, not sample-market data“), für die generierten Beispiel-Fixings ist es ein Artefakt. | Im `rebuild` die frischen `sampleFixings(valuationDate)` mit den **nutzergesetzten** Fixings mergen (Nutzer-Fixings gewinnen je Index/Datum; dazu im Store merken, welche Fixings per `PUT /api/market` kamen), oder den Beispielmarkt-Anteil als solchen markieren; Vertragstest „Stichtag +2 Monate → Fixings bis Vortag vorhanden“. |
| **R7-5** | Kosmetisch (−0,2) | Web `apps/web/src/lib/portfolio-io.ts:316–334` (CCS-Spalten `notional, spread, rate, fxSpot, direction, start, maturity, collateral`), API `apps/api/src/lib/csv-import.ts:138–147` (`domesticNotional, fxSpot, foreignNotional, spread, fixedRate, domesticPayReceive, frequency, collateralCurrency`, `effectiveDate, tenor`), Aliase `portfolio-io.ts:538` (`collateralcurrency` → `collateral`, aber kein `domesticnotional`) | **Die CCS-Vorlagen von Web und API sind nicht wechselseitig lesbar** – obwohl CHANGELOG/UI-Konzept „Spaltennamen identisch mit der API-CSV“ versprechen (gilt nur für die vier neuen Vorlagen). Probe: Web-CCS-Zeile (`type;id;pair;notional;fxSpot;start;tenor;spread;collateral`) an `POST /api/trades/import?type=CrossCurrencySwap` → **400 `CSV_INVALID` „CSV header lacks required column(s) for CrossCurrencySwap: domesticNotional“**; API-CCS-Zeile mit `domesticNotional` im Web-Import → Dialog „Nominal fehlt oder ≤ 0“ (Anhang C, Zeile 7). Die IRS-Vorlage ist kompatibel (`start` wird von der API akzeptiert → 200). Ein Bestand, der im Web als CSV exportiert wurde, lässt sich also nicht per API einspielen (und umgekehrt) – gerade für CCS, das häufigste Produkt mit CSA-Spalte. | Aliase beidseitig ergänzen: Web akzeptiert `domesticNotional`, `effectiveDate`, `domesticPayReceive`, `fixedRate`, `collateralCurrency`; API akzeptiert `notional`, `direction`, `rate`, `collateral`, `start` für CCS (wie schon `start` bei IRS); Vorlagen-Header angleichen (eine Quelle für Spaltennamen, z. B. gemeinsame Konstante im Core oder generierte Doku) und Round-Trip-Test „Web-Vorlage → API-Import“ für alle elf Typen. |

**Ohne Abzug, aber dokumentierenswert:**
- `POST /api/trades/import` hat keinen Validate-only-Modus (`?mode=create|upsert`, unbekannte Query-Parameter wie `dryRun=1` werden **still ignoriert** und die Zeilen gespeichert – Probe: zweiter Aufruf „skipped: exists“). Die Web-Vorschau (Fehlerdialog vor dem Import) deckt die Berater-Persona ab; ein TMS-Batch hätte gern `?dryRun=1` (200 mit `results`, nichts gespeichert). Strenge gegenüber unbekannten Query-Parametern gehört zur API-Dimension.
- IFRS-13-Level bleibt auf `VOL_IMPLAUSIBLE`-Flächen bei Level 2 (siehe R6-4-Rest) – Quant/Compliance.
- `tradeCurveIds`-Ergebnis für einen NOK-Swap ohne Kurve ist `[]` (Core-Helfer liefert nur Kurven, die existieren) – deshalb kann die Palette heute nicht einfach „fehlende Kurven“ abfragen; ein `requiredCurveIds(trade)` im Core würde R7-1 erleichtern.
- Kalender NO behandelt den 24.12. als Geschäftstag (Oslo Børs geschlossen, kein gesetzlicher Feiertag) – Konventionsfrage, kein Fehler.
- Core-Test `par-risk.test.ts` läuft im parallelen Vollauf in den 5-s-Default-Timeout (isoliert 3,9 s, grün) – wie in Runde 6 (`App.test.tsx`) ein Last-Flake; das Web hat `testTimeout` 10 s, der Core nicht (Architektur-Dimension).

---

## 4. Feature-Matrix – Statusänderungen gegenüber Runde 6 (nur DERIVA-Spalte)

| Feature | R6 | R7 | Beleg |
|---|---|---|---|
| Schnelleingabe: Fehler bei unbekannten Tokens in allen Zweigen, doppelte Beträge/Sätze, `imm`, `cash\|physical`, `ndf`, `barrier <typ> <level>` | 🔶 | ✅ | R6-1 |
| CSV-Vorlagen FX-Swap / Basis / Amort / IMM (UI + API), `stepUp`, Barriere-Spalten | ❌ | ✅ | R6-2 |
| API-CSV: unbesicherter CCS (`none`), Schema-Fehler je Zeile statt 400 gesamt | ❌ | ✅ | R6-3 |
| Web ↔ API CSV-Spaltenkompatibilität (CCS) | – | ❌ | R7-5 |
| Plausibilitätsprüfung `volType`/degenerierte Flächen (Core-Warnung, API `warnings[]`, UI-Toast, Wiederholung je Bewertung) | ❌ | ✅ | R6-4 |
| Registrierte Währungen NOK/SEK/DKK/PLN mit Konventionen und Kalendern, Bootstrap per API, „+ Kurve“ in der UI, Swap/FRA/Forward/Report/XVA/Dokumente/EMIR in NOK | ❌ | ✅ | R6-5 |
| Registrierung **weiterer** Währungen/Indizes per API/UI/Snapshot (CZK/HUF …) | ❌ | 🔶 (nur Core-Laufzeit) | R6-5 Rest |
| Neue-Währung-Fluss in der UI: Index-Default mit Kurve, Editor-Selects aus dem Register | – | ❌ | R7-1 |
| Vol-Flächen für neue Währung/Paar in der UI anlegbar | – | ❌ (API ✅) | R7-2 |
| API: Diskontkurven-Zuordnung für neue Währung ohne Snapshot-Roundtrip | – | ❌ | R7-3 |
| Beispiel-Fixings folgen dem Stichtag (API wie UI) | – | 🔶 (UI ✅, API ❌) | R7-4 |
| Historische Fixings im Beispielmarkt, Beispielbuch ohne `MISSING_FIXING` | ❌ | ✅ | R6-6 |
| Prämie als Cashflow-Leg, `barrier.hit`, Hedge-Ergebnis persistiert, Fixings-Editor mit Filter | – | ✅ | Abschnitt 2 |
| Excel / Live-Marktdaten / Rollen / Netting-CVA / VaR / CRIF / Bermudan / TARF / PDF / Inflation | ⏳ | ⏳ Roadmap | kein Abzug |

---

## 5. Verifizierte Positivbefunde (Auszug, alle reproduziert)

- **Instrumente & Fremdwährung:** NOK-OIS-Swap (ACT/365F, 1Y, NO), NOK-NIBOR-6M-Swap dual-curve, NOK-FRA (Index mit Kurve automatisch), EUR/NOK-Forward, EUR/NOK-FX-Swap und -CCS (mit korrekter `COLLATERAL_CURVE_MISSING`-Warnung „EUR\|NOK“), EUR/PLN-Forward per Palette; CZK per Core-Registrierung bootstrapbar und bewertbar. Vier neue Kalender mit korrekten beweglichen Feiertagen (Ostern 2027 = 28.03.) und der Abschaffung des Store Bededag.
- **Marktdaten:** 1 323 Beispiel-Fixings (UI 1 452 bei Stichtag 03.11.2026), Fixings-Editor mit Filter/Seiten, Snapshot-Roundtrip ID-gleich inkl. NOK-Kurve, Vol-Plausibilität auf drei Flächentypen, Snapshot-Import als Undo-Eintrag mit Hinweis-Toast.
- **Integration:** 11 CSV-Vorlagen in Web (Menü) und API (`?type=` Enum, generierte Beschreibung), deutsche Aliase, Zeilen-Schemaprüfung `CSV_ROW_INVALID`, `none`-Semantik, Barriere-Spalten inkl. `barrierHit`; JSON-Import unverändert.
- **Risiko/XVA/Dokumente für die neue Währung:** DV01 46.447 je Kurve `NOK-NOWA`, Par-Risiko, Vega, 16 Szenarien, Grid, CVA 2.563 (Swaption-Replikation), Termsheet/KID/Confirmation „Payer-Zinsswap (NOK)“, EMIR `SRCCSP` mit Notional-Ccy NOK; UI-Report „FAIR VALUE (BILATERAL, IFRS 13 LEVEL 2)“ mit Kurve `NOK-NOWA`, Reporting-Währung NOK in der Rotation, Hedge-Grundgeschäft in NOK.
- **API-Vertrag:** OpenAPI 3.1.0, **41 Operationen**, 44 Fehlercodes, 10 Warnungs-Präfixe (neu `BARRIER_STATE_UNKNOWN:`, `VOL_IMPLAUSIBLE:`), `GET /api/market` mit `currencies`/`indices`; Tests Core 378 / API 114 / Web 307 grün, E2E 395 Checks unter vitest 5 / vite 8.

---

## 6. Was für 100 noch fehlt

1. **R7-1 Fremdwährungs-Fluss rund machen** – Index-Default mit Kurve in der Palette (plus normalisierte Index-Token), Editor-Selects Währung/Index/Collateral aus dem Register und dem Markt (nicht G5-Konstanten). Größter Einzelposten, geschätzt zwei Stunden inkl. Tests.
2. **R6-5 Rest: Registrierung per API/Snapshot/UI** – `PUT /api/market { indices[], conventions{} }` (oder eigener Endpunkt), Snapshot-Block `indices`/`conventions` in der Snapshot-ID, „+ Währung“-Formular (Kalender, Tageszählungen, Indizes) in der Kurvenansicht; damit sind CZK/HUF/CNH ohne Codeänderung erreichbar.
3. **R7-2 „+ Fläche“** in der Marktansicht (Swaption/Caplet/FX) und **R7-3** Diskontzuordnung in `replaceCurve` + `discountCurveId` in `PUT /api/market`.
4. **R7-4** Beispiel-Fixings im API-Rebuild mit Nutzer-Fixings mergen; **R7-5** CCS-Spaltenaliase beidseitig und Round-Trip-Test Web-Vorlage → API.

Mit 1 und 5 läge die Dimension bei ≈ 99,1; 2–4 schließen die Lücke zu 100 für den v1-Scope.

---

## Anhang A – Core-Probe (`node probe-core-r7.mjs` gegen `dist`, Auszug)

```
engine deriva-pricing-core/0.2.0 valDate 2026-09-03 · snapshotId sample 3803d0191a972626
== R6-6 == fixings 1323 { EURIBOR-6M: 576, EURIBOR-3M: 576, ESTR: 171 } first 2024-06-03 last 2026-09-02
IRS-0001 (EURIBOR-6M ab 2024-06-17) PV -171352 warnings []
running EUR EURIBOR-3M ab 2025-03-05: warnings [] · running EUR ESTR ab 2026-03-05: warnings []
running USD SOFR / GBP SONIA / CHF SARON / JPY TONA ab 2025-09-05: MISSING_FIXING (nur EUR-Indizes im Beispielmarkt)      (Hinweis, kein Abzug)
IRS-0001 at 2026-10-05: fixings 1389 warnings []
== R6-4 ==
validateVolSurfaces(lognormal w/ normal numbers) [] · volSurfaceWarnings → ["VOL_IMPLAUSIBLE: swaptionVols.USD: median lognormal vol 0.86 % is below 1 % – … check the volType of the import"]
zero cube → "swaptionVols.EUR is degenerate – every vol is 0" · sample market → [] · fx atm ×100 → "8 of 8 lognormal vols above 300 %" · caplet lognormal → Warnung
USD swaption on lognormal-cube: PV 2 warnings ['VOL_IMPLAUSIBLE: swaption surface USD-SWAPTION-NORMAL: …'] · on sample cube PV 148506 [] · EUR swaption on zero cube PV 0 + Warnung
== R6-5 ==
knownCurrencies [CHF DKK EUR GBP JPY NOK PLN SEK USD] · knownIndices 21 (CIBOR-3M/6M DESTR … NIBOR-3M/6M NOWA POLONIA STIBOR-3M/6M SWESTR WIBOR-3M/6M)
NOK conv fixed 1Y 30/360 · NIBOR-6M 6M · NO · spotLag 2 · NOWA ACT/365F payLag 2 · calendars NO/SE/DK/PL ok
NO 2027-05-17 false · NO 2027-05-06 false · SE 2027-06-25 false · SE 2027-01-06 false · DK 2027-05-17 false · DK 2027-04-22 TRUE (Store Bededag abgeschafft) · PL 2027-05-03 false · PL 2027-11-11 false · PL 2027-05-27 false · DK/SE 2027-12-31 false
bootstrapCurve NOK-NOWA {curve,residuals,mergedQuotes} · validateMarket(m3) []
NOK OIS swap pv 0 par 4.000 % legs ACT/365F/1Y/NO · NOK vanilla default index NIBOR-6M → THROW CURVE_NOT_FOUND NOK-NIBOR-6M          (← R7-1)
NOK NIBOR-6M swap with curve pv 1333812 par 4.300 % · NOK cap → "No caplet vol surface – using 60bp normal vol" · NOK swaption → "70bp" (← R7-2)
NOK FRA 3x9 idx NOWA pv -159272 · EURNOK forward pv 46403 fair 11.7474 · EURNOK option (no surface) "using 8% vol" · with surface warnings []
snapshot round trip m3: idEqual true hasNok true disc NOK-NOWA · snapshot keys ohne indices/conventions                              (← R6-5 Rest)
registerRateIndex CZEONIA / PRIBOR-6M · registerSwapConventions CZK → knownCurrencies + CZK · CZK OIS bootstrap + swap pv 0 warnings []
registerRateIndex invalid → THROW INVALID_CURVE_SPEC 'overnight indices use tenor "1D"'
STANDARD 16 HIST 6 · curves 8 · fxVols 10 · swaptionVols EUR,USD,GBP,CHF,JPY · caplet 5
```

## Anhang B – API-Probe (`npx tsx probe-api*-r7.mts`, `app.inject`, Auszug)

```
openapi 3.1.0 ops 41 · error codes 44 · Warnungs-Präfixe: MISSING_FIXING MISSING_FX_FIXING SETTLES_TODAY EXPIRED EXPIRES_TODAY COLLATERAL_CURVE_MISSING VOL_TYPE_CONVERTED HAZARD_FLOORED BARRIER_STATE_UNKNOWN VOL_IMPLAUSIBLE
import ?type enum: InterestRateSwap,FxForward,CapFloor,Swaption,FxOption,CrossCurrencySwap,FRA,FxSwap,BasisSwap,AmortisingSwap,ImmSwap · Beschreibung nennt none/FxSwap/AmortisingSwap/stepUp/barrierType/ImmSwap
GET /api/market props: … fixings 1323, currencies [9], indices 21 · PUT /api/market props: valuationDate,fxSpots,fxFixings,swaptionVols,capletVols,fxVols,fixings,fxSpotDates,missingFixingPolicy (kein discountCurveId ← R7-3)
== R6-6 == GET /api/trades?price=1: IRS-0001 … FXO-0002 alle warnings []
PUT valuationDate 2026-11-03 → fixings 1323, letztes Fixing 2026-09-02 → EURIBOR-3M-Swap ab 15.09.2026: MISSING_FIXING … 2026-09-11 (UI: 1452 Fixings)   (← R7-4)
== R6-4 == PUT lognormal cube: 200 warnings [VOL_IMPLAUSIBLE …] · price USD swaption: 200 pv 2 warnings [VOL_IMPLAUSIBLE …] · report level 2 + Warnung · PUT zero cube 200 degenerate · PUT snapshot lognormal 200 + Warnung
== R6-5 == bootstrap NOK/NOWA 200 (8 nodes) · SEK/SWESTR 200 · DKK/DESTR 200 · PLN/POLONIA 200 · PLN/WIBOR-6M 200 · CZK/CZEONIA 422 UNKNOWN_INDEX · HUF/BUBOR-6M 422 UNKNOWN_INDEX   (← R6-5 Rest)
replaceCurve NOK-NOWA 200 → discountCurveId weiter G5 → NOK-Swap rejected NO_DISCOUNT_CURVE · PUT /api/market {discountCurveId} 400 VALIDATION_ERROR · PUT snapshot + discountCurveId.NOK 200   (← R7-3)
NOK swap import (NOWA) imported pv -444741 · (ohne Index → NIBOR-6M) rejected CURVE_NOT_FOUND
NOK report level 2, nennt NOWA · risk dv01 46447 byCurve [NOK-NOWA] · par risk 200 · vega 200 · scenarios 16 · grid 200 · xva cva 2563 Sorensen–Bollier · termsheet 5 "Indikatives Termsheet – Payer-Zinsswap (NOK)" · kid 9 · confirmation 5 · EMIR SRCCSP notionalCurrency NOK
EURNOK forward 200 46404 · EURNOK option "using 8% vol" 281688 → PUT fxVols EURNOK 200 → option warnings [] 267428 (← R7-2, API kann) · from-template CCS EURNOK 200 + COLLATERAL_CURVE_MISSING "EUR|NOK"
routes mit register/indices: keine
== R6-2/R6-3 == FxSwap/BasisSwap/AmortisingSwap/ImmSwap Round-Trip: je 200 imported 1 (pv -6970 / 94403 / -565381 / -119312) · deutsche Aliase 200
CCS none/USD/leer/unbesichert/XX: 200 imported 4 rejected 1 · IRS ok+baddate+id-mit-Leerzeichen+ohne-id: 200 imported 2 rejected 2 · FxOption barrier rows: imported 2 rejected 1 (Sideways)
Web-CCS-Spalten an API: 400 CSV_INVALID "lacks required column(s): domesticNotional" · Web-IRS-Spalten (start): 200   (← R7-5)
dryRun=1 / dryRun=true: Zeilen werden gespeichert (zweiter Aufruf "skipped: exists") – kein Validate-only-Modus (Hinweis)
hazard-curve 200 · audit chainValid true · historical scenarios 6
```

## Anhang C – UI-Probe (Playwright/Chromium 1194 gegen `vite preview :5001`, Auszug)

```
Palette R6-1: "irs sek 5y pay 3% 10m" → ⚠ Keine Kurve für SEK im Markt – … „+ Kurve“ … · "irs xyz …" → ⚠ Unbekannte Währung „XYZ“ – Währungen: EUR, USD, GBP, CHF, JPY, NOK, SEK, DKK, PLN; erwartet: …
  "irs 10y pay 3.1% 10m mtm" → ⚠ Unbekanntes Token „mtm“ · "ccs … foo" / "fxs … xyz" / "basis … foo" → ⚠ · "irs 10y pay 3.1% 10m 6m" → ⚠ Betrag doppelt angegeben („10m“ und „6m“) · "… 3.1% 3.2% …" → ⚠ Satz doppelt
  "fxo … barrier 1.05" / "barrier di" → ⚠ Barriere unvollständig – Format: barrier uo|ui|do|di <level> · "fxf … 31.02.2027" → ⚠ Ungültiges Datum
  "swpt usd 1y5y payer 3.5% 10m cash" → Payer-Swaption USD 1Yx5Y @ 3,500 % · Nominal 10.000.000 · Barausgleich ✓ · "fxo eurusd put 1.15 3m 9m barrier do 1.05" → … · Barriere Down-and-Out ✓
  "fxf eurusd 2m 1.1725 6m ndf" → … · NDF (Ausgleich in USD) ✓ · "imm 2y pay 3% 10m" → IMM-Payer-Swap EUR 2Y … IMM ab 16.09.2026 ✓ · "imm usd …" ✓ · "ois usd 2y pay 4% 10m" → SOFR ✓ · step / amort / @Kontrahent ✓
  "fxo eurnok call 11.5 1m 6m" → … ⚠ keine FX-Vol-Fläche für EUR/NOK (Fallback 8 %, Level 3) · "irs nok 5y pay 4% 10m nibor6m" → ⚠ Unknown rate index: NIBOR6M (englisch)
R6-2: Export-Menü 11 Vorlagen (Zinsswap, FX-Forward, Cap/Floor/Collar, Swaption, FX-Option, Cross-Currency-Swap, FRA, FX-Swap, Tenor-Basis-Swap, Amortisierender Swap, IMM-Swap)
  CSV-Import (FXS/BASIS/AMORT/IMM/IRS-Staffel/CCS(API-Spalten)/FXS-defekt) → Dialog "7 Nominal fehlt oder ≤ 0 · 8 Far-Valuta muss nach der Near-Valuta liegen" · "5 gültige Zeilen importieren" → "5 Trades aus CSV importiert"; Blotter: FXS-R7 BASIS-R7 AMORT-R7 IMM-R7 IRS-R7-STEP ✓ (CCS-R7 ✗ ← R7-5)
R6-6: Blotter IRS-0001 … -279.451 6.971 Live OK · kein ⚠ im Blotter · Markt: "1323 Fixings · neueste zuerst", Filter ✓ · nach "stichtag 03.11.2026": 1452 Fixings
R6-5 "+ Kurve": Währungen CHF, DKK (ohne Kurve), EUR, GBP, JPY, NOK (ohne Kurve), PLN (ohne Kurve), SEK (ohne Kurve), USD · NOK-Indizes NIBOR-3M | NIBOR-6M | NOWA (OIS, ACT/365F, NO)
  → Toast "Kurve NOK-NOWA aus 7 Quotes angelegt · Spot EUR/NOK 11,6200" (Rückgängig) · Tabs … |EUR/USD CSA|NOWA · Chip "Sample EoD · modifiziert" · Persistenz extraCurves [NOK-NOWA, NOK-NIBOR-6M]
  "irs nok 5y pay 4% 10m" → Vorschau ohne Hinweis → IRS-0003 "– – Indikation Fehler" · Pricing: "nicht bewertet – Eingaben prüfen · Kurve nicht im Markt-Snapshot: Kurve NOK-NIBOR-6M nicht im Markt-Snapshot"
  Editor IRS-0003: Währung = EUR [EUR/USD/GBP/CHF/JPY] · Index = EURIBOR-3M [7 feste] · Collateral [unbesichert/EUR-CSA/…/JPY-CSA]   (← R7-1, Screenshot 06)
  "ois nok 5y pay 4% 10m" → IRS-0004 PV 0, Par 4,0000 % ✓ · "fra nok 3x9 pay 4.5% 10m" → FRA-0002 PV -1.371 (NOWA) ✓ · "cap nok 5y 4.5% 10m" → CAP-0002 "Fehler"
  zweite Kurve NIBOR-6M ("Depots bis 6M, darüber Par-Swaps · Dual-Curve gegen NOK-NOWA") → IRS-0003 11.483 396 OK · CAP-0002 7.797 159 ⚠ 1 ("Keine Caplet-Vol-Fläche – 60 bp Normal-Vol verwendet")  (← R7-2)
  Report OIS NOK: "FAIR VALUE (BILATERAL, IFRS 13 LEVEL 2)", nennt NOK-NOWA ✓ · Reporting-Währung NOK nach 5× c ✓ · Hedge-Grundgeschäft Währung [EUR/USD/GBP/CHF/JPY/NOK] ✓
  Markt: FX-Vol-Paare weiter 10 (kein EUR/NOK), Cubes/Caplet ohne NOK, kein "+ Fläche" · FX-Fixing-Paare inkl. EUR/NOK ✓ · "fxo eurnok call 11.7 1m 6m" → PV 281.688 NOK (Fallback 8 %)
R6-4: Snapshot-Import (Lognormal-USD-Cube) → "Snapshot „Sample EoD“ importiert · ID 8b79d15b9804cac2 …" + "Snapshot importiert – Hinweis: Vol-Fläche unplausibel: Swaption-Cube USD – Median der Lognormal-Vols 0,86 % liegt unter 1 %; …" · Chip importiert · Ctrl+Z → "Rückgängig: Snapshot „Sample EoD“ importiert"
Hilfe (?): "+ Kurve" ✓ imm ✓ cash ✓ ndf ✓ barrier ✓ FX-Fixings ✓ Snapshot ✓ NOK ✓
page errors: [] · smoke.mjs (Port 5001): E2E OK (395 checks)
```

Probe-Skripte, Ausgaben und Screenshots (`shots-r7/01-blotter-after-import.png`, `02-add-curve.png`, `03-nok-pricing.png`, `04-market.png`, `05-editor.png`, `06-nok-irs-editor.png`, `07-nok-report.png`, `08-market-nok.png`, `09-nok-ois-report.png`, `10-nok-cap-report.png`) im Scratchpad der Review-Session.
