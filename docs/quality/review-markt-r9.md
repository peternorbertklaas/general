# Re-Review Runde 9: Marktabdeckung Features & Module (Dimension 1, Gewicht 20 %) – DERIVA 0.2.0

**Reviewer-Rolle:** Senior Product Analyst, Treasury- und Derivateberatungssoftware (LPA Capmatix/Captano, Finbridge, d-fine, Bloomberg SWPM/OVML/VCUB/DLIB/MARS, LSEG Swap Pricer, Murex, FIS Front Arena, Numerix, Quantifi, FINCAD, ORE/QuantLib) · **Stand:** 04.09.2026, Branch `claude/derivatives-trading-platform-1arsyu`, Commit `d470e6a` (Core-`dist` frisch gebaut, Arbeitsbaum sauber) · **Modus:** Review only, keine Quelldatei geändert
**Baseline:** `docs/quality/review-markt-r8.md` (Runde 8, Score 98 / 100) mit den Befunden R8-1…R8-5 und dem Rest von R7-5
**Geprüft (Laufzeit):** `npx vitest run` je Paket → **Core 421/421** (29 Dateien), **API 137/137** (9 Dateien), **Web 362/362** (41 Dateien); `vite build` erfolgreich; Web-App unter `vite preview --port 5001 --strictPort` mit Playwright/Chromium 1194 in drei Läufen durchgeklickt (`probe-web-r9.mjs`, `probe-web2-r9.mjs`, `probe-web3-r9.mjs`, Anhang C, `pageerror: []` in allen Läufen); offizielle E2E-Suite `apps/web/e2e/smoke.mjs` auf Port 5001 (Ergebnis Abschnitt 5); API per `buildApp` + `app.inject` (`probe-api-r9.mts` … `probe-api5-r9.mts`, Anhang B); Core-Proben gegen `packages/pricing-core/dist` (`probe-core-r9.mjs`, Anhang A); die elf Web-CSV-Vorlagen wurden aus `CSV_IMPORT_TEMPLATES` erzeugt (`web-templates/*.csv`) und an `POST /api/trades/import` geschickt, die elf API-Vorlagen (`CSV_TEMPLATES`, Header + Beispielzeile) durch den Web-Reader `tradesFromCsv` **und** durch den Blotter-Import der laufenden App. **Vollständiger Fremdwährungs-Fluss:** Kalender `CZ` (Prager Feiertage) + `CZEONIA`/`PRIBOR-6M` + CZK-Konventionen per API registrieren → `CZK-CZEONIA` bootstrappen → CZK-Swap importieren, Par-Risiko, Report, Termsheet, EMIR, Szenarien, XVA → Snapshot exportieren → in der Workstation importieren → `irs czk 5y pay 4% 100m` → Par-Risiko, Report, Kurvenansicht, Reporting-Währung CZK, Re-Export. Alle Zahlen unten stammen aus diesen Läufen.
**Roadmap-Regel:** In `02-epics-und-user-stories.md` / README als ⏳ dokumentierte Posten (Bermudan/CMS/Range Accrual, TARF/Participating Forward, Marktdaten-Adapter & Kurven-Governance, OIDC/Rollen/DB, Excel-Add-in, Monte-Carlo-Netting-XVA/FVA, PDF-Template-Service, Inflations-ZC-Swap, PRIIPs Annex-II-MC, SIMM/CRIF, VaR/Attribution, Batch-EoD/Webhooks, Designationsmemo, EMIR Tabelle 3/UPI/XML) werden **nicht** als Lücke gewertet.

---

## 1. Score

### **Marktabdeckung Features & Module: 99 / 100** (rechnerisch 98,8; Runde 1: 60, Runde 2: 86, Runde 3: 97, Runde 4: 98, Runde 5: 99, Runde 6: 98, Runde 7: 98, Runde 8: 98)

**In einem Satz:** Alle fünf Runde-8-Befunde sind in Core, API **und** Workstation nachweisbar geschlossen – der API-Snapshot mit Envelope `calendars`/`indices`/`conventions` registriert beim Import in der Workstation („registriert: 2 Indizes CZEONIA, PRIBOR-6M · Konventionen CZK · Kalender CZ“), `irs czk 5y pay 4% 100m` bewertet auf `CZK-CZEONIA` (PV −36.827 EUR, identisch mit der API), der Editor zeigt CZK/CZEONIA/CZK-CSA, die Kurvenansicht hat den Tab „CZEONIA (aus Snapshot)“ mit Pillars und Forwards, „+ Währung“/„+ Kalender“ registrieren HUF mit eigenem Feiertagskalender und „+ Kurve“ bewertet danach einen HUF-Swap mit **Par-Buckets auf HUF-HUFONIA** (641 vs. Zero-DV01 651), `POST /api/market/calendars` verschiebt Prager Zahltage tatsächlich (05.07.2027 → 07.07.2027, 05.07.2028 → 06.07.2028), Par-Risiko nennt Kurven ohne Quotes statt einer stillen Null, alle elf Web-Vorlagen importieren per API und alle elf API-Beispielzeilen im Web-Builder, der Kern lehnt ein CZK-Leg mit EURIBOR-6M ab. **Zu 100 fehlen fünf kleine Restposten**, die in der neunten Prüfung des durchgängigen Flusses sichtbar wurden: das Snapshot-Format trägt keine Bootstrap-Quotes, sodass Par-Risiko im Import-Modus (API und Workstation) leer bleibt, obwohl die API die Quotes kennt; „+ Währung“ ist im Import-Modus bedienbar, führt aber in eine Sackgasse (Toast verweist auf das gesperrte „+ Kurve“); die Schnelleingabe `basis [ccy]` baut für jede Währung EURIBOR-Legs und liefert für NOK/CZK einen Fehler-Trade; die dokumentierte API-Vorlagendatei (ohne Spalte `type`) wird vom Blotter-Import abgelehnt; und die Palette erkennt zur Laufzeit registrierte Index-Token (`pribor6m`) nicht. Summe der Abzüge −1,2.

**Abzugsherleitung** (Rubrik: kritisch −10…−25, fehlendes Kernfeature −3…−8, UX-Reibung −1…−3, kosmetisch −0,2…−1; Roadmap-dokumentiert = 0; keine Doppelzählung mit Quant/UI/Flows/API; Teilbeträge unterhalb der Bänder wie in den Vorrunden für Randfälle):

| Bereich | R1 | R2 | R3 | R4 | R5 | R6 | R7 | R8 | R9 | Restlücken (Nummern → Abschnitt 3) |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| A Instrumente & Strukturen | −10 | −3,7 | −0,8 | −0,5 | 0 | 0 | 0 | 0 | −0,3 | Schnelleingabe `basis [ccy]` mit festen EURIBOR-Legs → Fehler-Trade für NOK/CZK (R9-3) |
| B Kurven & Marktdaten | −5 | −0,7 | −0,2 | 0 | −0,3 | −0,5 | −0,9 | −0,9 | −0,4 | „+ Währung“ im Import-Modus als Sackgasse (R9-2 −0,2); Palette kennt registrierte Index-Token nicht (R9-5 −0,2) |
| C Modelle | −1 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | – |
| D Sensitivitäten | −2 | −0,8 | 0 | 0 | 0 | 0 | 0 | −0,4 | −0,3 | Snapshot ohne Bootstrap-Quotes → Par-Risiko im Import-Modus leer (R9-1) |
| E Szenarien / VaR | −1 | −0,5 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 16 + 6, Grid mit `fxCurrency CZK`; VaR = Roadmap |
| F XVA | −2,5 | −0,5 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | CVA/DVA CZK-Swap 1.078 / 1.219 EUR; Netting = Roadmap |
| G Hedge Accounting | −4 | −3,0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | – |
| H Regulatorik | −4 | −1,5 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | EMIR-Datensatz CZK (`SRCCSP`, Bewertungswährung CZK bei `reportingCurrency=CZK`) |
| I Dokumente | −2,5 | −1,0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | Termsheet CZK 200 (Markdown + Sections) |
| J Workflow / Beratung | −2,5 | −0,3 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | Freigabe = Roadmap |
| K UI | −0,5 | −1,3 | −0,8 | −1,5 | 0 | −0,5 | −0,5 | 0 | 0 | R8-1/R8-5 behoben (Envelope, „+ Währung“, Snapshot-Tabs) |
| L Integration | −2,5 | −0,5 | −0,3 | −0,2 | −0,2 | −0,7 | −0,2 | −0,3 | −0,2 | 11/11 Vorlagen beidseitig; API-Vorlagendatei ohne `type` im Blotter abgelehnt (R9-4) |
| M Admin | −2,5 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | Rollen/DB = Roadmap |
| Sonstiges (Beispielmarkt) | – | −0,5 | −0,5 | −0,2 | −0,2 | −0,2 | 0 | 0 | 0 | – |
| **Summe** | **−40** | **−14,3** | **−2,6** | **−2,4** | **−0,7** | **−1,9** | **−1,6** | **−1,6** | **−1,2** | → **98,8 ≈ 99 / 100** |

Einordnung (gleiche Skala, v1-Workstation-Scope, Einschätzung): Bloomberg SWPM+OVML+VCUB+MARS ≈ 85, LPA Capmatix OTC ≈ 75, ORE ≈ 60, Kyriba/Coupa ≈ 55. Die funktionale Abdeckung für Vanilla-Zins/FX ist in G5 + NOK/SEK/DKK/PLN vollständig und **für jede registrierbare Währung in API und Workstation** offen (CZK und HUF end-to-end geprüft, inkl. eigener Kalender); die Restdifferenz zu 100 liegt in Randbereichen des Multi-Currency-Flusses (Quotes im Snapshot, Import-Modus, Palette-Grammatik, Vorlagendatei).

---

## 2. Status der Runde-8-Befunde R8-1…R8-5 und des R7-5-Rests

Legende: **behoben** = im Code vorhanden **und** per Probe/UI reproduziert · **teilweise** = Kern vorhanden, benannter Rest · **offen** = unverändert.

| # | Befund R8 | Status | Nachweis (Datei / Probe / UI) | Rest |
|---|---|---|---|---|
| **R8-1** | Register erreicht die Workstation nicht (Envelope ignoriert, CZK in Palette unbekannt, Editor baut CZK-Swap mit EURIBOR-6M, kein „+ Währung“) | **behoben** | **Code:** `apps/web/src/lib/register-envelope.ts` (`validateEnvelope` mit Core-Validatoren `validateCustomCalendar`/`validateRateIndex`/`validateSwapConventions(…, { pendingIndices })`, `registerEnvelope` Kalender → Indizes → Konventionen, `exportEnvelope`, `unregisterEnvelope`), `snapshot-import.ts:71–74` (Envelope-Prüfung vor `deserializeMarket`), `views/AddCurrencyForm.tsx` (`buildCurrencyEnvelope`, „+ Kalender“), `register.ts:25` („Für CZK sind keine Swap-Konventionen registriert – … „+ Währung““), Core `pricing/price.ts:200` (Leg-Währung = Index-Währung). **UI (Anhang C, frischer LocalStorage):** Import des API-Snapshots `snapshot-czk-r9.json` → Toast „Snapshot „Sample EoD“ importiert · ID b8949627e496316a · **registriert: 2 Indizes CZEONIA, PRIBOR-6M · Konventionen CZK · Kalender CZ**“; `irs czk 5y pay 4% 100m` → „Payer-Swap CZK 5Y @ 4,000 % · Nominal 100.000.000 · **CZEONIA (Kurve vorhanden; PRIBOR-6M ohne Kurve)**“, Enter → „Angelegt: IRS-0004 · **PV −36.827 EUR**“ (API: −36.827), Editor Währung **CZK**, Index **CZEONIA** [CZEONIA / PRIBOR-6M (ohne Kurve)], Collateral […/CZK-CSA]; Risiko DV01 1.899 · FX-Delta CZKEUR −368 · Theta 45 · Gamma −0,92; `ois`/`fra`/`cap`/`floor`/`swpt`/`amort`/`imm czk …` und `fxf`/`fxo eurczk …`, `ccs eurczk` alle mit Vorschau; Reload → CZK-Trade weiter bewertet, Re-Export trägt `calendars`/`indices`/`conventions`; Reporting-Währungs-Zyklus EUR → USD → GBP → CHF → JPY → **CZK**. „+ Währung“ HUF mit OIS `HUFONIA`, IBOR `BUBOR-6M`, „+ Kalender“ `HU` (4 Feiertage, ISO und deutsch) → Toast „Registriert: 2 Indizes HUFONIA, BUBOR-6M · Konventionen HUF · Kalender HU“, Fokus auf „+ Kurve“, dort HUF (ohne Kurve) mit Indizes „BUBOR-6M (IBOR 6M, ACT/360, HU)“ / „HUFONIA (OIS, ACT/360, HU)“, Kurve aus 6 Quotes + Spot 395 → `irs huf 5y pay 6.5% 500m` → IRS-0003 PV −193.067 EUR. **Core:** `validateTrade` CZK-Leg mit `EURIBOR-6M` → `INVALID_TRADE` („currency CZK does not match the currency EUR of index EURIBOR-6M“), API 422; Editor-Index-Liste enthält nur CZK-Indizes. | Palette erkennt registrierte Index-Token nicht (`pribor6m` → „Unbekanntes Token“, R9-5); „+ Währung“ im Import-Modus ohne Folgeaktion (R9-2) |
| **R8-2** | Kein Kalender-Register außerhalb des Core (Fehltext führt ins Leere, CZK nur mit TARGET) | **behoben** | **API:** `routes/market.ts:367–420` (`POST /api/market/calendars`, 201/200, eingebaute IDs/Aliase → 400 `INVALID_CURVE_SPEC` `builtIn`, ungültiges Datum → 400 `INVALID_CALENDAR` `problems[]`, Audit `register.calendar`), `GET /api/market` `calendars` (12 eingebaute mit `builtIn: true` inkl. `US-SIFMA`, registrierte mit `holidays`-Zahl), Envelope `calendars[]` (`routes/snapshot.ts:64, 149–153, 205`), Fehltext `withCalendarHint` nennt den Endpunkt. **Probe (Anhang B):** `POST /calendars TARGET` → 400 builtIn; `["2027-02-30"]` → 400 `INVALID_CALENDAR`; `CZ` (13 Feiertage) → **201**, erneut → **200 replaced**; `POST /indices CZEONIA/PRIBOR-6M` mit `fixingCalendar: "CZ"` → 201/201; `HUFONIA` mit `HU` → 400 „unknown calendar "HU" (register it with **POST /api/market/calendars**)“; Snapshot-Envelope `calendars [["CZ",14]]`, Re-Import in frischer App → 200, Envelope mit `TARGET` → 400 `INVALID_CURVE_SPEC` `section calendars`, nichts registriert. **Wirkung:** Cashflow-CSV eines CZK-Swaps ab 05.07.2027 auf Kalender `CZ` → Start **07.07.2027**, Periodenende **06.07.2028** (05.07.2028 im Register), auf `TARGET` → 05.07.2027 / 05.07.2028. **Core:** `CustomCalendarJson`, `registerCalendar(json)`, `listCustomCalendars`, `isBuiltInCalendar`, `validateCustomCalendar`; `CZ+TARGET` joint → 06.07.2027 kein Geschäftstag. **UI:** „+ Kalender“ in „+ Währung“ (Anhang C, HU), Envelope-Export `calendars [CZ, HU]`. | `registerCalendarHolidays` (Feiertags-Feed für eingebaute Kalender) weiterhin nur Core – gehört zum Marktdaten-Adapter (US-2.10, Roadmap), kein Abzug |
| **R8-3** | Par-Risiko ignoriert Zusatz-/Import-Kurven, stille Null | **behoben** (Rest → R9-1) | **API:** `lib/store.ts:180–184` (`parRiskSpecs`: Sample + `runtimeCurves`), `routes/risk-extended.ts:48–56, 138–141, 162–166` (`curvesWithoutQuotes`, `PAR_RISK_INCOMPLETE:`), `POST /api/market/curves` antwortet `parRiskTracked: true`. **Probe:** CZK-Swap nach `/curves` → `curves [["CZK-CZEONIA", 6]] total 1852 curvesWithoutQuotes []`; Portfolio-Variante je Trade; nach Snapshot-Import in frischer App → `curves 0`, **`curvesWithoutQuotes ["CZK-CZEONIA"]`**, Warnung „PAR_RISK_INCOMPLETE: curve CZK-CZEONIA has no bootstrap quotes in the store …“ statt 0. **Web:** `PricingWorkspace.tsx:111–123` (`parSpecs` = Sample + `extraCurveSpec` je `extraCurves`, `parCoverage`), `:523–529` (Hinweis „Par-Risiko nur für Kurven mit Quotes (n von m)“), `:799` (Konvexitätstext nur bei voller Abdeckung). **UI:** HUF-Swap auf „+ Kurve“-Kurve → **„PAR-DV01 GESAMT 641 EUR · ZERO-DV01 651 EUR · DIFFERENZ −11 EUR · Konvexität der Quotes / Kurvenkopplung · HUF-HUFONIA Σ 641 EUR“**; CZK-Swap im Import-Modus → Warnung „Par-Risiko nur für Kurven mit Quotes (0 von 1). Die Kurven des importierten Snapshots tragen keine Bootstrap-Quotes – …“, Panel „0 EUR … Kurven ohne Quotes fehlen im Par-Risiko – Differenz nicht als Konvexität deutbar“. | Der Hinweis ist ehrlich, aber der EoD-Fluss (API bootstrappt, Workstation importiert) bleibt ohne Par-Buckets, weil das Snapshot-Format keine Quotes trägt → **R9-1** |
| **R8-4 / R7-5 Rest** | Nur 6 von 11 Web-Vorlagen API-lesbar (Spread-Einheit, `tenor`/`expiry`/`period`, FXF-Vokabular) | **behoben** | **API:** `lib/csv-import.ts:83–85` (`SPREAD_UNIT_NOTE`, Heuristik |x| ≥ 1 = bp wie im Web), `:117–122` (`alternative` FXF `buyCurrency/buyAmount/sellCurrency/sellAmount`), `:228–344` (Aliase `tenor`/`expiry`/`expirydate`/`period`, Vokabelregeln `canonicalColumn`), `:617–641`, `:710–711`. **Web:** `portfolio-io.ts` Vorlagen mit `-20 bp`/`5 bp`, `HEADER_ALIASES` mit `effectivedate`, `terminationdate`, `expirydate`, `tenor`, `period`, `pair`… **Probe:** echte Web-Vorlagen (Header + Beispielzeile) → API: **IRS −481.394 · FXF 2.769 · CAP 96.080 · SWPT 99.961 · FXO 52.360 · CCS 9.600 · FRA −1.124 · FXS 2.450 · BASIS −13.427 · AMORT −191.253 · IMM −119.312 – 11/11 `imported`**; API-Beispielzeilen (mit Spalte `type`) → `tradesFromCsv` 11/11 ohne Fehler, im Blotter der laufenden App 11/11 ohne Bewertungsfehler (CCS-CSV-1 „Cross-Currency-Swap EUR/USD 5Y ESTR −20,0 bp vs SOFR … 11.157“). | Die API-Vorlagendatei selbst (ohne `type`, Typ steckt in `?type=`) wird vom Blotter mit „Spalte „Typ“ fehlt“ abgelehnt → **R9-4** |
| **R8-5** | Snapshot-Kurven außerhalb des Sample-Sets in der Kurvenansicht unsichtbar | **behoben** | `CurvesView.tsx:365–389` (`fromSnapshot`: jede Kurve aus `baseMarket.curves`, die weder Sample noch `extraCurves` ist, als Tab `snap:<id>`, schreibgeschützt), `:586–588, 796, 850–854, 882–883` (Badge, Hinweis statt Quote-Tabelle, Pillar-Tabelle, Forwards). **UI:** nach Import Tabs `… HUFONIA · CZEONIA (aus Snapshot)`; Tab `curve-tab-CZK-CZEONIA` → Badge 1, Hinweis 1, **7 Pillars**, Forwards-Canvas 1; nach Reload weiterhin vorhanden. | Vergleichskurve für CZK „–“ (nur eine CZK-Kurve im Snapshot – korrekt) |

**Weitere Runde-8-Maßnahmen (geprüft, ohne eigene Befundnummer):** `POST /api/market/curves` im Import-Modus legt eine Dual-Curve `CZK-PRIBOR-6M` (Deposit + Swap-Quotes, `discountCurveId: CZK-CZEONIA`) auf den importierten Snapshot (`discountCurveSet false`, `parRiskTracked true`), danach CZK-Swap auf PRIBOR-6M `imported` (−2.546) und CZK-Swaption `imported` mit Fallback-Vol-Warnung; `PUT /api/market { valuationDate }` behält `CZK-CZEONIA` und die Diskontzuordnung (N8-01, Warnung `MISSING_FIXING` für CZEONIA korrekt, Fixing per `PUT fixings` setzbar → 1 324); Export-ETag `"<snapshotId>-<registerHash>"`; Hilfe-Overlay nennt „+ Kurve“, „+ Währung / + Kalender“, „+ Paar“, „+ Fläche“ (`HotkeyOverlay.tsx:48–51`); `swpt czk 1y5y payer 4% 50m cash` → „Underlying CZEONIA (Kurve vorhanden; PRIBOR-6M ohne Kurve) · ⚠ kein Swaption-Vol-Cube für CZK (Fallback-Vol, Level 3 – … „+ Fläche“)“; **44 OpenAPI-Operationen, 45 Fehlercodes (`UNKNOWN_CALENDAR`, `INVALID_CALENDAR`), 12 Warnungs-Präfixe (`MARKET_STATE_DROPPED`, `PAR_RISK_INCOMPLETE`)**.

**Runde-1…7-Posten:** unverändert geschlossen; G14/G24/G26 bleiben dokumentierte Roadmap (kein Abzug).

---

## 3. Neue Befunde (Runde 9)

Severity: **Niedrig** = Lücke zweiter Ordnung für eine v1-Persona · **Kosmetisch** = Voreinstellung/Konsistenz/Beispieldaten.

| # | Sev. (Abzug) | Ort | Befund (reproduziert) | Fix |
|---|---|---|---|---|
| **R9-1** | Niedrig (−0,3) | `packages/pricing-core/src/market/snapshot.ts:27–58` (`serializeMarket`: Kurven nur als `nodes`, kein `quotes`), `apps/api/src/routes/snapshot.ts:119–153` (Envelope `calendars`/`indices`/`conventions`, **kein `quotes`**), `apps/api/src/lib/store.ts:180–184` (`parRiskSpecs` kennt die Quotes der Laufzeitkurven – nur im Prozess), `apps/web/src/views/PricingWorkspace.tsx:112` (`if (s.marketSource === "import") return {}`), `apps/web/src/state/store.ts` (`extraCurves` mit Quotes werden beim Import „gemerkt“, nicht exportiert) | **Das Snapshot-Format trägt keine Bootstrap-Quotes, deshalb ist Par-Risiko im Import-Modus leer – in der API und in der Workstation.** API-Probe: `CZK-CZEONIA` per `POST /curves` (Quotes im Store) → `GET /api/market/snapshot` → Kurve mit Schlüsseln `id, currency, dayCount, interpolation, nodes, extrapolation, meta`, `JSON.stringify(snapshot).includes("quotes")` = **false**; `PUT` in frischer App → `POST /api/risk/par` → `curves 0 · curvesWithoutQuotes ["CZK-CZEONIA"] · PAR_RISK_INCOMPLETE`. UI: derselbe Snapshot → Karte „Par-Risiko nur für Kurven mit Quotes (**0 von 1**) … „Zum Sample-Markt“ wechseln oder die Kurve mit „+ Kurve“ aus Quotes anlegen“, „Berechnen“ → „PAR-DV01 GESAMT 0 EUR · ZERO-DV01 1.899 EUR · DIFFERENZ −1.899 EUR“. Der Hinweis ist korrekt (R8-3 erfüllt), aber der zentrale EoD-Fluss einer Treasury – Kurven im TMS/API aus Quotes bauen, Workstation importiert den Snapshot, Risk hedgt über Par-Buckets – endet ohne Par-Risiko; der einzige Ausweg („Zum Sample-Markt“ + „+ Kurve“ mit abgetippten Quotes) verwirft den EoD-Snapshot. Umgekehrt verliert der Workstation-Export einer „+ Kurve“-Kurve ihre Quotes ebenfalls. | Envelope-Block `quotes` (oder `curves[].quotes`/`curves[].spec`): API exportiert `parRiskSpecs()` je Laufzeitkurve (`CurveBuildSpec` mit `discountCurveId`/`referenceCurveIds`), Workstation `extraCurves`-Quotes; Import legt die Specs in `runtimeCurves` bzw. `extraCurves`-Register ab (Kurve bleibt die des Snapshots, Quotes dienen nur dem Bump) und `parRiskSpecs`/`parSpecs` bumpen sie; Snapshot-ID unverändert (Quotes sind Metadaten, wie der Register-Envelope – im ETag-Hash mitführen). Vertragstest „Snapshot-Roundtrip → Par-Buckets auf CZK-CZEONIA“ in API und Web. |
| **R9-2** | Kosmetisch (−0,2) | `apps/web/src/views/CurvesView.tsx:626–648` („+ Kurve“ `disabled={imported}` mit `IMPORT_LOCK`, „+ Währung“ ohne Sperre), `views/AddCurrencyForm.tsx:192` (Toast „… – jetzt mit „+ Kurve“ eine RON-Kurve anlegen“) | **„+ Währung“ ist im Import-Modus bedienbar und endet in einer Sackgasse.** UI (Import-Modus nach API-Snapshot): „+ Währung“ RON / `ROBOR-ON` → Toast „Registriert: Index ROBOR-ON · Konventionen RON – **jetzt mit „+ Kurve“ eine RON-Kurve anlegen**“, aber „+ Kurve“ ist gesperrt („Kurven stammen aus dem importierten Snapshot …“); `irs ron 5y pay 6% 10m` → „Keine Kurve für RON im Markt – … „+ Kurve“ …“. Die Registrierung landet zwar im Export-Envelope (`conventions [HUF, RON, CZK]`), eine RON-Kurve lässt sich aber erst nach „Zum Sample-Markt“ anlegen – ohne dass ein Hinweis das sagt. Verwandt: die HUF-„+ Kurve“-Kurve wird beim Import „gemerkt“ (Toast nennt es), der HUF-Swap zeigt im Import-Modus „Keine Diskontkurve für HUF – der importierte Snapshot enthält keine HUF-Kurve …“ – konsistent und klar formuliert, kein Abzug. | Im Import-Modus entweder „+ Währung“ mit demselben `IMPORT_LOCK`-Tooltip sperren oder den Toast kontextabhängig formulieren („registriert – Kurve nach „Zum Sample-Markt“ mit „+ Kurve“ oder per Snapshot mit RON-Kurve“); langfristig „+ Kurve“ im Import-Modus als **Zusatzkurve** auf den Snapshot erlauben (die API kann das bereits: `POST /curves` im Import-Modus, siehe Abschnitt 2). |
| **R9-3** | Niedrig (−0,3) | `apps/web/src/lib/quick-parser.ts:389` (Grammatik `basis [ccy] <tenor> <recTenor>/<payTenor> …`), `:412–450` (`rec = "EURIBOR-${a}"`, `pay = "EURIBOR-${b}"` fest, `ccy` wird übernommen) | **Die Schnelleingabe `basis [ccy]` baut für jede Währung EURIBOR-Legs.** UI: `basis czk 5y 3m/6m 5bp 10m` → Vorschau „Basis-Swap EURIBOR-3M +5,0 bp vs EURIBOR-6M 5Y · Nominal 10.000.000“ (ohne Währung), Enter → **BASIS-0001 im Blotter als „Fehler“**: „Ungültige Trade-Daten: … Leg 1: Währung CZK passt nicht zur Währung EUR des Index EURIBOR-3M – ein CZK-Float-Leg braucht einen CZK-Index …; Leg 2: …“, Editor Währung CZK, Legs EURIBOR-3M/EURIBOR-6M. Dasselbe trifft NOK (NIBOR-3M/NIBOR-6M sind registriert und wären der richtige Zielwert) und jede „+ Währung“ mit IBOR-Index. Dank der neuen Kern-Validierung (R8-1) ist der Fehler sichtbar statt stumm – aber die Grammatik verspricht `[ccy]`, und `6m/1d` (IBOR/OIS-Basis) wird als „Unbekanntes Token“ abgelehnt, obwohl `makeBasisSwap` beliebige Indizes nimmt. | Tenor → Index über das Register auflösen (`knownIndices().filter(i => i.currency === ccy && i.tenor === a)`), bei fehlendem Index deutscher Fehler „Für CZK ist kein 3M-Index registriert – registriert: PRIBOR-6M, CZEONIA“, `1d` → OIS-Index der Währung; Kurvenprüfung je Leg (`missingCurveNote`); Test `basis nok 5y 3m/6m` → NIBOR-3M/NIBOR-6M. |
| **R9-4** | Kosmetisch (−0,2) | `apps/api/src/lib/csv-import.ts:798–802` (`csvTemplateText`: Header ohne `type`, Typ kommt aus `?type=`), OpenAPI-Beschreibung von `POST /api/trades/import` (Vorlagen), `apps/web/src/lib/portfolio-io.ts:760` (`Spalte „Typ“ fehlt`), `views/Blotter.tsx:538–547` (kein Typ-Fallback im Import) | **Die dokumentierte API-Vorlagendatei wird vom Blotter abgelehnt.** UI (frischer LocalStorage): API-Datei `FxForward.csv` (Header `pair;baseAmount;rate;deliveryDate;id;…` + Beispiel) über „⤒ CSV importieren“ → Toast „**CSV-Import fehlgeschlagen: Spalte „Typ“ fehlt (IRS / FXF / …)**“, ebenso `CrossCurrencySwap.csv`; mit vorangestellter Spalte `type` importieren alle elf (Abschnitt 2). Der R8-4-Fix deckt Vokabular und Einheiten, nicht die Typ-Herkunft – ein Treasurer, der die in der OpenAPI dokumentierte Vorlage füllt, muss die Datei von Hand ergänzen. | Blotter-Import ohne Spalte `type`: Typ aus dem Dateinamen (`…-vorlage-ccs.csv`, `CrossCurrencySwap.csv`) oder aus dem Spaltensatz ableiten (eindeutige Signaturen: `domesticNotional` → CCS, `nearRate` → FXS, `payerReceiver`+`expiry` → SWPT …), sonst kleiner Typ-Dialog; alternativ API-Vorlage mit optionaler Spalte `type` ausliefern (der API-Reader ignoriert unbekannte Spalten). |
| **R9-5** | Kosmetisch (−0,2) | `apps/web/src/lib/quick-parser.ts:182` (`INDEX_RE = /^(euribor\|estr\|sofr\|sonia\|saron\|tona\|nowa\|stibor\|wibor\|cibor\|nibor\|polonia\|wiron\|swestr\|destr)/i`, statisch), `:223`, `:573–575`, `:640` (Index-Token nur bei `INDEX_RE`-Treffer → `normaliseIndexToken`), `lib/register.ts:54–65` (`normaliseIndexToken` fände `PRIBOR-6M`) | **Die Palette erkennt zur Laufzeit registrierte Indizes nicht als Index-Token.** UI (CZK registriert, PRIBOR-6M im Register): `irs czk 5y pay 4% 100m pribor6m` → „⚠ **Unbekanntes Token „pribor6m“** – Währungen: …; erwartet: irs|swap|ois|imm|amort [ccy] <tenor> pay|rec <rate%> [notional] [index] …“ statt (wie bei `nibor6m` in Runde 8) „Kurve CZK-PRIBOR-6M fehlt – …“ bzw. Bewertung, sobald die Kurve da ist; ebenso `pribor-3m`, `hufonia`, `bubor6m`. Der Editor kennt die Indizes (Select „PRIBOR-6M (ohne Kurve)“), die Grammatik-Hilfe nennt `[index]`. | `INDEX_RE` durch `findIndex`/`normaliseIndexToken` ersetzen (Token gilt als Index, wenn `normaliseIndexToken(t)` einen registrierten Namen liefert; Familienpräfixe nur für die Fehlermeldung), Test „`irs czk … pribor6m` → Kurvenhinweis / Bewertung“. |

**Ohne Abzug, aber dokumentierenswert:**
- `ccs eurczk 5y 10m` bewertet (PV 573 EUR, Collateral CZK) mit der ehrlichen Warnung „Keine EUR-Diskontkurve für Besicherung in CZK (Collateral-Kurve „EUR|CZK“ fehlt) – Diskontierung auf EUR-ESTR, Cross-Currency-Basis nicht gepreist“ – eine EUR-unter-CZK-CSA-Kurve lässt sich per API (`XccyBasis`-Quotes, `collateralDiscountCurveId`) bauen, in der Workstation nicht über „+ Kurve“ (nur Depo/OIS/Swap-Quotes). Für Sparkassen/Genossenschaftsbanken (EUR-zentriert) zweitrangig; Teil der Kurven-Governance (Roadmap).
- Der Snapshot-Envelope der Workstation exportiert auch Registrierungen ohne Kurve (`conventions [HUF, RON, CZK]`) – korrekt, aber ein Importeur bekommt Währungen „ohne Kurve“ angeboten.
- `PUT /api/market { valuationDate }` auf einen registrierten Index → `MISSING_FIXING` für CZEONIA (keine Beispiel-Fixings für Laufzeit-Indizes) – korrekt; `PUT fixings` setzt sie.
- `registerCalendarHolidays` (Feiertags-Feed für eingebaute Kalender) nur im Core – Marktdaten-Adapter (US-2.10), Roadmap.
- Effective Date auf einem Feiertag bleibt im Trade unadjustiert (EUR wie CZK), die Schedule adjustiert Start/Zahltage – marktüblich, konsistent.
- `ndf eurczk …` ist kein Kommando; NDF läuft über `fxf eurczk … ndf` (Grammatik dokumentiert).

---

## 4. Feature-Matrix – Statusänderungen gegenüber Runde 8 (nur DERIVA-Spalte)

| Feature | R8 | R9 | Beleg |
|---|---|---|---|
| Register erreicht die Workstation (Envelope-Import/-Export, „+ Währung“, Editor nur passende Indizes) | ❌ | ✅ | R8-1 |
| Kern: Leg-Währung = Index-Währung (`INVALID_TRADE`) | ❌ | ✅ | R8-1, Anhang A |
| Kalender registrierbar (API `POST /calendars`, Envelope `calendars`, UI „+ Kalender“), Zahltage folgen dem Kalender | ❌ (Core ✅) | ✅ | R8-2 |
| Par-Risiko für Zusatz-/Laufzeitkurven, fehlende Kurven benannt | ❌ (stille Null) | ✅ | R8-3 |
| Par-Risiko im Import-Modus (Quotes im Snapshot) | – | ❌ | R9-1 |
| Web ↔ API CSV-Kompatibilität (Einheiten, alle elf Vorlagen, beide Richtungen) | ❌ (6/11) | ✅ (11/11) | R8-4 |
| API-Vorlagendatei ohne `type` im Blotter | – | ❌ | R9-4 |
| Snapshot-Kurven außerhalb des Sample-Sets in der Kurvenansicht | ❌ | ✅ | R8-5 |
| Schnelleingabe `basis [ccy]` für Nicht-EUR | – | ❌ | R9-3 |
| Palette erkennt registrierte Index-Token | – | ❌ | R9-5 |
| Reporting-Währung aus registrierten Währungen (CZK im Zyklus, EMIR in CZK) | – | ✅ | Anhang B/C |
| CZK/HUF end-to-end in **API und Workstation** (Kalender, Register, Kurve, Trade, Risiko, Report, Termsheet, EMIR, Szenarien, XVA, Snapshot-Roundtrip) | 🔶 (API) | ✅ | Abschnitt 2 |
| Excel / Live-Marktdaten / Rollen / Netting-CVA / VaR / CRIF / Bermudan / TARF / PDF / Inflation | ⏳ | ⏳ Roadmap | kein Abzug |

---

## 5. Verifizierte Positivbefunde (Auszug, alle reproduziert)

- **Instrumente:** IRS/OIS/FRA/Cap/Floor/Swaption/Amort/IMM in CZK, FXF/FXO/CCS EUR/CZK über die Palette mit Vorschau; CZK-Swaption und -Cap per API mit Fallback-Vol-Warnung; Basis-/Amort-/IMM-Vorlagen beidseitig.
- **Marktdaten/Register:** Kalender → Indizes → Konventionen → Kurve → Swap vollständig per API und per UI („+ Währung“/„+ Kalender“/„+ Kurve“), Envelope in beide Richtungen, Reload-fest; Dual-Curve `CZK-PRIBOR-6M` im Import-Modus; `GET /api/market` mit `currencies`/`indices`/`conventions`/`calendars`; Audit `register.calendar|index|conventions`.
- **Risiko/Reporting Fremdwährung:** DV01 je Kurve, Par-Buckets auf Laufzeitkurven (API) und „+ Kurve“-Kurven (UI), Szenarien 16 + 6 mit `fxCurrency CZK`, Grid, CVA/DVA, Report Level 2 in EUR und CZK, Termsheet, EMIR JSON/CSV (`SRCCSP`, Bewertungswährung CZK), Portfolio-Report; UI-Report für den CZK-Swap mit Governance-Block.
- **Integration:** 11/11 Vorlagen Web → API (`imported` mit PV), 11/11 API-Zeilen → Web (Reader und Blotter), `dryRun`, ETag mit Register-Anteil, atomarer Envelope-Import.
- **API-Vertrag:** OpenAPI 3.1.0, **44 Operationen**, 45 Fehlercodes, 12 Warnungs-Präfixe; Tests Core 421 / API 137 / Web 362 grün, keine Page-Errors in drei Playwright-Läufen; **E2E-Suite** `apps/web/e2e/smoke.mjs` auf Port 5001 → **E2E OK (493 checks)**, Exit 0.

---

## 6. Was für 100 noch fehlt

1. **R9-1 Quotes im Snapshot** – Envelope-Block `quotes`/`curves[].spec` in API-Export, Workstation-Export und beiden Importen, damit Par-Risiko im Import-Modus rechnet (≈ 0,3). Größter Einzelposten.
2. **R9-3 `basis [ccy]` über das Register** – Tenor → Index der Währung, `1d` → OIS, deutscher Fehler bei fehlendem Index (≈ 0,3).
3. **R9-2 / R9-5 Import-Modus und Palette** – „+ Währung“ im Import-Modus sperren oder Toast kontextabhängig; `INDEX_RE` durch Register-Lookup ersetzen (je ≈ 0,2).
4. **R9-4 Typ-Herkunft im Blotter-Import** – Typ aus Dateiname/Spaltensignatur oder Dialog; alternativ `type`-Spalte in der API-Vorlage (≈ 0,2).

Mit 1 und 2 läge die Dimension bei ≈ 99,4; 3–4 schließen die Lücke zu 100 für den v1-Scope.

---

## Anhang A – Core-Probe (`node probe-core-r9.mjs` gegen `dist`, Auszug)

```
exports 256 · knownCurrencies CHF,DKK,EUR,GBP,JPY,NOK,PLN,SEK,USD · indices 21 · isBuiltInCalendar TARGET true / CZ false
validateCustomCalendar {CZ, ["2027-02-30"]} → ["calendar.holidays[0] "2027-02-30" is not a valid calendar date"] · {TARGET} → ["… built-in calendar and cannot be replaced (use registerCalendarHolidays for a holiday feed)"]
registerCalendar(json CZ) → CZ · isBusinessDay(2027-07-06, CZ) false · CZ+TARGET false · listCustomCalendars [{id CZ, name Prag, 5 holidays, weekendsAreHolidays true}]
validateRateIndex CZEONIA [] · PRIBOR tenor 1D → ["IBOR tenor must be like "3M" (got "1D")"] · Kalender HU → ["unknown calendar "HU" (register it with registerCalendar first)"]
registerRateIndex CZEONIA/PRIBOR-6M ok · validateSwapConventions CZK [] · ("HUF", czkConv) → ["currency CZK does not match HUF"] · registerSwapConventions CZK → knownCurrencies +CZK
validateTrade CZK-Leg mit EURIBOR-6M → ["trade.legs[1]: currency CZK does not match the currency EUR of index EURIBOR-6M – a CZK floating leg needs a CZK index (register one with registerRateIndex / POST /api/market/indices)"] · mit PRIBOR-6M → []
bootstrapCurve(CZK-CZEONIA, 6 Quotes) → makeVanillaSwap CZK legs [Fixed CZK – CZ 1Y] [Float CZK CZEONIA CZ 1Y] · priceTrade pv -36827 warnings [] · computeRisk dv01 1899 · parRisk curves [[CZK-CZEONIA, 6]] total 1852
STANDARD 16 · HIST 6 · VOL_PLAUSIBILITY {…, fxSmileMax 0.5}
```

## Anhang B – API-Probe (`npx tsx probe-api*-r9.mts`, `app.inject`, Auszug)

```
openapi 3.1.0 ops 44 · error codes 45 (NO_DISCOUNT_CURVE, CURVE_NOT_FOUND, UNKNOWN_INDEX, UNKNOWN_CALENDAR, INVALID_CALENDAR, INVALID_CURVE_SPEC …) · warning prefixes 12 (… MARKET_STATE_DROPPED, PAR_RISK_INCOMPLETE)
GET /api/market keys … currencies,indices,conventions,calendars · calendars TARGET*,DE*,US*,US-SIFMA*,UK*,CH*,JP*,NO*,SE*,DK*,PL*,WEEKEND*
== R8-2 == POST /calendars TARGET → 400 INVALID_CURVE_SPEC builtIn true · ["2027-02-30"] → 400 INVALID_CALENDAR problems[] · CZ (13 Feiertage) → 201 · CZ erneut → 200 replaced true
== Register == POST /indices CZEONIA (CZ) 201 · PRIBOR-6M (CZ) 201 · HUFONIA (HU) → 400 "unknown calendar "HU" (register it with POST /api/market/calendars)" · POST /conventions CZK 201
  GET /api/market currencies …,CZK,… · calendars custom [{CZ, builtIn false, name "Prag v2", holidays 14}]
POST /curves CZK-CZEONIA 200 discountCurveSet true parRiskTracked true · PUT fxSpots EURCZK 200 · CSV import CZK swap imported pv -36827
  trade legs [Fixed CZK – CZ 1Y ACT/360] [Float CZK CZEONIA CZ 1Y ACT/360] · price -36827 warnings [] · risk dv01 1899 byCurve {CZK-CZEONIA}
  par risk (R8-3) 200 curves [[CZK-CZEONIA, 6]] total 1852 curvesWithoutQuotes [] · report ifrs13Level 2 · termsheet 200 (markdown, sections) · EMIR SRCCSP notionalCurrency CZK valuationCurrency EUR MTMO
  scenarios {trades:[…], includeHistorical} 200 · grid fxCurrency CZK 200 · xva cva 1078 dva 1219 · CZK-Leg mit EURIBOR-6M → 422 INVALID_TRADE (R8-1 Kern)
== Kalender-Wirkung == Cashflow-CSV Swap CZK 05.07.2027–05.07.2028 6M auf CZ: Start 2027-07-07, Ende 2028-07-06 · auf TARGET: 2027-07-05 / 2028-07-05
== Snapshot == keys …,indices,conventions,calendars · ETag "b8949627e496316a-cb38c9b6127e2cf8" · calendars [[CZ,14]] indices [CZEONIA,PRIBOR-6M] conventions [CZK] curves 9
  Kurve CZK-CZEONIA im Snapshot: id,currency,dayCount,interpolation,nodes,extrapolation,meta (KEIN quotes ← R9-1)
  PUT in frischer App 200 source import · currencies +CZK · price CZK -36827 [] · par risk: curves 0 total 0 curvesWithoutQuotes [CZK-CZEONIA] PAR_RISK_INCOMPLETE (← R9-1)
  Envelope mit TARGET-Kalender → 400 INVALID_CURVE_SPEC {section calendars, entry TARGET, builtIn true}, nichts registriert
  PUT valuationDate 2026-10-01 → 200 warnings [] · CZK-CZEONIA 200 · disc CZK-CZEONIA · price -35560 [MISSING_FIXING CZEONIA 2026-09-07] · PUT fixings CZEONIA 200 → 1324
  Import-Modus: POST /curves CZK-PRIBOR-6M (Deposit+Swap, discountCurveId CZK-CZEONIA) 200 parRiskTracked true · Swap PRIBOR-6M imported -2546 · Swaption CZK imported 57633 "No swaption vol surface – 70bp" · par portfolio: bumped [CZK-PRIBOR-6M], ohne Quotes [CZK-CZEONIA]
  EMIR CSV reportingCurrency CZK: "…;IRS-CZK-1;;SRCCSP;100000000,00;CZK;-905680,98;CZK;2026-09-03T17:00:00Z;MTMO;…"
== R8-4 Web-Vorlagen → API == IRS imported -481394 · FXF 2769 · CAP 96080 · SWPT 99961 · FXO 52360 · CCS 9600 · FRA -1124 · FXS 2450 · BASIS -13427 · AMORT -191253 · IMM -119312 → 11/11
== API-Vorlagen → Web-Reader == roh (ohne type) → 11× THROW "Spalte „Typ“ fehlt" (← R9-4) · mit Spalte type → 11/11 trades 1 errors []
audit: register.calendar, register.index, register.conventions, trade.import.dryRun
```

## Anhang C – UI-Probe (Playwright/Chromium 1194 gegen `vite preview :5001`, Auszug)

```
== + Währung (Sample-Modus) == HUF, OIS HUFONIA, IBOR BUBOR-6M, + Kalender HU (15.03.2027 / 2027-08-20 / 2027-10-23 / 01.11.2027)
  Vorschau "2 Indizes HUFONIA, BUBOR-6M · Konventionen HUF · Kalender HU · Kurven-IDs HUF-HUFONIA, HUF-BUBOR-6M · Kalender HU (neu, 4 Feiertage, ab 15.03.2027)"
  Toast "Registriert: … – jetzt mit „+ Kurve“ eine HUF-Kurve anlegen" · Fokus add-curve · + Kurve Währungen [CHF, DKK (ohne Kurve), EUR, GBP, HUF (ohne Kurve), …] · Indizes HUF [BUBOR-6M (IBOR 6M, ACT/360, HU), HUFONIA (OIS, ACT/360, HU)]
  Kurve HUF-HUFONIA aus 6 Quotes · Spot EUR/HUF 395 → Tab HUFONIA · "irs huf 5y pay 6.5% 500m" → Payer-Swap HUF 5Y … HUFONIA (Kurve vorhanden; BUBOR-6M ohne Kurve) → IRS-0003 PV -193.067 EUR · Editor HUF/HUFONIA
  Par-Risiko HUF: PAR-DV01 GESAMT 641 EUR · ZERO-DV01 651 EUR · DIFFERENZ -11 EUR · HUF-HUFONIA Σ 641 EUR   (R8-3 ✓)
== API-Snapshot importieren == Toast "Snapshot „Sample EoD“ importiert · ID b8949627e496316a · registriert: 2 Indizes CZEONIA, PRIBOR-6M · Konventionen CZK · Kalender CZ · gemerkt, nach „Zum Sample-Markt“ wieder aktiv: Kurve HUF-HUFONIA"
  Marktansicht "Kurven: 9 · Fixings: 1323" · EUR/CZK-Spot ✓ · "irs czk 5y pay 4% 100m" → Payer-Swap CZK 5Y @ 4,000 % · Nominal 100.000.000 · CZEONIA (Kurve vorhanden; PRIBOR-6M ohne Kurve) → IRS-0004 PV -36.827 EUR
  Editor CZK / CZEONIA [CZEONIA, PRIBOR-6M (ohne Kurve)] / Collateral […, CZK-CSA] · Risiko DV01 1.899 · FX-Delta CZKEUR -368 · Theta 45 · Gamma -0,92
  Index → PRIBOR-6M: "Kurve CZK-PRIBOR-6M nicht im Markt-Snapshot – der importierte Snapshot enthält keine CZK-Kurve – Snapshot mit Kurve importieren oder „Zum Sample-Markt“ wechseln"
  Palette: ois/fra/cap/floor/amort/imm czk ✓ · swpt czk 1y5y payer 4% 50m cash → Underlying CZEONIA … ⚠ kein Swaption-Vol-Cube für CZK · fxf eurczk ✓ · fxo eurczk ⚠ keine FX-Vol-Fläche · ccs eurczk 5y 10m → CCS-0002 PV 573 EUR, Collateral CZK, Warnung EUR|CZK-Kurve fehlt
  "irs czk … pribor6m" → ⚠ Unbekanntes Token „pribor6m“ (← R9-5) · "basis czk 5y 3m/6m 5bp 10m" → Basis-Swap EURIBOR-3M vs EURIBOR-6M → BASIS-0001 Fehler "Währung CZK passt nicht zur Währung EUR des Index EURIBOR-3M …" (← R9-3)
  Par-Risiko CZK (Import): "Par-Risiko nur für Kurven mit Quotes (0 von 1) …" · Panel 0 EUR / 1.899 EUR / -1.899 "Kurven ohne Quotes fehlen im Par-Risiko" (← R9-1)
  Report: "Payer-Swap CZK 5Y · Nominal 100.000.000 CZK · Bewertungstag 03.09.2026 · Reporting EUR · Snapshot Sample EoD … Stufe 2 … CZEONIA"
  Kurvenansicht Tabs [… HUFONIA, CZEONIA (aus Snapshot)] · Tab CZK-CZEONIA: Badge 1, Hinweis 1, 7 Pillars, Forwards-Canvas 1 (R8-5 ✓) · + Kurve disabled true · + Währung disabled false
  + Währung RON im Import-Modus → Toast "… jetzt mit „+ Kurve“ eine RON-Kurve anlegen", + Kurve bleibt gesperrt (← R9-2) · "irs ron …" → Keine Kurve für RON
  Reporting-Währung Zyklus EUR → USD → GBP → CHF → JPY → CZK → EUR · Reload: 15 Zeilen, 1 Bewertungsfehler (IRS-0003 HUF: "Keine Diskontkurve für HUF – der importierte Snapshot enthält keine HUF-Kurve …"), Tabs unverändert
  Web-Export keys …,indices,conventions,calendars · calendars [CZ, HU] · indices [BUBOR-6M, CZEONIA, HUFONIA, PRIBOR-6M, ROBOR-ON] · conventions [HUF, RON, CZK] · curves 9
== CSV-Import (Blotter) == API-Zeilen mit type: CCS-CSV-1 "Cross-Currency-Swap EUR/USD 5Y ESTR -20,0 bp vs SOFR … 11.157 Indikation OK" · IRS/FXF/CAP/SWPT/FXO/FRA/FXS/BASIS/AMORT/IMM je 1 Trade ohne Bewertungsfehler (11/11)
  API-Rohdatei FxForward.csv / CrossCurrencySwap.csv → "CSV-Import fehlgeschlagen: Spalte „Typ“ fehlt (IRS / FXF / …)" (← R9-4)
page errors: [] (drei Läufe)
```

Probe-Skripte, Ausgaben (`probe-*-r9.log`, `gen-templates-r9.log`, `e2e-r9.log`), Vorlagen (`web-templates/`, `api-templates/`), Snapshots (`snapshot-czk-r9.json`, `snapshot-web-export-r9.json`) und Screenshots (`shots/01-add-currency.png`, `02-huf-par.png`, `03-czk-pricing.png`, `04-curves-import.png`) im Scratchpad der Review-Session.
