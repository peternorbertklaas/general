# Re-Review Runde 8: Marktabdeckung Features & Module (Dimension 1, Gewicht 20 %) – DERIVA 0.2.0

**Reviewer-Rolle:** Senior Product Analyst, Treasury- und Derivateberatungssoftware (LPA Capmatix/Captano, Finbridge, d-fine, Bloomberg SWPM/OVML/VCUB/DLIB/MARS, LSEG Swap Pricer, Murex, FIS Front Arena, Numerix, Quantifi, FINCAD, ORE/QuantLib) · **Stand:** 04.09.2026, Branch `claude/derivatives-trading-platform-1arsyu`, Commit `1cd266e` (Core-`dist` gebaut 08:21 UTC, keine Quelldatei neuer als `dist`; Arbeitsbaum sauber) · **Modus:** Review only, keine Quelldatei geändert
**Baseline:** `docs/quality/review-markt-r7.md` (Runde 7, Score 98 / 100) mit den Befunden R7-1…R7-5 und dem Rest von R6-5 (Register-Endpunkte, `dryRun`, `UNKNOWN_INDEX`-Hinweis)
**Geprüft (Laufzeit):** `npx vitest run` je Paket → **Core 396/396** (28 Dateien, 14,6 s, kein Timeout mehr – `testTimeout` 20 s wirkt), **API 124/124** (8 Dateien), **Web 335/335** (38 Dateien); `vite build` erfolgreich; Web-App unter `vite preview --port 5001 --strictPort` mit Playwright/Chromium 1194 durchgeklickt (`probe-web-r8.mjs`, `probe-web2-r8.mjs`, Anhang C, `pageerror: []`); offizielle E2E-Suite `apps/web/e2e/smoke.mjs` auf Port 5001 → **E2E OK (437 checks)**; API per `buildApp` + `app.inject` (`probe-api-r8.mts`, `probe-api2-r8.mts`, `probe-api3-r8.mts`, Anhang B); Core-Proben gegen `packages/pricing-core/dist` (`probe-core-r8.mjs`, Anhang A); die elf Web-CSV-Vorlagen wurden mit den echten Vorlagen-Headern und Beispielzeilen aus `CSV_IMPORT_TEMPLATES` erzeugt (`web-templates-r8/*.csv`) und gegen `POST /api/trades/import` geschickt. Alle Zahlen unten stammen aus diesen Läufen.
**Roadmap-Regel:** In `02-epics-und-user-stories.md` / README als ⏳ dokumentierte Posten (Bermudan/CMS/Range Accrual, TARF/Participating Forward, Marktdaten-Adapter & Kurven-Governance, OIDC/Rollen/DB, Excel-Add-in, Monte-Carlo-Netting-XVA/FVA, PDF-Template-Service, Inflations-ZC-Swap, PRIIPs Annex-II-MC, SIMM/CRIF, VaR/Attribution, Batch-EoD/Webhooks, Designationsmemo, EMIR Tabelle 3/UPI/XML) werden **nicht** als Lücke gewertet.

---

## 1. Score

### **Marktabdeckung Features & Module: 98 / 100** (rechnerisch 98,4; Runde 1: 60, Runde 2: 86, Runde 3: 97, Runde 4: 98, Runde 5: 99, Runde 6: 98, Runde 7: 98)

**In einem Satz:** Vier der fünf Runde-7-Befunde sind in Core, API **und** UI nachweisbar geschlossen – die Schnelleingabe wählt nach „+ Kurve NOK-NOWA“ den Index **mit Kurve** („NOWA (Kurve vorhanden; NIBOR-6M ohne Kurve)“), normalisiert `nibor6m` und meldet `nibor9m` deutsch, der Editor zeigt **NOK / NOWA / NOK-CSA** statt EUR/EURIBOR-3M, „+ Fläche“ legt Swaption-Cube, Caplet-Fläche und EUR/NOK-Smile an (der NOK-Cap bewertet danach ohne Fallback-Hinweis, alles überlebt den Reload), `POST /api/market/curves` setzt die Diskontkurve und `PUT /api/market` akzeptiert `discountCurveId`/`collateralDiscountCurveId` mit 422/400-Prüfung, die Beispiel-Fixings folgen dem Stichtag (1 452 + Nutzer-Fixings, kein `MISSING_FIXING`), `?dryRun=1` speichert nichts und unbekannte Query-Parameter antworten 400, und CZK/HUF sind per `POST /api/market/indices|conventions` ohne Codeänderung bootstrap- und handelbar (Import, Report Level 2, DV01, Termsheet, EMIR-Datensatz mit `notionalCurrency CZK`, Snapshot-Envelope mit `indices`/`conventions`). **Die Bewertung bleibt trotzdem bei 98, weil die Prüfung des durchgängigen Fremdwährungs-Flusses fünf Restposten zeigt, die ein Treasurer sofort bemerkt:** das API-Register erreicht die Workstation nicht (der CZK-Snapshot aus der API importiert zwar, doch die Palette lehnt `irs czk …` als „Unbekannte Währung“ ab, die CZK-Kurve ist in der Kurvenansicht unsichtbar, und der Editor baut einen CZK-Swap mit **EURIBOR-6M**-Leg, den der Kern anstandslos bewertet); Par-Sensitivitäten ignorieren jede nicht mitgelieferte Kurve (UI: „Par-DV01 gesamt 0 EUR · Zero-DV01 399 EUR · Differenz −399 EUR“ für den NOK-Swap, API: `curves: []`) – eine stille Null im Risikobericht; die CSV-Vorlagen von Web und API sind für **fünf von elf** Typen weiterhin nicht wechselseitig lesbar (CCS-Beispielzeile wird an der Spread-Einheit abgelehnt, API-CCS an `tenor`, FXF/FXO/FRA an den Vokabularen); Kurven eines importierten Snapshots außerhalb des Sample-Sets haben in der Kurvenansicht keinen Tab („Kurven: 12“, acht sichtbar); und Kalender lassen sich nirgends außer im Core-Code registrieren, obwohl die Fehlermeldung genau dazu auffordert. Die Summe der Abzüge bleibt bei −1,6.

**Abzugsherleitung** (Rubrik: kritisch −10…−25, fehlendes Kernfeature −3…−8, UX-Reibung −1…−3, kosmetisch −0,2…−1; Roadmap-dokumentiert = 0; keine Doppelzählung mit Quant/UI/Flows/API; Teilbeträge unterhalb der Bänder wie in den Vorrunden für Randfälle):

| Bereich | R1 | R2 | R3 | R4 | R5 | R6 | R7 | R8 | Restlücken (Nummern → Abschnitt 3) |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| A Instrumente & Strukturen | −10 | −3,7 | −0,8 | −0,5 | 0 | 0 | 0 | 0 | alle 18 Bauformen bewertet (IRS/OIS/Basis/Amort/IMM/Cap/Collar/Swaption cash/FRA/FXF/NDF/FXS/FXO/Barrier/Digital/CCS/MtM-CCS/Fest-Fest-CCS, Anhang A) |
| B Kurven & Marktdaten | −5 | −0,7 | −0,2 | 0 | −0,3 | −0,5 | −0,9 | −0,9 | Register erreicht die Workstation nicht: Envelope ignoriert, CZK in Palette/Kurvenansicht unbekannt, kein „+ Währung“ (R8-1 −0,5); kein Kalender-Register-Pfad (R8-2 −0,2); Snapshot-Kurven außerhalb des Sample-Sets ohne Tab (R8-5 −0,2) |
| C Modelle | −1 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | – |
| D Sensitivitäten | −2 | −0,8 | 0 | 0 | 0 | 0 | 0 | −0,4 | Par-Risiko ignoriert Zusatz-/Import-Kurven, zeigt stille Null (R8-3) |
| E Szenarien / VaR | −1 | −0,5 | 0 | 0 | 0 | 0 | 0 | 0 | 16 + 6; VaR = Roadmap |
| F XVA | −2,5 | −0,5 | 0 | 0 | 0 | 0 | 0 | 0 | Netting = Roadmap |
| G Hedge Accounting | −4 | −3,0 | 0 | 0 | 0 | 0 | 0 | 0 | – |
| H Regulatorik | −4 | −1,5 | 0 | 0 | 0 | 0 | 0 | 0 | EMIR-Datensatz CZK (`SRCCSP`, Notional-Ccy CZK, `?reportingCurrency=`) |
| I Dokumente | −2,5 | −1,0 | 0 | 0 | 0 | 0 | 0 | 0 | Termsheet CZK-Swap 5 Abschnitte |
| J Workflow / Beratung | −2,5 | −0,3 | 0 | 0 | 0 | 0 | 0 | 0 | Freigabe = Roadmap |
| K UI | −0,5 | −1,3 | −0,8 | −1,5 | 0 | −0,5 | −0,5 | 0 | R7-1 behoben (Palette-Index-Default, Editor-Listen aus dem Register) |
| L Integration | −2,5 | −0,5 | −0,3 | −0,2 | −0,2 | −0,7 | −0,2 | −0,3 | CSV-Vorlagen Web ↔ API für 5 von 11 Typen nicht wechselseitig lesbar (R7-5 Rest / R8-4) |
| M Admin | −2,5 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | Rollen/DB = Roadmap |
| Sonstiges (Beispielmarkt) | – | −0,5 | −0,5 | −0,2 | −0,2 | −0,2 | 0 | 0 | Beispiel-Fixings folgen dem Stichtag auch in der API (R7-4 behoben) |
| **Summe** | **−40** | **−14,3** | **−2,6** | **−2,4** | **−0,7** | **−1,9** | **−1,6** | **−1,6** | → **98,4 ≈ 98 / 100** |

Einordnung (gleiche Skala, v1-Workstation-Scope, Einschätzung): Bloomberg SWPM+OVML+VCUB+MARS ≈ 85, LPA Capmatix OTC ≈ 75, ORE ≈ 60, Kyriba/Coupa ≈ 55. Die funktionale Abdeckung für Vanilla-Zins/FX ist in G5 + NOK/SEK/DKK/PLN vollständig und **innerhalb der API** jetzt für jede registrierbare Währung offen; die Restdifferenz zu 100 liegt in der **Kopplung Register ↔ Workstation** (Envelope, Kurvenansicht, Kalender), im Par-Risiko für Zusatzkurven und in der CSV-Vorlagenfamilie.

---

## 2. Status der Runde-7-Befunde R7-1…R7-5 und des R6-5-Rests

Legende: **behoben** = im Code vorhanden **und** per Probe/UI reproduziert · **teilweise** = Kern vorhanden, benannter Rest · **offen** = unverändert.

| # | Befund R7 | Status | Nachweis (Datei / Probe / UI) | Rest |
|---|---|---|---|---|
| **R7-1** | Neuer Fremdwährungs-Fluss endet in „Fehler“-Trade: Palette wählt Default-Index ohne Kurve, Editor-Selects fest auf G5 (zeigen EUR/EURIBOR-3M für NOK-Trade), Index-Token nicht normalisiert | **behoben** | `apps/web/src/lib/register.ts` (`defaultIndexFor` Konvention → OIS → beliebiger Index mit Kurve `:24–35`, `normaliseIndexToken` `:54–65`, `currencyOptions`/`indexOptions` `:77–100`), `quick-parser.ts:297–317` (`chooseIndex` mit Vorschau-Notiz, `unknownIndexError`), `:286–290` (`missingCurveNote`), `TradeEditor.tsx:185–214` (`useRegisterOptions`: Währungen mit Diskontkurve zuerst, „(ohne Kurve)“, Collateral aus `discountCurveId`). **UI (Anhang C):** vor der Kurve `irs nok 5y pay 4% 10m` → „⚠ Keine Kurve für NOK im Markt – … „+ Kurve“ …“; nach „+ Kurve NOK-NOWA“ → **„Payer-Swap NOK 5Y @ 4,000 % · Nominal 10.000.000 · NOWA (Kurve vorhanden; NIBOR-6M ohne Kurve)“**, Enter → „Angelegt: IRS-0003 · PV 0 EUR“, Blotter `IRS-0003 … 10.000.000 NOK … 0 399 Indikation OK`; `… nibor6m` / `… nibor-6m` → Vorschau mit „⚠ Kurve NOK-NIBOR-6M fehlt – in der Kurvenansicht mit „+ Kurve“ anlegen“; `… nibor9m` → „⚠ Unbekannter Index „nibor9m“ – für NOK registriert: NIBOR-3M, NIBOR-6M, NOWA“ (deutsch); `cap nok …` → NOWA-Cap, `fra nok 3x9` → NOWA; **Editor IRS-0003:** Währung = **NOK** [EUR/CHF/GBP/JPY/NOK/USD/DKK (ohne Kurve)/PLN (ohne Kurve)/SEK (ohne Kurve)], Index = **NOWA** [NOWA/NIBOR-3M (ohne Kurve)/NIBOR-6M (ohne Kurve)], Collateral [unbesichert/EUR-CSA/…/JPY-CSA/**NOK-CSA**]; Risiko-Tabelle DV01 399, FX-Delta NOKEUR, Theta, Gamma. API konsistent: NOK-Swap ohne Index → `rejected CURVE_NOT_FOUND` (Default NIBOR-6M, Anhang B) | Die API-Vorlage wählt weiterhin den Konventions-Index ohne Kurvenprüfung (`rejected` statt Auto-Fallback auf NOWA) – bewusst konservativ, dokumentiert, ohne Abzug |
| **R7-2** | Vol-Flächen für neue Währung/Paar nur per API, nicht in der Marktansicht anlegbar (Fallback 60/70 bp bzw. 8 %, Level 3) | **behoben** | `MarketView.tsx:462–700` (`AddVolSurfaceForm`: Swaption-Cube je Währung mit Diskontkurve ohne Cube, Caplet je Index mit Kurve, FX je Spot-Paar ohne Fläche; Achsen aus Vorlagenfläche, Werte flach oder kopiert; `validateVolSurfaces`/`volSurfaceWarnings` vor dem Anlegen; undo-fähig, persistiert), Toolbar `:1660–1681`, Palette-Warnung nennt „+ Fläche“ (`quick-parser.ts:330`). **UI:** Toolbar „Vol-Flächen (20): 5 Swaption-Cubes · 5 Caplet-Flächen · 10 FX-Paare **+ Fläche**“; Art-Optionen „Swaption-Cube (Währung) | Caplet-Fläche (Index) | FX-Fläche (Paar)“; Swaption-Währungen **NOK**, Vorlagen EUR/USD/GBP/CHF/JPY, Vorschau „NOK: 11 × 9 (Expiry × Tenor)“ → Toast „Swaption-Cube NOK angelegt (flach 60 bp)“; Caplet NOK/NOWA „9 × 8 (Expiry × Strike)“ → „Caplet-Fläche NOK-NOWA angelegt“; FX EUR/NOK → „FX-Vol-Fläche EUR/NOK angelegt (flach 8,0 %)“; Toolbar danach **23** Flächen, FX-Paare inkl. EUR/NOK; `swpt nok …` und `fxo eurnok …` ohne ⚠, `cap nok 5y 4.5% 10m` → CAP-0002 **PV 5.155 EUR ohne Fallback-Hinweis**; nach Reload weiter 23 Flächen und Tab „NOWA“ | Hilfe-Overlay (`?`) nennt „+ Kurve“, aber nicht „+ Fläche“/„+ Paar“ (`HotkeyOverlay.tsx:47`) – UI-Dimension, hier ohne Abzug |
| **R7-3** | API kennt die Regel „erste Kurve einer Währung = Diskontkurve“ nicht; `discountCurveId` nicht per `PUT /api/market` | **behoben** | `routes/market.ts:305–356` (`isDiscountCurve ?? m.discountCurveId[ccy] === undefined`, Antwort `discountCurveSet`/`discountCurveId`, Audit-Detail), `:451–600` (`PUT /api/market` mit `discountCurveId`/`collateralDiscountCurveId`: Existenz → 422 `CURVE_NOT_FOUND`, Währung → 400 `INVALID_REQUEST`, Markt bei Fehler unverändert). **Probe:** `POST /curves NOK-NOWA` → 200 `discountCurveSet true`, `GET /api/market` `discountCurveId.NOK = NOK-NOWA`, NOK-Swap-Import → **`imported`** (vorher `NO_DISCOUNT_CURVE`); zweite Kurve NOK-NIBOR-6M → `discountCurveSet false`, Zuordnung bleibt NOWA; `PUT { discountCurveId: { NOK: "NOK-NIBOR-6M" } }` → 200, zurück → 200, `NOK-X` → **422 CURVE_NOT_FOUND**, `EUR-ESTR` für NOK → **400 INVALID_REQUEST** „denominated in EUR, not NOK“, `collateralDiscountCurveId { "NOK\|EUR": "NOK-NOWA" }` → 200; `isDiscountCurve: false` → `false`. Vertragstest `review-r7.test.ts` | – |
| **R7-4** | API behält alte Beispiel-Fixings beim Stichtagswechsel (1 323 statt 1 452) → `MISSING_FIXING` nur in der API | **behoben** | `lib/store.ts:97–129` (`rebuiltFixings`: frische `sampleFixings(neuer Stichtag)`, Nutzer-Fixings = alles, was nicht im Sample-Set des alten Stichtags war, gewinnen je Index/Datum). **Probe:** Nutzer-Fixings EURIBOR-3M@02.09.2026 = 9,99 % und SOFR@02.09.2026 gesetzt, `PUT { valuationDate: "2026-11-03" }` → **1 453** Fixings (1 452 Sample + 1 SOFR-Nutzerfixing), letztes 02.11.2026, EURIBOR-3M@02.09.2026 weiter **0,0999** (Nutzer gewinnt); EURIBOR-3M-Swap ab 15.09.2026 → `warnings []` (vorher `MISSING_FIXING … 2026-09-11`) | Snapshot-ID ändert sich nach Stichtagswechsel gegenüber Runde 6 – im CHANGELOG „Geändert“ dokumentiert |
| **R7-5** | CCS-CSV-Spalten Web ≠ API, Dateien nicht wechselseitig importierbar | **teilweise** | **Header:** API `csv-import.ts:230–239, 303–316` (Identitäts-Aliase `notional`/`payReceive`/`rate`/`maturity`/`start` + Vokabelregeln → `domesticNotional`/`domesticPayReceive`/`fixedRate`/`tenor`/`effectiveDate`), Web `portfolio-io.ts:326–331` (`headers`: CCS-Vorlage schreibt `domesticNotional`, `fixedRate`, `domesticPayReceive`, `effectiveDate`, `collateralCurrency`), Doku nennt beide. **Probe:** alte Web-CCS-Header (`notional;…;direction;start;maturity;collateral`) → **200, kein `CSV_INVALID` mehr** (Header akzeptiert) | **(a) Einheit des Spreads:** die Beispielzeile der neuen Web-Vorlage (`spread = -20`, Web liest ≥ 1 als bp: `portfolio-io.ts:1019–1020`) wird von der API als Dezimalzahl gelesen → **`rejected CSV_ROW_INVALID „legs.0.spread must be >= -1“`**; dasselbe für die BASIS-Vorlage (`5` → „must be <= 1“); `-20 bp` und `-0,002` importieren. **(b) API-CCS-Zeile → Web:** die API-Vorlage nutzt `tenor` (Pflichtspalte `:141`), das Web liest für CCS nur `rec.maturity` (`portfolio-io.ts:1024`; Alias `tenor` → Spalte `tenor` `:543`, nicht `maturity`) → Dialog „**2 Laufzeit fehlt (z. B. 5Y)** · 3 Laufzeit fehlt“, Button „Keine gültige Zeile“ (Anhang C). **(c) weitere Vorlagen:** Web-FXF (`buyCurrency/buyAmount/sellCurrency/sellAmount`) → API **400 `CSV_INVALID` „lacks pair, baseAmount, rate“**; Web-FXO (`expiry`) → 400 „lacks expiryDate“ (`ALIASES` hat `verfall: "expiry"`, aber keinen Identitätseintrag `expiry`, daher greift `:312` nicht); Web-FRA (`period`, `start` leer) → `rejected „column start is required“`; umgekehrt API-FXF-Zeile im Web → „Kauf-/Verkaufswährung fehlt“. **Ergebnis: 6 von 11 Web-Vorlagen importieren per API (IRS, CAP, SWPT, FXS, AMORT, IMM), 5 nicht (CCS, BASIS, FRA, FXF, FXO)** → **−0,3** (R8-4) |
| **R6-5 Rest** | Registrierung weiterer Währungen/Indizes nur Core-Laufzeit (kein API-/Snapshot-/UI-Pfad), `dryRun` fehlt, `UNKNOWN_INDEX` ohne Hinweis | **teilweise** | **API:** `routes/market.ts:359–449` (`POST /api/market/indices` → 201/200, eingebauter Name → 400 `INVALID_CURVE_SPEC` `details.builtIn true`; `POST /api/market/conventions` → 201/200), `GET /api/market` mit `conventions`, `RegisterStore` (`store.ts:139–164`), Snapshot-Envelope `indices`/`conventions` (`routes/snapshot.ts:56–61, 134–138, 186–222`). **Probe:** `EURIBOR-6M` → **400 builtIn**; CZEONIA/PRIBOR-6M (Kalender TARGET) → **201/201**; Konventionen CZK → **201**; `GET /api/market` `currencies` enthält **CZK**, `conventions.CZK`, `indices` CZEONIA/PRIBOR-6M; `POST /curves CZK-CZEONIA` → `discountCurveSet true`; CZK-Swap-Import `imported`, Report **`ifrs13Level 2`** (PV 10.464 riskFree), DV01 1.889 auf `CZK-CZEONIA`, Termsheet 200, Szenarien 200, **EMIR `SRCCSP` `notionalCurrency CZK`**; Snapshot-Export trägt `indices` (CZEONIA, PRIBOR-6M) und `conventions` (CZK), Re-Import in frischer App → 200, CZK-Swap bewertet; Envelope mit eingebautem `SOFR` → **400 INVALID_CURVE_SPEC**; HUF/BUBOR-6M ohne Registrierung → 422 `UNKNOWN_INDEX`, `spec.index`-Beschreibung und `ErrorResponse.code` verweisen auf `POST /api/market/indices|conventions`. **`dryRun`:** JSON `?dryRun=1` → `dryRun: true`, `skipped` bei vorhandener ID, kein `version`; CSV `?dryRun=true` → `imported`, danach `GET /api/trades/IRS-DRY-CSV` **404** (nichts gespeichert); `?dryrun=1`, `?validate=1` → **400 VALIDATION_ERROR**; Audit `trade.import.dryRun`, `register.index`, `register.conventions` | **(a) Workstation:** die Web-App liest den Envelope nicht (`snapshot-import.ts:69–73` reicht `indices`/`conventions` unverändert an `deserializeMarket`, nichts ruft `registerRateIndex`/`registerSwapConventions`; keine „+ Währung“/„+ Index“-Form) → **R8-1**; **(b) Kalender:** kein Register-Pfad in API/Snapshot/UI → **R8-2** |

**Weitere Runde-7-Maßnahmen (geprüft, ohne eigene Befundnummer):** Kalender NO 24.12. und DK Freitag nach Himmelfahrt sind Feiertage (Probe: NO 24.12.2026/2027 `false`, DK 15.05.2026 und 07.05.2027 `false`, PL 24.12.2026 `false`); `isBuiltInIndex` öffentlich, `registerRateIndex("EURIBOR-6M")` → `INVALID_CURVE_SPEC`; `VOL_PLAUSIBILITY.fxMin 0,05 % / fxMedianMin 0,2 %` – EURDKK-Fläche mit 0,5 % ohne `VOL_IMPLAUSIBLE:`; Prämien-Leg auf FRA/FXF/FXS, `tradeCurrencies(…, { upfront })` (Quant-Dimension); Web: Zusatzkurve und Spot überleben Snapshot-Import/Reload (Tab „NOWA“ nach Reload, EUR/NOK-Spot), „+ Paar“ in der FX-Spot-Karte, Fokus nach „Kurve anlegen“ auf dem neuen Tab; 43 OpenAPI-Operationen, 44 Fehlercodes, 10 Warnungs-Präfixe; Core-Test `par-risk.test.ts` kein Timeout mehr.

**Runde-1…6-Posten:** unverändert geschlossen; G14/G24/G26 bleiben dokumentierte Roadmap (kein Abzug).

---

## 3. Neue Befunde (Runde 8)

Severity: **Niedrig** = Lücke zweiter Ordnung für eine v1-Persona · **Kosmetisch** = Voreinstellung/Konsistenz/Beispieldaten.

| # | Sev. (Abzug) | Ort | Befund (reproduziert) | Fix |
|---|---|---|---|---|
| **R8-1** | Niedrig (−0,5) | `apps/web/src/lib/snapshot-import.ts:25–74` (prüft `deriva.market/1`, reicht `indices`/`conventions` durch, registriert nichts), `apps/web/src/state/store.ts:898–920` (`loadSnapshot` → `deserializeMarket`), `apps/web/src/lib/register.ts:77–83` (`currencyOptions` nimmt jede Währung aus `discountCurveId`, auch ohne Konventionen), `TradeEditor.tsx:198` (`indexOptions(ccy)` → leer für unbekannte Währung, `current` bleibt), `quick-parser.ts:146` (`knownCurrencies()` = `SWAP_CONVENTIONS` des Web-Prozesses); keine „+ Währung“-Form | **Das per API registrierte Register erreicht die Workstation nicht – der Multi-Currency-Fluss „Index/Konvention per API registrieren → Snapshot exportieren → in der Workstation bewerten“ bricht in der UI ab.** UI (Anhang C, frischer LocalStorage): Import des API-Snapshots `snapshot-czk-r8.json` (12 Kurven, Envelope `indices` CZEONIA/PRIBOR-6M, `conventions` CZK) → Toast „Snapshot „Sample EoD“ importiert · ID f902da174832329b“, Marktansicht „Kurven: 12“, FX-Spot EUR/CZK vorhanden – aber `irs czk 5y pay 4% 100m` und `ois czk …` → **„⚠ Unbekannte Währung „CZK“ – Währungen: EUR, USD, GBP, CHF, JPY, NOK, SEK, DKK, PLN“**; Kurvenansicht ohne CZK/CZEONIA (siehe R8-5); Editor (`n s`): Währungs-Select bietet **CZK** (aus `discountCurveId`), Umstellung auf CZK → Trade bewertet **PV −6.796** mit Index-Select **„EURIBOR-6M (ohne Kurve)“** als einziger Option – ein CZK-Nominal-Swap, dessen Float-Leg EURIBOR-6M projiziert; der Kern akzeptiert das (API-Probe: CZK-Legs mit `index: EURIBOR-6M` → **200, PV −6.103.968, `warnings []`** – Leg-Währung ≠ Index-Währung wird nicht geprüft, Quant-Dimension). Die API-Seite ist rund (Abschnitt 2, R6-5 Rest); nur der Web-Pfad fehlt. Für den EoD-Fluss (TMS/API registriert CZK, Berater bewertet in der Workstation) ist das die entscheidende Lücke. | `readSnapshotJson`/`loadSnapshot`: Envelope `indices`/`conventions` prüfen (Core `registerRateIndex`/`registerSwapConventions`, eingebaute Namen → Fehltext) **vor** `deserializeMarket` registrieren, im persistierten Snapshot behalten und beim Hydrieren erneut registrieren; Snapshot-Export der Workstation mit demselben Envelope (aus `knownIndices()`/`SWAP_CONVENTIONS` minus eingebaute); Kurvenansicht „+ Währung“ (Konventionen, Kalender-Auswahl, OIS-/IBOR-Index) analog „+ Kurve“, persistiert wie `extraCurves`; `currencyOptions` nur Währungen mit Konventionen, sonst „(nicht registriert)“ mit Hinweis; Core: `validateTrade` → `INVALID_TRADE` bei `leg.currency !== getIndex(leg.index).currency`. Unit-Test „CZK-Snapshot mit Envelope → `irs czk` bewertbar“. |
| **R8-2** | Kosmetisch (−0,2) | `packages/pricing-core/src/curves/index-definitions.ts:291–298` (`checkCalendar`: „register it with registerCalendar first“), `dates/calendar.ts:356–364` (`CustomCalendar(name, holidays)`), `:415–418` (`registerCalendar`), `apps/api/src/routes/market.ts` (nur `/indices`, `/conventions`), `routes/snapshot.ts:61` (Envelope ohne `calendars`) | **Eine neue Währung kann per API nur mit fremdem Kalender registriert werden.** Probe: `POST /api/market/indices { CZEONIA, fixingCalendar: "CZ" }` → **400 `INVALID_CURVE_SPEC` „unknown calendar "CZ" (register it with registerCalendar first)“** – der Hinweis führt ins Leere, weil es weder `POST /api/market/calendars` noch einen Snapshot-Block noch ein UI gibt; die einzige Wahl ist `TARGET` (so alle CZK-Proben), womit Prager Feiertage (5./6. Juli, 28. September, 28. Oktober, 17. November) als Geschäftstage gelten und Schedules/Spot-Daten falsch rollen. Im Core geht es (Probe: `registerCalendar(new CustomCalendar("CZ", [...]), "CZK")` → 06.07.2027 `isBusinessDay false`, danach `registerRateIndex(CZEONIA, CZ)` ok); `registerCalendarHolidays` (Feed-Overlay) existiert ebenfalls nur im Core. | `POST /api/market/calendars { id, name, holidays: ["JJJJ-MM-TT", …], weekendsAreHolidays }` (→ `CustomCalendar` + `registerCalendar`, eingebaute IDs → 400; optional `PUT …/calendars/{id}/holidays` für Feed-Overrides = `registerCalendarHolidays`), Envelope `calendars[]` im Snapshot-Export/-Import, `GET /api/market` `calendars`; Fehltext nennt den Endpunkt; UI „+ Kalender“ in der „+ Währung“-Form (R8-1). |
| **R8-3** | Kosmetisch (−0,4) | API `apps/api/src/routes/risk-extended.ts:101, 124` (`sampleBootstrapSpecs(m.valuationDate, ctx.market.getQuotes())` – nur Sample-Kurven), `lib/store.ts:61, 91–96` (`QUOTE_KEY_BY_CURVE` = Sample-Kurven, `setCurveQuotes` → `false` für alle anderen, `parRiskTracked` nur im Audit `market.ts:345`), Web `apps/web/src/views/PricingWorkspace.tsx:150` (`parRisk(s.market, trade, ccy, sampleBootstrapSpecs(s.valuationDate, s.quotes))` ohne `extraCurves`) | **Par-Sensitivitäten ignorieren jede Kurve, die nicht aus dem Sample-Quote-Set stammt – und zeigen das als Null.** UI: NOK-Swap (Kurve NOK-NOWA per „+ Kurve“, 10 Quotes) → Karte „Par-Sensitivitäten (Quote-Bumps)“ nach „Berechnen“: **„PAR-DV01 GESAMT (+1 BP JE QUOTE) 0 EUR · ZERO-DV01 (PARALLELER SHIFT) 399 EUR · DIFFERENZ −399 EUR · Konvexität der Quotes / Kurvenkopplung“** – die Differenz wird fachlich als Konvexität erklärt, ist aber schlicht die fehlende Kurve. API: `POST /api/risk/par` für `IRS-NOK-1` → 200 `{"curves":[],"total":0}`, für `IRS-CZK-1` ebenso `curves [] total 0` (EUR-Sample-Trade: 4 Kurven, 54 Buckets); `POST /api/market/curves` liefert kein `parRiskTracked` in der Antwort (nur Audit). Für eine Treasury, die NOK-/CZK-Exposure über Par-Buckets hedgt, ist eine stille Null gefährlicher als ein fehlendes Feature. | Web: Specs aus `sampleBootstrapSpecs` **plus** `extraCurveSpec(c)` je `extraCurves` (Quotes liegen vor, `store.ts:605ff`); Import-Modus ohne Quotes → Hinweis „Par-Risiko nur für Kurven mit Quotes (n von m)“ statt 0; API: `RegisterStore`/`MarketStore` merkt sich `spec.quotes` je `POST /api/market/curves|bootstrap` (nicht nur Sample-Keys), `parRisk` erhält alle Specs, Antwort `curvesWithoutQuotes: [...]` + Warnung `PAR_RISK_INCOMPLETE:`; `curveSummary` mit `parRiskTracked`; Vertragstest „NOK-Swap nach `/curves` hat Par-Buckets auf NOK-NOWA“. |
| **R8-4** | Kosmetisch (−0,3, verbucht bei R7-5) | Web `apps/web/src/lib/portfolio-io.ts:239–243` (FXF: `buyCurrency/buyAmount/sellCurrency/sellAmount`), `:287–307` (FXO: `expiry`), `:326–348` (CCS `maturity`, Beispiel `-20`), `:396–398` (BASIS „≥ 1 = bp“, Beispiel `5`), FRA `period`; API `csv-import.ts:105–111` (FxForward: `pair/baseAmount/rate`), `:129–134` (`expiryDate`), `:141–143` (`tenor`, `-20 bp`), `:217–288` (`ALIASES` ohne `expiry`, `tenor`, `period`), `:372–390` (Spread-Parser: nur Suffix `%`/`bp` skaliert) | **Die elf Vorlagen sind nur für sechs Typen wechselseitig lesbar** (Detail in Abschnitt 2, R7-5). Probe mit den echten Web-Dateien: IRS/CAP/SWPT/FXS/AMORT/IMM → `imported` mit PV; **CCS** → `spread must be >= -1`, **BASIS** → `spread must be <= 1`, **FRA** → `column "start" is required`, **FXF** → 400 `CSV_INVALID`, **FXO** → 400 `CSV_INVALID`; API-CCS → Web „Laufzeit fehlt“, API-FXF → Web „Kauf-/Verkaufswährung fehlt“ (API-FXO importiert im Web). CHANGELOG/README versprechen „wechselseitig importierbar“ – gilt für die Header, nicht für die Beispielzeilen. | Eine Quelle für Spaltennamen **und** Einheiten: gemeinsame Vorlagen-Definition im Core (`CSV_TEMPLATES` mit `columns`, `aliases`, `unit`), Web und API importieren sie; Spread-Regel vereinheitlichen (Vorschlag: API übernimmt die Web-Heuristik „|x| ≥ 1 ohne Suffix = bp“ **oder** Web schreibt `-20 bp` in Vorlage/Export – letzteres ist eindeutig); Aliase `tenor` ↔ `maturity`, `expiry` ↔ `expiryDate`, `period` ↔ `start`, FXF `buy*/sell*` ↔ `pair/baseAmount/rate` beidseitig; Round-Trip-Test „Web-Vorlage (Header + Beispiel) → API → `imported`“ und „API-Beispiel → Web-Builder“ für alle elf Typen (heute prüft `review-r7.test.ts` nur CCS-Header). |
| **R8-5** | Kosmetisch (−0,2) | `apps/web/src/views/CurvesView.tsx:102–110` (`QUOTE_SETS` = acht Sample-Kurven), `:357–369` (`sets = QUOTE_SETS + extraCurves`, importierte Kurven fehlen), `:374–376` (Vergleichskurve nur gleiche Währung des gewählten Sets) | **Kurven eines importierten Snapshots, die nicht zum Sample-Set gehören, sind in der Kurvenansicht unsichtbar.** UI (frischer LocalStorage, API-Snapshot mit 12 Kurven inkl. NOK-NOWA, NOK-NIBOR-6M, NOK-NOWA-2, CZK-CZEONIA): Marktansicht „Kurven: 12“, Kurvenansicht Tabs **„€STR \| EUR 6M \| EUR 3M \| SOFR \| SONIA \| SARON \| TONA \| EUR/USD CSA“** (8), Seitentext nennt weder NIBOR noch NOWA-2 noch CZEONIA, Vergleichskurve nur `EUR-*`; „+ Kurve“ im Import-Modus disabled („Kurven stammen aus dem importierten Snapshot“). Ein Prüfer (IPV-Fluss: EoD-Snapshot aus dem TMS importieren, Kurve nachsehen) kann die NOK-Diskontkurve, auf der der Report basiert, nicht ansehen; der Tab „NOWA“ erschien in der ersten Probe nur, weil die Kurve zuvor per „+ Kurve“ angelegt worden war (`extraCurves` überlebt den Import). | Sets aus `Object.keys(baseMarket.curves)` ableiten: jede Kurve, die nicht in `QUOTE_SETS`/`extraCurves` ist, als schreibgeschützten Tab „(aus Snapshot)“ mit Pillar-Tabelle, Forwards-Chart und Meta (Index, Interpolation, Referenzdatum) – Quotes-Tabelle ausgeblendet; Label aus Index/Kurven-ID; Vergleichskurve über alle Kurven gleicher Währung; Test „Snapshot mit Fremdkurve → Tab sichtbar“. |

**Ohne Abzug, aber dokumentierenswert:**
- Der Kern akzeptiert ein Float-Leg, dessen Index-Währung nicht zur Leg-Währung passt (CZK-Leg mit `EURIBOR-6M` → PV −6,1 Mio ohne Warnung) – Ursache der Editor-Anomalie in R8-1; Validierung gehört in `validateTrade` (Quant-/Pricing-Dimension).
- `POST /api/trades/from-template` kennt nur `CrossCurrencySwap` und `FRA`; die übrigen Bauformen laufen über JSON-Trades oder die elf CSV-Vorlagen – konsistent mit dem OpenAPI-Text, kein Abzug.
- Hilfe-Overlay (`?`) nennt „+ Kurve“, nicht „+ Fläche“/„+ Paar“ (UI-Dimension).
- `POST /api/market/curves` antwortet ohne `parRiskTracked` (nur im Audit) – Teil des Fixes zu R8-3.
- `EMIR` `valuationCurrency` folgt `?reportingCurrency=` (Default EUR, mit `NOK` → NOK) – korrekt, aber für Fremdwährungsbücher dokumentierenswert.

---

## 4. Feature-Matrix – Statusänderungen gegenüber Runde 7 (nur DERIVA-Spalte)

| Feature | R7 | R8 | Beleg |
|---|---|---|---|
| Neue-Währung-Fluss in der UI: Index-Default mit Kurve, Index-Token normalisiert, Editor-Selects aus Register + Markt | ❌ | ✅ | R7-1 |
| Vol-Flächen (Swaption/Caplet/FX) für neue Währung/Paar in der Marktansicht | ❌ | ✅ | R7-2 |
| API: Diskontzuordnung bei `POST /curves`, `discountCurveId`/`collateralDiscountCurveId` in `PUT /api/market` | ❌ | ✅ | R7-3 |
| Beispiel-Fixings folgen dem Stichtag (API wie UI), Nutzer-Fixings gewinnen | 🔶 | ✅ | R7-4 |
| Web ↔ API CSV-Kompatibilität (CCS-Header) | ❌ | ✅ | R7-5 |
| Web ↔ API CSV-Kompatibilität (Einheiten, alle elf Vorlagen) | ❌ | ❌ (6/11) | R7-5 Rest / R8-4 |
| Registrierung weiterer Währungen/Indizes per API (`POST /api/market/indices\|conventions`), Snapshot-Envelope, Audit | ❌ | ✅ | R6-5 Rest |
| Register erreicht die Workstation (Envelope-Import, „+ Währung“) | ❌ | ❌ | R8-1 |
| Kalender registrierbar (API/Snapshot/UI) | – | ❌ (Core ✅) | R8-2 |
| `?dryRun=1` im Import, unbekannte Query-Parameter → 400 | ❌ | ✅ | R6-5 Rest |
| Par-Risiko für Zusatz-/Import-Kurven | – | ❌ (stille Null) | R8-3 |
| Snapshot-Kurven außerhalb des Sample-Sets in der Kurvenansicht | – | ❌ | R8-5 |
| CZK/HUF end-to-end **in der API** (Import, Report, Risiko, Dokumente, EMIR, Snapshot-Roundtrip) | ❌ | ✅ | Abschnitt 2 |
| Excel / Live-Marktdaten / Rollen / Netting-CVA / VaR / CRIF / Bermudan / TARF / PDF / Inflation | ⏳ | ⏳ Roadmap | kein Abzug |

---

## 5. Verifizierte Positivbefunde (Auszug, alle reproduziert)

- **Instrumente:** 18 Bauformen auf dem Sample-Markt ohne Warnung bewertet (Anhang A: IRS −104.454, OIS, Basis 94.403, Amort, IMM −119.312, Cap 120.100, Collar, Cash-Swaption 99.961, FRA, FXF/NDF, FXS, FXO/Barrier/Digital „analytic“, CCS/MtM-CCS/Fest-Fest-CCS); NOK-Swap/-Cap/-Swaption/-FRA und EUR/NOK-Option in der UI mit Kurve **und** Flächen ohne Fallback.
- **Marktdaten:** „+ Kurve“ (10 Quotes, Spot), „+ Fläche“ ×3, „+ Paar“, Snapshot-Import/-Export mit Envelope (API), 1 452 Sample-Fixings je Stichtag plus Nutzer-Fixings, Vol-Plausibilität mit Peg-Toleranz; Kalender NO/DK korrigiert.
- **Register (API):** 201/200/400 mit `builtIn`, `GET /api/market` `currencies`/`indices`/`conventions`, Snapshot-Envelope, Audit `register.*`; CZK bootstrap-, handel-, report- und meldefähig.
- **Integration:** `dryRun` JSON/CSV, geschlossene Query-Parameter, sechs Web-Vorlagen direkt API-fähig, CCS-Header-Aliase beidseitig.
- **Risiko/Dokumente Fremdwährung:** DV01 je Kurve (`CZK-CZEONIA`), 16 Szenarien, Termsheet CZK, EMIR `SRCCSP`/CZK; UI-Risikotabelle NOK (DV01, FX-Delta NOKEUR, Theta, Gamma).
- **API-Vertrag:** OpenAPI 3.1.0, **43 Operationen**, 44 Fehlercodes, 10 Warnungs-Präfixe; Tests Core 396 / API 124 / Web 335 grün, keine Page-Errors in zwei Playwright-Läufen.

---

## 6. Was für 100 noch fehlt

1. **R8-1 Register in die Workstation** – Envelope `indices`/`conventions` beim Snapshot-Import registrieren (und exportieren), „+ Währung“-Form in der Kurvenansicht, `currencyOptions` nur mit Konventionen, Core-Validierung Leg-Währung = Index-Währung. Größter Einzelposten (≈ 0,5).
2. **R8-3 Par-Risiko für alle Kurven mit Quotes** – Web: `extraCurves`-Specs mitgeben; API: Quotes je `/curves|/bootstrap` merken; fehlende Kurven benennen statt 0 (≈ 0,4).
3. **R8-4 / R7-5 CSV-Vorlagenfamilie** – eine Quelle für Spalten **und** Einheiten, Aliase `tenor`/`expiry`/`period`/FXF-Vokabular, Round-Trip-Test aller elf Vorlagen in beide Richtungen (≈ 0,3).
4. **R8-2 Kalender-Register** (`POST /api/market/calendars`, Envelope, UI) und **R8-5** Snapshot-Kurven als schreibgeschützte Tabs (je ≈ 0,2).

Mit 1 und 2 läge die Dimension bei ≈ 99,3; 3–4 schließen die Lücke zu 100 für den v1-Scope.

---

## Anhang A – Core-Probe (`node probe-core-r8.mjs` gegen `dist`, Auszug)

```
engine deriva-pricing-core/0.2.0 valDate 2026-09-03 snapshotId 3803d0191a972626 · exports 247 (registerCalendar, registerCalendarHolidays, CustomCalendar, registerRateIndex, registerSwapConventions …)
fixings 1323 · sampleFixings(2026-11-03) 1452
== N7-4 == NO 2026-12-24 false · NO 2027-12-24 false · DK 2027-05-07 false · DK 2026-05-15 false · PL 2026-12-24 false · SE 2027-06-25 false
== Register == knownCurrencies CHF,DKK,EUR,GBP,JPY,NOK,PLN,SEK,USD · indices 21 · isBuiltInIndex [EURIBOR-6M true, czeonia false]
registerRateIndex EURIBOR-6M → THROW INVALID_CURVE_SPEC "built-in index and cannot be replaced"
registerRateIndex CZEONIA calendar CZ → THROW INVALID_CURVE_SPEC unknown calendar "CZ" (register it with registerCalendar first)      (← R8-2)
registerCalendar(new CustomCalendar("CZ", [...]), "CZK") → {name CZ, 2027-07-06 isBusinessDay false} · danach CZEONIA ok · PRIBOR-6M ok · registerSwapConventions CZK ok
CZK OIS swap pv 0 par 4.000 % warnings [] curves [CZK-CZEONIA] · risk dv01 46276 byCurve [CZK-CZEONIA] · termsheet 5 · emir CZK · snapshot roundtrip idEqual true (keys ohne indices/conventions – Core-Dokument, Envelope ist API)
registerSwapConventions HUF ohne Index → THROW INVALID_CURVE_SPEC "BUBOR-6M is not a registered index"
== Instrumente == irs -104454 · ois -18915 · basis 94403 · amort -133973 · imm -119312 · cap 120100 · collar 103211 · swpt(cash) 99961 · fra -13179 · fxf 465 · ndf 465 · fxs -8101 · fxo 60855 analytic · barrier 60855 · digital 62934 · ccs 9600 · ccsMtm 9560 · ccsFixFix 397938 – alle warnings []
VOL_PLAUSIBILITY {lognormalMin 0.001, lognormalMax 3, normalMin 1e-5, normalMax 0.1, lognormalMedianMin 0.01, normalMedianMax 0.05, fxMin 0.0005, fxMedianMin 0.002} · EURDKK 0,5 % → []
STANDARD 16 HIST 6
```

## Anhang B – API-Probe (`npx tsx probe-api*-r8.mts`, `app.inject`, Auszug)

```
openapi 3.1.0 ops 43 · error codes 44 · Warnungs-Präfixe 10 · Routen /api/market/indices, /api/market/conventions (kein …/calendars ← R8-2)
PUT /api/market props: …,discountCurveId,collateralDiscountCurveId · GET /api/market props: …,currencies,indices,conventions · import query: reportingCurrency,type,mode,dryRun
== R7-3 == POST /curves NOK-NOWA 200 discountCurveSet true → disc.NOK NOK-NOWA · NOK-Swap-Import IRS-NOK-1 imported / ohne Index rejected CURVE_NOT_FOUND
  zweite Kurve NIBOR-6M discountCurveSet false · PUT discountCurveId NOK→NIBOR 200 · NOK-X 422 CURVE_NOT_FOUND · EUR-ESTR 400 INVALID_REQUEST "denominated in EUR, not NOK" · collateralDiscountCurveId 200 · isDiscountCurve:false → false
== R7-4 == PUT fixings (EURIBOR-3M@2026-09-02 0.0999, SOFR) 200 · valuationDate 2026-11-03 → fixings 1453, letztes 2026-11-02, Nutzer-Fixing 0.0999 erhalten · EURIBOR-3M-Swap ab 15.09.2026 warnings []
== R7-5 == alte Web-CCS-Header → 200 (Header ok) aber rejected CSV_ROW_INVALID "legs.0.spread must be >= -1" (spread -20) · neue Web-Vorlage ebenso
  echte Web-Vorlagen → API: AMORT imported -191253 · BASIS rejected "spread must be <= 1" · CAP imported 96080 · CCS rejected "spread >= -1" · FRA rejected 'column "start" is required' · FXF 400 CSV_INVALID lacks pair, baseAmount, rate · FXO 400 CSV_INVALID lacks expiryDate · FXS imported 2450 · IMM imported -119312 · IRS imported -481394 · SWPT imported 99961   (← R8-4)
  CCS spread "-20 bp" imported · "-0,002" imported · "-20" rejected · Basis "5" rejected
== R6-5 Rest == POST /indices EURIBOR-6M 400 INVALID_CURVE_SPEC builtIn true · CZEONIA mit Kalender CZ 400 "unknown calendar" · CZEONIA/PRIBOR-6M (TARGET) 201/201 · POST /conventions CZK 201
  GET /api/market currencies CHF,CZK,DKK,EUR,GBP,JPY,NOK,PLN,SEK,USD · conventions.CZK · indices CZEONIA,PRIBOR-6M · POST /curves CZK-CZEONIA 200 discountCurveSet true
  CZK swap import imported pv 0 · report 200 fairValue.ifrs13Level 2 · risk dv01 1889 byCurve CZK-CZEONIA · par risk 200 curves [] total 0 (← R8-3) · scenarios 200 · termsheet 200 · EMIR SRCCSP notionalCurrency CZK
  NOK par risk: {"curves":[],"total":0} (EUR-Sample-Trade: EUR-ESTR:18, EUR-EURIBOR-6M:17, EUR-EURIBOR-3M:14, EUR-ESTR-USDCSA:5) · POST /curves Antwort ohne parRiskTracked
  bootstrap HUF/BUBOR-6M 422 UNKNOWN_INDEX · spec.index-Beschreibung nennt POST /api/market/indices|conventions · ErrorResponse.code nennt UNKNOWN_INDEX → indices
  snapshot keys …,indices,conventions (CZEONIA,PRIBOR-6M / CZK, 12 Kurven) · PUT snapshot in frischer App 200 → CZK swap price 200 · Envelope mit SOFR 400 INVALID_CURVE_SPEC
  CZK-Legs mit index EURIBOR-6M → 200 pv -6103968 warnings [] (Leg-Währung ≠ Index-Währung ungeprüft ← Hinweis Quant)
== dryRun == JSON ?dryRun=1 {dryRun true, statuses [skipped]} · CSV ?dryRun=true imported → GET 404 · ?dryrun=1 400 · ?validate=1 400 · Audit: trade.import.dryRun, register.index, register.conventions, chainValid true
```

## Anhang C – UI-Probe (Playwright/Chromium 1194 gegen `vite preview :5001`, Auszug)

```
R7-1 vor Kurve: "irs nok 5y pay 4% 10m" → ⚠ Keine Kurve für NOK im Markt – … „+ Kurve“ … · "… nibor9m" → ⚠ Unbekannter Index „nibor9m“ – für NOK registriert: NIBOR-3M, NIBOR-6M, NOWA
+ Kurve NOK/NOWA (10 Quotes, Spot 11,62) → Toast "Kurve NOK-NOWA aus 10 Quotes angelegt · Spot EUR/NOK 11,6200"
R7-1 nach Kurve: "irs nok 5y pay 4% 10m" → ✓ Payer-Swap NOK 5Y @ 4,000 % · Nominal 10.000.000 · NOWA (Kurve vorhanden; NIBOR-6M ohne Kurve) · "… nibor6m"/"nibor-6m" → ✓ … ⚠ Kurve NOK-NIBOR-6M fehlt – … „+ Kurve“ anlegen
  "cap nok …" → NOWA · "swpt nok …" → ⚠ kein Swaption-Vol-Cube für NOK (Fallback-Vol, Level 3 – … „+ Fläche“ anlegen) · "fra nok 3x9" → NOWA · "fxo eurnok …" → ⚠ keine FX-Vol-Fläche … „+ Fläche“
  Enter → "Angelegt: IRS-0003 · PV 0 EUR" · Blotter: IRS-0003 Payer-Swap NOK 5Y Exporteur AS 10.000.000 NOK 07.09.2031 0 399 Indikation OK
  Editor: Währung = NOK [EUR/CHF/GBP/JPY/NOK/USD/DKK (ohne Kurve)/PLN (ohne Kurve)/SEK (ohne Kurve)] · Index = NOWA [NOWA/NIBOR-3M (ohne Kurve)/NIBOR-6M (ohne Kurve)] · Collateral […/NOK-CSA]
  Risiko: DV01 399 · FX-Delta 1 % NOKEUR 0 · Theta 14 · Gamma -0,19 · Par-Sensitivitäten nach "Berechnen": PAR-DV01 GESAMT 0 EUR · ZERO-DV01 399 EUR · DIFFERENZ -399 EUR "Konvexität der Quotes / Kurvenkopplung"   (← R8-3)
R7-2: Toolbar "Vol-Flächen (20): 5 Swaption-Cubes · 5 Caplet-Flächen · 10 FX-Paare + Fläche" · Art: Swaption-Cube (Währung) | Caplet-Fläche (Index) | FX-Fläche (Paar)
  Swaption NOK (Vorlagen EUR/USD/GBP/CHF/JPY, "NOK: 11 × 9") → "Swaption-Cube NOK angelegt (flach 60 bp)" · Caplet NOK/NOWA "9 × 8" → angelegt · FX EUR/NOK → "FX-Vol-Fläche EUR/NOK angelegt (flach 8,0 %)"
  Toolbar 23 Flächen · FX-Paare … EUR/NOK · "swpt nok"/"fxo eurnok" ohne ⚠ · "cap nok 5y 4.5% 10m" → CAP-0002 PV 5.155 EUR, kein Fallback-Text · nach Reload: 23 Flächen, Tab NOWA
R7-5: API-CCS-Zeilen (tenor, -20 bp / -0,002) → Dialog "2 Laufzeit fehlt (z. B. 5Y) · 3 Laufzeit fehlt" · Button "Keine gültige Zeile" · Blotter ohne CCS-API-* (← R8-4)
  API-FXF-Zeile → "Kauf-/Verkaufswährung fehlt" · API-FXO-Zeile → importiert
R6-5 Rest (frischer LocalStorage): Snapshot snapshot-czk-r8.json → "Snapshot „Sample EoD“ importiert · ID f902da174832329b" · Marktansicht "Kurven: 12 · Fixings: 1323 · Vol-Flächen: 20", EUR/CZK-Spot ✓
  Kurvenansicht Tabs: €STR|EUR 6M|EUR 3M|SOFR|SONIA|SARON|TONA|EUR/USD CSA (8 von 12; kein CZEONIA, kein NIBOR, kein NOWA-2) · Vergleichskurve nur EUR-* · "+ Kurve" disabled (Import-Modus)   (← R8-5)
  "irs czk 5y pay 4% 100m" / "ois czk …" → ⚠ Unbekannte Währung „CZK“ – Währungen: EUR, USD, GBP, CHF, JPY, NOK, SEK, DKK, PLN   (← R8-1)
  "fxf eurczk 3m 24.6 1m" → ✓ · "fxo eurczk call 25 1m 6m" → ✓ … ⚠ keine FX-Vol-Fläche für EUR/CZK (Fallback 8 %, Level 3 – … „+ Fläche“)
  Editor (n s): Währung [EUR/CHF/CZK/GBP/JPY/NOK/USD/DKK (ohne Kurve)/…] → CZK gewählt → PV -6.796, Index = "EURIBOR-6M (ohne Kurve)" [einzige Option]   (← R8-1)
Hilfe (?): "+ Kurve" ✓ · "+ Fläche" ✗ · "+ Paar" ✗ · NOK ✓ · CZK ✓ · Snapshot ✓
page errors: [] (beide Läufe)
```

Probe-Skripte, Ausgaben (`probe-*-r8.log`), Vorlagen (`web-templates-r8/`), Snapshot (`snapshot-czk-r8.json`) und Screenshots (`shots-r8/01-nok-editor.png`, `02-add-vol.png`, `03-market-nok.png`, `04-czk-snapshot.png`, `05-czk-editor.png`) im Scratchpad der Review-Session.
