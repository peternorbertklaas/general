# DERIVA – Review Runde 9: UI/UX & Hotkeys (Dim. 3) und User Flows (Dim. 4)

**Reviewer-Rolle:** Senior UX-Designer · Accessibility-Auditor · Trading-Desk-Power-User
**Datum:** 2026-09-04 · **Modus:** Re-Review only, keine Quellcode-Änderungen · **Baseline:** `review-ui-r8.md` (Runde 8: UI 98, Flows 98,3)

## 0. Prüfstand

| | |
|---|---|
| Repo-Stand | Branch `claude/derivatives-trading-platform-1arsyu`, HEAD `d470e6a`; Web-Fix-Commit `99c417b` („feat(web,product): Register-Envelope, „+ Währung“, Zeilen-Roving, Extras“). Web-Diff seit R8 (`1cd266e`): 26 Dateien, +3.381/−210 (Quelle ohne Tests/E2E: 20 Dateien, +2.249/−192) – neu `views/AddCurrencyForm.tsx` („+ Währung“ / „+ Kalender“), `lib/register-envelope.ts` (Envelope `indices`/`conventions`/`calendars` validieren, registrieren, exportieren), `lib/swaption.ts` (`swaptionWithUnderlyingIndex`); geändert `state/store.ts` (+461: `extraSpots`/`extraVolSurfaces`/`extraRegister`, `importDiscards`, `MessageContext`), `views/MarketView.tsx` (+188: `useRowNav` mit Tab-Zyklus und ↵-Rückkehr, `RowKeysHint`, `key={i}`, „+ Paar“/„+ Fläche“ strukturell, Envelope-Export), `views/CurvesView.tsx` (+112: Snapshot-Tabs, „+ Währung“, Sperr-Tooltip), `components/TradeEditor.tsx` (+141: Feld „Underlying-Index“, `legIndex`, „(nicht registriert)“), `hooks/useGridNav.ts` (`rows`/`cols`-Klemmung, ↵-Capture), `lib/i18n.ts` (`curveRepairMessage`, `translateRegisterDetail`), `lib/portfolio-io.ts` (`bp`-Suffix, API-Vokabular), `lib/quick-parser.ts` (Swaption mit `chooseIndex`), `PricingWorkspace.tsx` (Par-Risiko-Abdeckung), `App.tsx`/`HedgeView.tsx` (Fokus nach `d`/Reset), `HotkeyOverlay.tsx` |
| Bundle | `vite build` (vite 8.2.2/Rolldown) frisch: `index-BoozvIfD.js` 117,4 KB (38,6 KB gz), `store-DZBUu84y.js` 94,3 KB (27,8 KB gz), `core-BLVabhhM.js` 267,3 KB (86,4 KB gz), `react-DLh8G0Wt.js` 192,9 KB (61 KB gz), `echarts-BDKLMkZV.js` 560,6 KB (185,4 KB gz, lazy), `MarketView` 47,9 KB (13,4 gz), `PricingWorkspace` 66,1 KB (17,7 gz), `CurvesView` 37,2 KB (11,2 gz), `Popover` 46,8 KB, `register` 2,3 KB, `sw.js` 4,9 KB; `scripts/size-limit.mjs`: **Initial-Load 229,6 kB gz (8 Dateien)** (R8: 220,6), „Service worker precache: 19 assets“, „Size budget OK“. Preview `vite preview --port 5011 --strictPort` |
| Unit-/E2E-Tests | `npx vitest run` in `apps/web`: **41 Dateien, 362 Tests grün**, keine Timeouts (R8: 2 Last-Timeouts). `E2E_PORT=5012 PLAYWRIGHT_CHROMIUM_PATH=/opt/pw-browsers/chromium node e2e/smoke.mjs`: **E2E OK (493 checks)** |
| Browser-Audit | Playwright + Chromium (`/opt/pw-browsers/chromium`), Locale `de-DE`, Clipboard-Rechte, Viewports **1440×900** und **1024×768**, Dark + Light, Print-Emulation + `page.pdf()`, Offline (`context.setOffline`) in **frischem Browserkontext**, CDP-Drosselung (1,5 Mbit/s, 250 ms), `page.route`-Abbruch des ECharts-Chunks (R7-05-Muster erneut), **zweiter frischer Browserkontext** für Snapshot-Envelope-Import (HUF, CZK). Skripte in `…/scratchpad/r9-ui/`: `verify.mjs` (39 Checks: alle R8-Befunde, Markt R8-1/3/4/5, Randfälle), `verify2.mjs` (12: saubere Wiederholungen, Index-Token, Vorbelegung), `verify3.mjs` (6: Hedge-Fokus, 1024-Formular, Druck, Tabfolgen), `flows.mjs`/`flows2.mjs`/`flows3.mjs` (37 + 16 + 4: 7 Journeys inkl. CZK-Journey und API-Snapshot), `sweep.mjs` (**128 Zustände**: 20 Views/Zustände × 2 Viewports × 2 Themes inkl. neu „+ Währung“ mit „+ Kalender“, Snapshot-Kurven-Tab, Markt mit DKK-Flächen/„+ Paar“-Spot, DKK-Swaption-Editor, Par-Risiko unter Import; 11 Editoren, 4 Dokumente + Kundenmodus, Export-Menü, CSV-Dialog, Import-Override, Toast, Stichtag-Popover, Kontextmenü, Onboarding; a11y + Kontrast + Überlauf + Textscan (neu: Backtick-Scan) + Screenshot je Zustand; 10 Druck-PDFs), `lazy.mjs` (6), `misc.mjs` (Restore-Toast), `api-snap.mts` (API-Snapshot mit Envelope über `buildApp`/`inject`). Messwerte `results-{v,v2,v3,f,f2,f3,s,l,misc}.json`, 159 Screenshots, 10 PDFs. Rot gelaufene Checks wurden einzeln nachgeprüft; Sonden-Artefakte (kontaminierter Zustand nach abgebrochenen Abschnitten – SEK-Kurve doppelt, „+ Fläche“-Vorschlag SEK statt DKK –, `active()`-Text an Buttons, `data-testid="fixings-row-first"` statt `TR`, Tab-Zählung mit 10 Paar-Buttons, OIS-1W-Quote bewegt IRS-0001 < 1 EUR, Undo-Stack nach Reload leer, „Datums-Vorlagen“-Button als Zeilen-Control) sind aus den Befunden entfernt |
| Konsole | **Keine** JS-Fehler, keine Warnungen, keine fehlgeschlagenen Requests in allen Läufen (Ausnahme: absichtlich abgebrochene Chunks) |
| Tastatur | Chords `o t/g/k/c/p/r`, `x c/b`, `y i`, `y y`, `g …`, `n s/c/w/f/o/b/a/i/x/z/r`; Tab-Sequenzen ab Skip-Link (Blotter, Pricing, Kurven, Markt, „+ Währung“-Formular); Fixings-/FX-Fixings-/CDS-Zeilen `↓/PgDn/End/Home/↵/F2/Tab/⇧Tab/↵/Esc/Tab`; Vol-Gitter `←/→/↑/↓/End/Ctrl+End/↵/F2/↵/Esc/Tab` (alle drei, alle FX-Paare); „+ Kurve“/„+ Paar“/„+ Fläche“/„+ Währung“/„+ Kalender“ per `↵`; `Shift+P`, `Shift+K`, `Shift+T`, `t`, `d` (Pricing + Blotter), `Shift+D`, `Space`, `r`, `]`/`0`, `Alt+2`, `?`; `Ctrl+Z` auf Spot-Override, „+ Paar“, „+ Fläche“, „+ Währung“, Registrierung entfernen, Import, Verwerfen, Verlassen, Kurve anlegen/entfernen, Hedge-Reset, Fixing-Wert/-Datum/-Zeile |

Screenshot-Verzeichnis: `/tmp/claude-0/-home-user-general/ba34afa7-bb32-5710-8abf-0fcec9f55ee0/scratchpad/r9-ui/` (Übersicht Abschnitt 8).

---

## 1. Scores

### 1.1 UI/UX & Hotkeys: **98,8 / 100** (R1 62 → R2 88 → R3 91 → R4 95 → R5 96 → R6 98 → R7 98 → R8 98)

**Verifiziert behoben (Belege Abschnitt 2):** alle sechs R8-Befunde. Zeilentabellen des Marktes: `↵` → Index-Select, **`Tab` zyklisch über Index → Datum → Datums-Vorlagen → Wert → ✕ Entfernen → Index**, `⇧Tab` rückwärts, `Esc` → Zeile; **Wert per Tastatur „2,5“ + `↵` → übernommen (Chip „modifiziert“), Fokus auf derselben `tr`-Instanz** (stabiler `key={i}`, kein Remount), Index-Wechsel mit `↓` im Select behält Fokus und Knoten (R8-01); FX-Fixings (5 Controls) und CDS (3 Controls) vollständig erreichbar, `↵` kehrt zur Zeile zurück. FX-Gitter: nach `Ctrl+End` auf EUR/USD (7,4) behält **jedes der 10 Paare genau einen Tabstopp** (6×3-Flächen: Zelle 5,2 geklemmt) (R8-02). `↵`-Commit kehrt zur **selben Zelle** zurück (Swaption 1,1 → `→` 1,2; FX 0,0; Caplet 0,0), Fixing-Wert/-Datum → `TR`, Amortisationsplan → `TR`; Hinweistexte „↵ übernimmt und kehrt zur Zelle/Zeile zurück“ (R8-03). „+ Paar“ komplett per Tastatur: `↵` auf dem Button → `Tab`, `Tab` → Kurs „11,25“ → `↵` legt **Spot EUR/SEK 11,2500** an, Fokus zurück auf „+ Paar“, Badge „angelegt“ + ✕ (R8-04). `d` → **„Bezeichnung“** im Pricing bzw. **neue Blotter-Zeile** (`tr[data-id=CAP-0002]`), Hedge-Reset → **„Effektivität testen“** (R8-05). Unter Import: Badge/Pricing-Fehler „… der importierte Snapshot enthält keine DKK-Kurve – Snapshot mit Kurve importieren oder „Zum Sample-Markt“ wechseln“, gesperrter DKK-Tab mit Tooltip „nach „Zum Sample-Markt“ wieder aktiv“ (R8-06). Neu: **„+ Währung“ / „+ Kalender“** (Vorschau „2 Indizes CZEONIA, PRIBOR-6M · Konventionen CZK · Kalender CZ (neu, 6 Feiertage, ab 05.07.2027)“, deutsche Validierung, Toast, Fokus auf „+ Kurve“, Chips zum Entfernen, Undo), **Snapshot-Kurven als schreibgeschützte Tabs** „(aus Snapshot)“ mit Badge, Hinweis, 19 Pillars und Chart, **Par-Risiko-Abdeckung** („Par-Risiko nur für Kurven mit Quotes (0 von 4) …“), Swaption-Editor **„Underlying-Index“** mit Kurvenhinweis. **Sweep über 128 Zustände: 0 unbenannte Steuerelemente/Composites/Dialoge, 0 doppelte IDs, 0 positive Tabindizes, 0 Überschriftensprünge, 0 Tabellen ohne Namen, keine Seite scrollt horizontal; 7.406 gemessene Textpaare, alle Textfarben ≥ 4,5:1** (einzige Werte < 4,5 wie in R7/R8: Rail-Icon-Glyphen 18 px im aktiven Light-Tab 4,13:1 – Nicht-Text ≥ 3:1 –, Logo-Glyphe auf Gradient = Mess-Artefakt). Druck: Report 1 Seite, KID/Geeignetheit/Confirmation 3, Termsheet 1, Hedge 1 (Einheit 3 px), Kurven 2, Markt 4, Blotter 1, Pricing 1, Rail/Status/Topbar ausgeblendet. Lazy: Kaltstart Shell 410 ms, `g s` 436 ms, gedrosselt Skeleton (`aria-busy`, `aria-live=polite`, „Ansicht wird geladen …“) 973 ms ohne Doppel-Platzhalter, warme Chords 23–163 ms, `Alt+2` 79 ms, Reduced Motion respektiert. Offline nach dem **ersten** Besuch: 21 Cache-Einträge, alle 8 Views mit Diagrammen, Amortisations-Editor, „+ Kurve“-, „+ Währung“-, „+ Kalender“-, „+ Fläche“-, „+ Paar“-Formulare, **„+ Währung“-Registrierung offline** („Registriert: Index CZEONIA …“), Termsheet und KID.

**Abzüge (kumuliert −1,2):**
- Niedrig −0,3: **„+ Währung“-Formular läuft bei 1024 px über die Karte** – Feld „Fixing-Lag des IBOR-Index“ endet 61 px rechts der Karte, `main` scrollt 45 px horizontal (einziger Überlauf im Sweep) (R9-01).
- Kosmetisch −0,3: **„+ Kurve“ und „+ Währung“ per `↵` übergeben den Fokus nicht ans Formular** – das Formular wird *oberhalb* der Toolbar gerendert, `Tab` vom Button führt zu „+ Währung“/„Vergleichskurve“, erst ~10× `⇧Tab` über die Kurven-Tabs erreicht die Felder; „+ Paar“/„+ Fläche“ (Formular hinter dem Button) sind mit einem `Tab` im Formular (R9-02).
- Kosmetisch −0,2: **„+ Währung“ unter importiertem Snapshot** – Toast „Registriert: … – jetzt mit „+ Kurve“ eine HUF-Kurve anlegen“, „+ Kurve“ ist dort aber gesperrt und der Fokus fällt auf `body` (R8-06-Muster an neuer Stelle) (R9-03).
- Kosmetisch −0,2: **Zeile per Tastatur entfernen (✕ in Fixings/FX-Fixings) setzt den Fokus auf `body`** – die Rückkehr-Logik greift nur, solange die `tr` noch im Dokument steht (R9-04).
- Kosmetisch −0,2: **Literale Backticks in der Oberfläche** – Kartentitel „+ Währung registrieren … wie \`POST /api/market/indices|conventions|calendars\`“ und Tooltip „+ Kalender … (Kern \`CustomCalendar\`)“ (R9-05).

### 1.2 User Flows: **98,1 / 100** (R1 57 → R2 82 → R3 94 → R4 98 → R5 97 → R6 98 → R7 97,5 → R8 98,3)

- (a) **Indikation im Kundengespräch: sehr gut, vollständig per Tastatur** – `Ctrl+K` → `collar 7y 3,5/1,5 6m @Kunde GmbH` → `↵` in **404 ms** bis PV 48.973, Fokus „Bezeichnung“; `Shift+P` → „Zero-Cost-Collar“; `y i` Indikationstext; `g s` 481 ms, Heatmap `→ ↵` → What-if, `o t` mit Stress-Banner, **397 ms**, Fokusfalle, `Esc` → `main#main`; `o k`, `o g` (§ 64 Abs. 4 WpHG), `o c`, `o r` Hashes + MaRisk; `Shift+K` ohne CVA/DVA/Marge. Kein Abzug.
- (b) **Treasurer: CSV-Import mit allen 11 Vorlagen in beiden Vokabularen: sehr gut** – 11 Vorlagen, jede importiert ihre Beispielzeile **ohne Dialog, 0 Fehler-Badges**; **CCS-/Basis-Vorlage mit `bp`-Suffix** (`-20 bp`, `5 bp`, Markt R8-4); **API-Vokabular** importiert und bewertet: CCS `pair/domesticNotional/effectiveDate/tenor/fxSpot/spread/collateralCurrency/domesticPayReceive` (`-20 bp` ≡ `-0,002`: gleicher Spread im Editor), FXF `pair/baseAmount/rate/deliveryDate` (−2 Mio. → Verkauf EUR), FXO `expiryDate`, Swaption `payerReceiver/expiry/tenor/settlement`, FRA `start=3x9`, BASIS `0,05 %` ≡ `0,0005` (gleicher PV); defekte Datei → „4 Zeilen übersprungen“ mit deutschen Ursachen (FXF nennt **beide** Vokabulare), Kollision → Ersetzen; `o p`; **„Portfolio als JSON“ (27 Trades) → Import → Umbenennen → 54 Trades, 0 Fehler**. Kein Abzug.
- (c) **Prüfer: Marktdaten → What-if → Vergleich → Snapshot → Overrides → Undo: sehr gut** – Quote `↑↵` → „modifiziert“; `]` in **86 ms**; Vergleich unter What-if; `Ctrl+Z`. Snapshot Export → Quote → Import → **ID `3803d0191a972626` und Report-Hash `5cc9a93b71d397fb` identisch**, Toast „verworfen: Quote-Änderungen (Rückgängig stellt sie wieder her)“; Spot-Override 1,25 → „importiert · modifiziert“, andere ID, Report-Label, **überlebt den Reload**, `Ctrl+Z` → Hash zurück; Undo des Imports; Snapshot 30.10.2026 setzt den Stichtag, überlebt den Reload; **ungültige Envelopes deutsch** („Feld „indices“ muss eine Liste sein“, „Index „ESTR“ ist im Kern eingebaut …“, „Kalender „HU“ ist nicht registriert – … mit „+ Kalender“ anlegen“). Kein Abzug.
- (d) **Hedge Accounting: sehr gut** – `↵` auf „Effektivität testen“ → „✓ effektiv“; Ratio `↓` → Veraltet-Badge, „Erneut testen“, Markdown „veraltet“ ohne ISO/camelCase; Reload behält Ergebnis + Ratio 99; **Reset → Fokus „Effektivität testen“ (R8-05 ✓) → Toast-Undo (erster Tabstopp ab Skip-Link) / `Ctrl+Z` → Ratio 99, Verdict, Badge zurück, auch nach Reload**; CAP Innerer Wert + Vol einfrieren → CoH-Karte. Kein Abzug.
- (e) **„+ Währung“ CZK mit „+ Kalender“ → „+ Kurve“ → „+ Paar“ → „+ Fläche“ → Swap/Swaption/FX-Option/Cap/Forward → Export mit Envelope → frischer Browser → API-Snapshot: sehr gut** – `irs czk …` vorher „Unbekannte Währung „CZK““; „+ Währung“ per `↵`: CZK vorbelegt, OIS CZEONIA, IBOR PRIBOR-6M, „+ Kalender“ CZ „Prag“ mit 6 Feiertagen (deutsche Daten je Zeile; „abc“ → „Feiertag „abc“ nicht lesbar – Datum als 06.07.2027 oder 2027-07-06“), Vorschau, **6,5 s Bedienzeit**, Toast „Registriert: 2 Indizes CZEONIA, PRIBOR-6M · Konventionen CZK · Kalender CZ – jetzt mit „+ Kurve“ eine CZK-Kurve anlegen“, **Fokus auf „+ Kurve“**; `↵` → „+ Kurve“ mit **CZK/CZEONIA** vorbelegt (Index-Liste „CZEONIA (OIS, ACT/360, CZ)“, „PRIBOR-6M (IBOR 6M, ACT/360, CZ)“), Kurve aus 6 Quotes, Fokus auf dem Tab; `irs czk 5y pay 4% 100m` → Vorschau „· CZEONIA (Kurve vorhanden; PRIBOR-6M ohne Kurve)“ → „FX-Spot fehlt … mit „+ Paar“ ergänzen“ → **„+ Paar“ schlägt EURCZK vor, `Tab`,`Tab`, 24,6, `↵`** → Swap **PV −150.306**, Editor CZEONIA / „PRIBOR-6M (ohne Kurve)“ / CZK-CSA; `Shift+P` Par 3,2000 %, `y i` mit CZK, 16 Szenarien, Report nennt CZK-CZEONIA (5×) und „Kalender CZ“; „+ Fläche“ schlägt **CZK** vor (Cube 11×9 flach 60 bp), Caplet CZK-CZEONIA, FX EUR/CZK (flach 8 %), je ein Gitter-Tabstopp; **`swpt czk 1y5y payer 4% 50m` → „Underlying CZEONIA (Kurve vorhanden; PRIBOR-6M ohne Kurve)“ → PV 3.445, Editor „Underlying-Index“ CZEONIA, kein Level 3** (R8-F1 ✓); `fxo eurczk call 25 3m 5m` PV 45.153 ohne Level 3, `cap czk` 2.411, `fxf eurczk 6m 24.8 5m` −24.993; Reload: 0 Fehler-Badges; Snapshot-Export mit **Envelope (CZEONIA, PRIBOR-6M, CZK, CZ:6 Feiertage), CZK-Kurve, EURCZK-Spot, CZK-Cube, EUR/CZK-Smile, CZK-Caplet**, Toast „Register-Envelope (4 Einträge)“; **frischer Browser:** Import in 1,25 s, Toast „registriert: 2 Indizes CZEONIA, PRIBOR-6M · Konventionen CZK · Kalender CZ“, **identische ID `73175735ef3500c6`**, Spot/Cube/Smile da, Portfolio-JSON → **18 Trades, 0 Fehler, 0 Level-3-Zeilen, Swaption PV 3.445 identisch**, Reload ok, Kurven-Tab „CZEONIA (aus Snapshot)“; **API-Snapshot** (`POST /api/market/calendars|indices|conventions|curves`, `PUT fxSpots`, `GET /api/market/snapshot` via `buildApp`): Import mit Registrierungs-Toast, `irs czk` → PV −59.175 auf CZEONIA, Reload ok, Export re-emittiert den Envelope; „Zum Sample-Markt“ → 0 Fehler. DKK/NOK-Regression (R8 (e)): `swpt dkk` direkt nach „+ Kurve“ bewertbar, `Shift+P` liefert Par; **„+ Paar“-Spots und „+ Fläche“-Flächen überleben Import → Reload → Verlassen → Reload** (R8-F2 ✓). **Reibung −0,4 (R9-F1):** der **Index-Token registrierter Indizes wird in der Schnelleingabe nicht erkannt** – `irs huf 5y pay 6% 100m bubor6m` / `bubor-6m` / `BUBOR-6M` / `hufonia` → „Unbekanntes Token“, obwohl BUBOR-6M registriert ist und der Editor ihn listet (`cibor6m` für DKK funktioniert); **Reibung −0,3 (R9-F2):** „+ Kurve“ nach „+ Währung HUF“ belegt **DKK** vor (erste Währung ohne Kurve alphabetisch), CZK nur zufällig richtig; **Reibung −0,2 (R9-F3):** Snapshot mit Kurve einer **nicht registrierten** Währung (HUF-HUFONIA ohne Envelope): Editor sagt „HUF (nicht registriert)“ + „+ Währung“-Hinweis, die Schnelleingabe aber nur „Unbekannte Währung „HUF“ – Währungen: …“ ohne den Weg.
- (f) **Onboarding / Persistenz / Offline / Lazy: gut mit einer Lücke** – Theme, View, Stichtag 30.10.2026, Feldänderung überleben den Reload („Bestand aus lokalem Speicher geladen (13 Trades) · Zurücksetzen“); Onboarding deutsch; Offline nach dem ersten Besuch komplett (s. o.); Chunk-Ausfall mit Recovery (R7-05, E2E). **Reibung −1,0 (R9-F4):** die Toast-Aktion **„Zurücksetzen“ des Restore-Toasts ersetzt den Bestand ohne Rückfrage und ohne Undo** – nach jedem Reload ist sie 8 s lang der **erste Tabstopp** hinter dem Skip-Link: `Tab`, `↵` → `resetPortfolio()`, geänderter Festsatz 2,95 → 3,15, Hedge-Doku weg, „Nichts rückgängig zu machen“ (bei 213 importierten Trades Datenverlust).
- Performance (213 Trades): Import 305 ms, `j`×10 405 ms, Palette 183 ms, `]` 208 ms, Szenarien 1.095 ms, Markt 465 ms, Portfolio-Report 570 ms, Reload 907 ms, localStorage 118 KB.

---

## 2. Status der Runde-8-Befunde

Legende: ✅ behoben · 🔶 teilweise · ❌ offen. Belege = Feld in `results-v.json` (`verify.mjs`), `results-v2/v3.json`, `results-f/f2/f3.json`, `results-s.json` oder Screenshot.

| # | Titel (R8) | Status | Beleg / Rest |
|---|---|---|---|
| R8-01 | Markt-Zeilentabellen: nur erstes Control per Tastatur erreichbar | ✅ | `MarketView.tsx:63-113` `rowControls`, `useRowNav` (`Tab`/`⇧Tab` zyklisch, `↵`/`Esc` Capture mit `document.contains(tr)`), `RowKeysHint`, `:1365` `key={i}`; `v.fx {enter „Index Fixing 1323“, tabHops [Datum, Datums-Vorlagen, Wert, entfernen, Index], shiftTab entfernen, esc TR}`, `v2.fx.wert {v0 2,03 → v1 2,5, focus TR, sameRow true, modified 1}`, `v.fx.idx {↓ im Select: sameNode true, focus Index Fixing, inTable}`, `v.fx.date {↑↵ → TR, modified 1}`, `v.fxf {stops 1, hops Paar → Datum → Datums-Vorlagen → Kurs → entfernen → Paar, commit TR}`, `v.cds {stops 1, hops Tenor → Spread → entfernen → Tenor, esc TR}`, Hinweistext `fixings-keys-hint`; Doku `03-ui-konzept-und-hotkeys.md:116` aktualisiert; E2E R8-01-Checks (`v-market-rows.png`). **Rest → R9-04:** ✕ per Tastatur → Fokus `body` |
| R8-02 | FX-Gitter ohne Tabstopp nach Wechsel auf kleinere Fläche | ✅ | `useGridNav.ts:41-54` `GridNavOptions.rows/cols`, `clamp`, `MarketView.tsx:275/:181/:363` mit Dimensionen; `v.fxSwitch {last 7,4; EUR/GBP…GBP/CHF: rows 6, cols 3, stops 1, active 5,2; EUR/USD 8×5 active 7,4}`; E2E „every FX-vol surface keeps exactly one grid tab stop“ |
| R8-03 | `↵`-Commit in Gitter/Fixing/Amortisation → `body` | ✅ | `useGridNav.ts:131-141` (`Escape` **und** `Enter` im Capture), `useRowNav.onKeyDownCapture`, `TradeEditor.tsx:519-527` (Amortisation), `GridKeysHint` „↵ übernimmt und kehrt zur Zelle zurück“; `v.grid {cell0 1,1 → ↵ „Swaption-Vol 3M × 2Y“ → 70 ↵ → gridcell 1,1, edited 1, → 1,2; Esc verwirft (79 bleibt), fxCommit 0,0 → 0,0, capCommit 0,0 → 0,0}`, `v.amort {afterCommit TR, hint „↵ übernimmt und kehrt zur Zeile zurück“}`, `v2.fx.wert.focus TR` |
| R8-04 | „+ Paar“: `↵` im Kurs-Feld legt nicht an | ✅ | `MarketView.tsx:824-830` `onKeyDown Enter → submit()` am Kurs-Wrapper (NumInput feuert `onChange` live, daher der getippte Kurs); `v2.pp {↵ auf add-spot → Fokus bleibt, Tab „Neues Währungspaar“, Tab „Spot EUR/SEK“, 11,25 ↵ → row 1, value 11,25, formOpen 0, focus add-spot, toast „Spot EUR/SEK 11,2500 angelegt“, badge angelegt, ✕ 1}`; `↵` im Spot-Feld von „+ Kurve“ legt weiterhin nicht an (`v2.pp.curveEnter.tab 0` – wegen Quotes-Textarea vertretbar, wie R8) |
| R8-05 | Fokus `body` nach `d` und Hedge-Reset | ✅ | `App.tsx:286-290` (`focusEditorField()` im Pricing, `focusWhenPresent(tr[data-id])` sonst), `HedgeView.tsx:429-430`; `v2.dup {pricing: crumb „/ Pricing“, focus Bezeichnung, IRS-0003; blotter: focus TR CAP-0002, toast „Dupliziert: CAP-0002“}`, `f2.f6.reset.focus „hedge-test:Effektivität testen“`, `f.f6.reset.focus` ebenso; E2E „after the confirmed hedge reset the focus is on „Effektivität testen““ |
| R8-06 | Reparaturhinweis unter Import nennt gesperrtes „+ Kurve“ | ✅ | `i18n.ts:568-586` `MessageContext`/`curveRepairMessage`, `store.ts` `priceAll(…, msgCtx())`, `CurvesView.tsx:617` Tooltip des gesperrten Tabs; `v.j.imp.badges` „Keine Diskontkurve für DKK konfiguriert – der importierte Snapshot enthält keine DKK-Kurve – Snapshot mit Kurve importieren oder „Zum Sample-Markt“ wechseln“, `pricingErr` identisch, `addCurve.tabTitle „… nach „Zum Sample-Markt“ wieder aktiv“` (`v-import-hint.png`). **Rest → R9-03:** derselbe Widerspruch im „+ Währung“-Toast unter Import |
| R8-F1 | Swaption in neuer Währung unbewertbar (Underlying CIBOR-6M ohne Kurve) | ✅ | `quick-parser.ts:781-800` `chooseIndex` + `swaptionWithUnderlyingIndex`, Vorschau „· Underlying DESTR (Kurve vorhanden; CIBOR-6M ohne Kurve)“; `TradeEditor.tsx:1222/:1249-1261` Feld „Underlying-Index“ (`legIndex`), `i18n` „… oder im Editor den Underlying-Index wechseln“; `v.j.swpt {pv 27.851, err „“, underlyingIdx DESTR, idxOpts [DESTR, CIBOR-3M (ohne Kurve), CIBOR-6M (ohne Kurve)]}`, `v.j.swptCibor.err „Kurve DKK-CIBOR-6M nicht im Markt-Snapshot – … „+ Kurve“ anlegen oder im Editor den Underlying-Index wechseln“` + Feldhinweis, `v.j.swptEditor {n w → DKK: err 0, idx DESTR}`, `v.j.eurSwpt {EURIBOR-6M → ESTR: PV 99.961 → 78.394, err 0}`, `f2.czk.swpt {Underlying CZEONIA, PV 3.445}`, `f2.dkk.swptPar` mit Par (`v-swpt-dkk-editor.png`) |
| R8-F2 | „+ Paar“-Spots / „+ Fläche“-Flächen gehen bei Import → Verlassen verloren | ✅ | `store.ts` `extraSpots`/`extraVolSurfaces` (strukturell, `buildMarket(…, extras)`, `importDiscards`, `UndoEntry kind:"extras"`, Persistenz + Hydration), `MarketView.tsx` `addExtraSpot`/`setExtraVolSurface`, Zeilen-Badge „angelegt“ + ✕; `v2.f2.imp.toast „… gemerkt, nach „Zum Sample-Markt“ wieder aktiv: Kurve …, Spot EUR/SEK, Swaption-Cube DKK, FX-Fläche EUR/DKK“` ohne „verworfen“, `v2.f2 {leave.spots enthält EURSEK, swptBtns DKK, fxPairs EUR/DKK; final.badges 0, swpt.pv identisch, fxo.pv identisch, level3 false}`, `v.j.dateDiscard {badges 0, spots EURSEK, cube}`, `v.j.snap {reimport id identisch, spots [EURDKK, EURSEK], swpt enthält DKK}`, `v.j.rmSpot {✕ → „FX-Spot fehlt … „+ Paar““, Ctrl+Z → 0}`, „Markt zurücksetzen“ entfernt Extras (`v2.f2.reset`); E2E R8-F2-Checks (`v-blotter-extras.png`) |
| Markt R8-1 (Web) | Register-Envelope, „+ Währung“, `currencyOptions` nur mit Konventionen | ✅ | `register-envelope.ts`, `AddCurrencyForm.tsx`, `register.ts:19-25/:96-100`; `v.cur {problemEur „eingebaut“, problem0 „OIS-Index angeben“, calProblem „Kalender-ID“, calBad „nicht lesbar“, previewCal „2 Indizes HUFONIA, BUBOR-6M · Konventionen HUF · Kalender HU … 3 Feiertage“, after.toast „Registriert: …“, focus add-curve, Kurve HUF-HUFONIA, qe.irs „HUFONIA (Kurve vorhanden; BUBOR-6M ohne Kurve)“, trade {PV, ccy HUF, idx HUFONIA, idxOpts BUBOR-6M (ohne Kurve)}, envelope {indices [BUBOR-6M, HUFONIA], conventions [HUF], calendars [HU:3], spot 395}, exportToast „Register-Envelope (4 Einträge)“, removeRefused „HUF wird von der Kurve HUF-HUFONIA verwendet“, removed → Editor „HUF (nicht registriert)“ + Hinweis, undo2.badges 0, fresh {toast registriert, PV −32.763, reload 0, Tab „HUFONIA (aus Snapshot)“, „HUF ist bereits registriert (z. B. aus einem Snapshot-Envelope)“}`, `v.sc.ccyOpts „HUF (nicht registriert) [disabled]“` (`v-add-currency-form.png`, `v-pricing-huf.png`, `v-fresh-envelope-import.png`). **Rest → R9-F1/F2/F3, R9-01/02/03/05** |
| Markt R8-3 (Web) | Par-Risiko mit Zusatzkurven, Hinweis statt 0 | ✅ | `PricingWorkspace.tsx:107-123` `parSpecs`/`parCoverage`, `:522-530` Warnung, `:797` Differenzhinweis; `v.j.par {coverage 0, „DKK-DESTR · Σ 616 EUR“, total 616 EUR, note „Konvexität der Quotes / Kurvenkopplung“}`, `v.j.parImp.coverage „Par-Risiko nur für Kurven mit Quotes (0 von 4). Die Kurven des importierten Snapshots tragen keine Bootstrap-Quotes – „Zum Sample-Markt“ wechseln oder …“` |
| Markt R8-4 (Web) | CSV-Vorlagen `bp`-Suffix, API-Aliasse | ✅ | `portfolio-io.ts:330-357/:596-616` (`spreadOf`, `fixedrate`/`payerreceiver`/`end`, FXF `pair/baseAmount/rate`, CCS `tenor`, FRA `start=3x9`); `v.csv.ccs.example „…;-20 bp;…“`, `basis „;5 bp;“`, 11 Vorlagen `added 1, dlg 0, err 0`, `v.csv.api` ccs/fx/swptfra/basis je `added 2, dlg 0, err 0`, Spread CCS-API-1 = CCS-API-2, BASIS-API-1 = -2 (Spread und PV) (`v-blotter-api-csv.png`) |
| Markt R8-5 (Web) | Snapshot-Kurven als schreibgeschützte Tabs | ✅ | `CurvesView.tsx:100-108/:372-381/:620-624/:792-797/:880-889`; `v.sc {tabs „NOWA (aus Snapshot)“, „HUFONIA (aus Snapshot)“, nokDisabled false, badge „aus Snapshot (schreibgeschützt)“, note „… ohne Bootstrap-Quotes …“, pillars 19, canvas 1, quotesHidden true, removeBtn 0}` (`v-curves-snapshot-tab.png`); Sweep-Zustand `curves-snapshot-tab` a11y-sauber |

Zusätzlich verifiziert (kein R8-Befund): Import-Modus-Sperren (R6-04), „Vol-Typ fehlt“/Envelope-Fehltexte, CSV-Dialog (R6-06), Fokus nach `Esc` aus Chord-Dokument (R6-03), Amortisationsplan-Roving (R6-02), Undo-Semantik Import/Verwerfen/Verlassen (R6-F2), Spot-Override (R6-F1), Hedge-Ergebnis nach Reload (R5-F3), Editor-Listen aus dem Register (R7-02), ECharts-Chunk-Recovery (R7-05, E2E), Hilfe-Overlay nennt „+ Währung / + Kalender“, „+ Paar“, „+ Fläche“, Zeilen- und Gittermodell mit `Tab`/`⇧Tab` und „↵ übernimmt“.

---

## 3. Neue Befunde (Runde 9)

Schweregrade wie in R1–R8. Reproduktion gegen das Preview-Bundle; Belege in `results-*.json` (Feldname) bzw. Screenshot.

| # | Schwere | Wo | Was ist falsch (Beleg) | Konkreter Fix |
|---|---|---|---|---|
| R9-01 | Niedrig (Responsivität) | `views/AddCurrencyForm.tsx:396-433` (IBOR-Zeile: `label.row` mit Input + Tenor + Tageszählung, daneben `label` „Fixing-Lag“ und Hinweis-`span`; die `label.row`-Kinder sind `nowrap`) | Bei **1024×768** endet das Feld „Fixing-Lag des IBOR-Index“ **61 px rechts der Karte**, `.main` scrollt **45 px horizontal** – der einzige Überlauf im Sweep über 128 Zustände (`s.summary.overflow [1024-dark/light-curves-add-currency: mainScroll 45, clipped card:62]`, `v3.form1024 {cardW 636, over [{Fixing-Lag des IBOR-Index, 61}]}`, mit und ohne „+ Kalender“; 1440 px: 0) (`1024-dark-curves-add-currency.png`, `v3-add-currency-1024.png`). | Zeilen als `row wrap` mit `flex: 1 1 auto`/`min-width: 0` auf den `label.row`-Gruppen, „Fixing-Lag“ in die IBOR-Gruppe ziehen (wie „Spot-Lag“ in der Kopfzeile) oder die Karte als CSS-Grid (`repeat(auto-fit, minmax(220px, 1fr))`); Sweep-Check „kein Überlauf“ für `curves-add-currency` ins E2E. |
| R9-02 | Kosmetisch (Tastatur) | `views/CurvesView.tsx:560-579` (Formulare `AddCurveForm`/`AddCurrencyForm` werden **vor** `div.toolbar` gerendert), `:626-646` Buttons „+ Kurve“/„+ Währung“ ohne Fokusübergabe; im Markt stehen „+ Paar“-/„+ Fläche“-Formulare **hinter** dem Button | `↵` auf „+ Kurve“ → Fokus bleibt auf dem Button, **`Tab` → „+ Währung“**, `⇧Tab` ×2 → „EUR/USD CSA“ (Kurven-Tab); `↵` auf „+ Währung“ → `Tab` → „Vergleichskurve“, `⇧Tab` ×2 → „+ Kurve“ – die Felder liegen ~10 Tabstopps rückwärts (`f2.ff {curve {afterEnter add-curve, tab add-currency, formBeforeButton true}, ccy {tab „Vergleichskurve (gleiche Währung)“, formBeforeButton true}, vol {tab add-vol-kind}}`). Der Tastaturnutzer öffnet das Formular und muss es suchen; „+ Paar“/„+ Fläche“ sind mit einem `Tab` drin. | Nach dem Öffnen `focusWhenPresent('[data-testid="add-curve-ccy"]')` bzw. `'[data-testid="add-currency-code"]'` (R7-03-Muster), oder die Formulare unter der Toolbar rendern (DOM-Reihenfolge = Tab-Reihenfolge); `Esc` im Formular → Button (heute nur „Abbrechen“). |
| R9-03 | Kosmetisch (Fehlertext/Fokus) | `views/AddCurrencyForm.tsx:192-195` (Toast „… jetzt mit „+ Kurve“ eine …-Kurve anlegen“ unabhängig von `marketSource`), `CurvesView.tsx:575` `focusWhenPresent('[data-testid="add-curve"]')` auf **disabled** Button | Unter importiertem Snapshot: „+ Währung“ ist aktiv, Registrierung klappt, Toast sagt „jetzt mit „+ Kurve“ eine HUF-Kurve anlegen“ – **„+ Kurve“ ist gesperrt** (`IMPORT_LOCK`) und der **Fokus fällt auf `body`** (`v.j.ccyImp.after {toast „… „+ Kurve“ …“, focus BODY, addCurveDisabled true}`, `v-add-currency-import.png`). Dasselbe Muster wie R8-06, an der neuen Stelle nicht angewandt. | Toast kontextabhängig („… registriert – nach „Zum Sample-Markt“ mit „+ Kurve“ eine HUF-Kurve anlegen“ oder „… Snapshot mit HUF-Kurve importieren“), Fokus auf „Zum Sample-Markt“ bzw. „+ Währung“ statt auf den gesperrten Button; `focusWhenPresent` sollte `:not([disabled])` verlangen. |
| R9-04 | Kosmetisch (Fokus) | `views/MarketView.tsx:104-110` `onKeyDownCapture` (`if (tr && document.contains(tr)) tr.focus()` – nach „Entfernen“ ist die `tr` weg), Buttons „Fixing n entfernen“ / „FX-Fixing n entfernen“ | Zeile per Tastatur löschen: `↵` → `Tab`×4 → „Fixing 1323 entfernen“ → `↵` → Zeile weg (Chip „modifiziert“, 1263 → 1262 ausgeblendet), **Fokus `body`**, `Tab` beginnt am Seitenanfang (`v2.fx.remove {focus BODY, inTable false, modified 1}`, FX-Fixings `v2.fxf.after {focus BODY, table 0}`). Maus-Nutzer merken nichts; die R8-01-Serie „mehrere Zeilen entfernen“ ist so nicht flüssig. | Vor dem Entfernen den Nachbarn merken (`tr.nextElementSibling ?? previousElementSibling`) und nach dem Re-Render fokussieren (bzw. `useTableNav.setActive(min(i, n-1))` + `focusWhenPresent('tbody tr[tabindex="0"]')`); leere Tabelle → „+ Zeile“-Button. |
| R9-05 | Kosmetisch (Text) | `views/AddCurrencyForm.tsx:219` (`h3`-Untertitel), `:280` (`title` von „+ Kalender“) | Markdown-Backticks werden literal angezeigt: „Konventionen, OIS-/IBOR-Index und Kalender wie \`POST /api/market/indices|conventions|calendars\` · im Snapshot-Envelope exportiert“ und Tooltip „… (Kern \`CustomCalendar\`)“ (`s.summary.textHits[curves-add-currency].backticks`, `v.cur.backticks true`, `1440-dark-curves-add-currency.png`). Einziger Treffer des neuen Backtick-Scans über 128 Zustände. | `<code className="mono">POST /api/market/indices|conventions|calendars</code>` bzw. „(Kern-Typ CustomCalendar)“ ohne Backticks; Textscan `` /`[^`]+`/ `` in den E2E-Sweep. |

### Flow-Befunde

| # | Schwere | Wo | Was ist falsch (Beleg) | Konkreter Fix |
|---|---|---|---|---|
| R9-F1 | Niedrig (Erlernbarkeit/Fehlerpfad) | `lib/quick-parser.ts:182` `INDEX_RE = /^(euribor|estr|sofr|sonia|saron|tona|nowa|stibor|wibor|cibor|nibor|polonia|wiron|swestr|destr)/i` – nur Token dieser Familien gelangen zu `normaliseIndexToken` (`register.ts:65`, das das Register kennt) | Nach „+ Währung“ HUF (HUFONIA, BUBOR-6M) und „+ Kurve“ HUF-HUFONIA: `irs huf 5y pay 6% 100m bubor6m`, `… bubor-6m`, `… BUBOR-6M`, `… hufonia` → **„⚠ Unbekanntes Token „bubor6m“ – Währungen: …“** statt „⚠ Kurve HUF-BUBOR-6M fehlt“ bzw. HUFONIA-Vorschau; `irs dkk … cibor6m` liefert „⚠ Kurve DKK-CIBOR-6M fehlt – …“ (`v2.tok.q`, `v.cur.qe.bubor`). Grammatik-Hilfe verspricht `[index]`, Editor und „+ Kurve“ kennen BUBOR-6M; die Schnelleingabe der neuen Währung ist damit ohne Index-Wahl (Default via `chooseIndex` funktioniert: `v2.tok.second {BUBOR-6M-Kurve → idx BUBOR-6M, err 0}`). | `INDEX_RE` durch `normaliseIndexToken(t) !== undefined` ersetzen (Register statt Liste) oder die Familien aus `knownIndices()` ableiten (`new RegExp('^(' + names.map(base).join('|') + ')', 'i')`, bei Registrierung neu bauen); Test „Token eines registrierten Index“ in `review-r8.test.ts`. |
| R9-F2 | Kosmetisch (Erlernbarkeit) | `views/CurvesView.tsx:227-228` (`withoutCurve[0]` = erste Währung ohne Kurve alphabetisch), `:573-576` `onDone(ccy)` kennt die Währung, gibt sie aber nicht weiter | Nach „+ Währung HUF“ (Toast „… jetzt mit „+ Kurve“ eine HUF-Kurve anlegen“, Fokus „+ Kurve“) öffnet `↵` das Formular mit **DKK** (`v2.tok.curveFormCcy DKK`, `v.cur.curveForm.sel DKK`); für CZK stimmt es nur, weil C < D (`f2.czk.curve.ccy CZK`). Der Nutzer muss die eben registrierte Währung erst wieder auswählen. | `AddCurveForm` mit `initialCurrency` aus dem letzten „+ Währung“-Ergebnis (State in `CurvesView`, von `onDone(code)` gesetzt) bzw. zuletzt registrierte Währung des `extraRegister` bevorzugen. |
| R9-F3 | Kosmetisch (Fehlerpfad) | `lib/quick-parser.ts:338-342` `unknownTokenError` (`isCcy` = Register), kein Blick auf `opts.curveCurrencies` | Snapshot mit Kurve einer nicht registrierten Währung (HUF-HUFONIA ohne Envelope): `irs huf 5y pay 6% 100m` → „⚠ Unbekannte Währung „HUF“ – Währungen: EUR, USD, GBP, CHF, JPY, NOK, SEK, DKK, PLN“ – der Markt **hat** eine HUF-Diskontkurve; der Editor sagt an derselben Stelle „HUF (nicht registriert)“ + „Für HUF sind keine Swap-Konventionen registriert – in der Kurvenansicht mit „+ Währung“ registrieren (oder Snapshot mit „conventions“/„indices“ importieren)“ (`v.sc.qeHuf`, `v.sc.ccyOpts`). | In `unknownTokenError` prüfen, ob `tok` in `opts.curveCurrencies` liegt → `unregisteredCurrencyHint(ccy)` (bereits in `register.ts:24`) statt „Unbekannte Währung“. |
| R9-F4 | Niedrig (Datenverlust ohne Rückfrage) | `App.tsx:152-160` Restore-Toast „Bestand aus lokalem Speicher geladen (n Trades)“ mit Aktion **„Zurücksetzen“ → `resetPortfolio()`** ohne `confirm`, ohne Undo-Eintrag; Toast-Stack ist der erste Tabstopp hinter dem Skip-Link (R3-Design) | Nach **jedem Reload** ist „Zurücksetzen“ 8 s lang der **erste Tabstopp**: `Tab`, `↵` → Beispielportfolio geladen, geänderter Festsatz 2,95 → 3,15, Hedge-Dokumentation und Testergebnis weg, **„Nichts rückgängig zu machen“** (`misc.rt {tab1 „Zurücksetzen“, afterEnter {dialog null, rate 3,15, toast „Beispielportfolio geladen“}, afterUndo „Nichts rückgängig zu machen“}`; in `flows.mjs` f6 reproduziert: Hedge-Ratio 99 → 100, Verdict weg). Alle anderen destruktiven Aktionen (Kurve entfernen, Hedge-Reset, Markt zurücksetzen, Löschen) fragen nach oder sind rückgängig; bei 213 importierten Trades bedeutet das Datenverlust per Fehltipp. | `confirm(„Bestand (n Trades, Marktänderungen, Hedge-Dokumentation) durch das Beispielportfolio ersetzen?“)` wie bei „Markt zurücksetzen“, **und/oder** `pushUndo`-Eintrag für `resetPortfolio` (Trades, Markt, Hedge in einem `UndoEntry`), Toast danach mit „Rückgängig“; alternativ die Aktion aus dem Restore-Toast nehmen (Reset gibt es im Export-Menü). |

Ohne Abzug notiert: R9-03 wirkt auch als Flow-Reibung (Import → „+ Währung“), ist aber dieselbe Ursache wie in Dim. 3 (kein Doppelabzug). Der **Undo-Stack überlebt den Reload nicht** („Nichts rückgängig zu machen“ nach Spot-Override → Reload → `Ctrl+Z`) – Overrides selbst bleiben; Design, kein Abzug. „+ Fläche“ schlägt bei mehreren Währungen ohne Cube die **erste Währung mit Kurve ohne Cube** vor (DKK vor SEK) – nachvollziehbar. Ein API-Snapshot, dessen Kurve dieselbe ID wie eine „+ Kurve“-Kurve trägt (CZK-CZEONIA), erscheint unter dem aktiven „+ Kurve“-Tab statt als „(aus Snapshot)“ – Quotes sind im Import-Modus ohnehin gesperrt, korrekt. Markt-Druck jetzt 4 Seiten (R8: 3, mehr Hinweistexte) – im Rahmen.

---

## 4. User Journeys (Schritt für Schritt, tastaturgeführt)

### (a) Berater bewertet Collar → Termsheet/KID/Erklärung → Report
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `Ctrl+K` · `collar 7y 3,5/1,5 6m @Kunde GmbH` · `↵` | **404 ms** bis PV 48.973; Kontrahent „Kunde GmbH“; Fokus auf „Bezeichnung“ | – |
| `Esc` · `Shift+P` | „Zero-Cost-Collar: Floor-Strike übernommen (Prämie 0)“, PV ≈ 0, Rückgängig im Toast | – |
| `y i` | Indikationstext mit Prämie, PV, DV01, Kontrahent, Stichtag | – |
| `g s` · Heatmap `→ ↵` · `o t` | Szenarien 481 ms; What-if; Termsheet mit Stress-Banner; `Esc` → `main#main` | – |
| `0` · `o t` · `Tab`×6 · `Esc` | Termsheet **397 ms**, Fokusfalle (Drucken/Markdown erreichbar, Rail nie) | – |
| `o k` · `o g` · Kunde · Erzeugen · `o c` · `o r` | KID, Geeignetheitserklärung § 64 Abs. 4 WpHG, Confirmation, Report mit Hashes/MaRisk | – |
| `Shift+K` | Kundenansicht ohne CVA/DVA/Margenformel | – |

### (b) Treasurer: 11 CSV-Vorlagen in beiden Vokabularen → defekte Datei → Kollision → Portfolio → JSON-Roundtrip
| Schritt | Beobachtung | Reibung |
|---|---|---|
| Export ▾ → 11× „⤓ Vorlage …“ | `deriva-import-vorlage-{irs,fxf,cap,swpt,fxo,ccs,fra,fxs,basis,amort,imm}.csv`; CCS `-20 bp`, Basis `5 bp` (Markt R8-4 ✓) | – |
| 11× „⤒ CSV importieren“ | je „1 Trades aus CSV importiert“, kein Dialog, 0 Fehler-Badges | – |
| API-Vokabular (CCS `tenor`, FXF `pair/baseAmount/rate`, FXO `expiryDate`, SWPT `payerReceiver/expiry/tenor`, FRA `start 3x9`, BASIS `0,05 %`) | je 2 Trades, 0 Dialog, 0 Fehler; `-20 bp` ≡ `-0,002`, `0,05 %` ≡ `0,0005` (gleicher PV) | – |
| defekte Datei (5 Zeilen, 4 defekt) | „4 Zeilen übersprungen“, deutsche Ursachen, FXF nennt beide Spaltensätze, „1 gültige Zeile importieren“, Fehler-CSV | – |
| Kollision | Überspringen/Ersetzen/Umbenennen → „2 Trades aus CSV importiert“ | – |
| `o p` · „⤓ Portfolio als JSON“ · „⤒ JSON importieren“ | Portfolio-Report; 27 Trades → Kollisionsdialog → Umbenennen → 54 Trades, 0 Fehler | – |

### (c) Prüfer: Marktdaten → What-if → Vergleich → Snapshot → Overrides → Undo
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `g c` · Quote `↑↑↵` · `]` | „modifiziert“; `]` 86 ms | – |
| `g b` · `Space` · `j` · `Space` · `g v` · `0` · `Ctrl+Z` | Vergleich unter What-if; „Rückgängig: Quote OIS 1W 2,0150 → 2,0200 %“ | – |
| `g m` · Export · Quote ändern · Import · `o r` | ID `3803d0191a972626` und Report-Hash identisch; Toast „verworfen: Quote-Änderungen“ | – |
| Spot EUR/USD 1,25 · `o r` · Reload · `Ctrl+Z` | „importiert · modifiziert“, andere ID, Reload behält 1,25; Undo (ohne Reload dazwischen) stellt Hash wieder her | Undo-Stack nicht persistent (Design) |
| `Ctrl+Z` auf Import · Stichtag 30.10. · Reload | jede Marktquellen-Aktion rückgängig; Stichtag überlebt Reload | – |
| ungültige Envelopes (`indices: "x"`, ESTR, Kalender HU fehlt) | deutsch: „muss eine Liste sein“, „im Kern eingebaut“, „nicht registriert – … „+ Kalender““ | – |

### (d) Hedge Accounting
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `g h` · „Effektivität testen“ `↵` | „✓ effektiv“, Herleitung | – |
| Ratio `↓` `Tab` · Markdown | Veraltet-Badge, „Erneut testen“, Markdown „veraltet“, deutsch | – |
| Reload | Ergebnis, Ratio 99, Badge vorhanden | – |
| „Zurücksetzen“ `↵` · Skip-Link `Tab` `↵` | Rückfrage; **Fokus „Effektivität testen“** (R8-05 ✓); Undo → Ratio 99, „✓ effektiv“, Badge zurück, auch nach Reload; `Ctrl+Z` ebenso | – |
| CAP · Innerer Wert · Vol einfrieren | CoH-Karte, „✓ effektiv“ | – |

### (e) „+ Währung“ CZK/HUF → „+ Kalender“ → „+ Kurve“ → „+ Paar“ → „+ Fläche“ → Swap/Swaption/FX-Option → Export → frischer Browser → API-Snapshot
| Schritt | Beobachtung | Reibung |
|---|---|---|
| `irs czk …` vorher | „⚠ Unbekannte Währung „CZK“ – Währungen: …“ | – |
| `g c` · „+ Währung“ `↵` | Formular (CZK vorbelegt, TARGET, 1Y/ACT/360, Spot-Lag 2); Fokus bleibt auf dem Button, `Tab` führt weg | **R9-02** |
| OIS CZEONIA · IBOR PRIBOR-6M · „+ Kalender“ · CZ „Prag“ · 6 Feiertage | Vorschau „2 Indizes … · Kalender CZ (neu, 6 Feiertage, ab 05.07.2027)“; „abc“ → „Feiertag „abc“ nicht lesbar – Datum als 06.07.2027 oder 2027-07-06“; Kartentitel mit Backticks | **R9-05**; bei 1024 px Überlauf **R9-01** |
| „Währung registrieren“ `↵` | Toast „Registriert: … – jetzt mit „+ Kurve“ eine CZK-Kurve anlegen“, **Fokus „+ Kurve“**, 6,5 s | – |
| `↵` auf „+ Kurve“ | CZK/CZEONIA vorbelegt (HUF-Variante: **DKK** vorbelegt), Index-Optionen mit Kalender „CZ“, Kurve aus 6 Quotes, Fokus auf dem Tab | **R9-F2** (HUF) |
| `irs czk 5y pay 4% 100m` · `↵` | „· CZEONIA (Kurve vorhanden; PRIBOR-6M ohne Kurve)“ → „FX-Spot fehlt … „+ Paar““ | – |
| `g m` · „+ Paar“ `↵` · `Tab` `Tab` · 24,6 · `↵` | Vorschlag EURCZK; Spot angelegt (24,6), Fokus „+ Paar“, Badge „angelegt“ + ✕; Swap PV −150.306 | – (R8-04 ✓) |
| `Shift+P` · `y i` · `g s` · `o r` | Par 3,2000 %, Indikation mit CZK, 16 Szenarien, Report mit CZK-CZEONIA und Kalender CZ | – |
| „+ Fläche“ `↵` ×3 | Cube CZK (Vorschlag), Caplet CZK-CZEONIA, FX EUR/CZK; Badges, ein Tabstopp je Gitter | – |
| `swpt czk 1y5y payer 4% 50m` · `fxo eurczk call 25 3m 5m` · `cap czk …` · `fxf eurczk …` | Swaption „Underlying CZEONIA“ PV 3.445 (Editor-Feld „Underlying-Index“), Option 45.153, Cap 2.411, Forward −24.993 – **kein Level 3** | – (R8-F1 ✓) |
| `irs huf … bubor6m` (HUF-Variante) | „⚠ Unbekanntes Token „bubor6m““ – DKK `cibor6m` funktioniert | **R9-F1** |
| Reload · Snapshot exportieren · Portfolio als JSON | 0 Fehler; Datei mit Envelope (4 Einträge), CZK-Kurve, Spot, Cube, Smile, Caplet | – |
| **frischer Browser** · Import Snapshot · Import JSON | Toast „registriert: 2 Indizes CZEONIA, PRIBOR-6M · Konventionen CZK · Kalender CZ“, ID identisch, 18 Trades, 0 Fehler, 0 Level 3, Swaption PV identisch, Reload ok, Tab „CZEONIA (aus Snapshot)“; „+ Währung“ → „CZK ist bereits registriert (z. B. aus einem Snapshot-Envelope) – „+ Kurve“ genügt“ | – |
| **API-Snapshot** (`buildApp`: calendars → indices → conventions → curves → fxSpots → snapshot) | Import mit Registrierungs-Toast, `irs czk` PV −59.175, Reload ok, Export re-emittiert Envelope | – |
| Prüfer-Snapshot (ohne CZK/DKK) importieren · „+ Währung“ | Fehler nennen Snapshot + „Zum Sample-Markt“ (R8-06 ✓); „+ Währung“-Toast nennt gesperrtes „+ Kurve“, Fokus `body` | **R9-03** |
| Snapshot mit HUF-Kurve ohne Envelope · `irs huf …` | Editor „HUF (nicht registriert)“ + „+ Währung“-Hinweis; Schnelleingabe nur „Unbekannte Währung“ | **R9-F3** |
| „Zum Sample-Markt“ · Reload · `Ctrl+Z`-Ketten | Kurven, „+ Paar“-Spots, „+ Fläche“-Flächen bleiben (R8-F2 ✓); Registrierung entfernen wird bei bestehender Kurve verweigert, nach Kurvenentfernung möglich, `Ctrl+Z` ×2 stellt Bewertung wieder her | – |

### (f) Offline / Lazy Views / Persistenz / Chunk-Ausfall
| Schritt | Beobachtung | Reibung |
|---|---|---|
| frischer Browser, **ein** Aufruf, offline, Reload | Chip „⚠ offline – lokaler Bestand“, alle 8 Views mit Charts, `n a`-Editor, „+ Kurve“/„+ Währung“/„+ Kalender“/„+ Fläche“/„+ Paar“-Formulare, Registrierung offline, Termsheet, KID; 21 Cache-Einträge | – |
| Kaltstart · `g s` sofort | 436 ms, Skeleton kurz | – |
| gedrosselt · `g s` | 973 ms mit Skeleton (`aria-busy`, `aria-live`), danach Kurven 105 / Markt 123 / Hedge 77 / Report 27 ms | – |
| warm · alle Chords · `Alt+2` | 23–163 ms, 0 Skeletons, 79 ms | – |
| Reload mit lokalem Bestand · `Tab` · `↵` | Restore-Toast; **„Zurücksetzen“ = erster Tabstopp, ohne Rückfrage, ohne Undo** – Bestand ersetzt | **R9-F4** |
| Feldänderung · Theme · Stichtag · Reload | alles wiederhergestellt, Toast „Bestand aus lokalem Speicher geladen (13 Trades)“ | – |
| ECharts-Chunk 404 · Retry / Wiederbetreten | Karte „Diagramm nicht verfügbar“, Retry mit `?retry=` (E2E R7-05) | – |

---

## 5. Hotkey-Matrix (verifiziert)

| Aktion | Tasten | Ergebnis |
|---|---|---|
| Termsheet / Erklärung / KID / Confirmation / Portfolio-Report / Report | `o t` / `o g` / `o k` / `o c` / `o p` / `o r` | ✅ Termsheet 397 ms; Fokus nach `Esc` auf `main#main` |
| Indikation / Zeile kopieren | `y i` / `y y` | ✅ auch für CZK-/HUF-Trades |
| Neue Trades | `n s/c/w/f/o/b/a/i/x/z/r` | ✅ 11 Editoren; Fokus auf „Bezeichnung“; `d` → „Bezeichnung“ (Pricing) / neue Zeile (Blotter) (R8-05 ✓) |
| What-if ±10 bp / Reset | `]` `[` `\` · `+` `-` `0` | ✅ 86–208 ms, auch im Import-Modus |
| Par-Satz / fairer Preis | `Shift+P` | ✅ Collar, CZK-Swap (3,2000 %), DKK-Swaption (R8-F1 ✓) |
| Kundenmodus / Stichtag / Theme / Hilfe | `Shift+K` / `Shift+T` / `t` / `?` | ✅ Hilfe nennt „+ Währung / + Kalender“, „+ Paar“, „+ Fläche“, Zeilen-/Gittermodell |
| Duplizieren / Löschen / Vergleich / Neu bewerten | `d` / `Shift+D` / `Space` / `r` | ✅ Toasts mit Rückgängig |
| Rückgängig | `Ctrl+Z` | ✅ Trades, Quotes, Vols, Fixings (Wert/Datum/Zeile), Spot-Overrides, „+ Paar“, „+ Fläche“, **„+ Währung“ / Registrierung entfernen**, Import/Verwerfen/Verlassen, Kurve anlegen/entfernen, Hedge-Doku inkl. Ergebnis; **nicht:** Restore-Toast „Zurücksetzen“ (R9-F4), nach Reload leer |
| Tabellen (Zeilen) | `↑/↓` `Home/End` `PgUp/PgDn` `↵`/`F2` · im Feld `Tab`/`⇧Tab` `↵` `Esc` · `Tab` | ✅ ein Zeilen-Tabstopp: Blotter, Cashflows, Amortisationsplan, Fixings/FX-Fixings/CDS; **alle Controls der Zeile erreichbar, `↵` kehrt zur Zeile zurück** (R8-01/03 ✓); **Rest:** ✕ per Tastatur → `body` (R9-04) |
| Vol-Gitter | `←/→/↑/↓` `Home/End` `Ctrl+Home/End` `PgUp/PgDn` `↵`/`F2` `↵` `Esc` `Tab` | ✅ ein Tabstopp je Gitter und je FX-Paar (R8-02 ✓), `↵` kehrt zur Zelle zurück (R8-03 ✓), `Esc` verwirft |
| „+ Kurve“ / „+ Währung“ / „+ Kalender“ / „+ Paar“ / „+ Fläche“ | `↵` auf Button · Felder · `↵` auf „… anlegen/registrieren“ | ✅ Fokus danach auf Tab/„+ Kurve“/Button; `↵` im Kurs-Feld legt an (R8-04 ✓); **Rest:** „+ Kurve“/„+ Währung“ ohne Fokusübergabe ans Formular (R9-02) |
| Toast-Aktion | `Tab` ab Skip-Link · `↵` | ✅ erster Stopp „Rückgängig (Ctrl+Z)“; ⚠ nach Reload ist es „Zurücksetzen“ des Restore-Toasts (R9-F4) |
| Palette | `Ctrl+K` · `↑/↓` · `↵` · `Esc` | ✅ Token-Fehler, Index-Hinweise, „Underlying …“, `stichtag …`; **Rest:** Token registrierter Indizes (R9-F1) |
| Ansichten | `g …` · `Alt+1…8` · Rail | ✅ Prefetch, 23–163 ms warm |

---

## 6. Barrierefreiheits-Sweep (128 Zustände: alle Views inkl. „+ Währung“/„+ Kalender“, Snapshot-Kurven-Tab, Markt mit Extras, DKK-Swaption-Editor, Par-Risiko unter Import; 11 Editoren, 4 Dokumente + Kundenmodus, Dialoge/Menüs/Popover, 1440 + 1024, Dark + Light)

| Prüfung | Ergebnis |
|---|---|
| Unbenannte Inputs/Checkboxen/Buttons/Links/Bilder/Dialoge/Composite-Rollen, doppelte IDs, positive Tabindizes, leere `kbd` | **0** in allen 128 Zuständen (`summary.a11yBad []`), inkl. „+ Währung“ („Währung (ISO-Code)“, „Kalender der Währung“, „Name des OIS-Index“, „Fixing-Lag des OIS-Index“, „Kalender-ID“, „Feiertage des Kalenders“, Checkbox im `label`, Fehler `role=alert`), Snapshot-Tab (Badge mit `title`), FX-Spot-Zeile („Spot EUR/SEK entfernen“), Swaption-Editor („Underlying-Index“) |
| Tabellen ohne Namen | **0** |
| Überschriften | `h1` „DERIVA“ einmal, `h2` = View-Titel, `h3` Karten (auch „+ Währung registrieren“); **0 Sprünge** |
| Landmarks / Live-Region | `nav[aria-label=Hauptnavigation]`, `main#main` (`tabIndex=-1`), Skip-Link erster Tabstopp, Toast-Stack `role=status` direkt dahinter; Skeleton `aria-busy` + `aria-live=polite` |
| Rollen / Roving | Blotter `grid`; Amortisationsplan, Fixings, FX-Fixings, CDS `row`-Zeilen mit einem Tabstopp und Tab-Zyklus im Feld; Vol-Gitter `grid`/`row`/`gridcell` (Klemmung bei Flächenwechsel); Heatmaps `grid`; Kurven-Tabs `group` mit `aria-pressed` |
| Kontrast | **7.406 Textpaare gemessen, alle Textfarben ≥ 4,5:1** (Dark + Light, 1440 + 1024); < 4,5 nur Rail-Icon-Glyphen 18 px im aktiven Light-Tab (4,13:1, Nicht-Text ≥ 3:1, Label ≥ 4,5:1) und die Logo-Glyphe auf Gradient (Mess-Artefakt) – unverändert seit R7 |
| Texte | keine Engine-Texte, keine ISO-Daten außer Format-Beispielen (Hilfe, Feiertags-Fehler „… oder 2027-07-06“), Domänenbegriffe „Live“, „Cash“, „Float“, „Reset“ (MtM-Reset), „Modified Following“, Grammatik-Beispiele „3.1%“/„3.5“; **1 Treffer literale Backticks** („+ Währung“-Karte, R9-05) |
| Layout | keine horizontale Scrollbarkeit **außer** „+ Währung“ bei 1024 px (45 px, R9-01); keine beschnittene Karte sonst; Vol-Gitter und „+ Fläche“/„+ Paar“ bei 1024 px in der Karte |
| Tabstopps (Seite gesamt, 1440 dark) | Blotter 40 (Zeile nach **34** Tabs ab Skip-Link), Pricing 66–79 je Editor, Kurven 56 (+ „+ Währung“-Formular 74), Markt 64 (Fixings-Zeile nach 30 Tabs; mit DKK-Extras 78), Szenarien 39, Report 44, Vergleich 23, Hedge 40 |
| Bewegung | Skeleton-Puls unter `prefers-reduced-motion: reduce` aus |

---

## 7. Was für 100 noch fehlt

1. **R9-F4** Restore-Toast „Zurücksetzen“: Rückfrage und/oder Undo-Eintrag für `resetPortfolio` – ~30 min.
2. **R9-F1** `INDEX_RE` durch das Register ersetzen, damit `bubor6m`/`hufonia` erkannt werden – ~20 min.
3. **R9-01 / R9-02** „+ Währung“-Formular bei 1024 px umbrechen; „+ Kurve“/„+ Währung“ fokussieren nach `↵` das erste Feld (oder Formulare unter der Toolbar) – ~30 min.
4. **R9-F2 / R9-F3 / R9-03** „+ Kurve“ mit der eben registrierten Währung vorbelegen; „Unbekannte Währung“ mit „+ Währung“-Hinweis bei Snapshot-Kurve ohne Register; „+ Währung“-Toast/Fokus unter Import – ~30 min.
5. **R9-04 / R9-05** Fokus auf Nachbarzeile nach ✕; Backticks aus Kartentitel/Tooltip – ~15 min.

Erwartete Wirkung bei Umsetzung 1–5: UI/UX & Hotkeys ≈ 100, User Flows ≈ 100.

---

## 8. Artefakte

Basis: `/tmp/claude-0/-home-user-general/ba34afa7-bb32-5710-8abf-0fcec9f55ee0/scratchpad/r9-ui/`

- Skripte/Messwerte: `lib.mjs` (Helfer, Kontrast-Sweep, a11y-Audit), `verify.mjs` (39), `verify2.mjs` (12), `verify3.mjs` (6), `flows.mjs` (37), `flows2.mjs` (16), `flows3.mjs` (4), `sweep.mjs` (7 Sammel-Checks über 128 Zustände), `lazy.mjs` (6), `misc.mjs` (Restore-Toast), `api-snap.mts` (API-Snapshot mit Envelope); `results-{v,v2,v3,f,f2,f3,s,l,misc}.json`, `verify*.log`, `flows*.log`, `sweep.log`, `lazy.log`; `../r9-e2e.log` (E2E OK, 493), `../r9-vitest-web.log` (41 Dateien, 362 Tests), `../r9-preview.log`
- Downloads: `v-snap-pre.json`, `v-snap-extras.json`, `v-snap-huf.json` (Envelope HUF/HU), `v-snap-nok-huf.json`, `v2-snap-pre.json`, `f-snap-pre.json`, `f-snap-czk.json` (Envelope CZK/CZ, Kurve, Spot, Cube, Smile, Caplet), `f-snap-api-out.json`, `api-snap-czk.json` (API), `f-snapshot.json`, `f-snap-date.json`, `f-snap-bad{1,2,3}.json`, `f2-snapshot.json`, `f3-snapshot.json`, `v-tmpl-0…10.csv`, `tmpl-0…10.csv`, `v-api-{ccs,fx,irs,swptfra,basis}.csv`, `f-import*.csv`, `f-portfolio.json`, `f-portfolio-czk.json`, `f-portfolio-200.json`, `f-portfolio-report*.json`, `f-hedge-doc.md`
- PDFs (10): `report-print.pdf`, `doc-{termsheet,kid,suitability,confirmation}-print.pdf`, `hedge-print.pdf`, `curves-print.pdf`, `curves-add-currency-print.pdf`, `market-print.pdf`, `blotter-print.pdf`, `pricing-print.pdf`
- Sweep-Screenshots (128): `{1440,1024}-{dark,light}-{blotter,pricing,pricing-fxo,pricing-swpt,curves,curves-add,curves-add-currency,market,market-add-spot,market-add-vol,market-dkk,pricing-dkk,pricing-swpt-dkk,scenarios,report,compare,hedge,palette,help}.png`, `{1440,1024}-dark-editor-{irs,cap,swpt,fxf,fxo,basis,amort,imm,fxs,ccs,fra}.png`, `{1440,1024}-dark-doc-{termsheet,kid,suitability,confirmation,termsheet-customer}.png`, `{1440,1024}-dark-{export-menu,csv-errors,market-import-override,curves-import,curves-snapshot-tab,pricing-par-coverage,toast,valdate-popover,context-menu,onboarding}.png`, `print-{report,doc-*}.png`
- Befunde: `1024-dark-curves-add-currency.png` (R9-01), `v3-add-currency-1024.png` (R9-01), `v-add-currency-import.png` (R9-03), `v2-fixing-removed.png` (R9-04), `1440-dark-curves-add-currency.png` (R9-05), `v-add-currency-form.png`, `v-market-rows.png` (R8-01/03 ✓), `v-swpt-dkk-editor.png` (R8-F1 ✓), `v-import-hint.png` (R8-06 ✓), `v-blotter-extras.png` (R8-F2 ✓), `v-pricing-huf.png`, `v-fresh-envelope-import.png`, `v-curves-snapshot-tab.png` (Markt R8-5 ✓), `v-blotter-api-csv.png` (Markt R8-4 ✓), `f-add-currency-czk.png`, `f-curves-czk.png`, `f-pricing-czk.png`, `f-market-czk.png`, `f-pricing-swpt-czk.png`, `f-blotter-czk.png`, `f-report-czk.png`, `f-fresh-czk-import.png`, `f-api-snapshot-czk.png`, `f-blotter-templates.png`, `v2-after-reset.png`, `l-skeleton-scenarios.png`, `l-offline-reload.png`, `l-offline-first.png`
