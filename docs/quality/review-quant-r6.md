# Re-Review Runde 6: Pricing-Korrektheit & Methodik (Dimension 2, Gewicht 20 %) – DERIVA `@deriva/pricing-core`

**Reviewer-Rolle:** Senior Quant (Multi-Curve, ISDA-Konventionen, Black/Bachelier/SABR, Garman-Kohlhagen/Reiner-Rubinstein, XVA, Hedge Accounting IFRS 9 / HGB § 254) · **Stand:** 04.09.2026, Branch `claude/derivatives-trading-platform-1arsyu`, Commit `c031daf` (dist-Build 04.09. 04:25 UTC, kein Quellfile neuer als dist) · **Modus:** Review only, keine Quell-, Test- oder Golden-Datei geändert
**Baseline:** `docs/quality/review-quant-r5.md` (Runde 5, Score 97 / 100), Befunde N5-1…N5-5 und die R5-Liste „Was für 100 noch fehlt" (Punkte 1–6); Maßnahmenprogramm Runde 5 → 6 (`05-scorecard-runde-5.md`, CHANGELOG „Core (Review Runde 5)").
**Geprüft:** der komplette Diff `77f2366..c031daf` unter `packages/pricing-core` (31 Dateien, zeilenweise gelesen: `risk/sensitivities.ts` (`stillValuedInRolled`, `splitCashflowsWithin`, `computeTheta`, `ThetaDetail.valueTodayOnRollDate`), `pricing/fx-pricer.ts` (`fxOptionLifecycle`, `settledPayoff`, Lebenszyklus-Zweig in `priceFxOption`), `pricing/price.ts` (`checkOptionalNumber`/`checkOptionalBoolean`/`checkStepSchedule`/`checkUpfront`, `mtmReset`, Barrier-Enum, Digital, FX-Swap-Datumsordnung), `xva/cva.ts` (`CDS_PREMIUM_ACCRUAL_PER_YEAR`, Midpoint-Bootstrap, `validateCreditInputs`/`assertValidCreditInputs`), `market/vol-validation.ts` (neu, 246 Zeilen), `market/snapshot.ts`, `models/vol-surfaces.ts`/`fx-vol-surface.ts` (Pricing-Time-Guards), `pricing/swaption-pricer.ts` (Wortlaut), `reporting/valuation-report.ts`/`portfolio-report.ts` (Methodiktexte), `market/sample-market.ts` (vier Kreuz-Flächen), `errors.ts` (fünf neue Codes), `curves/bootstrap.ts`, `hedge/hedge.ts`, `math/rootfind.ts`, `models/black.ts`/`sabr.ts`, `instruments/builders.ts`, `dates/*` (nur `PricingError`-Umstellung), `index.ts` (kuratiert, 325 Exporte), `pricing/review-r5.test.ts` (613 Zeilen, 31 Tests), `testing/golden.test.ts`/`market-r2.test.ts`, `test-data/golden/cds-hazard-bootstrap.json`, `tools/quantlib-golden.py`, `tools/gen-index.mjs`, READMEs), dazu erneut `pricing/fra-pricer.ts`, `pricing/leg-pricer.ts`, `pricing/swap-pricer.ts`, `pricing/capfloor-pricer.ts`, `reporting/emir.ts`, Research 3.x.
**Methode:** `npx vitest run` → **352 / 352 grün** (24 Dateien, 7,7 s). Drei neue Probe-Skripte gegen `dist/index.js` (`probe-r6a.mjs`: N5-1 über alle Instrumenttypen mit Cashflow am Roll-Datum, N5-2 in allen fünf Lebenszyklus-Zuständen inkl. Put/Short/Barrier/Digital/Drittwährung, N5-3/N5-4-Codes, N5-5 gegen QuantLib, Vol-Flächen-Validierung mit legitimen Grenzformen, Snapshot-Determinismus; `probe-r6b.mjs`: Kreuz-Flächen, IFRS-13-/Methodiktexte je Zustand, Portfolio mit verfallenen Optionen, `hazardFromSpread` vs. Bootstrap; `probe-r6c.mjs`: Upfront-Prämien und Theta, FX-Swap Near heute, Expiry = Lieferung = heute). Die Runde-5-Skripte `probe-r5a.mjs`/`probe-r5d.mjs` wurden erneut ausgeführt und gegen ihre R5-Ausgabe gediff't (nur die erwarteten Änderungen), `probe-r5b-dump.mjs` + `ql-compare-r5b.py` gegen **QuantLib 1.43** (Tageszählungen, Schedules, SABR, 72 Barriers, Digitals, GK-Greeks, OLS unverändert ≤ 5·10⁻¹⁵; CDS-Block neu), `ql-cds-r5.py` für die CDS-Referenz.

---

## 1. Score

### **Pricing-Korrektheit & Methodik: 98 / 100** (rechnerisch 97,9; Runde 5: 97, Runde 4: 97, Runde 3: 95, Runde 2: 90, Runde 1: 59)

**In einem Satz:** Alle fünf Befunde der fünften Runde sind nachweislich und mit Regressionstests behoben – Theta zählt jeden Cashflow genau einmal (Forward am Vortag der Lieferung −484,86 statt +122.030; FRA, Cap, CCS-Nominalaustausch, Swaption und FX-Option am Roll-Datum ebenfalls korrekt), FX-Optionen haben einen vollständigen Lebenszyklus (fünf Zustände, Ausübung am Verfall-Fixing, gelieferte Optionen 0 mit EMIR-Delta 0), `resettingLegIndex`, Digital-Payout, Barrier-Enum, Float-Typen, FX-Swap-Datumsordnung und `CreditInputs` werden validiert, der CDS-Bootstrap accruet ACT/360 mit Midpoint-Protection und liegt in der Überlebenswahrscheinlichkeit ≤ 6,2·10⁻⁵ neben QuantLib –; die Vendor-Cross-Checks bleiben bitgenau; **neu kommt ein Befund derselben Klasse wie N5-1 hinzu: die Upfront-Prämie ist kein Cashflow der `legs`, so dass das 1-Tages-Theta am Tag vor der Prämienzahlung ≈ +Prämie beträgt (Swaption +99.817 auf PV 77.737, Cap +49.856, FX-Option +199.078, IRS-Fee +99.683)** – dazu vier kleine Punkte (Hazard-Shortcut `s/(1 − R)` ohne 365/360 gegen den neuen Bootstrap, IFRS-13-Hinweistext „MtM-Reset" bei FX-Optionen, `valueTodayOnRollDate` für Swaption-Platzhalter, Barrier-Knock-Zustand nach Verfall nur aus dem Fixing).

**Abzugsherleitung** (Rubrik: kritisch −10…−25, fehlendes Kernfeature −3…−8, Reibung −1…−3, kosmetisch −0,2…−1):

| Klasse | Abzug | Befunde |
|---|---:|---|
| Kritisch | 0 | – |
| Hoch | 0 | – |
| Mittel (neu) | −1,2 | N6-1 Upfront-Prämie fehlt in der Cashflow-Liste → 1-Tages-Theta am Vortag der Prämienzahlung ≈ +Prämie (Swaption, Cap/Floor, FX-Option, IRS-Fee), fließt in `RiskReport`, Report-Theta-Zeile und Portfolio-Summe |
| Niedrig (neu) | −0,7 | N6-2 `hazardFromSpread` = s/(1 − R) ohne den ACT/360-Faktor des Bootstraps → flacher Pfad (Web-Report ohne CDS-Kurve) 1,4 % niedrigere Hazards, CVA −1,0 % gegenüber derselben Quote als Kurve (−0,3) · N6-3 IFRS-13-Hinweis für `MISSING_FX_FIXING` spricht bei verfallenen FX-Optionen von „MtM-Reset" (−0,2) · N6-5 Barrier-Knock-Zustand verfallener Optionen nur aus dem Verfall-Fixing, kein Hit-Flag; lebende Option jenseits der Barrier ohne Hinweis (−0,2) |
| Kosmetisch (neu) | −0,2 | N6-4 `valueTodayOnRollDate` weist für Swaption-/verfallene Optionsplatzhalter den vollen Optionswert als „Value-Today-Austausch" aus |
| Offen aus Runde 5 | 0 | alle N5-Befunde behoben |
| **Summe** | **−2,1** | → **97,9 ≈ 98 / 100** |

Zur Einordnung: N6-1 ist kein Regress aus Runde 5, sondern die verbliebene Lücke derselben Regel („jeder Cashflow genau einmal"): die Pricer ziehen den Upfront direkt vom PV ab (`swaption-pricer.ts:98–102`, `capfloor-pricer.ts:213–216`, `swap-pricer.ts:77–79`, `fx-pricer.ts:439–443`), ohne ihn als `Cashflow` auszuweisen, so dass `splitCashflowsWithin` ihn am Roll-Datum nicht kennt. Ein reines Modell-/Formel-Review läge bei ≈ 99,7 (nur N6-2).

---

## 2. Status der Runde-5-Befunde

Legende: **behoben** = reproduziert und mit Zahl belegt · **teilweise** = Kern behoben, Restpunkt benannt · **offen** = unverändert.

| # | Befund R5 | Status | Nachweis (Probe gegen `dist`, Anhang A) |
|---|---|---|---|
| **N5-1** | Theta-Doppelzählung am Tag vor der FX-Lieferung (Forward +122.029,94 auf PV 122.999,66) | **behoben** (Rest → N6-1, N6-4) | `risk/sensitivities.ts:162–171` (`stillValuedInRolled`: gleicher `legIndex`/`paymentDate`/`kind`/`currency` mit DF > 0 im gerollten Ergebnis), `:195–232` (`splitCashflowsWithin`: Cashflow zählt als Cash **genau dann**, wenn er in PV(t) bewertet ist (DF > 0, fällig ≤ t + d) und im gerollten PV nicht mehr vorkommt; sonst `valueToday`), `:234–243` (`computeTheta` mit `rolledResult`, `ThetaDetail.valueTodayOnRollDate`). Probe: **FX-Forward Lieferung morgen zu 1,15: Theta −484,86 = PV(t+1) − PV(t)** (R5: +122.029,94), Cash 0, `valueTodayOnRollDate` 122.514,80; Lieferung heute: Theta 0,00 (Cash 122.514,80 = PV); gestern geliefert: 0; **FX-Swap** Near morgen −29,32 (R5: +122.485,48), Near geliefert/Far morgen +495,90 = PV(t+1) − PV(t) exakt; Near heute/Far 3M +455,54 (Cash = Near-PV). Kontrollen über alle Instrumenttypen mit Cashflow am Roll-Datum: **FRA** Settlement morgen 0,57 = Betrag − PV (Cash 10.282,47, `dfRolled` 0 → gezählt), **Swaption** Expiry morgen −161,52 = PV(t+1) − PV(t) (physisch und Cash-IRR, Platzhalter DF 1 bleibt im gerollten PV), **Cap** Caplet morgen −122,42 (Cash 61.640,14, Roll-Pricer lässt ≤ val fallen), **CCS** Initial Exchange morgen −59,82 (Cash −19.351,20 = beide Nominale zum Value-Today-Kurs der CSA-Währung), **MtM-CCS** −450,25, **lebende FX-Option** Expiry morgen −7.970,62 (gerollt → `expires-today`, intrinsisch), verfallene Option Lieferung morgen −435,57, Lieferung heute 0,00; **Portfolio** (Forward, Swap, FRA, Swaption, Option) Summe −7.461,26 ohne Phantomsprung; 7-Tage-Theta Forward Lieferung in 3 d −1.454,35 mit Cash 122.514,80. Methodiktext (`valuation-report.ts:737`, `portfolio-report.ts:247`) nennt die Einfachzählung. Tests `review-r5.test.ts:57–132`. |
| **N5-2** | Verfallene/gelieferte FX-Optionen behalten Payoff, Delta und EMIR-Wert ohne Warnung | **behoben** (Rest → N6-3, N6-5) | `pricing/fx-pricer.ts:218–247` (`EXPIRED_PREFIX`, `EXPIRES_TODAY_PREFIX`, `fxOptionLifecycle` mit fünf Zuständen), `:261–279` (`settledPayoff`: ausgeübte Vanilla = Terminposition sign·(F_del − K)·DF_q mit Delta sign·DF_f, Digital Cash/Asset, Barrier Knock am Fixing, Rebate·DF_q), `:371–421` (Zweig `!alive`: `greeksMethod: "settled-payoff"`, Vega/Gamma/Theta/Rho 0, Fixing aus `ctx.fxFixings` in beiden Quotierungen, Spot-Proxy mit `MISSING_FX_FIXING:`), `:320` (`dfQ = 0` geliefert), `:468` (`amount = dfQ > 0 ? … : 0`), `analytics.lifecycle`. Probe (Call K 1,10, 10 Mio., Spot 1,1625): **geliefert (3 d / 1 Y): PV 0,00, DF 0, Δ 0, FX-Delta 0, DV01 0, Theta 0, Vega 0, EMIR 0 / δ 0, Warnung „already settled"** (R5: 625.000 / δ 1 / keine Warnung); verfallen–Lieferung morgen: 622.950,37 = 1e7·(F_del − 1,10)·DF, Δ 116.218,66 (= DF_f · 1 %), Vega 0, `EXPIRED:` + `MISSING_FX_FIXING:`; Lieferung heute: 622.514,80 zu DF 1 mit `SETTLES_TODAY:`, Theta 0; verfällt heute: 624.691,99 (intrinsisch auf F_del, `EXPIRES_TODAY:`, kein Fixing-Hinweis); Put K 1,20 verfallen 376.951,05 mit Δ −116.218,66; Short Call −622.950,37; **Fixing 1,05 (OTM) → 0,00 ohne `MISSING_FX_FIXING`**, Fixing 1,20 → identisch zum Spot-Proxy (Wert aus dem heutigen Forward, nicht aus dem Fixing-Niveau) ✓; Barrier UpOut 1,30 = Vanilla-Termin, UpOut 1,15 Rebate 0,01 → 99.990,14 = Rebate·DF, Δ 0; DownIn 1,10 (Fixing 1,1625) → 0; Digital USD 1 Mio. → 999.901,42 (Δ 0), EUR 1 Mio. → 1.162.186,60 (Δ 11.621,87 = 1 % des Asset-Betrags), GBP → 1.349.293,81 über den GBPUSD-Forward; Expiry = Lieferung = heute → `settles-today` mit Fixing-Entscheid. Report: `Lebenszyklus:`-Zeile je Zustand (`valuation-report.ts:717–723`), `staticMethodology` ergänzt; Portfolio-Report zählt die Warnungen (4 bei 2 verfallenen + 1 gelieferter Forward). Tests `:137–276`. **Rest:** Barrier-Knock ausschließlich aus dem Verfall-Fixing (N6-5), IFRS-13-Hinweistext (N6-3). |
| **N5-3** | `mtmReset.resettingLegIndex` unvalidiert → `TypeError` | **behoben** | `pricing/price.ts:224–233` (`Number.isInteger` und `[0, legs.length)`, `mtmReset` muss Objekt sein, zwei Legs). Probe: 5, −1, 1,5, `"1"`, `null`, `true`, `mtmReset: true` → `PricingError INVALID_TRADE` „trade.mtmReset.resettingLegIndex must be an integer in [0, 2)"; 0 und 1 gültig. `probe-r5a` Diff bestätigt (R5: `TypeError … reading 'effectiveDate'`, `"1"` still koerziert). Test `:292–301`. |
| **N5-4** | Rest-Validierung (Digital-Payout, Barrier-Typ, Float-`spread`, `observationShift`, FX-Swap-Daten, `CreditInputs`, Swaption-Wortlaut) | **behoben** | `price.ts:98–133` (`checkOptionalNumber`, `checkOptionalBoolean`, `checkStepSchedule` mit streng steigenden Daten), `:149–170` (`rateSchedule`, `spread`, `gearing`, `spreadSchedule`, `observationShift`, `compounding`-Enum), `:174–184` (`checkUpfront`), `:186` (`BARRIER_TYPES`), `:298–301` (FX-Swap Far > Near), `:310–339` (Barrier-Enum/Objekt, Digital `payout > 0` und 3-Buchstaben-Währung, Barrier + Digital unzulässig), `:347` (`checkUpfront` für alle Trades); `xva/cva.ts:570–630` (`validateCreditInputs`/`assertValidCreditInputs`), `:641` (`computeXva`); `swaption-pricer.ts:103–105`. Probe: Digital −100 / NaN / `"1e6"` / Währung „US" → `INVALID_TRADE` (R5: PV −60,88 bzw. `NON_FINITE_PV`); Barrier „Sideways", `level: "1.2"`, Barrier + Digital → `INVALID_TRADE`; Float `spread: null`/`"0.001"`/NaN, `gearing: "2"`, `observationShift: "yes"`, `compounding: "Daily"`/`"None"`, unsortiertes `spreadSchedule`, ISO-String als Schedule-Datum, `capRate: "0.03"`, `lookbackDays: 2.5` → `INVALID_TRADE`; Fixed `rateSchedule` NaN, `upfront.amount` NaN, `upfront.date` als ISO-String → `INVALID_TRADE`; FX-Swap Far < Near und Far = Near → `INVALID_TRADE` (R5: PV 41.801,84), gültig −17.416,40; `computeXva` ohne `cptyRecovery` / 1,5 / Hazard −0,02 / `recovery` statt `cptyRecovery` → `INVALID_CREDIT_CURVE` (R5: NaN, −20.555,12, −29.438,87), gültig CVA 24.666,14 / DVA 11.110,15 (unverändert zu R5); Swaption Expiry = heute „expires today – intrinsic value shown", gestern „expired". Weiterhin ohne Abzug: Collar ohne `floorStrike` (−206.239,50, `warnings []`), Festsatz 500 % ohne Plausibilitätswarnung; Digital in Währung ohne Spot → `NO_FX_SPOT` (korrekt klassifiziert). Tests `:281–408`. |
| **N5-5** | CDS-Bootstrap Prämien-Accrual ACT/365F statt ACT/360 (Hazards −1,4 %) | **behoben** | `xva/cva.ts:128–134` (`CDS_PREMIUM_ACCRUAL_PER_YEAR = 365/360`), `:136–163` (Doku mit Formel und QuantLib-Referenz), `:203–216` (`period`: Prämie `Δ·365/360·[DF_end·Q + DF_mid·½ΔQ]`, Protection `DF_mid·ΔQ`). Probe gegen **QuantLib 1.43** (`PiecewiseFlatHazardRate`/`SpreadCdsHelper`, ISDA-Engine, flach 2 %, R 40 %), Struktur 100/150/200/250 bp: **Überlebenswahrscheinlichkeit Engine − QL: −4,5·10⁻⁵ (1Y), −5,9·10⁻⁵ (3Y), −6,2·10⁻⁵ (5Y), −3,8·10⁻⁵ (10Y)** (R5: +1,4·10⁻⁴ … +3,6·10⁻³); Hazards rel. +2,7·10⁻³ (1Y, QL 168,10 bp vs. Engine 168,56 bp – QLs eigene spätere Pillars der flachen 100-bp-Kurve liegen bei 168,57 bp, der 1Y-Unterschied ist QLs erste Periode), −3,0·10⁻⁴ / −4,2·10⁻⁴ / −1,1·10⁻⁴; flach 100 bp: 168,56 bp auf allen Pillars (R5: 166,67); undiskontiert 168,98 = s·365/360/(1 − R) exakt; CVA 10Y-Payer aus CDS-Kurve 21.055,65 (R5 20.853,67, +0,97 %). Golden `cds-hazard-bootstrap.json` (unabhängiger Python-Bootstrap 1e-9, QL-Block `done`, Version 1.43) mit `golden.test.ts:399–448`; `market-r2.test.ts:585–598` auf 365/360 angepasst (Toleranz unverändert 2e-3). Research 3.x nennt „Prämien-Leg ACT/360, Faktor 365/360, Accrual-on-Default und Protection zum Periodenmittelpunkt". **Rest:** der flache Shortcut `hazardFromSpread` blieb bei s/(1 − R) (N6-2). |

### 2.2 R5-Liste „Was für 100 noch fehlt" (Abschnitt 5)

| Pkt. | Thema | Status |
|---|---|---|
| 1–5 | N5-1 … N5-5 | **behoben** (s. o.) |
| 6 | Ohne Abzug: FX-Reset-`fixingLag`, Normal-/Lognormal-SABR-Hinweis in Research 3.2, Collar-`floorStrike`, `vegaUnit`, SABR-Fallback-Hinweis, `meta.label` aus der Snapshot-ID | **teilweise**: `meta.label` ist laut CHANGELOG/Web (R5-F2) nicht mehr Teil des UI-Hashes (Core-ID unverändert dokumentiert); die übrigen Punkte unverändert – weiterhin ohne Abzug |

### 2.3 Weitere Maßnahmen des Programms 5 → 6 mit Core-Bezug (verifiziert)

- **Vol-Flächen-Validierung (Markt R5-1):** `market/vol-validation.ts` prüft Dimensionen (Zeilen = Expiries, Spalten = Tenors/Strikes, FX-Vektoren = Verfälle), Achsen streng steigend (Expiries > 0, Strikes auch negativ zulässig), endliche nicht-negative Vols (RR/BF negativ zulässig), `volType`-Enum, `shift ≥ 0`, Key ↔ `currency`/`pair` (auch invertiert und `eur/usd`), SABR-Parameter (β ∈ [0, 1], ρ ∈ (−1, 1), ν ≥ 0, Key `<expiry>x<tenor>`), `rr10`/`bf10` nur gemeinsam; `deserializeMarket` → `INVALID_VOL_SURFACE` mit `details.key`/`problems`, `validateMarket` listet; Pricing-Time-Guards `assertSwaptionSurface`/`assertCapletSurface`/`assertFxSurface` (WeakSet-Cache je Objekt) werfen `INVALID_VOL_SURFACE` statt `TypeError`. Probe: Beispielmarkt sauber; legitime Grenzformen (Ein-Expiry-FX-Fläche, negative Caplet-Strikes, Vol 0, SABR β 0/ρ 0/ν 0, Key `eur/usd`, invertierter Key) akzeptiert; USDCHF-Fläche mit falscher Zeilenlänge im Snapshot → `INVALID_VOL_SURFACE key USDCHF`. Laufzeit: `computeRisk(Swaption, bucketed + vega + theta)` 10 ms, 20× `priceTrade(Swaption)` 1 ms – kein messbarer Overhead. Hinweis ohne Abzug: der Identitäts-Cache erkennt eine **In-Place-Mutation nach der ersten Validierung** nicht (Probe: `atm.length = 1` nach erfolgreichem Pricing → `TypeError`); der Core dokumentiert „immutable by convention", die Web-App erzeugt bei Vol-Änderungen neue Objekte (`MarketView.tsx` liest nur).
- **Kreuz-Flächen (Markt R5-2):** USDCHF, GBPJPY, CHFJPY, GBPCHF (`sample-market.ts:528–583`). Probe: alle zehn Paare bewerten ATM-Optionen ohne 8-%-Fallback, Level 2, Vega-Bucket `fx:<pair>` vorhanden; implizite Korrelationen der indikativen ATM-Niveaus (1Y): ρ(EURUSD, EURCHF) 0,325, ρ(GBPUSD, USDJPY) −0,271, ρ(EURCHF, EURJPY) 0,262, ρ(EURGBP, EURCHF) 0,202 – plausibel und konsistent zur Herleitung im Kommentar; Smiles monoton (Put-Seite hoch, z. B. GBPJPY 12,30 → 10,60 %). Alle Flächen einheitlich `deltaConvention: "Spot"` (auch die JPY-Kreuze, konsistent zur bestehenden USDJPY-Fläche und dort so dokumentiert).
- **Fehlerdisziplin (N5-07):** `grep "throw new Error" src` (ohne Tests) → **0 Treffer**; fünf neue Codes (`INVALID_VOL_SURFACE`, `INVALID_CURVE_SPEC`, `NUMERICAL_FAILURE`, `INVALID_HEDGE_RELATIONSHIP`, `INVALID_SNAPSHOT`) mit Doku in `errors.ts:36–70`; Probe: `fxFixings[0].rate: 0` → `INVALID_SNAPSHOT` (R5: plain `Error`).
- **Kuratierter `index.ts` (ADR-024):** kein `export *` mehr; `node tools/gen-index.mjs` regeneriert und gegen `src/index.ts` verglichen → **kein fehlender Export** (325 Exporte in `dist`).

---

## 3. Neue Befunde (Runde 6)

Severity wie R1–R5: **Mittel** = Methodik-/Konventionsabweichung mit begrenzter, aber realer Wirkung · **Niedrig** = Inkonsistenz/Doku/Validierung · **Kosmetisch** = Grenzfall ohne praktische Wirkung im v1-Scope.

| # | Sev. | Datei:Zeile | Befund | Fix |
|---|---|---|---|---|
| **N6-1** | Mittel (−1,2) | `pricing/swaption-pricer.ts:98–102`, `pricing/capfloor-pricer.ts:213–216`, `pricing/swap-pricer.ts:77–79`, `pricing/fx-pricer.ts:439–443` (Upfront wird mit `upfront.date > val` direkt vom PV abgezogen, **ohne `Cashflow`-Eintrag in `legs`**), `risk/sensitivities.ts:195–232` (`splitCashflowsWithin` kennt nur `legs[].cashflows`) | **Upfront-Prämie fehlt in der Cashflow-Liste → 1-Tages-Theta am Vortag der Prämienzahlung ≈ +Prämie.** Beim Roll auf t + 1 fällt der Upfront aus dem PV (`date > val` wird falsch), wird aber nicht als gezahlter Cashflow addiert – dieselbe Lücke wie N5-1, nur in umgekehrter Richtung. Probe (Bewertung 03.09., Prämie am 04.09.): **Swaption 2y5y + Upfront 100 k: PV 77.736,70, PV(t+1) 177.553,30, Theta +99.816,59 (korrekt −183,41** = PV(t+1) − 100.000 − PV(t); ohne Upfront −177,82); **Cap 5Y + 50 k: Theta +49.855,97** (korrekt ≈ −141); **FX-Call + 200 k USD: +199.078,39** (korrekt ≈ −902); **IRS + Fee 100 k: +99.683,48** (korrekt ≈ −311); Prämie am Spot-Datum (T+2) oder in 10 d: Theta −183,41 korrekt, d. h. der Fehler tritt genau am Tag vor der Zahlung auf – im Standardfall (Prämie value spot) am **ersten Bewertungstag nach Handelsabschluss**. `cashflows` und `valueTodayOnRollDate` sind 0; die Cashflow-Liste enthält nur den `OptionPayoff`-Platzhalter; der Upfront ist in Cashflow-Tabelle, EMIR-Export und Portfolio-Cashflows nicht sichtbar. Portfolio-Report `totals.theta` 99.816,59 für eine einzelne Swaption. Wirkung: `RiskReport.theta`, `thetaDetail.carry`, Theta-Zeile des Berichts und Portfolio-Theta-Summe am Vortag jeder Prämienzahlung um den vollen Prämienbetrag falsch, ohne Warnung. | Upfront als `Cashflow { kind: "Premium", paymentDate: upfront.date, amount: −upfront.amount, discountFactor: df }` in einer eigenen Leg oder der ersten Leg ausweisen (dann greift die Einfachzählungsregel automatisch, und Cashflow-Tabelle/EMIR/CVA-Grid sehen die Prämie) – oder in `splitCashflowsWithin` `trade.upfront` mit `date ∈ (t, t + d]` als Cash zählen. Test: Swaption/Cap/FX-Option/IRS mit Prämie morgen → Theta ≈ Theta ohne Prämie ± Diskontierungseffekt; Prämie am Spot-Datum unverändert. |
| **N6-2** | Niedrig (−0,3) | `xva/cva.ts:657–660` (`hazardFromSpread = spread / (1 − recovery)`), Nutzer `apps/web/src/views/ReportView.tsx:101, 103` (`cptyHazard`/`ownHazard` aus Spread-Eingabe; `cptyHazardCurve` nur bei geladenen CDS-Quotes) | **Flacher Spread-Shortcut ohne den ACT/360-Faktor des Bootstraps.** Seit N5-5 liefert `bootstrapHazardCurve` für eine flache Quote λ = s·(365/360)/(1 − R)·(1 + O(Δ²)); `hazardFromSpread` blieb bei s/(1 − R). Probe: 100 bp / 40 %: **166,67 bp vs. Bootstrap 168,54…168,35 bp (−1,4 %)**, 50 bp: 83,33 vs. 84,27, 300 bp: 500,00 vs. 505,62; CVA 10Y-Payer flach 20.852,13 vs. Kurve aus derselben Quote 21.053,90 (**−0,97 %**), bei 50 bp −1,01 %, 300 bp −0,83 %. Wirkung: der Web-Report ohne CDS-Kurve (nur Spread-Eingabe) und die DVA (`ownHazard` immer flach) sind ≈ 1 % niedriger als mit derselben Quote als Kurve; `market-r2.test.ts:597` musste den Faktor explizit anmultiplizieren. | `hazardFromSpread` um `CDS_PREMIUM_ACCRUAL_PER_YEAR` erweitern (λ ≈ s·365/360/(1 − R)) oder als reine Faustformel dokumentieren und im Web den Spread über einen Ein-Pillar-Bootstrap in den Hazard wandeln; Test `hazardFromSpread(s, R) ≈ bootstrapHazardCurve([{5Y, s}], R).hazards[0]` auf 1e-3. |
| **N6-3** | Niedrig (−0,2) | `reporting/valuation-report.ts:211–212` (`hints.push("MtM-Reset ohne FX-Fixing für einen vergangenen Reset-Termin – heutiger Kurs als Näherung (Fixing nachladen)")` für jedes `MISSING_FX_FIXING`) | Die R4-1-Hinweiszeile im IFRS-13-`rationale` ist auf den MtM-Reset-CCS formuliert; seit N5-2 senden auch verfallene FX-Optionen `MISSING_FX_FIXING:`. Probe (Call verfallen, Lieferung morgen, kein Fixing): `ifrs13Level(...).rationale` = „… Level 2. Hinweis: **MtM-Reset ohne FX-Fixing für einen vergangenen Reset-Termin** …" – fachlich falscher Text im Prüferreport (die Methodikzeile `Lebenszyklus:` daneben ist korrekt). | Hinweis aus der Warnung ableiten (Text nach dem Präfix) oder je `trade.type` formulieren („Ausübungsentscheid der verfallenen Option ohne Verfall-Fixing – heutiger Spot als Näherung"); Test auf den FX-Options-Wortlaut. |
| **N6-4** | Kosmetisch (−0,2) | `risk/sensitivities.ts:162–171` (`stillValuedInRolled` ohne `kind`-Filter), `pricing/swaption-pricer.ts:127–133` (Platzhalter `OptionPayoff`, `paymentDate: expiryDate`, `discountFactor: 1`), `:30–43` (`ThetaDetail.valueTodayOnRollDate`-Doku „Value-Today-Austausch") | `valueTodayOnRollDate` weist für Swaptions mit Expiry ≤ t + 1 und für verfallene FX-Optionen den **vollen Optionswert** als „Value-Today-Austausch" aus (Swaption Expiry morgen: 375.135,53; bereits verfallene Swaption: 375.135,53; verfallene FX-Option Lieferung morgen: 623.011,79 = undiskontierter Payoff). Der Theta-Wert selbst ist korrekt (Platzhalter bleibt im gerollten PV); die Kennzahl ist nur für FX-Legs sinnvoll und wird so in `ThetaDetail` beschrieben. | In `splitCashflowsWithin` `kind === "OptionPayoff"` bei Swaption vom `valueToday`-Betrag ausnehmen (wie im Legacy-Pfad) bzw. nur `kind: "Notional"`/FX-Legs zählen; Test: Swaption Expiry morgen → `valueTodayOnRollDate` 0. |
| **N6-5** | Niedrig (−0,2) | `pricing/fx-pricer.ts:271–277` (`settledPayoff`: `breached = sFix ≥/≤ level` nur aus dem Verfall-Fixing), `:392–396` (Warnungstext nennt „knocked out" ohne Hinweis auf die fehlende Pfadbeobachtung), `:422–428` (lebende Barrier: kein Hinweis, wenn der Spot bereits jenseits der Barrier steht) | Für verfallene Barrier-Optionen entscheidet allein das Fixing des Verfalltags über den Knock-Zustand – ein zwischenzeitlicher Touch ist nicht darstellbar (kein `barrier.hit`-Flag, keine Beobachtungshistorie). Probe: UpIn 1,15 mit Fixing 1,05 → 0,00, obwohl der Spot in den Wochen zuvor bei 1,1625 lag (Knock-in wäre plausibel); UpOut 1,15 mit Fixing 1,1625 → Rebate (korrekt, falls nie unter 1,15 zurück – irrelevant für Out). Code-Kommentar (`:265–267`) benennt die Näherung, die Warnung und der Report nicht („Barrier-Entscheid am FX-Fixing des Verfalltags" ohne Vorbehalt). Ebenso ohne Abzug-Relevanz, aber ohne Hinweis: lebende UpOut 1,15 bei Spot 1,1625 → PV 0,00 und `warnings []` (mathematisch korrekt ausgeknockt, Reiner-Rubinstein liefert 0; UpIn = Vanilla ✓). | Optionales `barrier.hit?: boolean` (bzw. `knockDate`) am Trade, das den Fixing-Entscheid übersteuert; Warnung „Knock-Zustand aus dem Verfall-Fixing abgeleitet – Touch-Ereignisse vor dem Verfall nicht berücksichtigt"; für lebende Optionen mit Spot jenseits der Barrier eine `BARRIER_TRIGGERED:`-Warnung. |

**Ohne Abzug, aber dokumentierenswert (bewusste Näherungen, korrekt gekennzeichnet oder ohne praktische Wirkung):**
- **CDS-Bootstrap-Grid:** Kuponraster t_j = 0,25·j in ACT/365F-Jahren (91,25 Tage) statt Kalenderquartale, DF am nächsten ganzen Tag – in der Golden-Datei benannt, Wirkung gegenüber QuantLib ≤ 6,2·10⁻⁵ in Q. Der 1Y-Hazard weicht 0,27 % von QuantLib ab, weil QLs erste Periode anders behandelt wird (QLs eigene spätere Pillars der flachen Kurve: 168,57 bp = Engine); README/Test dokumentieren das korrekt.
- **`expires-today` ohne Fixing:** intrinsischer Wert auf dem heutigen Spot ohne `MISSING_FX_FIXING`-Hinweis (bewusst; EoD-Konvention, Cut-Zeit nicht modelliert).
- **FX-Delta verfallener Optionen ohne Fixing:** der Ausübungsentscheid folgt dem gebumpten Spot (±1 %) – nahe am Strike springt das Delta zwischen 0 und DF_f; mit geladenem Fixing stabil.
- **Vol-Flächen-Cache:** Identitäts-Cache erkennt In-Place-Mutation nicht (s. 2.3).
- **Unverändert aus R4/R5 (ohne Abzug):** Collar ohne `floorStrike`, Festsatz 500 %, FX-Reset-`fixingLag`, Normal-/Lognormal-SABR-Hinweis, `vegaUnit`, SABR-Fallback-Hinweis.

---

## 4. Verifizierte Positivbefunde Runde 6 (QuantLib-Cross-Checks und Invarianten)

| Bereich | Nachweis (Probe A/B/C, Anhang A/B) |
|---|---|
| Tageszählungen, Schedules, SABR, Barriers, Digitals, GK-Greeks, OLS vs. QuantLib 1.43 | `probe-r5b-dump.mjs` + `ql-compare-r5b.py` erneut: 7 × 10 Tageszählungen ≤ 5,6·10⁻¹⁷, ICMA exakt; 9 Schedules datumsidentisch; SABR 32 Fälle ≤ 4,4·10⁻¹⁵; **72 Barrier-Fälle ≤ 3,9·10⁻¹⁶**; Digitals ≤ 1,1·10⁻¹⁵; GK-Greeks ≤ 4,9·10⁻¹⁵; OLS = numpy – **keine Regression durch den Runde-5-Diff** |
| CDS-Bootstrap vs. QuantLib | Struktur 100/150/200/250 bp: ΔQ ≤ 6,2·10⁻⁵ an allen Pillars, Δλ rel ≤ 4,2·10⁻⁴ (3Y–10Y), 1Y +2,7·10⁻³ (QL-Stub); flach: 168,56 bp = QLs spätere Pillars 168,57 bp; undiskontiert = s·365/360/(1 − R) |
| Theta-Einfachzählung | 11 Instrument-/Zustandskombinationen mit Cashflow am Roll-Datum: Theta = PV(t+1) + Cash − PV(t) mit jedem Cashflow genau einmal (FX-Forward/-Swap/-Option via Value-Today im gerollten PV; FRA/Cap/Swap-Kupon/Nominalaustausch via Cash) |
| FX-Option Lebenszyklus | 13 Zustände × (PV, DF, Δ, FX-Delta, DV01, Theta, Vega, EMIR-Wert/-Delta, IFRS-13-Level, Warnungspräfixe) konsistent; Fixing in beiden Quotierungen; Put-Call-Symmetrie (Put Δ −DF_f); Short = −Long; Barrier-Rebate·DF; Digital Cash/Asset/Drittwährung |
| Validierung | 30 Negativfälle → `INVALID_TRADE`/`INVALID_CREDIT_CURVE`/`INVALID_SNAPSHOT`/`INVALID_VOL_SURFACE`, keine plain `Error`/`TypeError` mehr aus dem Core (Probe `probe-r5a` Diff: die drei R5-`TypeError`/`Error`-Fälle jetzt `PricingError`) |
| XVA-Invarianten | Flat-Hazard CVA 24.666,14 / DVA 11.110,15 unverändert zu R5 (Validierung ohne Wertänderung); CVA-Kurve vs. flach +0,97 % (= N5-5-Effekt) |
| Snapshot-Determinismus | Vollständiger Beispielmarkt (10 FX-Flächen) → JSON → Kontext: ID identisch, 10Y-Payer-PV bit-identisch; USDCHF-ATM-Bump ändert die ID; malformierte Fläche im Snapshot → `INVALID_VOL_SURFACE` mit Key |
| Kreuz-Flächen | 10 / 10 Paare ohne Fallback, Level 2, implizite Korrelationen 0,20…0,33 bzw. −0,27 plausibel, Smiles monoton |
| Tests | 352 / 352 grün (24 Dateien); `review-r5.test.ts` deckt N5-1…N5-5, Markt R5-1/R5-2, N5-07 mit den Reviewer-Zahlen aus R5 ab; Golden `cds-hazard-bootstrap.json` gegen unabhängigen Python-Bootstrap 1e-9 |

---

## 5. Was für 100 noch fehlt

1. **N6-1** Upfront-Prämie als `Cashflow` (`kind: "Premium"`) in `legs` ausweisen, damit `splitCashflowsWithin` sie am Roll-Datum zählt (Theta am Vortag der Prämienzahlung ≈ Theta ohne Prämie) – Nebeneffekt: Prämie in Cashflow-Tabelle, EMIR-Cashflows und CVA-Grid sichtbar; Tests für Swaption/Cap/FX-Option/IRS mit Prämie morgen und am Spot-Datum.
2. **N6-2** `hazardFromSpread` mit `CDS_PREMIUM_ACCRUAL_PER_YEAR` (oder Web-Spread-Eingabe über Ein-Pillar-Bootstrap); Test gegen den Bootstrap.
3. **N6-3** IFRS-13-Hinweistext für `MISSING_FX_FIXING` je Trade-Typ (FX-Option: „Ausübungsentscheid ohne Verfall-Fixing").
4. **N6-4** `valueTodayOnRollDate` nur für FX-Legs/Nominale (Swaption-Platzhalter ausnehmen).
5. **N6-5** `barrier.hit`-Flag bzw. Knock-Datum am Trade, Warnung zur Fixing-basierten Knock-Entscheidung, `BARRIER_TRIGGERED:` für lebende Optionen jenseits der Barrier.
6. Ohne Abzug: FX-Reset-`fixingLag`, Normal-/Lognormal-SABR-Hinweis in Research 3.2, Collar-`floorStrike` Pflicht oder Warnung, `vegaUnit`, SABR-Fallback-Hinweis, Mutations-Erkennung der Vol-Flächen-Guards (z. B. Hash statt Identität).

---

## Anhang A – Probe-Ergebnisse (Auszug, Bewertungstag 03.09.2026, Sample-Markt, `dist` 04.09. 04:25 UTC)

```
tests: 24 files, 352/352 passed, 7.7 s ; exports 325 ; grep "throw new Error" src (non-test) → 0
N5-1 theta single count (t+1 = 2026-09-04):
  fx fwd delivering tomorrow: PV 122,999.66 PV(t+1) 122,514.80 theta −484.86 cash 0.00 vt 122,514.80  (expected −484.86 ; R5 +122,029.94)
  fx fwd delivering today: theta 0.00 cash 122,514.80 ; delivered yesterday: 0.00
  fx swap near tomorrow: PV 67,715.02 theta −29.32 (R5 +122,485.48) ; near delivered / far tomorrow: PV −11,010.70 theta +495.90 = PV(t+1) − PV(t) ; near today: theta 455.54 cash 122,514.80
  FRA settling tomorrow: PV 10,281.89 amount 10,282.47 PV(t+1) 0.00 theta 0.57 cash 10,282.47 (= amount − PV)
  swaption expiring tomorrow: PV 375,135.53 PV(t+1) 374,974.01 theta −161.52 cash 0 vt 375,135.53 (expected −161.52) ; cash-IRR −161.37 ✓
  cap caplet paying tomorrow: Σ 61,640.14 theta −122.42 cash 61,640.14 ; CCS initial exchange tomorrow: theta −59.82 cash −19,351.20 ; MtM CCS −450.25
  fx call expiring tomorrow (K 1.16): PV 32,958.30 PV(t+1) 24,987.68 [expires-today] theta −7,970.62 ; expired call delivering tomorrow −435.57 ; settling today 0.00
  portfolio (fwd, swap, FRA, swaption, option) theta −7,461.26 ; 7-day theta fwd delivering in 3d −1,454.35 cash 122,514.80
N5-2 lifecycle (call K 1.10, 10m, spot 1.1625):
  expires-today pv 624,691.99 Δ 116,192.71 vega 0 EMIR δ 0.9995 [EXPIRES_TODAY]
  expired/delivery tomorrow pv 622,950.37 Δ 116,218.66 dv01 −0.17 θ −435.57 vega 0 EMIR 622,950.37 δ 0.9997 [EXPIRED, MISSING_FX_FIXING]
  settles-today pv 622,514.80 df 1 θ 0 [EXPIRED, MISSING_FX_FIXING, SETTLES_TODAY]
  delivered 3d / 1Y: pv 0.00 df 0 Δ 0 fxΔ 0 dv01 0 θ 0 vega 0 EMIR 0 δ 0 ["FX option already settled …"]  (R5: 625,000 / δ 1 / [])
  put K 1.20 expired 376,951.05 Δ −116,218.66 ; short call −622,950.37 ; UpOut 1.30 = vanilla forward ; UpOut 1.15 rebate .01 → 99,990.14 Δ 0 ; DownIn 1.10 → 0
  digital USD 1m 999,901.42 Δ 0 ; EUR 1m 1,162,186.60 Δ 11,621.87 ; GBP 1m 1,349,293.81
  fixing 1.05 (OTM) → pv 0.00 no MISSING_FX_FIXING ; fixing 1.20 → 624,691.99 (value from today's forward) ; expiry = delivery = today → settles-today
  ALIVE UpOut 1.15 @ spot 1.1625 → pv 0.00 warnings [] ; UpIn → vanilla 672,493.23 (fxBarrier S>H: 0 / 0.066459 = GK)
N5-3/N5-4: resettingLegIndex 5/−1/1.5/"1"/null/true, mtmReset:true → INVALID_TRADE ; digital −100/NaN/"US" → INVALID_TRADE ; barrier Sideways/"1.2"/barrier+digital → INVALID_TRADE
  float spread null/"0.001"/NaN, gearing "2", observationShift "yes", compounding Daily/None, unsorted spreadSchedule, ISO date, capRate "0.03", lookbackDays 2.5 → INVALID_TRADE
  rateSchedule NaN / upfront NaN / upfront ISO date → INVALID_TRADE ; fx swap far<near, far=near → INVALID_TRADE (ok −17,416.40)
  computeXva {hazard only} / recovery 1.5 / hazard −0.02 / "recovery" key → INVALID_CREDIT_CURVE ; valid cva 24,666.14 dva 11,110.15 (= R5)
  swaption expiry today "expires today" ; yesterday "expired" ; collar w/o floorStrike −206,239.50 [] ; fixed 500 % OK (unchanged notes)
N5-5 CDS vs QuantLib 1.43 (flat 2 %, R 40 %):
  1Y  engine λ 168.56 Q 0.98328531 | QL 168.10 Q 0.98333051 | ΔQ −4.52e-5 Δλ +2.73e-3
  3Y  297.88 / 0.92633994 | 297.97 / 0.92639890 | −5.90e-5 / −3.00e-4 ; 5Y 482.81 / 0.84107396 | 483.01 / 0.84113639 | −6.24e-5 / −4.15e-4 ; 10Y 539.20 / 0.64212560 | 539.26 / 0.64216341 | −3.78e-5 / −1.14e-4
  flat 100 bp: 168.56 × 4 (QL 168.10 / 168.57 / 168.57 / 168.57) ; undiscounted 168.98 = s·365/360/(1−R) ; hazardFromSpread 166.67 (−1.37 %) → N6-2
  CVA 10Y payer: flat 20,852.13 vs curve 21,053.90 (+0.97 %) ; 50 bp +1.01 % ; 300 bp +0.83 %
N6-1 upfront theta (premium 2026-09-04): swaption 2y5y +100k: PV 77,736.70 PV(t+1) 177,553.30 theta +99,816.59 (correct −183.41 ; no upfront −177.82)
  premium at spot 07.09. → −183.41 ✓ ; in 10 d → −183.40 ✓ ; cap +50k → +49,855.97 (no upfront −141.24) ; fx call +200k USD → +199,078.39 (−901.89) ; IRS fee +100k → +99,683.48 (−310.94)
  cashflows listed: OptionPayoff 2028-09-04 only ; portfolio theta 99,816.59
N6-3 ifrs13Level(expired option, no fixing).rationale: "… Hinweis: MtM-Reset ohne FX-Fixing für einen vergangenen Reset-Termin – heutiger Kurs als Näherung (Fixing nachladen)."
N6-4 expired swaption theta −161.52 vt 375,135.53 ; expired fx option vt 623,011.79
vol validation: sample clean ; single-expiry FX [] ; rr10 w/o bf10 → problem ; eur/usd key [] ; USDEUR key [] ; negative caplet strikes accepted ; atm 0 [] ; sabr "1x5" [] "1Yx5Y" → problem ; shift −0.01 → problem
  in-place mutation after validation → TypeError (identity cache) ; computeRisk(swaption) 10 ms ; 20× priceTrade(swaption) 1 ms
cross pairs: USDCHF 7.723 % / GBPJPY 10.926 % / CHFJPY 9.758 % / GBPCHF 7.326 % (1Y ATM, L2, no fallback) ; ρ 0.325 / −0.271 / 0.262 / 0.202
snapshot: roundtrip id equal, pv bit-identical ; USDCHF atm bump changes id ; malformed USDCHF → INVALID_VOL_SURFACE key USDCHF
```

## Anhang B – QuantLib-Cross-Checks (QuantLib 1.43, `PYTHONPATH=…/pyql`, erneut ausgeführt)

```
A day counts: 10 pairs × 7 conventions OK, max |diff| 5.55e-17 ; ACT/ACT ICMA 4 cases diff 0
B schedules: 9/9 IDENTICAL
C SABR: 32 cases max rel 4.44e-15
D barriers vs AnalyticBarrierEngine: 72 cases max |diff| 3.89e-16
E digitals: max |diff| 1.11e-15
F GK greeks: max |diff| 4.9e-15
G CDS (ql-cds-r5.py): term 1Y Q 0.98333051 λ 168.0999 | 3Y 0.92639890 / 297.9715 | 5Y 0.84113639 / 483.0090 | 10Y 0.64216341 / 539.2619
   flat100: 1Y 168.0999 | 3Y 168.5714 | 5Y 168.5714 | 10Y 168.5708  (engine 168.56 on every pillar)
H OLS: engine = numpy
```

Probe-Skripte: `probe-r6a.mjs`, `probe-r6b.mjs`, `probe-r6c.mjs` (neu), `probe-r5a.mjs`/`probe-r5d.mjs` (Diff gegen R5-Ausgabe), `probe-r5b-dump.mjs` + `ql-compare-r5b.py`, `ql-cds-r5.py` im Review-Scratchpad; alle Zahlen des Hauptteils daraus reproduziert.
