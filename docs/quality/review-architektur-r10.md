# DERIVA – Re-Review Runde 10: API, Architektur, Code-Qualität (Dim. 5) und Dokumentation & Compliance (Dim. 6)

**Rolle:** Principal Engineer · Security-Reviewer · Audit/Compliance (IFRS 13, IDW, MiFID II, EMIR Refit, MaRisk, DORA) · **Modus:** Review only, keine Quelländerungen
**Baseline:** `docs/quality/review-architektur-r9.md` (Runde 9: Dim. 5 = 98,7, Dim. 6 = 99,7; Findings N9-01…N9-04, Reste N8-02, N4-03, N4-10)
**Stand:** Branch `claude/derivatives-trading-platform-1arsyu`, Commit `8fbf579` (2026-09-04), Version **0.3.0** in allen vier `package.json` und in `packages/pricing-core/src/version.ts`. Das neunte Maßnahmenprogramm liegt als **vier Commits** vor: `f71cb26` `fix(core)` (20 Dateien, +1 058/−127), `4c83c3b` `feat(web,product)` (38 Dateien, +2 133/−354), `e68cb34` `feat(api,ci,docs)` (24 Dateien, +1 113/−158), `8fbf579` `docs(release)` (2 Dateien). Arbeitskopie sauber (`git status` leer – auch nach allen Testläufen, dem `vite build` in ein Scratch-Verzeichnis und allen Probes). **Tags:** `git tag -l` → `v0.1.0` → `2cb1571`, `v0.2.0` → `93c138f`, **`v0.3.0` → `e68cb34`** (alle annotiert, Tagger Peter Schroeder, `v0.3.0` 14:46:54 Z – eine Sekunde vor dem `docs(release)`-Commit, also auf dem letzten Programm-Commit, wie CONTRIBUTING es vorschreibt); `git ls-remote --tags origin` → **0 Zeilen** (Exit 0): die Tags sind weiterhin nicht veröffentlicht; Ursache (Push-Recht nur auf den Arbeitsbranch, HTTP 403) und Nachholschritt an drei Stellen dokumentiert – aus dieser Umgebung nicht behebbar, ehrlich beschrieben.

**Methode (alles reproduzierbar ausgeführt, Node 22.22.2, pnpm 10.33):**
- `COMMITLINT_FROM=9d52a89 pnpm lint:commits` → **Exit 1**: `f71cb26`, `4c83c3b`, `e68cb34` „found 0 problems“, **`8fbf579` „✖ header must not be longer than 72 characters, current length is 74 [header-max-length]“** (→ N10-01). `npx eslint . --max-warnings 0` grün (57,8 s; `recommendedTypeChecked` jetzt für Core, API **und** `apps/web/src/**`, `eslint.config.js:32-77`), `pnpm -r run typecheck` grün (40 s), `pnpm format:check` grün; `node packages/pricing-core/tools/gen-index.mjs --check` → `OK: public 405 names, internal 98 names, 43 modules, 157 names imported by the apps` (R9: 402 – die drei neuen öffentlichen Namen `DEFAULT_REBATE_AT`, `rebateConvention`, `rolledMeta` = Diff von `index.ts` in `f71cb26` = ADR-024 R9); `node packages/pricing-core/tools/gen-version.mjs` lässt `src/version.ts` unverändert (kein Drift, `git status` leer).
- `npx vitest run --coverage` je Paket. **Core 435 Tests** (30 Dateien, 58 s; Lines 94,66 %, Branches 82,96 %, Functions 97,51 %, Statements 91,78 % – Schwellen 80/70/80/80). **API 151 Tests** (11 Dateien, isoliert 126 s; Lines 98,19 %, Branches 88,68 %, Functions 96,37 %, Statements 96,12 % – Schwellen 75/60/70/75). **Web 391 Tests** (44 Dateien; Lines 82,90 %, Statements 80,45 %, Branches 75,18 %, Functions 72,10 % – Schwellen 78/75/68/50). Alle Zahlen = Aufgabenstellung = CHANGELOG Z. 11-13 = Commit-Bodies. **Aber:** auf diesem Host (Load 6–12 durch parallele Reviewer-Läufe) schlugen die API- und Web-Suiten in vier von fünf Läufen an **Timeouts** fehl – API: `beforeAll` (`buildApp` + `app.ready()`) in `review-r4.test.ts`/`review-r8.test.ts` > 10 s (Vitest-Default `hookTimeout`, 30 Tests übersprungen); Web: `App.r6`/`App.r7`/`App.r8` je ein Test > 10 s (`testTimeout: 10000`). Isoliert ist die API grün, die drei Web-Dateien einzeln grün (`App.r6` 3/3 in 14 s), die volle Web-Suite erst mit `--testTimeout=30000` (CLI, keine Quelländerung) → 391/391 (→ N10-03). `apps/web/e2e/smoke.mjs`: 514 `check(`-Aufrufstellen (529 Laufzeit-Checks laut Commit; nicht gestartet – Ports der anderen Reviewer).
- `npx vite build --outDir <scratch> --emptyOutDir` + `node scripts/size-limit.mjs <scratch>` (vite 8.2.2/Rolldown): `index` **38,6 kB** gzip, `store` 28,1, `core` 84,4, `react` 58,8, `Popover` 15,0, `register` 1,0, CSS 7,3, `echarts` 179,0 lazy, **Initial-Load 233,5 kB** (8 Dateien, Budget 260, = CHANGELOG Z. 13), 19 precachete Assets, „Size budget OK“.
- `pnpm audit --prod --audit-level=high`: diesmal erreichbar → **„1 vulnerabilities found, Severity: 1 moderate“, Exit 0** – das Gate `high` hält; die eine Moderate ist die dokumentierte, befristete `echarts@5.6.0`-Ausnahme (SECURITY „Akzeptierte Advisories“, Ablauf 2026-12-31).
- API-Probing mit `buildApp()` + `app.ready()` + `app.inject` (`scratchpad/probe-r10.mts`, 12 Instanzen): OpenAPI-Dokument (Operationen, `operationId`s, Komponenten, 415 ⇔ `requestBody`, Code-Katalog, Präfixe, Status-Menge), Envelope `quotes` (Export aus Laufzeitkurve → Import in frische Instanz → Par-Risiko → ETag/304 → Re-Export), Import-Roll (Meta/Label/EMIR-Feld 23, vorwärts/rückwärts/zurück auf den Import-Tag), Diskontkurven-Regel (DKK-Szenario), Collateral-Währungsprüfung (Core `validateMarket`, `PUT /api/market`, Snapshot-Import), alle elf CSV-Vorlagen mit Spalte `type` (Round-Trip und Widerspruch), zusammengesetzte Kalender-Ids im Envelope (nur anstehende Komponenten, unbekannte Komponente), Core-`rollMarket`/`emirValuationTimestamp` direkt gegen `dist`.
- Diff-Abgleich der vier Commits (`git show --stat`, Datei-Diffs) gegen Commit-Botschaften und CHANGELOG; Doku-Abgleich README, CHANGELOG, CONTRIBUTING, SECURITY, Architektur §4, ADR-023/-024/-027 (R9-Ergänzungen), Epics/Release-Plan, UI-Konzept, Regulatorik-Mapping, Research §2.3.

---

## 1. Scores

| Dimension | R1 | R2 | R3 | R4 | R5 | R6 | R7 | R8 | R9 | **R10** | Δ R9→R10 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 5 · API, Architektur, Code-Qualität (15 %) | 58 | 72 | 90 | 94 | 96 | 98 | 98 | 97,8 | 98,7 | **99,2** | +0,5 |
| 6 · Dokumentation & Compliance (10 %) | 62 | 76 | 94 | 98 | 99 | 99 | 99 | 99,5 | 99,7 | **99,7** | ±0 |

### 1.1 Dimension 5 – Begründung (99,2/100)

**Was jetzt trägt (verifiziert):**
- **Envelope-Import über die Prozessgrenze (N9-01 geschlossen):** `substitutePendingCalendar` (`apps/api/src/lib/register-validation.ts:64-69`) zerlegt eine Kalender-Id an `+`, entfernt jede im Envelope anstehende Komponente und behält die bekannten (`CZ-X9+TARGET` → `TARGET`; nur anstehende Komponenten → `TARGET`); unbekannte Komponenten bleiben stehen. `envelope-fresh-process.test.ts` ist eine eigene Datei (frischer Worker mit leerem Register – im Test geprüft) **und** importiert den exportierten Envelope in einem Kindprozess `node --import tsx` (Z. 150-156): 200, `discountCurveId.CZK`, CZK-Swap auf `CZ-X9+TARGET` bewertet bitgleich zum Exporter, fünf Par-Buckets, Re-Export-ETag `"<id>-<hash16>"`. Eigene Probe: Envelope mit `CZ-P10`, `HU-P10` und Index auf `CZ-P10+HU-P10` (nur anstehende Komponenten) → **200**, `indices: ["CZEONIA-P10"]`; Index auf `CZ-P10+NOPE-P10` → **400 `INVALID_CURVE_SPEC`** „unknown calendar "NOPE-P10" (register it with POST /api/market/calendars) – nothing registered, market unchanged“, `source` danach `sample`. Endpunkt-Text (`routes/market.ts:378`), Snapshot-Beschreibung und ADR-027 R9 sagen genau das.
- **Import-Roll ohne veralteten Zeitstempel (N9-02 geschlossen):** `MarketStore.rebuild` (`store.ts:205-212`) setzt `meta: rolledMeta(rolled.meta, valuationDate)`; `rolledMeta` (`store.ts:295-302`) entfernt `snapshotTime`, sofern er nicht den neuen Stichtag datiert, und markiert das Label idempotent. Probe: Import mit `snapshotTime: 2026-09-03T17:00Z`, Label `EOD-0903` → `PUT { valuationDate: "2026-12-01" }` → 200, `warnings: []`, `meta = { source: "probe", label: "EOD-0903 (rolled to 2026-12-01)" }`, **`GET /api/emir/valuations` → `2026-12-01T17:00:00Z`** (vorher `2026-09-03T17:00:00Z`); Roll rückwärts auf 2026-08-03 → Label `(rolled to 2026-08-03)`, Feld 23 `2026-08-03T17:00:00Z`. Der Core zieht mit: `rollMarket` (`risk/sensitivities.ts:350-372`) wendet dieselbe Regel an, `emirValuationTimestamp` (`reporting/emir.ts:129-131`) ignoriert zusätzlich einen `snapshotTime` vor dem Bewertungstag. OpenAPI: „everything else as imported“ ist präzisiert (`updateMarket`, Test `review-r9.test.ts:190-192`).
- **Diskontkurven-Regel im Rebuild (N9-03 geschlossen):** `store.ts:252-258` – nach dem Übernehmen der alten Zuordnungen erhält jede nachgebootstrappte Laufzeitkurve `if (body.isDiscountCurve !== false && !discountCurveId[ccy]) discountCurveId[ccy] = curve.id`; die Warnung der verlorenen Zuordnung nennt den Ersatz (`store.ts:259-266`). Probe (Reviewer-Szenario): Import mit `DKK-DESTR` → `DKK-ALT` im Import-Modus (`discountCurveSet: false`) → `discardImport` → **`discountCurveId.DKK = DKK-ALT`**, Warnung „discountCurveId.DKK = DKK-DESTR (curve not in the rebuilt market; DKK-ALT is now the discount curve of DKK)“, EUR/DKK-Forward bewertet 200 (PV 25 636,68 DKK) statt 422 `NO_DISCOUNT_CURVE`; `isDiscountCurve: false` bleibt respektiert (Test).
- **Collateral-Währungsprüfung im Core (N8-02-Rest geschlossen):** `market/snapshot.ts:177-184` – `validateMarket` meldet fehlende Kurve und fremde Währung mit demselben Text wie die API; Probe direkt gegen `dist`: `validateMarket({ …sample, collateralDiscountCurveId: { "EUR|USD": "USD-SOFR" } })` → `["Collateral discount curve USD-SOFR for EUR|USD is denominated in USD, not EUR"]`; `PUT /api/market` → 400 `INVALID_REQUEST` `details { key, curveId, currency, curveCurrency }`; Snapshot-Import → 422 `SNAPSHOT_INVALID` mit **einem** Eintrag (der API-Duplikatfilter `routes/snapshot.ts:237-240` greift). Damit lehnt auch der Workstation-Import (`apps/web/src/lib/snapshot-import.ts` über `validateMarket`) die Zuordnung ab.
- **Typbewusstes Lint für `apps/web` (N4-03 geschlossen):** `eslint.config.js:32-35` wendet `recommendedTypeChecked` auf `apps/web/src/**` an, `restrict-template-expressions` mit `allowNullish` (Z. 63-70), nur in Web-Tests `no-unnecessary-type-assertion`/`unbound-method` geparkt (Z. 71-77, begründet); Lint grün mit `--max-warnings 0`.
- **Envelope-Block `quotes` (Markt R9-1, architektonisch sauber):** `RuntimeCurveQuotes` (`lib/curve-specs.ts:35`) ist exakt der Body von `POST /api/market/curves`; `MarketStore.rememberQuotes`/`listQuotes` (`store.ts:180-187`) legen die Specs in denselben Slot wie geladene Kurven, `parRiskSpecs()` liefert sie, `RegisterStore.hash(quotes)` (`store.ts:403-409`) hasht sie in den ETag. Probe: ohne Envelope ETag `"3803d0191a972626"` = `X-Market-Snapshot-Id`; nach `POST /curves NOK-NOWA` ETag `"1783bdae3356440e-63c74a9bc9bb8b4e"`, `quotes: [{ curveId: "NOK-NOWA", spec }]`, `If-None-Match` mit vollem ETag → 304, mit reiner Markt-ID → 200; Import in frische Instanz → 200 `quotes: ["NOK-NOWA"]`, **gleiche Snapshot-ID**, `POST /api/risk/par` → 5 Buckets, `total` 4 435, `curvesWithoutQuotes: []`; Re-Export-ETag identisch (Round-Trip). Vertrag: Komponente **`CurveBuildSpec`**, referenziert vom Body `spec` von `POST /api/market/bootstrap|curves` und von `MarketSnapshot.quotes.items.spec`; geschlossenes Schema (Zusatzfeld → 400).
- **CSV-Vorlagen mit Spalte `type` (Markt R9-4):** alle elf `csvTemplateText(type)` beginnen mit `type` + Web-Token (`IRS`, `FXF`, `CAP`, `SWPT`, `FXO`, `CCS`, `FRA`, `FXS`, `BASIS`, `AMORT`, `IMM`) und importieren 1/1 (Probe); `csvTypeMismatch` (`csv-import.ts:86-93`) lehnt eine widersprechende Zeile mit Zeilennummer, `CSV_ROW_INVALID` und Hinweis auf den richtigen `?type=` ab (Probe: `IRS` in der FxForward-Datei → „… – import this row with ?type=InterestRateSwap“); die OpenAPI-Beschreibung enthält alle elf Dateien wörtlich; Token-Liste per Test gegen `CSV_TRADE_TYPES` der Web-Datei verglichen.
- **Vertrag insgesamt:** OpenAPI 3.1.0 mit **44 Operationen / 44 eindeutigen `operationId`s** (`contract.test.ts:280-323` friert genau 44 ein), **20 Komponenten** (`CurveBuildSpec` neu), 415 ⇔ `requestBody` **0 Abweichungen**, **45 Codes** in `ErrorResponse.code.examples` = `API_ERROR_CODES` (24 Core + 21 API, Diff leer, jeder Code in der Beschreibung erklärt), **12 Warnungs-Präfixe** alle beschrieben, Status-Menge `200/201/204/304/400/404/409/412/413/415/422/428/429/500` (= ADR-023).
- **CI (`.github/workflows/ci.yml`):** unverändert vollständig – `commitlint`-Job (PR-Range/Push-Range/Null-`before`), Matrix `[22, 24]`, Drift-Check `version:gen` + `git diff --exit-code`, typbewusstes Lint, Coverage aller Pakete, Audit mit Retry, E2E auf Node 22, Artefakte ohne Source-Maps.
- **Code-Qualität unverändert sauber:** kein `new Error(` im Core außerhalb von Tests, keine `any`-Annotationen und kein `console.*` in Core-/API-Quellen (Grep), `apps/*` importiert nichts aus `@deriva/pricing-core/internal`, Größenbudget mit Luft (38,6/90, 233,5/260).

**Abzüge (−0,8):**
- **Commit-Hygiene: `docs(release)`-Betreff 74 Zeichen, `fix` für ein Feature-Programm** (−0,3, **N10-01, Niedrig**): `8fbf579` „docs(release): Tag v0.3.0 lokal gesetzt, Veröffentlichung durch Maintainer“ verletzt `header-max-length` 72 (`commitlint.config.mjs`) – `COMMITLINT_FROM=9d52a89 pnpm lint:commits` endet mit **Exit 1**; der CI-Job `commitlint` prüft bei einem Push die Range `before…sha` (`ci.yml:43-51`) und wäre für den Push dieses Commits **rot** – das Gate, das Runde 8 eingeführt hat, ist auf der eigenen Programm-Range gebrochen. Dazu: `f71cb26` ist als `fix(core)` typisiert, liefert aber drei neue öffentliche Exporte (`DEFAULT_REBATE_AT`, `rebateConvention`, `rolledMeta`), neue Validierungsregeln und eine Golden-Datei – CONTRIBUTING Z. 34 verlangt „`feat` für neue Funktionen (auch in einem Fix-Programm), `fix` nur für Korrekturen“, und der CHANGELOG begründet die Minor-Version ausdrücklich mit diesen Core-Änderungen. Fix: Betreff auf ≤ 72 kürzen (z. B. „docs(release): Tag v0.3.0 lokal gesetzt, Push durch Maintainer“, 62) – da der Commit bereits im Branch liegt, per `git commit --amend`/Force-Push nur, wenn der Branch das erlaubt, sonst als Lehre für den nächsten Release-Commit; künftig `feat(core)` für Programme mit neuen öffentlichen Namen; optional ein lokaler `commit-msg`-Hook (husky/lefthook), damit die Regel vor dem Push greift.
- **`quotes`-Eintrag für eine Sample-Kurven-Id wird angenommen und ersetzt die Sample-Kurve beim `discardImport` still** (−0,3, **N10-02, Niedrig**): `quotesProblems` (`register-validation.ts:122-139`) prüft Kurve-im-Snapshot, Währung, `spec.id`, Index und Duplikate – nicht, ob `curveId` eine **Sample-Kurve** ist. `rememberQuotes` (`store.ts:180-184`) legt den Eintrag dann als Laufzeitkurve ab, und `rebuild` bootstrappt ihn im Sample-Modus **über** die frisch gebaute Sample-Kurve (`store.ts:234-239`). Probe: Envelope mit `quotes: [{ curveId: "EUR-ESTR", spec: <OIS-Spec, Niveau 1 %> }]` → **200 `quotes: ["EUR-ESTR"]`** → `PUT { discardImport: true }` → 200 mit den Warnungen „imported snapshot … discarded – sample market rebuilt“ und „discountCurveId.NOK …“ – **aber `EUR-ESTR` ist jetzt die 1-%-Kurve** (DF[5] 0,9175 statt Sample 0,9843), also Diskont- und Projektionskurve jedes EUR-Geschäfts, ohne `MARKET_STATE_DROPPED:`-Eintrag; der Export trägt weiter `quotes: ["EUR-ESTR"]`. Die Zusage „nichts geht mehr still verloren“ (SECURITY Z. 15, ADR-027 R8) und die ADR-027-R9-Grenze „Quotes von Sample-Kurven wandern nicht in den Block“ gelten damit nur für den Export, nicht für den Import. Weder API-Export noch Workstation (`store.ts:813` „Kurve … existiert bereits“) erzeugen einen solchen Envelope – der Fall braucht eine handgebaute Datei, deshalb Niedrig. Fix: `quotesProblems` lehnt Sample-Ids ab („quotes: curve EUR-ESTR is a sample curve – its quotes are the store's (`POST /api/market/curves` replaces them)“) **oder** `rememberQuotes` behandelt sie wie `rememberCurve` (Quote-Set des Stores ersetzen, dann mit Warnung `MARKET_STATE_DROPPED:`/Audit-Eintrag sichtbar); Test „Envelope-`quotes` mit Sample-Id → 422 bzw. dokumentierte Ersetzung“.
- **Integrationstests ohne Reserve gegen Last** (−0,2, **N10-03, Niedrig**): `apps/api/vitest.config.ts` setzt `testTimeout: 20000`, aber keinen `hookTimeout` – der Vitest-Default von 10 s gilt für `beforeAll(buildApp + ready)` (`review-r4.test.ts:23`, `review-r8.test.ts:98`, ~2 s kalt, unter Last > 10 s): auf diesem Host (Load 6–8) fielen zwei Dateien mit „Hook timed out in 10000ms“ aus, **30 Tests übersprungen**, Exit 1 – obwohl kein Test fehlgeschlagen ist. `apps/web/vite.config.ts` hält `testTimeout: 10000`; die drei App-Integrationstests `App.r6`/`App.r7`/`App.r8` brauchen unter Last 20–50 s (vier Läufe, 1–3 Ausfälle je Lauf; einzeln und mit `--testTimeout=30000` grün). Die CI führt `pnpm -r run test -- --coverage` aus (Standard-Workspace-Concurrency 4 → API- und Web-Tests parallel auf einem 2-Core-Runner) – dieselbe Lastsituation. Fix: `hookTimeout: 20000` in der API-Konfiguration; für die drei App-Round-Dateien ein eigener Timeout (`it(…, 30_000)` wie bereits in `envelope-fresh-process.test.ts:187`) oder `testTimeout: 20000` für `App.*.test.tsx`; alternativ `--workspace-concurrency=1` im CI-Testschritt. Kein Befund an der Fachlogik – alle 435/151/391 Tests bestehen isoliert.
- **Zur Kenntnis, ohne Abzug:** `rolledMeta` existiert doppelt – Core (`risk/sensitivities.ts:364-372`, öffentlich) und API (`store.ts:295-302`, „kept here so the API's contract holds against any core version“) – im Workspace ist die Core-Version gepinnt, die Duplikation ist ohne Nutzerwirkung, aber eine Drift-Quelle (die API-Variante akzeptiert `undefined`, die Core-Variante nicht); nach Roll vor und zurück auf den Import-Tag lautet das Label `EOD-0903 (rolled to 2026-09-03)` und `snapshotTime` bleibt entfernt – korrekt (die Kurven wurden zweimal gerollt), nur kosmetisch überraschend; `printRoutes` zeigt 43 Pfadzeilen (Mehrfachmethoden je Zeile) – jede der 44 Operationen hat ihre Route (Vertragstest); der Web-Editor bietet weiterhin `"default"` als UI-Wert „Standard (bei Berührung)“ (`TradeEditor.tsx:1594`) – das ist die Anzeige „nicht gesetzt“, der Core liefert `analytics.rebateAt` nur noch `hit`/`expiry` (Vertragstext geprüft); `vite.config.ts:53-55` nennt weiterhin die R7-Messung, Ist heute 82,9/80,5/75,2/72,1 – Gate-Abstände 4,9/5,5/7,2/22,1 Punkte.

### 1.2 Dimension 6 – Begründung (99,7/100)

**Was jetzt trägt (verifiziert):**
- **Drei Sätze nachgezogen (N9-04 geschlossen):** CONTRIBUTING Z. 21 „44 Operationen“ (Test `review-r9.test.ts:480-481` verbietet „43“), SECURITY Z. 17 „`"snapshotId"` bzw. `"snapshotId-registerHash"`, sobald der Export einen Envelope trägt – … oder `quotes`“ (= `snapshotExportEtag`), Regulatorik-Mapping Z. 5 und CHANGELOG Z. 24 „die Tags sind lokal gesetzt, Veröffentlichung durch Maintainer ausstehend“; CHANGELOG-Smile-Satz auf „50 Vol-Punkte absolut“ korrigiert (Quant-Hinweis).
- **CHANGELOG-Stichproben gegen die vier Diffs (fünf Aussagen, alle wahr):** (1) Z. 11 „`substitutePendingCalendar`: `CZ-X9+TARGET` → `TARGET`, `CZ-X9+US` → `US`“ → `e68cb34` `register-validation.ts:64-69` ✓ (Probe/Test); (2) Z. 11 „`rolledMeta`, idempotent“ → `store.ts:284-302` ✓ (Probe: zweiter Roll ersetzt die Marke); (3) Z. 12 „`emirValuationTimestamp` ignoriert zusätzlich einen `snapshotTime`, der vor dem Bewertungstag liegt“ → `f71cb26` `emir.ts:129-131` ✓; (4) Z. 13 „`recommendedTypeChecked` für `apps/web/src/**`; `restrict-template-expressions` mit `allowNullish`; nur in Web-Tests geparkt: `no-unnecessary-type-assertion` und `unbound-method`“ → `4c83c3b` `eslint.config.js:32-77` ✓; (5) Z. 13 „Erstladung 233,5 kB gzip, 19 precachete Assets, Größenbudget grün“ → Messung 233,5 / 19 / OK ✓. Zusätzlich: Z. 12 „`makeFxOption` setzt `rebateAt: "hit"` explizit“ → `builders.ts:259` ✓; Z. 12 „`cvaFxForward` nimmt den Prämientermin ins Monatsgitter“ → `cva.ts:414` ✓; Z. 12 „neu öffentlich `DEFAULT_REBATE_AT`, `rebateConvention`“ + Z. 12 „`rolledMeta`“ → `index.ts` +3 = `gen-index` 402 → 405 ✓; Zählwerte 435/151/391 (30/11/44 Dateien), 44/45/12/20 ✓ (Messung/Probe). Nicht prüfbar: „E2E 529 Checks“ (nicht gestartet; 514 Aufrufstellen), Node-24-Aussagen (kein Node 24 in der Sandbox).
- **Commit-Botschaften vs. Diff:** alle vier Bodies zutreffend (Stat-Abgleich: `f71cb26` ausschließlich `packages/pricing-core`; `4c83c3b` `apps/web` + `docs/product` + `eslint.config.js` – Scope `web,product` vertretbar, das Lint-File ist die Web-Regel; `e68cb34` API/Doku/Versionen – Scope `api,ci,docs`, obwohl `ci.yml` selbst unverändert blieb, „ci“ deckt hier Toolchain/Version-Drift ab); `8fbf579` beschreibt ehrlich den lokalen Tag.
- **ADR-Ergänzungen konsistent mit dem Code:** ADR-023 R9 (Z. 147: additiv `quotes`/`CurveBuildSpec`/`type`-Spalte/Vertragstexte, „weiterhin 44 Operationen, 45 Codes, 12 Präfixe“, 20 Komponenten, **fünf** Verhaltensänderungen) = Vertrag/Probe = CHANGELOG „Geändert“ Z. 18 (fünf Punkte, identisch); ADR-024 R9 (Z. 159: sechs Verhaltensänderungen der Core-Oberfläche, „Neu öffentlich: `DEFAULT_REBATE_AT`, `rebateConvention`, `rolledMeta`“ = +3) = `gen-index`; ADR-027 R9 (Z. 189: Zerlegung an `+`, Kindprozess-Test, `quotes`-Semantik, Snapshot-ID unverändert, ETag über Register **und** Quotes, Grenzen) = `register-validation.ts`/`store.ts`/`snapshot.ts` – mit der in N10-02 benannten Lücke auf der Import-Seite.
- **Produkt-Doku = ausgelieferte UI:** UI-Konzept Z. 76 (Undo inkl. „Beispielportfolio laden“) ↔ `store.ts:2348-2360` (`kind: "portfolio"`), Z. 150 „nie eine destruktive Aktion (R9-F4)“ ↔ `App.tsx:148` und Rückfrage `store.ts:2655`; Z. 143 „Register statt Liste (`isIndexToken`)“ ↔ `quick-parser.ts:194`; Z. 151 Inline-Formulare/`focusWhenPresent` ↔ `focus.ts:16`; Z. 26 „Rebate-Zahlung (bei Berührung / bei Verfall / Standard)“ ↔ `TradeEditor.tsx:1590-1595`; Epics US-7.5/US-5.8 (CSV-Typerkennung `csvTypeFromFileName`/`csvTypeFromColumns` ↔ `portfolio-io.ts:649/670`, `quotes` für „+ Kurve“ ↔ `snapshotQuoteSpecs` `store.ts:770`). Roadmap (README Z. 154-156 = Epics v1.0/v1.1/v1.2 Z. 177-179 = Compliance §8 = SECURITY „Bekannte Grenzen“) ehrlich und deckungsgleich.
- **CONTRIBUTING/SECURITY/Compliance:** Testbeschreibung Runde 9 (Z. 21 inkl. Begründung der eigenen Datei für den Prozessgrenzen-Test), Lint-Absatz Runde 9 (Z. 26 = `eslint.config.js`), `type`-Spalten-Regel (Z. 23), Release-Stand (Z. 48 „`v0.3.0` (→ `e68cb34`) lokal gesetzt“ = `git tag`); SECURITY Z. 15/17 = Probe; Regulatorik-Mapping Stand „Review-Runde 9“, §8 (10 Punkte) = Roadmap, §9 eingehalten; Research §2.3 unverändert korrekt.

**Abzüge (−0,3):**
- **Stand-Sätze zu `v0.3.0` und Versionsbezeichnung uneinheitlich** (−0,2, **N10-04, Niedrig**): `8fbf579` hat den Tag gesetzt und zwei Stellen aktualisiert, drei weitere nicht – CHANGELOG **Z. 7** „Der Tag `v0.3.0` **wird** am Ende dieser Qualitätsrunde auf den letzten Programm-Commit gesetzt“ (Z. 92 sagt drei Absätze später richtig „`v0.3.0` → `e68cb34` … ebenfalls lokal gesetzt“ – derselbe Widerspruch wie N9-04, eine Zeile tiefer); Regulatorik-Mapping **Z. 5** „`v0.3.0` folgt am Ende dieser Qualitätsrunde“; CONTRIBUTING **Z. 48** „Eine spätere Runde erhöht die Version (`0.2.1`/`0.3.0`)“ – 0.3.0 ist die aktuelle. Der N9-04-Test (`review-r9.test.ts:484-488`) prüft nur die alte Formulierung („die Tags werden am Ende dieser Qualitätsrunde gesetzt“) und sieht die neue nicht. Zusätzlich tragen SECURITY (Überschrift Z. 7 „(v0.2)“, Z. 25 „in v0.2“, Z. 33 „Stand v0.2“), Regulatorik-Mapping §8 „(Stand v0.2)“ und Epics Z. 4/176 „umgesetzt in v0.2 … v0.2 (dieses Repository)“ die alte Versionslinie, während Mapping Z. 5 „Stand: v0.3“ und alle `package.json` 0.3.0 sagen. Fix: die drei Sätze angleichen („ist gesetzt“, „`0.3.1`/`0.4.0`“), Versionslabel einheitlich „v0.3“ (oder bewusst „v0.2-Linie“ benennen), Test auf das Muster `/wird am Ende dieser Qualitätsrunde .* gesetzt/` verallgemeinern.
- **Release-Tags nicht veröffentlicht (N4-10, Rest)** (−0,1): `v0.1.0`/`v0.2.0`/`v0.3.0` annotiert und auf den dokumentierten Commits; `git ls-remote --tags origin` liefert 0 Zeilen, die Vergleichs-Links CHANGELOG Z. 94-96 zeigen ins Leere. Ursache und Nachholschritt (`git push origin v0.1.0 v0.2.0 v0.3.0`) sind ehrlich dokumentiert – aus dieser Umgebung ist mehr nicht möglich; der Rest bleibt, bis die Links auflösen.
- **Zur Kenntnis, ohne Abzug (Doku-Seite von N10-02, dort gewertet):** ADR-027 R9 „Quotes von Sample-Kurven … wandern nicht in den Block“ beschreibt den Export; dass der Import einen solchen Eintrag annimmt und beim `discardImport` die Sample-Kurve ersetzt, steht nirgends – mit dem Fix nachziehen.

---

## 2. Status der Runde-9-Findings und Reste

| # | Runde 9 | Status | Evidenz |
|---|---|---|---|
| N9-01 | Envelope-Import in frischem Prozess scheitert an zusammengesetzten Kalender-Ids | **behoben** | `substitutePendingCalendar` (`register-validation.ts:64-69`) zerlegt an `+`; `envelope-fresh-process.test.ts` (eigene Datei, leeres Register geprüft, Import im Kindprozess `node --import tsx`, PV bitgleich, Par-Buckets); Probe: nur anstehende Komponenten → 200, unbekannte Komponente → 400 ohne Registrierung; Endpunkt-Text, Snapshot-Beschreibung, ADR-027 R9, CHANGELOG „Geändert“ (1). |
| N9-02 | Gerollter Import behält `meta.snapshotTime`/Label (EMIR-Feld 23) | **behoben** | `rolledMeta` in API (`store.ts:295-302`) und Core (`sensitivities.ts:364-372`), `emirValuationTimestamp` ignoriert veraltete Zeitstempel (`emir.ts:129-131`); Probe: Feld 23 `2026-12-01T17:00:00Z` nach Roll, Label `(rolled to 2026-12-01)`, `snapshotTime` weg; Test N9-02 (2), OpenAPI `updateMarket`/EMIR-Beschreibung präzisiert. |
| N9-03 | Diskontkurven-Regel beim Rebuild nicht angewandt | **behoben** | `store.ts:252-266`; Probe DKK: `discountCurveId.DKK = DKK-ALT`, Warnung nennt den Ersatz, Forward bewertet; `isDiscountCurve: false` respektiert (Test). |
| N9-04 | Drei veraltete Sätze (43 Operationen, Snapshot-ETag, „Tags werden gesetzt“) | **behoben** – neue Stand-Sätze offen (N10-04) | CONTRIBUTING Z. 21, SECURITY Z. 17, Mapping Z. 5/CHANGELOG Z. 24 angepasst und per Test gesichert; `8fbf579` ließ CHANGELOG Z. 7 und Mapping Z. 5 („`v0.3.0` wird/folgt“) sowie CONTRIBUTING Z. 48 stehen. |
| N8-02 | `validateMarket` ohne Collateral-Währungsprüfung | **behoben** | `market/snapshot.ts:177-184`; Probe Core direkt, `PUT /api/market` 400, Import 422 mit einem (nicht doppeltem) Eintrag; Workstation-Import läuft über `validateMarket`. |
| N4-03 | Typbewusstes Lint für `apps/web` | **behoben** | `eslint.config.js:32-77`, `npx eslint . --max-warnings 0` grün (57,8 s); CONTRIBUTING Z. 26 beschreibt die zwei geparkten Test-Regeln. |
| N4-10 | Tags nicht veröffentlicht | **lokal erledigt (inkl. `v0.3.0`), Veröffentlichung offen** | `git tag -l` drei annotierte Tags auf den dokumentierten Commits; `git ls-remote --tags origin` 0 Zeilen; 403-Grund an drei Stellen (→ −0,1 in Dim. 6). |

Bilanz N9-01…N9-04: **4 von 4 behoben**; N8-02-Rest und N4-03-Rest behoben; N4-10 bleibt Maintainer-Schritt.

---

## 3. Neue Findings

Schwere: **Kritisch** (falsches Ergebnis/Compliance-Fehler/Sicherheitslücke), **Hoch**, **Mittel**, **Niedrig**.

| # | Schwere | Dim. | Ort | Problem | Konkreter Fix |
|---|---|---|---|---|---|
| N10-01 | **Niedrig** | 5 | Commit `8fbf579` (Betreff 74 Zeichen), `commitlint.config.mjs` (`header-max-length` 72), `.github/workflows/ci.yml:43-51`; Commit `f71cb26` (Typ `fix(core)`), CONTRIBUTING Z. 34 | `COMMITLINT_FROM=9d52a89 pnpm lint:commits` → Exit 1 („header must not be longer than 72 characters, current length is 74“); der CI-Job `commitlint` wäre für den Push mit `8fbf579` rot – das eigene R8-Gate ist auf der Programm-Range verletzt. `f71cb26` liefert drei neue öffentliche Exporte (`DEFAULT_REBATE_AT`, `rebateConvention`, `rolledMeta`), neue Validierungsregel und Golden als `fix` statt `feat` – gegen CONTRIBUTING („`feat` für neue Funktionen, auch in einem Fix-Programm“) und gegen die eigene Minor-Begründung im CHANGELOG. | Betreff ≤ 72 (z. B. „docs(release): Tag v0.3.0 lokal gesetzt, Push durch Maintainer“); wenn der Branch es erlaubt `--amend`, sonst als Regel für den nächsten Release-Commit; `feat(core)` für Programme mit neuen öffentlichen Namen; optional lokaler `commit-msg`-Hook (husky/lefthook), damit die Regel vor dem Push greift. |
| N10-02 | **Niedrig** | 5 | `apps/api/src/lib/register-validation.ts:122-139` (`quotesProblems`), `lib/store.ts:180-184` (`rememberQuotes`), `:232-239` (Rebuild-Schleife) | Ein `quotes`-Eintrag mit der Id einer **Sample-Kurve** wird angenommen (Probe: `EUR-ESTR` mit 1-%-Spec → 200 `quotes: ["EUR-ESTR"]`); `discardImport` meldet „sample market rebuilt“, bootstrappt aber `EUR-ESTR` aus dem importierten Spec über die Sample-Kurve (DF[5] 0,9175 statt 0,9843) – EUR-Diskont- und Projektionskurve still ersetzt, keine `MARKET_STATE_DROPPED:`-Warnung, Export trägt den Eintrag weiter. Nur per handgebautem Envelope erreichbar (API-Export und Workstation erzeugen ihn nicht), daher Niedrig; widerspricht aber „kein stiller Verlust“ und der ADR-027-Grenze. | `quotesProblems` lehnt Sample-Ids ab (422 `SNAPSHOT_INVALID` „… is a sample curve – its quotes are the store's“) **oder** `rememberQuotes` ersetzt wie `rememberCurve` das Quote-Set des Stores mit sichtbarer Warnung/Audit; Test; ADR-027 R9 „Grenzen“ um die Import-Seite ergänzen. |
| N10-03 | **Niedrig** | 5 | `apps/api/vitest.config.ts` (kein `hookTimeout`, Default 10 s; `beforeAll` in `review-r4.test.ts:23`, `review-r8.test.ts:98`), `apps/web/vite.config.ts` (`testTimeout: 10000`; `App.r6/r7/r8.test.tsx`), `ci.yml:87` (`pnpm -r run test -- --coverage`, API und Web parallel) | Unter Last (Load 6–8) fallen API-Dateien am Hook-Timeout aus (30 Tests übersprungen, Exit 1 ohne fehlgeschlagenen Test) und 1–3 App-Integrationstests am 10-s-Limit (20–50 s Laufzeit; vier Läufe reproduziert; isoliert bzw. mit `--testTimeout=30000` alle 391 grün). Auf einem 2-Core-Runner mit parallelen API-/Web-Tests dieselbe Situation → flackernde CI ohne fachlichen Grund. | `hookTimeout: 20000` in `apps/api/vitest.config.ts`; für `App.r6/r7/r8` eigener Timeout (`it(…, 30_000)` wie `envelope-fresh-process.test.ts:187`) oder `testTimeout: 20000` für `App.*.test.tsx`; alternativ `--workspace-concurrency=1` im CI-Testschritt. |
| N10-04 | **Niedrig** | 6 | `CHANGELOG.md:7`; `docs/compliance/01-regulatorik-mapping.md:5`, `:101`; `CONTRIBUTING.md:48`; `SECURITY.md:7, 25, 33`; `docs/product/02-epics-und-user-stories.md:4, 176`; `apps/api/src/review-r9.test.ts:484-488` | „Der Tag `v0.3.0` **wird** … gesetzt“ / „`v0.3.0` **folgt** am Ende dieser Qualitätsrunde“ – er ist gesetzt (`git tag -l`, CHANGELOG Z. 92, CONTRIBUTING Z. 48 sagen es richtig); „Eine spätere Runde erhöht die Version (`0.2.1`/`0.3.0`)“ – 0.3.0 ist aktuell; Versionslabel „v0.2“ in SECURITY-Überschrift/Z. 25/33, Mapping §8 und Epics Z. 4/176 neben „Stand: v0.3“ (Mapping Z. 5) und 0.3.0 in allen `package.json`. Der N9-04-Test schützt nur die alte Formulierung. | Drei Sätze angleichen („ist gesetzt“, „`0.3.1`/`0.4.0`“), Versionslabel einheitlich (v0.3 bzw. bewusst „v0.2-Linie“), Test auf `/wird am Ende dieser Qualitätsrunde .* gesetzt|folgt am Ende dieser Qualitätsrunde/` verallgemeinern. |

---

## 4. Was für 100 noch fehlt

**Dimension 5 (API, Architektur, Code-Qualität)**
1. Commit-Hygiene auf der eigenen Range wiederherstellen (N10-01): Betreff ≤ 72, `feat` für Programme mit neuen öffentlichen Namen, optional lokaler Hook.
2. Sample-Kurven-Ids im `quotes`-Envelope ablehnen oder sichtbar ersetzen (N10-02) – eine Bedingung in `quotesProblems` plus Test.
3. Timeout-Reserven der Integrationstests (N10-03): `hookTimeout` in der API, eigene Timeouts für `App.r6/r7/r8`.
4. (Ohne Abzug) `rolledMeta` nur einmal – die API importiert die Core-Funktion.

**Dimension 6 (Dokumentation & Compliance)**
1. Drei Stand-Sätze und die Versionslabel angleichen, Test verallgemeinern (N10-04).
2. Tags `v0.1.0`/`v0.2.0`/`v0.3.0` durch einen Maintainer veröffentlichen (`git push origin v0.1.0 v0.2.0 v0.3.0`) – lokal ist alles vorbereitet (N4-10-Rest).

---

## 5. Kurzfazit

Das neunte Maßnahmenprogramm hat alle vier Architektur-/Doku-Befunde der Runde 9 und beide fachlichen Reste (N8-02 im Core, N4-03 für die Workstation) reproduzierbar geschlossen: der exportierte Envelope mit zusammengesetzten Kalender-Ids importiert in einem Prozess, der den Kalender nie gesehen hat – belegt mit einem Kindprozess-Test, der die Prozessgrenze wirklich überschreitet; der Import-Roll trägt keinen veralteten EMIR-Zeitstempel mehr und markiert das Label; der Rebuild wendet die Diskontkurven-Regel an; `validateMarket` prüft die Collateral-Währung auch für die Workstation; das typbewusste Lint deckt alle drei Pakete. Der neue Envelope-Block `quotes` ist architektonisch sauber gelöst – dieselbe Form wie der Bootstrap-Body, derselbe Store-Slot wie geladene Kurven, Snapshot-ID unverändert, ETag folgt der Repräsentation – und hält den Round-Trip inklusive Par-Risiko. Jede geprüfte Zahl – 435/151/391 Tests, 44 Operationen, 45 Codes, 12 Präfixe, 20 Komponenten, 405 öffentliche Namen, Initial-Load 233,5 kB, Coverage über allen Gates – stimmt mit der Messung überein, jede CHANGELOG-Stichprobe hält gegen den Diff, und `pnpm audit` war diesmal erreichbar (eine dokumentierte Moderate, Gate hält).

Die neuen Befunde sind durchweg niedrig und liegen am Rand: der Release-Commit bricht mit 74 Zeichen das eigene commitlint-Gate, ein handgebauter `quotes`-Eintrag für eine Sample-Kurve ersetzt sie beim `discardImport` ohne Warnung, die Integrationstests haben unter Last keine Timeout-Reserve, und drei Stand-Sätze zu `v0.3.0` sind mit dem Tag nicht mitgezogen. Dimension 5 steigt auf 99,2; Dimension 6 bleibt bei 99,7 – die Tags liegen weiterhin nur lokal, und das kann nur ein Maintainer ändern.
