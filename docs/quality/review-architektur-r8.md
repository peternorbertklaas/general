# DERIVA – Re-Review Runde 8: API, Architektur, Code-Qualität (Dim. 5) und Dokumentation & Compliance (Dim. 6)

**Rolle:** Principal Engineer · Security-Reviewer · Audit/Compliance (IFRS 13, IDW, MiFID II, EMIR Refit, MaRisk, DORA) · **Modus:** Review only, keine Quelländerungen
**Baseline:** `docs/quality/review-architektur-r7.md` (Runde 7: Dim. 5 = 98, Dim. 6 = 99; Findings N7-01…N7-06, Reste N6-03a, N4-03, N4-10)
**Stand:** Branch `claude/derivatives-trading-platform-1arsyu`, Commit `1cd266e` (2026-09-04 08:47 UTC). Das siebte Maßnahmenprogramm liegt als **drei Commits** vor: `c6e3ed7` `fix(core)` (28 Dateien, +1 746/−101), `55aea60` `fix(web)` (31 Dateien, +2 425/−187), `1cd266e` `feat(api,ci,docs)` (25 Dateien, +1 132/−76). Arbeitskopie sauber (`git status` leer – auch nach allen Testläufen, dem `vite build` in ein Scratch-Verzeichnis und allen Probes); weiterhin **kein Tag** (`git tag -l` leer).
**Methode (alles reproduzierbar ausgeführt, Node 22.22.2, pnpm 10.33):**
- `pnpm lint` (grün, 0 Warnungen), `pnpm format:check` (grün), `pnpm -r run typecheck` (grün); `node packages/pricing-core/tools/gen-index.mjs --check` → `OK: public 391 names, internal 98 names, 43 modules, 143 names imported by the apps` (exit 0; R7: 390/137 – neu öffentlich `isBuiltInIndex`, ADR-024 R7).
- `npx vitest run --coverage` je Paket, sequentiell: **Core 396 Tests** (28 Dateien, 15,5 s; Lines 94,38 %, Branches 82,43 %, Functions 97,05 %, Statements 91,36 %), **API 124 Tests** (8 Dateien, 53 s; Lines 98,27 %, Branches 88,77 %, Functions 96,77 %, Statements 96,14 %), **Web 335 Tests** (38 Dateien, 51 s; Lines 81,14 %, Statements 78,33 %, Branches 72,44 %, Functions 70,2 %) – alle Schwellen gehalten; Zahlen = Aufgabenstellung (396/124/335) und = CHANGELOG Z. 39/40/41; Web-Ist = Kommentar in `apps/web/vite.config.ts:53-55` („81.1 / 78.3 / 72.4 / 70.2“) exakt. `apps/web/e2e/smoke.mjs` enthält 429 `check(`-Aufrufstellen (437 Laufzeit-Checks laut Commit; nicht gestartet – Ports der anderen Reviewer).
- **Node-20-Gegenprobe der `engine-strict`-Sperre:** Kopie von `package.json`/`.npmrc` im Scratch-Verzeichnis, `PATH=/opt/node20/bin` (v20.20.2) `pnpm install --offline` → sofort `ERR_PNPM_UNSUPPORTED_ENGINE … Expected version: >=22.22 Got: v20.20.2` (exit 1); unter Node 22.22.2 dieselbe Installation grün. Node 24 ist in der Sandbox nicht vorhanden – die CHANGELOG-Aussage „API-Tests unter Node 24 grün“ (Z. 39) ist hier nicht prüfbar.
- `pnpm audit --prod --audit-level=high`: Registry nach 90 s ohne Antwort (Timeout) – **nicht auswertbar aus der Sandbox**, kein Abzug; Lockfile-Stand `echarts@5.6.0` (dokumentierte Moderate-Ausnahme bis 2026-12-31), `jsdom@30.0.1` (`engines ^22.22.2 || ^24.15.0 || >=26`), `vitest@5.0.0`, `vite@8.2.2`, `@vitejs/plugin-react@6.1.1`, `undici@8.10.1`.
- API-Probing mit `buildApp()` + `app.ready()` + `app.inject` (`scratchpad/probe-r8.mts`, `probe2-r8.mts`, `probe3-r8.mts`): OpenAPI-Dokument (Operationen, `operationId`s, 415 ⇔ `requestBody`, Code-Katalog, Präfixe, Komponenten, Import-Query, Snapshot-Envelope), Medientypen, ETag (Body = Header, Pattern, `W/` → 412), `GET /api/market` (`currencies`/`indices`/`conventions`), Register-Endpunkte (201/200/400 `builtIn`, Schema- und Core-Validierung), `POST /api/market/curves` Diskontzuordnung, `PUT /api/market` mit `discountCurveId`/`collateralDiscountCurveId` (422/400/200), CZK-Swap-Import, Snapshot-Export/-Import mit Envelope (Round-Trip, Built-in → 400, Teilregistrierung), `dryRun` (CSV/JSON, `dryrun`/`validate`/`yes` → 400, Store-Zähler), CCS-Web-Aliase, Audit-Aktionen, `UNKNOWN_INDEX`, Stichtagswechsel nach Kurven-/Vol-/Snapshot-Änderungen, Collateral-Zuordnung in fremder Währung.
- `npx vite build --outDir <scratch>` + `node scripts/size-limit.mjs <scratch>` (vite 8.2.2/Rolldown): `index` 36,7 kB gzip, `store` 23,0, `core` 79,7, `react` 58,8, CSS 7,3, `echarts` 179,0 lazy, **Initial-Load 220,6 kB** (8 Dateien), 19 precachete Assets, „Size budget OK“ – Zahlen identisch mit CHANGELOG Z. 41.
- Diff-Abgleich der drei Commits (`git show --stat`, Datei-Diffs) gegen Commit-Botschaften und CHANGELOG (Stichproben siehe 1.2); Doku-Abgleich README, CHANGELOG, CONTRIBUTING, SECURITY, Architektur §2/§3/§4, ADR-020/-023/-024/-026/-027, Epics (US-2.13/2.14/8.9/9.3/10.7, Release-Plan), UI-Konzept (Punkte 6/10/11/22, Markt-/Pricing-/Kurven-Zeile, Hotkey-Tabelle, § Barrierefreiheit/Architektur), Regulatorik-Mapping (Stand, §8, §9), Research §2.3.

---

## 1. Scores

| Dimension | R1 | R2 | R3 | R4 | R5 | R6 | R7 | **R8** | Δ R7→R8 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 5 · API, Architektur, Code-Qualität (15 %) | 58 | 72 | 90 | 94 | 96 | 98 | 98 | **97,8** | −0,2 |
| 6 · Dokumentation & Compliance (10 %) | 62 | 76 | 94 | 98 | 99 | 99 | 99 | **99,5** | +0,5 |

### 1.1 Dimension 5 – Begründung (97,8/100)

**Was jetzt trägt (verifiziert):**
- **CI-Matrix und Node-Anforderung konsistent (N7-01 geschlossen):** `.github/workflows/ci.yml:33` `node: [22, 24]` mit Begründungskommentar, `package.json#engines` Root und API `>=22.22`, `.nvmrc`/`.node-version` = `22`, `.npmrc` `engine-strict=true`; `review-r7.test.ts:57-68` liest alle Stellen ein und verbietet eine 20 in der Matrix. Die Sperre greift real (Node-20-Probe oben: `ERR_PNPM_UNSUPPORTED_ENGINE` vor jedem Download). ADR-020-Ergänzung R7 dokumentiert Entscheidung und verworfene Alternativen (jsdom ≤ 27, happy-dom).
- **Vertragstext `StoredTrade.etag` korrekt (N7-02 geschlossen):** `schemas.ts:1559` „Strong ETag … identical to the `ETag` header … a weak validator never matches … 412“, Pattern `^"\d+-[0-9a-f]+"$`; Probe: Header `"1-23201cc6c422a04b"` = Body, Pattern erfüllt, `If-Match: W/…` → 412 `PRECONDITION_FAILED` mit `currentEtag`; Test `review-r7.test.ts` („N7-02“) grept zusätzlich alle Antwortschemas auf „weak ETag“.
- **Core-Suite lasttolerant (N7-03 geschlossen):** `packages/pricing-core/vitest.config.ts` `testTimeout: 20_000` mit Begründung; `bootstrap-extended.test.ts:261-269` Best-of-2 mit `process.env.CI ? 3_000 : 300`; laut Diff ebenso `risk-r2.test.ts`/`par-risk.test.ts`. 396/396 grün mit Coverage.
- **Commit-Granularität weitgehend hergestellt (N6-03a):** drei Commits je Programm (Core / Web / API+CI+Doku) statt eines Sammel-Commits; Typ/Scope konventionell, Bodies beschreiben den Diff zutreffend (Stat-Abgleich: `c6e3ed7` nennt Review-Berichte/Scorecard/ADR-024, die tatsächlich enthalten sind; `55aea60` „Produkt-Doku aktualisiert“ = `docs/product/*` im Stat; `1cd266e` Toolchain/API/Doku wie beschrieben). Reste siehe Abzüge.
- **Register-Endpunkte sauber in den Vertrag eingefügt (ADR-027):** Probe: `POST /api/market/indices` neu → **201** (`replaced: false`), gleicher Name erneut → **200** (`replaced: true`), `euribor-6m` → **400 `INVALID_CURVE_SPEC`** `details: { index: "EURIBOR-6M", builtIn: true }`, Schema-Verstoß (`dayCount: ACT/999`, `{ name }`) → 400 `VALIDATION_ERROR`, unbekannter Kalender → 400 `INVALID_CURVE_SPEC` (Core-Text); `POST /api/market/conventions` CZK → 201, erneut → 200, mit unbekanntem `floatIndex` → 400 `INVALID_CURVE_SPEC`; `GET /api/market` listet danach `CZK` in `currencies`, `conventions.CZK`, 21 → 23 Indizes; `X-Market-Snapshot-Id` bleibt bei Registrierungen gleich (dokumentierte Grenze). `POST /api/market/curves` CZK-OIS → 200 `discountCurveSet: true`, `discountCurveId: CZK-CZEONIA-P8`; zweite CZK-Kurve → `discountCurveSet: false`; CZK-Swap per CSV → `imported=1`, PV −90 252,51 CZK. Audit-Aktionen `register.index`, `register.conventions`, `curve.replace`, `trade.import.dryRun` vorhanden.
- **`PUT /api/market` Diskontzuordnung (R7-3, API-Seite):** unbekannte Kurve → 422 `CURVE_NOT_FOUND` mit `details`, Kurve in fremder Währung → 400 `INVALID_REQUEST`, gültig → 200 mit vollständigem `discountCurveId`-Mapping; `collateralDiscountCurveId` unbekannte Kurve → 422 (Währungsprüfung fehlt hier – N8-02).
- **`dryRun` (Probe):** CSV mit drei Zeilen → 200 `{ total 3, imported 1, skipped 1, rejected 1, dryRun: true }`, Ergebnisse ohne `version`, Store-Zähler unverändert (11 → 11); `?dryrun=1`, `?validate=1`, `?dryRun=yes` → 400 `VALIDATION_ERROR` (Store unverändert); JSON `?dryRun=true` → nichts gespeichert; `?dryRun=0` → echter Import (`version: 1`, Store 12); fehlender Pflicht-Header bleibt 400 `CSV_INVALID`.
- **CCS-Aliase (R7-5):** Web-Kopfzeile `pair;notional;start;maturity;rate;direction;collateral;fxSpot;id` und API-Kopfzeile `domesticNotional;effectiveDate;tenor;fixedRate;domesticPayReceive;collateralCurrency` liefern denselben Trade (PV 157 607,97 in beiden Fällen).
- **Vertrag insgesamt:** OpenAPI 3.1.0 mit **43 Operationen / 43 eindeutigen `operationId`s** (neu `registerIndex`, `registerConventions`), 415 ⇔ `requestBody` **0 Abweichungen**, 44 Codes in `ErrorResponse.code.examples` = `API_ERROR_CODES` (Diff leer), `INVALID_CURVE_SPEC` enthalten und beschrieben, 10 Warnungs-Präfixe alle in der Beschreibung, Status-Menge `200/201/204/304/400/404/409/412/413/415/422/428/429/500` (= ADR-023), Komponenten inkl. `RateIndex`/`SwapConventions`, `MarketSnapshot` mit `indices`/`conventions`, Import-Query `reportingCurrency, type, mode, dryRun` mit `additionalProperties: false`, `?type`-Enum 11, Medientypen `application/json` + `text/csv`; `text/plain` → 415, `text/csv` auf `/api/nope` → 404, defektes JSON → 400 `INVALID_JSON`.
- **Code-Qualität unverändert sauber:** kein `new Error(` im Core außerhalb von Tests (Grep 0), keine `any`-Typannotationen und kein `console.*` in Core/API-Quellen (Treffer nur in Beschreibungstexten), `apps/*` importiert nichts aus `@deriva/pricing-core/internal` (Grep 0), `package.json#exports` `"."` + `"./internal"`; Web-Größenbudget mit Luft (Startchunk 36,7/90, Initial 220,6/260, `core` 79,7/100, `react` 58,8/65, `echarts` 179,0/190), kein ECharts im Startpfad.

**Abzüge (−2,2):**
- **Stichtagswechsel der API verwirft Laufzeitkurven, Diskontzuordnung, Vol-Overrides und importierte Snapshots stillschweigend** (−1,0, **N8-01, Mittel**): `MarketStore.rebuild` (`apps/api/src/lib/store.ts:97-110`) baut `buildSampleMarket(valuationDate, quotes)` und übernimmt aus dem alten Kontext nur Fixings (R7-4), FX-Fixings, Spots, Spot-Daten, Credit und Fixing-Policy. Probe: DKK-OIS per `POST /api/market/curves` (200, `discountCurveSet: true`), DKK-Swap importiert (PV −94 626,45), EUR-Cube per `PUT /api/market { swaptionVols }` ersetzt → `PUT /api/market { valuationDate: "2026-10-01" }` → **200 mit `warnings: []`**, danach `discountCurveId.DKK` fehlt, `GET /api/market/curves/DKK-DESTR` → 404, Cube wieder `EUR-SWAPTION-NORMAL`, `GET /api/trades?price=1` für den DKK-Swap → `error: "No discount curve configured for DKK", code: NO_DISCOUNT_CURVE`, `POST /api/price` → 422. Dasselbe für CZK (`CZK-ALT-P8` → 404, 422). Nach `PUT /api/market/snapshot` (Import mit `EURUSD: 1.2345`, Label `IMPORTED-Q8`) und Stichtagswechsel entsteht ein **Hybridmarkt**: Kurven/Vols des Beispielmarkts (`label: Sample EoD`), aber der importierte Spot 1,2345 bleibt. Damit ist genau das R7-3-Ergebnis („DKK-/NOK-Swap bewertet ohne `NO_DISCOUNT_CURVE`“) nach dem ersten EoD-Roll wieder weg, ohne Fehler, ohne Warnung – die Workstation löst denselben Fall seit R7-F1 mit `rebuildMarket` **inklusive** Zusatzkurven und Spots. Die OpenAPI-Beschreibung nennt nur, was überlebt („FX fixings, spots, spot dates, credit data and the fixing policy survive as before“), README Z. 81 nur „Beispiel-Fixings folgen dem neuen Stichtag, geladene Fixings bleiben“. Fix: `MarketRepository` merkt sich Nicht-Sample-Kurven mit ihren Quotes (`setCurveQuotes` liefert für sie heute `false`) und bootstrappt sie im `rebuild` neu (wie `rebuildMarket` im Web), trägt `discountCurveId`/`collateralDiscountCurveId` und Vol-Overrides weiter; für den Import-Modus entweder Roll des importierten Markts (Kurven per `rollMarket`) oder 409/400 „Snapshot importiert – Stichtagswechsel verwirft ihn, `discardImport: true` senden“; mindestens ein Warnungs-Präfix (`MARKET_STATE_DROPPED:`) in der Antwort und ein Vertragstest „Zusatzkurve überlebt Stichtagswechsel“. (Möglicher Überlapp mit der Marktabdeckungs-Review – falls dort gewertet, nur einmal zählen.)
- **`collateralDiscountCurveId` ohne Währungsprüfung** (−0,3, N8-02, Niedrig): `routes/market.ts:536-542` prüft für Collateral-Zuordnungen nur die Existenz der Kurve, nicht ihre Währung (für `discountCurveId` wird geprüft, Z. 523-534 → 400 `INVALID_REQUEST`); der Core (`validateMarket`) prüft ebenfalls nur `discountCurveId`. Probe: `PUT /api/market { collateralDiscountCurveId: { "EUR|CZK": "CZK-CZEONIA-Z8" } }` (CZK-Kurve mit 8 %) → **200**; `IRS-0001` mit `collateralCurrency: "CZK"` → PV **−226 935,87** statt −279 451,08 (EUR-Cashflows auf einer CZK-Kurve diskontiert, +19 %), `warnings: []`, Snapshot-Round-Trip → 200. Fix: `curve.currency !== key.split("|")[0]` → 400 `INVALID_REQUEST` in der Route und als `problems[]`-Prüfung in `validateMarket`.
- **Snapshot-ETag ignoriert den API-Envelope** (−0,3, N8-03, Niedrig): `GET /api/market/snapshot` liefert `ETag = "<snapshotId>"`, der Envelope `indices`/`conventions` ändert aber die Repräsentation, nicht die ID. Probe: ETag vor Registrierung merken → `POST /api/market/indices` (201) → `GET` mit `If-None-Match` altem Wert → **304**, obwohl der frische Body jetzt `indices: ["CZEONIA-Q8"]` trägt – ein cachender Client sieht die Registrierung nie (RFC 9110 § 8.8.3: der Validator muss sich mit der Repräsentation ändern). Fix: ETag der Export-Route aus `snapshotId` + Hash des Registers bilden (`X-Market-Snapshot-Id` bleibt die reine Markt-ID); Vertragstest „Registrierung ändert Snapshot-ETag, nicht `X-Market-Snapshot-Id`“.
- **Envelope-Import nicht atomar** (−0,2, N8-04, Niedrig): `routes/snapshot.ts:206-225` registriert Einträge sequentiell; scheitert der zweite, bleibt der erste prozessweit registriert und wird exportiert. Probe: `indices: [PARTIAL-OK, PARTIAL-BAD(fixingCalendar NOPE-CAL)]` → 400 `INVALID_CURVE_SPEC` mit `details.registered.indices: ["PARTIAL-OK-Q8"]`, Markt unverändert, aber `GET /api/market` und der Export führen `PARTIAL-OK-Q8`. Die Beschreibung sagt es ehrlich („entries registered before the failing one stay registered“), der Zustand widerspricht aber der Import-Zusage „ersetzt nur, wenn jeder Schritt passt“. Fix: alle Einträge zuerst gegen den Core validieren (öffentliches `validateRateIndex`/`validateSwapConventions` oder Registrierung in eine Kopie), dann anwenden.
- **Commit-Hygiene-Reste** (−0,2, N6-03a teilweise): Betreffzeilen **81/94/92 Zeichen** (CONTRIBUTING Z. 33 „Betreff ≤ 72 Zeichen“); `c6e3ed7` `fix(core)` enthält vier Review-Berichte, Scorecard und ADR-Text (CONTRIBUTING Z. 34: Review-Berichte als `docs(quality)`), `55aea60` `fix(web)` enthält `docs/product`, `1cd266e` berührt `apps/web/vite.config.ts`; Typ `fix` für neue Funktionen („+ Fläche“, „+ Paar“, `useGridNav`, `lib/register.ts`) statt `feat`; weiterhin kein Commit-Lint.
- **Typbewusstes Lint weiter offen** (−0,2, N4-03-Rest): `eslint.config.js` ohne `recommendedTypeChecked`; CONTRIBUTING Z. 26 „geplant für v0.3“.
- **Zur Kenntnis, ohne Abzug:** Rolldown-Advisory über den 560-kB-ECharts-Chunk (`chunkSizeWarningLimit: 500`, bewusst); `Popover`-Chunk (13,9 kB gzip) liegt im Initial-Load (8 Dateien) – innerhalb des Budgets; Web-Gate-Abstände jetzt 3,1/3,3/4,4 Punkte (Lines/Statements/Branches); `echarts@5.6.0` bleibt die befristete Ausnahme; Release-Tag in Dim. 6 gewertet.

### 1.2 Dimension 6 – Begründung (99,5/100)

**Was jetzt trägt (verifiziert):**
- **CHANGELOG/CONTRIBUTING wahr (N7-04 geschlossen):** CHANGELOG Z. 37 sagt jetzt „nicht konfigurationsneutral … 78 / 68 / 75, Functions 50 unverändert, `testTimeout: 10000`, Core-Exclude, `tsconfig` `node`“ und verweist auf die Node-Anforderung; CONTRIBUTING Z. 13 „Node ≥ 22.22“, Z. 36 „Der Sprung war **nicht** konfigurationsneutral“ mit denselben Zahlen – beides = `apps/web/vite.config.ts:56`, `packages/pricing-core/vitest.config.ts`, Diff `f93e099`.
- **Zahlen angeglichen (N7-05 geschlossen):** „Web 307 (34 Dateien)“ in Z. 36 und Z. 37; Bundle-Zahlen Z. 37 „34,1 + 22,8, 219,3 kB“ (R7-Messung) und Z. 41 „36,7 + 23,0, `core` 79,7, `react` 58,8, 220,6 kB, `echarts` 179,0, 19 Assets“ = frischer Build dieses Commits **exakt**; CONTRIBUTING Z. 34 nennt ehrlich „85, 108 und 97 Dateien“; ADR-026 Z. 172 „nur 2–3 Punkte unter Ist … 18 bei Functions“ und `vite.config.ts:53` „2–3 points below“ mit gemessenen 81,1/78,3/72,4/70,2 = Messung.
- **Präfixlisten vollständig (N7-06 geschlossen):** README Z. 90 „alle zehn aus `WARNING_PREFIXES`“ und Architektur §4 Z. 75 nennen alle 10 = `WARNING_PREFIXES` (10, Probe).
- **Neue Doku konsistent mit dem Code:** ADR-027 (Kontext „neun Währungen und 21 Indizes“ = Probe; 201/200/400-Semantik, Envelope nur bei Registrierungen, nicht Teil der Snapshot-ID, prozessweit, Tests) = `routes/market.ts`/`snapshot.ts`/`review-r7.test.ts`; ADR-023 R7-Ergänzung (zwei neue Operationen, `conventions`, `discountCurveId`/`collateralDiscountCurveId` mit 422/400, `isDiscountCurve`/`discountCurveSet`, `dryRun`-Enum, drei Verschärfungen) = Probe; ADR-020 R7 (vier Stellen, Alternativen) = Dateien; ADR-024 R7 (acht Verhaltensänderungen, `isBuiltInIndex` neu öffentlich, `testTimeout`) = Core-Diff; ADR-026 Zahlen = Messung; README Z. 30 Quickstart-Voraussetzung, Z. 80-91 (43 Operationen, Register-Endpunkte, `dryRun`, `NO_DISCOUNT_CURVE`-/`UNKNOWN_INDEX`-Hinweise, CCS-Aliase, Komponenten `RateIndex`/`SwapConventions`) = Probe; SECURITY Z. 15 (geschlossene Query-Parameter, Register vor Auth) = Probe/ADR-018; CONTRIBUTING Z. 21/23 (Testbeschreibung R7, CCS-Aliase, `additionalProperties: false`, `dryRun` teilt den Handler-Pfad) = `trades.ts:335-337, 402-412`; Epics US-2.13/2.14/8.9/10.7/9.3 und UI-Konzept (Markt-Zeile R7-01/R7-2/R7-F1, Pricing-Zeile Register-Listen/Fokus, Kurven-Zeile R7-F1/R7-03, Hotkey-Tabelle Z. 116-117, § Barrierefreiheit, § Architektur `echarts-lib.ts`/Retry) = ausgelieferter Code laut Stat/Diff (`hooks/useGridNav.ts`, `lib/register.ts`, `lib/focus.ts`, `components/echarts-lib.ts`, `MarketView.tsx` +704) und Testdateien (`App.r7.test.tsx`, `store.r7.test.ts`, `review-r7.test.ts`); Regulatorik-Mapping Stand „Review-Runde 7“ (Z. 5), §8 (10 Punkte) deckt sich mit README-Roadmap und Release-Plan (v1.0 Adapter/Governance/Persistenz+Auth/PDF/EMIR/DORA-Doku; v1.1 Strukturen/Inflation/Netting-CVA/Excel/Batch; v1.2 SIMM/VaR/Validierung/Worker), §9 „Testzahlen nur in Scorecards“ eingehalten (Architektur §6); Research §2.3 Konventionstabelle NOK/SEK/DKK/PLN unverändert = `index-definitions.ts`.
- **CHANGELOG-Stichproben gegen die drei Diffs (fünf Aussagen, alle wahr):** (1) Z. 40 „`priceFra`, `priceFxForward`, `priceFxSwap` honorieren `upfront` als `Premium`-Leg“ → `c6e3ed7` `fra-pricer.ts:50-52, 87` (`upfrontPremiumLeg(ctx, trade, reporting, 1)`), `fx-pricer.ts` +85/−… ✓; (2) Z. 40 „DK Freitag nach Himmelfahrt, NO 24.12.“ → `calendar.ts:259` (`fromYMD(y, 12, 24)`), `:307` (`easter + 40`) ✓; (3) Z. 40 „`registerRateIndex` lehnt eingebaute Namen ab … `isBuiltInIndex`“ → `index-definitions.ts:300-305, 332-338`, `index.ts:99` ✓; (4) Z. 39 „`engines` Root und API `>=22.22`, `.nvmrc`/`.node-version`, `engine-strict`, Matrix 22/24“ → `1cd266e` Diff aller sechs Dateien ✓; (5) Z. 41 „Web 335 (38 Dateien), Startchunk 36,7 + 23,0 kB, 220,6 kB, 19 Assets“ → Messung ✓; zusätzlich Z. 38 „dieselben 1 452 Fixings“ → Probe `fixings: 1452` nach Stichtagswechsel auf 03.11.2026 ✓, „unbekannte Query-Parameter → 400“ ✓, „Währung → 400 `INVALID_REQUEST`“ ✓. Nicht prüfbar: „API-Tests unter Node 24 grün“ (Z. 39, kein Node 24 in der Sandbox), „E2E 437 Checks“ (nicht gestartet).
- **Commit-Botschaften vs. Diff:** alle drei Bodies zutreffend (siehe 1.1); die Zählwerte „335 Unit-Tests (38 Dateien), E2E 437“, „43 OpenAPI-Operationen, 124 API-Tests“ stimmen mit Messung/Vertrag.
- **Compliance-Mapping** inhaltlich unverändert korrekt (MiFID II Art. 24/25, DelVO 2017/565 Art. 50/54, §§ 63/64 WpHG; IFRS 13/IDW RS HFA 47 vs. IFRS 9/§ 254 HGB/IDW RS HFA 35; EMIR ITS 2022/1860 Tabelle 2 Felder 21–26/30/31; MaRisk AT 4.3.4/4.3.5/BTO 2.2.1; DORA Art. 5–30 organisatorisch abgegrenzt); Roadmap ehrlich (⏳/🔶/❌ = README = Release-Plan; SECURITY „Bekannte Grenzen“ = §8).

**Abzüge (−0,5):**
- **Veraltete Technik-Fakten in Architektur/README/Research** (−0,2, N8-05, Niedrig): Architektur §2 Z. 29 „React 19, **Vite 7**“ (Lockfile `vite@8.2.2`; CHANGELOG Z. 37 und CONTRIBUTING Z. 36 sagen 8.2); README Z. 141 und Architektur §3 Z. 36 „Kalender TARGET/US/UK/CH/JP/DE“ ohne NO/SE/DK/PL (seit R6 im Core, seit R7 golden-getestet – CHANGELOG Z. 40 „Berichtstext nennt NO/SE/DK/PL“); Architektur §4 Z. 78 Komponentenliste ohne `RateIndex`/`SwapConventions` (README Z. 86 vollständig, Dokument enthält beide); Research §2.3 Z. 100 „FX-Vol-Flächen für Kreuze mit diesen Währungen fehlen (Fallback 8 %, Level 3, Palette warnt)“ – seit R7 legt „+ Fläche“ (US-2.14) sie an und die Palette nennt diesen Weg. Fix: vier Sätze nachziehen.
- **Release-Hygiene (N4-10 offen, entschärft)** (−0,3): weiterhin kein Tag; CHANGELOG-Statusnote (Z. 7/69), CONTRIBUTING „Release“ und Mapping-Stand sind vorbereitet, die Vergleichs-Links zeigen auf nicht existierende Tags; die Begründung ist dokumentiert und nachvollziehbar.
- **Zur Kenntnis, ohne Abzug (Doku-Seite von N8-01, dort gewertet):** README Z. 81, OpenAPI-Beschreibung von `PUT /api/market` und ADR-023 R7 (3) beschreiben beim Stichtagswechsel nur, was überlebt (Fixings, Spots, Credit …), nicht, dass Laufzeitkurven, Diskont-/Collateral-Zuordnungen, Vol-Overrides und ein importierter Snapshot verworfen werden – mit dem Fix von N8-01 wird der Satz „Sample-Markt wird neu aufgebaut“ ohnehin zu ersetzen sein.

---

## 2. Status der Runde-7-Findings und Reste

| # | Runde 7 | Status | Evidenz |
|---|---|---|---|
| N7-01 | CI-Matrix Node 20 deterministisch rot; `engines >=20`, CONTRIBUTING „Node ≥ 20“ | **behoben** | `ci.yml:33` `[22, 24]` + Kommentar; `package.json`/`apps/api/package.json` `>=22.22`; `.nvmrc`/`.node-version` `22`; `.npmrc` `engine-strict=true` (Node-20-Probe: `ERR_PNPM_UNSUPPORTED_ENGINE` sofort); CONTRIBUTING Z. 13/36, README Z. 30, ADR-020 R7, Epics US-9.3; Test `review-r7.test.ts:57-68`. |
| N7-02 | `StoredTrade.etag` als „weak ETag `W/`“ dokumentiert | **behoben** | `schemas.ts:1559` „Strong ETag … identical to the `ETag` header“, Pattern `^"\d+-[0-9a-f]+"$`; Probe Body = Header `"1-23201cc6c422a04b"`, `W/` → 412; Test grept Antwortschemas auf „weak ETag“. |
| N7-03 | Core ohne `testTimeout`, Wall-Clock-Assertion `< 300 ms` | **behoben** | `vitest.config.ts` `testTimeout: 20_000` (Kommentar N7-03); `bootstrap-extended.test.ts:261-269` Best-of-2, `CI ? 3_000 : 300`; laut Diff auch `risk-r2`/`par-risk`; 396/396 grün mit Coverage. |
| N7-04 | CHANGELOG/CONTRIBUTING „ohne Änderung an Coverage-Schwellen“ falsch, „Node ≥ 20“ | **behoben** | CHANGELOG Z. 37 („nicht konfigurationsneutral … 78/68/75, `testTimeout`, Core-Exclude, `tsconfig`“), Z. 39; CONTRIBUTING Z. 13/36 – alle Zahlen = Konfiguration. |
| N7-05 | Zahlen-Inkonsistenzen (33/34 Dateien, Bundle, „85 Dateien“, „5–15 Punkte“) | **behoben** | CHANGELOG Z. 36/37 „34 Dateien“; Bundle-Zahlen Z. 37/41 = Build (36,7/23,0/79,7/58,8/220,6/179,0/19 exakt); CONTRIBUTING Z. 34 „85, 108 und 97“; ADR-026 Z. 172 und `vite.config.ts:53` „2–3 Punkte“ mit Ist 81,1/78,3/72,4/70,2 = Messung. |
| N7-06 | Präfixlisten README 8/10, Architektur 6/10 | **behoben** | README Z. 90 und Architektur §4 Z. 75 je 10 = `WARNING_PREFIXES` (Probe 10, alle im Katalog beschrieben). |
| N6-03a | Monolithischer Commit, Scope unvollständig, kein Commit-Lint | **weitgehend behoben** | Drei Commits je Programm mit passendem Typ/Scope und zutreffenden Bodies; Reste: Betreff 81/94/92 > 72 Zeichen, Review-Berichte/ADR im `fix(core)`, Produkt-Doku im `fix(web)`, `fix` für neue Funktionen, kein commitlint (→ −0,2). |
| N4-03 | Tooling-Reste (`recommendedTypeChecked`) | **teilweise** | `eslint.config.js` ohne typbewusste Regeln; CONTRIBUTING Z. 26 „geplant für v0.3“ (→ −0,2). |
| N4-10 | Kein Tag | **offen (entschärft)** | `git tag -l` leer; Prozess dokumentiert (→ −0,3 in Dim. 6). |

Bilanz N7-01…N7-06: **6 von 6 behoben**; Reste: N6-03a weitgehend behoben, N4-03 teilweise, N4-10 offen.

---

## 3. Neue Findings

Schwere: **Kritisch** (falsches Ergebnis/Compliance-Fehler/Sicherheitslücke), **Hoch**, **Mittel**, **Niedrig**.

| # | Schwere | Dim. | Ort | Problem | Konkreter Fix |
|---|---|---|---|---|---|
| N8-01 | **Mittel** | 5 | `apps/api/src/lib/store.ts:97-110` (`MarketStore.rebuild`), `routes/market.ts:544` (`if (req.body.valuationDate) m = ctx.market.rebuild(…)`), Doku README Z. 81, OpenAPI-Beschreibung `updateMarket`, ADR-023 R7 (3) | `PUT /api/market { valuationDate }` baut den **Beispielmarkt** neu und übernimmt nur Fixings/FX-Fixings/Spots/Spot-Daten/Credit/Policy: per `POST /api/market/curves` angelegte Kurven (DKK, CZK), die R7-3-Diskontzuordnung, `collateralDiscountCurveId`, per `PUT` ersetzte Vol-Flächen und ein importierter Snapshot (Kurven/Vols/Label) sind danach weg – 200 mit `warnings: []`. Probe: DKK-Swap (PV −94 626,45) → nach Stichtagswechsel `NO_DISCOUNT_CURVE` (422 / `pv: null` in `GET /api/trades?price=1`), `GET /api/market/curves/DKK-DESTR` → 404, Cube `EUR-BUMPED` → `EUR-SWAPTION-NORMAL`; nach Snapshot-Import + Stichtagswechsel Hybridmarkt (Sample-Kurven, importierter Spot 1,2345). Die Workstation löst denselben Fall seit R7-F1 (`rebuildMarket` mit Zusatzkurven/Spots), die API nicht; die Doku nennt nur, was überlebt. | `MarketRepository` verwaltet Nicht-Sample-Kurven mit Quotes (heute `setCurveQuotes` → `false`) und bootstrappt sie im `rebuild` neu; `discountCurveId`/`collateralDiscountCurveId`/Vol-Overrides übernehmen; im Import-Modus Roll des importierten Markts oder 409/400 mit explizitem `discardImport: true`; Antwort mit Warnungs-Präfix für Verworfenes; Vertragstest „Zusatzkurve und Zuordnung überleben den Stichtagswechsel“; README/OpenAPI/ADR-023 nachziehen. |
| N8-02 | **Niedrig** | 5 | `apps/api/src/routes/market.ts:536-542`; Core `market/snapshot.ts` `validateMarket` (prüft nur `discountCurveId`) | `collateralDiscountCurveId` wird nur auf Existenz, nicht auf Währung geprüft (anders als `discountCurveId`, Z. 523-534 → 400 `INVALID_REQUEST`). Probe: `{ "EUR|CZK": "CZK-CZEONIA-Z8" }` (CZK-Kurve 8 %) → 200; `IRS-0001` mit `collateralCurrency: "CZK"` → PV −226 935,87 statt −279 451,08 (+19 %), keine Warnung, Snapshot-Round-Trip 200. | Währungsprüfung `curve.currency === key.split("|")[0]` → 400 `INVALID_REQUEST` in der Route; gleiche Prüfung als `problems[]` in `validateMarket`; Test. |
| N8-03 | **Niedrig** | 5 | `apps/api/src/routes/snapshot.ts:131-140` (`etag = "<snapshotId>"`, Envelope angehängt) | Der Export-ETag ändert sich nicht, wenn der Envelope `indices`/`conventions` hinzukommt. Probe: ETag vor Registrierung → `POST /api/market/indices` 201 → `GET /api/market/snapshot` mit `If-None-Match` altem ETag → **304**, frischer Body trägt `indices: ["CZEONIA-Q8"]`; ein cachender Client sieht die Registrierung nie (RFC 9110 § 8.8.3). | ETag der Export-Route aus `snapshotId` + Hash von `listIndices()/listConventions()`; `X-Market-Snapshot-Id` bleibt Markt-ID; Vertragstest. |
| N8-04 | **Niedrig** | 5 | `apps/api/src/routes/snapshot.ts:206-225` | Envelope-Import registriert sequentiell; scheitert Eintrag n, bleiben 1…n−1 prozessweit registriert und werden exportiert (Probe: `details.registered.indices: ["PARTIAL-OK-Q8"]` bei 400, `GET /api/market` und Export führen den Namen) – die Beschreibung sagt es, die Import-Zusage „ersetzt nur, wenn jeder Schritt passt“ gilt für das Register damit nicht. | Erst alle Einträge validieren (öffentliches `validateRateIndex`/`validateSwapConventions` im Core oder Registrierung in eine Kopie des Registers), dann anwenden; Test „ungültiger zweiter Eintrag registriert nichts“. |
| N8-05 | **Niedrig** | 6 | `docs/architecture/01-architektur.md:29, 36, 78`; `README.md:141`; `docs/research/03-domaene-markt-methodik-regulatorik.md:100` | Veraltete Fakten: „Vite 7“ (Lockfile 8.2.2), Kalenderliste ohne NO/SE/DK/PL (Core seit R6, Golden seit R7), Komponentenliste ohne `RateIndex`/`SwapConventions`, „FX-Vol-Flächen … fehlen (Fallback 8 %, Level 3)“ obwohl „+ Fläche“ (US-2.14) sie seit R7 anlegt. | Vier Sätze aktualisieren („Vite 8 (Rolldown)“, „TARGET/US/UK/CH/JP/DE/NO/SE/DK/PL“, Komponenten ergänzen, Research-Satz auf „im Beispielmarkt nicht enthalten; per ‚+ Fläche‘ bzw. `PUT /api/market { fxVols }` anlegbar“). |

---

## 4. Was für 100 noch fehlt

**Dimension 5 (API, Architektur, Code-Qualität)**
1. Stichtagswechsel der API ohne stillen Verlust von Laufzeitkurven, Zuordnungen, Vol-Overrides und importierten Snapshots (N8-01) – die Workstation zeigt mit `rebuildMarket` das Zielverhalten.
2. Währungsprüfung für `collateralDiscountCurveId` in Route und `validateMarket` (N8-02); Export-ETag mit Register-Anteil (N8-03); atomarer Envelope-Import (N8-04).
3. Commit-Betreff ≤ 72 Zeichen, Review-Berichte als `docs(quality)`, `feat` für neue Funktionen, commitlint (N6-03a-Rest); `recommendedTypeChecked` (N4-03-Rest); echarts 6 vor Ablauf der Ausnahme (2026-12-31).

**Dimension 6 (Dokumentation & Compliance)**
1. Vier veraltete Sätze in Architektur §2/§3/§4, README § Methodik und Research §2.3 nachziehen (N8-05); zusammen mit dem N8-01-Fix die Beschreibung des Stichtagswechsels (README Z. 81, OpenAPI `updateMarket`, ADR-023) vervollständigen.
2. Tags `v0.1.0`/`v0.2.0` setzen (N4-10) – Doku und Release-Prozess sind vorbereitet.

---

## 5. Kurzfazit

Das siebte Maßnahmenprogramm hat alle sechs Architektur- und Doku-Befunde der Runde 7 geschlossen und dabei die Form gewahrt, die CONTRIBUTING seit Runde 5 verlangt: drei Commits je Programm mit zutreffenden Botschaften. Die Node-Anforderung steht an sechs Stellen konsistent und wird durch `engine-strict` real erzwungen (Node 20 scheitert vor dem ersten Download), der `StoredTrade`-Vertrag beschreibt den starken ETag, die Core-Suite ist lasttolerant, CHANGELOG und CONTRIBUTING sagen über den Toolchain-Sprung jetzt das, was der Diff zeigt, und jede geprüfte Zahl – Tests 396/124/335, 43 Operationen, 44 Codes, 10 Präfixe, Bundle 36,7/23,0/79,7/58,8/220,6 kB, Coverage 81,1/78,3/72,4/70,2, 1 452 Fixings – stimmt mit der Messung überein. Die neuen Register-Endpunkte, `dryRun`, die Diskontzuordnung und die CCS-Aliase sind sauber in Vertrag, Katalog, Audit, ADR-027 und Doku eingefügt; alle Probes antworten wie dokumentiert.

Der gewichtigste neue Befund ist eine Lücke zwischen den beiden Fassaden des Cores: die Workstation baut den Markt beim Stichtagswechsel seit R7-F1 mit Zusatzkurven und Spots neu, die API baut weiterhin den Beispielmarkt und verwirft dabei still Laufzeitkurven, die frisch gelieferte Diskontzuordnung, Vol-Overrides und importierte Snapshots – ein DKK-Buch ist nach dem ersten EoD-Roll wieder unbewertbar. Dazu kommen drei kleine Vertrags-/Konsistenzposten (Collateral-Zuordnung ohne Währungsprüfung, Export-ETag ohne Register-Anteil, nicht-atomarer Envelope-Import), Reste der Commit-Hygiene und vier veraltete Doku-Sätze. Dimension 5 hält knapp ihr Niveau (97,8), Dimension 6 legt auf 99,5 zu; der Weg zu 100 ist auf beiden Seiten ein Tag Arbeit an `MarketStore.rebuild`, drei Prüfungen in zwei Routen und einer Handvoll Sätze.
